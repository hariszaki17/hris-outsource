# Highest-precedence dev override (set by Claude to use the REAL Go API + demo data).
# Beats .env.development. Delete this file to return to MSW mocks (.env.development).
VITE_ENABLE_MSW=false
VITE_API_BASE_URL=http://localhost:8080/api/v1
