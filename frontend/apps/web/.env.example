# Base URL of the Go API (CONVENTIONS §2). Defaults to /api/v1 if unset.
VITE_API_BASE_URL=/api/v1

# Set to "true" to serve the Orval-generated MSW mocks in dev (no Go API needed).
# First run once: pnpm dlx msw init public/ --save
VITE_ENABLE_MSW=false
