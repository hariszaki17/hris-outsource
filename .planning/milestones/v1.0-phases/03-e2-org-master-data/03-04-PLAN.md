---
phase: 03-e2-org-master-data
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - backend/internal/domain/org_masterdata.go
  - backend/internal/repository/org/masterdata_repo.go
  - backend/internal/service/org/masterdata_service.go
  - backend/internal/handler/org/masterdata_dto.go
  - backend/internal/handler/org/masterdata_handler.go
  - backend/internal/server/server.go
  - backend/cmd/api/main.go
  - backend/cmd/seed/seed.go
autonomous: true
requirements: [ORG-04]

must_haves:
  truths:
    - "HR/super_admin can list/create/update/soft-delete leave types, attendance codes, and overtime rules; reads allow the broader role set per spec; lists are cursor-paginated with the documented filters (is_annual, is_billable, service_line, status)."
    - "Overtime rule min_minutes below 30 returns RULE_VIOLATION (422); duplicate name/code returns 409; soft-delete sets status INACTIVE and returns 204."
    - "Responses match LeaveType/AttendanceCode/OvertimeRule schemas exactly (booleans, numeric rates, nullable service_line_id, UPPERCASE status)."
  artifacts:
    - path: "backend/internal/handler/org/masterdata_handler.go"
      provides: "leave-type/attendance-code/overtime-rule handlers"
      min_lines: 120
    - path: "backend/internal/service/org/masterdata_service.go"
      provides: "master-data CRUD + min_minutes rule + audit"
      min_lines: 120
  key_links:
    - from: "backend/internal/server/server.go"
      to: "master-data handlers"
      via: "routes /leave-types, /attendance-codes, /overtime-rules"
      pattern: "overtime-rules"
    - from: "masterdata_service.go"
      to: "audit.Record"
      via: "audit on every write"
      pattern: "audit\\.Record"
---

<objective>
Implement the operational master-data slice (ORG-04): leave types, attendance codes, and overtime rules — domain, repository, service (CRUD + soft-delete + the min_minutes>=30 rule), handlers, routes, deps wiring, and seed. Runs in parallel with 03-02/03-03 (distinct files; shared server.go/main.go/seed.go are coordination points).

Purpose: These three sets feed E5 (attendance codes), E6 (leave types/quotas), and E7 (overtime rules), and back the master-data hub screens.
Output: masterdata_* files in the `org` package, 12 endpoints mounted, seed extended.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/reference/backend-build-conventions.md
@.planning/phases/03-e2-org-master-data/03-CONTEXT.md
@.planning/phases/03-e2-org-master-data/03-01-SUMMARY.md   # sqlc method names
@.planning/phases/03-e2-org-master-data/03-02-SUMMARY.md   # org package + server.go/main.go/seed coordination contract

<interfaces>
Reference slice: foundations service/repository/handler. Sibling 03-02 created the `org` packages and the ORG route group. Add masterdata_*.go alongside; use a separate MasterDataService + MasterDataRepository in the `org` package (cleanest for parallel merge). Reuse the package's mapErr/null/cursor helpers (do not redeclare).

apperr: apperr.Conflict("CONFLICT") for dup name/code; apperr.Rule("RULE_VIOLATION", fields) (422) for min_minutes<30; apperr.NotFound(); apperr.Conflict(<entity>_IN_USE) for soft-delete-while-referenced (TODO Phase 7/8 — no referencing tables yet, so the guard is a no-op TODO and soft-delete succeeds in Phase 3).
RBAC: all three reads allow [hr_admin,super_admin,shift_leader,agent] (overtime-rules reads: [hr_admin,super_admin,shift_leader]); writes [super_admin,hr_admin] scope global.
Status: DB lowercase; DTO UPPERCASE. min_minutes default 30.
</interfaces>

# Contract — match EXACTLY:
@docs/api/E2-identity/openapi.yaml   # /leave-types 2040–2203; /attendance-codes 2205–2359; /overtime-rules 2361–2509; schemas LeaveType 3299, AttendanceCode 3342, OvertimeRule 3391 (+write reqs)
@docs/api/CONVENTIONS.md             # §8 cursor, §9 filtering, §11 errors, §16.1 audit, §17 RBAC, §18 pickers
@docs/epics/E2-identity/prds/operational-master-data.md   # LT-/AC-/OR-/MD- rules + Gherkin (min_minutes=30, dup name/code, soft-delete)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain + repository for the 3 master-data entities</name>
  <read_first>
    - backend/internal/repository/sqlc/leave_types.sql.go + attendance_codes.sql.go + overtime_rules.sql.go (generated names from 03-01)
    - backend/internal/repository/org/companies_repo.go (mapping/helpers/WithTx from sibling 03-02)
    - docs/api/E2-identity/openapi.yaml 3299–3456 (exact field names + types: integers, real/float rates, nullable service_line_id)
  </read_first>
  <action>
Create `backend/internal/domain/org_masterdata.go`:
- LeaveType (ID, Name, Code, Description, DefaultAnnualQuota int, IsAnnual bool, RequiresDocument bool, Color, Status).
- AttendanceCode (ID, Code, Label, Description, Color, IsWorkday, IsPaid, IsBillable, NeedsVerification bool, Status).
- OvertimeRule (ID, Name, ServiceLineID *string, WeekdayRate, RestdayRate, HolidayRate float32, MinMinutes int, MaxMinutesPerDay int, PreApprovalRequired bool, Status).
- Filters: LeaveTypeFilter (Status, IsAnnual *bool, Limit, cursor), AttendanceCodeFilter (Status, IsBillable *bool, Limit, cursor), OvertimeRuleFilter (Status, ServiceLine *string, Limit, cursor).

Create `backend/internal/repository/org/masterdata_repo.go`: MasterDataRepository struct (pool,q) implementing for each entity: List, GetByID, Create(tx, params), Update(tx, params), SetStatus(tx,id,status). Map nullable service_line_id correctly. Reuse package helpers.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/domain/... ./internal/repository/org/... 2>&1 | head; test -f internal/domain/org_masterdata.go && grep -q "MinMinutes" internal/domain/org_masterdata.go && grep -q "func (r \*MasterDataRepository)" internal/repository/org/masterdata_repo.go</automated>
  </verify>
  <done>domain + repository for the 3 entities compile; OvertimeRule.ServiceLineID is *string; rates are float32; filters carry is_annual/is_billable/service_line.</done>
</task>

<task type="auto">
  <name>Task 2: Service + handlers/DTOs for master data</name>
  <read_first>
    - backend/internal/service/org/companies_service.go (Service shape, InTx+audit, cursor, ClampLimit, status lowercasing — from 03-02)
    - backend/internal/handler/org/companies_handler.go + companies_dto.go (handler+DTO+cursor decode conventions)
    - docs/api/E2-identity/openapi.yaml 2040–2509 (statuses + error codes; createOvertimeRule min_minutes<30→422 RULE_VIOLATION; dup name/code→409)
    - docs/epics/E2-identity/prds/operational-master-data.md
  </read_first>
  <action>
Create `backend/internal/service/org/masterdata_service.go` (MasterDataService{repo, txm, now}). For EACH of the 3 entities implement List / Get / Create / Update / SoftDelete following the foundations pattern:
- List: ClampLimit+1, lowercase status filter, apply is_annual/is_billable/service_line filters, encode cursor from last (CreatedAt,ID).
- Get: ErrNotFound→apperr.NotFound().
- Create: InTx Create + audit.Record (entity_type 'leave_type' | 'attendance_code' | 'overtime_rule'); dup name/code (unique index) → apperr.Conflict("CONFLICT").
- Update: load (404); InTx Update + audit before/after; dup→409.
- SoftDelete: load (404); TODO(Phase 7/8) 409 <ENTITY>_IN_USE when referenced; InTx SetStatus 'inactive' + audit; (204 at handler).
- OvertimeRule Create/Update extra rule: if min_minutes < 30 → apperr.Rule("RULE_VIOLATION", {"min_minutes":"Minimal 30 menit."}) (422) BEFORE the tx. Default min_minutes to 30 when omitted (pointer nil).

Create `backend/internal/handler/org/masterdata_dto.go`: response structs matching schemas EXACTLY —
- leaveTypeResponse{id,name,code,description,default_annual_quota,is_annual,requires_document,color,status}
- attendanceCodeResponse{id,code,label,description,color,is_workday,is_paid,is_billable,needs_verification,status}
- overtimeRuleResponse{id,name,service_line_id(*string, nullable json),weekday_rate,restday_rate,holiday_rate,min_minutes,max_minutes_per_day,pre_approval_required,status}
plus write-request structs (use pointers for optional fields). Mappers UPPERCASE status. Rates serialize as numbers (float32 → json number; ensure 1.5/2.0/3.0 round-trip — use float64 in the response struct if needed to avoid float32 noise).

Create `backend/internal/handler/org/masterdata_handler.go`: handlers for all 12 ops (List/Create/Update/SoftDelete per entity, 4 each minus get — FE inventory uses List/Create/Update/SoftDelete only; do NOT add GET-by-id unless trivial). chi.URLParam("leave_type_id"|"attendance_code_id"|"overtime_rule_id"). SoftDelete → 204. Create → 201. Reuse org-package decode/cursor/limit helpers.

NOTE on scope: the FE inventory (.planning/reference/fe-endpoint-inventory.md) calls List/Create/Update/SoftDelete for each of the 3 entities (12 ops). GET-by-id ops in the spec are NOT FE-used — out of scope; do not implement them.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/service/org/... ./internal/handler/org/... 2>&1 | head; grep -q "RULE_VIOLATION" internal/service/org/masterdata_service.go && grep -q "needs_verification" internal/handler/org/masterdata_dto.go && grep -q "service_line_id" internal/handler/org/masterdata_dto.go && grep -q "StatusNoContent" internal/handler/org/masterdata_handler.go</automated>
  </verify>
  <done>service + handlers + DTOs compile; min_minutes<30→RULE_VIOLATION (422); dup→409; soft-delete→204; nullable service_line_id; audit on every write; UPPERCASE status.</done>
</task>

<task type="auto">
  <name>Task 3: Routes (server.go — COORDINATION POINT), deps wiring, seed master data</name>
  <read_first>
    - .planning/phases/03-e2-org-master-data/03-02-SUMMARY.md (ORG group append position, Deps field convention, seed insert order)
    - backend/internal/server/server.go (current ORG groups from 03-02/03-03)
    - backend/cmd/api/main.go (current org deps construction)
    - backend/cmd/seed/seed.go (idempotent insert pattern)
    - docs/api/E2-identity/openapi.yaml x-rbac per op
  </read_first>
  <action>
COORDINATION POINT (server.go, main.go, seed.go shared with 03-02/03-03). Append a NEW dedicated ORG master-data route group AFTER the existing ORG groups — do not edit other groups. Sub-groups:
- reads RequireRole(SuperAdmin,HRAdmin,ShiftLeader,Agent): GET /leave-types, GET /attendance-codes. reads RequireRole(SuperAdmin,HRAdmin,ShiftLeader): GET /overtime-rules. (Match each op's x-rbac roles exactly — overtime-rules read excludes agent.)
- writes RequireRole(SuperAdmin,HRAdmin): for each entity `r.With(d.Idempotency.Handler).Post("/<entity>", ...)`, `Patch("/<entity>/{<id>}", ...)`, `Delete("/<entity>/{<id>}", ...)`. (POST is idempotency-wrapped per spec IdempotencyKey param; PATCH/DELETE are not flagged → no wrapper.)

Add handler to Deps (e.g. `OrgMasterData *orghttp.MasterDataHandler`); construct in cmd/api/main.go and add to Deps literal.

Seed (cmd/seed/seed.go): idempotently insert (ON CONFLICT (id) DO NOTHING) the spec's canonical seeds:
- leave_types: SWP-LT-001 'Cuti Tahunan' code ANNUAL default_annual_quota=12 is_annual=true requires_document=false color '#188E4D'; SWP-LT-002 'Cuti Sakit' code SICK is_annual=false requires_document=true color '#E07A2A'. status='active'.
- attendance_codes: SWP-AC-001 code PRESENT label 'Hadir' color '#0F8B8D' is_workday=true is_paid=true is_billable=true needs_verification=true; SWP-AC-002 code LATE label 'Terlambat' color '#E07A2A' (same flags). status='active'.
- overtime_rules: SWP-OTR-001 'Default OT' service_line_id NULL weekday_rate 1.5 restday_rate 2.0 holiday_rate 3.0 min_minutes 30 max_minutes_per_day 240 pre_approval_required true status='active'.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && grep -q "/leave-types" internal/server/server.go && grep -q "/attendance-codes" internal/server/server.go && grep -q "/overtime-rules" internal/server/server.go && grep -q "SWP-LT-001" cmd/seed/seed.go && grep -q "SWP-AC-001" cmd/seed/seed.go && grep -q "SWP-OTR-001" cmd/seed/seed.go</automated>
  </verify>
  <done>Backend builds/vets clean; 12 master-data routes mounted with per-op RBAC (overtime-rules read excludes agent); seed has the canonical leave types, attendance codes, and default OT rule.</done>
</task>

</tasks>

<verification>
- `cd backend && go build ./... && go vet ./... && gofmt -l internal cmd` clean.
- POST /overtime-rules with min_minutes=20 → 422 RULE_VIOLATION; DELETE /leave-types/{id} → 204.
</verification>

<success_criteria>
ORG-04 endpoints implemented end-to-end; min_minutes rule + dup/soft-delete behavior correct; per-op RBAC matches spec; seed has the canonical master-data rows.
</success_criteria>

<output>
Create `.planning/phases/03-e2-org-master-data/03-04-SUMMARY.md` noting Deps field added, route paths, seeded master-data ids, and the final list of ORG route groups in server.go (so 03-05 contract tests and 03-06 E2E know the full surface).
</output>
