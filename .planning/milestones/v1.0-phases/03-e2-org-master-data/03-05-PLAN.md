---
phase: 03-e2-org-master-data
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - backend/internal/handler/org/companies_handler_test.go
  - backend/internal/handler/org/serviceline_handler_test.go
  - backend/internal/handler/org/masterdata_handler_test.go
autonomous: true
requirements: [ORG-01, ORG-02, ORG-03, ORG-04]

must_haves:
  truths:
    - "Each E2 org/master endpoint has a Go contract test asserting the EXACT JSON field names, types, and status codes from the OpenAPI spec — the drift gate replacing server codegen."
    - "Tests cover: 200/201/204 happy shapes, 404 not-found, 409 conflict codes (CONFLICT/SERVICE_LINE_IN_USE/POSITION_IN_USE), 422 RULE_VIOLATION (min_minutes), 400 GEOFENCE_RADIUS_INVALID, 403 RBAC (super_admin-only service-line create rejects hr_admin), and PageEnvelope cursor shape."
    - "`go test ./internal/handler/org/...` passes."
  artifacts:
    - path: "backend/internal/handler/org/companies_handler_test.go"
      provides: "company+site contract tests"
      min_lines: 150
    - path: "backend/internal/handler/org/masterdata_handler_test.go"
      provides: "leave-type/attendance-code/overtime-rule contract tests"
      min_lines: 150
  key_links:
    - from: "org handler tests"
      to: "org Service + fakeRepo"
      via: "httptest + auth.WithPrincipal, no DB"
      pattern: "httptest"
---

<objective>
Write Go contract tests for every E2 org/master endpoint, asserting the exact response shapes and status/error codes against the OpenAPI spec. Copy the foundations contract-test pattern (httptest + real Service wired to in-memory fakeRepo + principal injection).

Purpose: This is the drift gate that replaces server-side OpenAPI codegen (oapi-codegen can't parse 3.1). Without it, BE responses can silently diverge from the FE-generated client.
Output: Three *_test.go files under internal/handler/org covering all ~28 endpoints' shapes + error/RBAC cases.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/reference/backend-build-conventions.md
@.planning/phases/03-e2-org-master-data/03-02-SUMMARY.md
@.planning/phases/03-e2-org-master-data/03-03-SUMMARY.md
@.planning/phases/03-e2-org-master-data/03-04-SUMMARY.md

<interfaces>
COPY this pattern verbatim:
@backend/internal/handler/foundations/handler_test.go   # fakeTx, fakeRepo, httptest router, principal injection, JSON field assertions

Key mechanics from that file:
- type fakeTx (only Exec is a no-op; rest panic) — reuse for the service's InTx+audit.Record path.
- a fakeTxRunner implementing InTx(ctx, fn) { return fn(&fakeTx{}) }.
- a fakeRepo struct implementing the service's Repository port with in-memory slices/maps; toggle error returns to exercise 404/409.
- build a chi router with rbac.RequireRole wrapping routes, inject principal via auth.WithPrincipal on the request context (request a specific role to test 403).
- assert: w.Code == expected; decode body into map[string]any; assert exact keys + types (e.g. body["data"].([]any), body["next_cursor"], body["has_more"]; for an item assert "geofence_active" present and boolean; "status" == "ACTIVE").
</interfaces>

# Contract being asserted:
@docs/api/E2-identity/openapi.yaml   # response examples are the source of truth for field names + status codes (see line ranges in 03-02..04 plans)
@docs/api/CONVENTIONS.md             # §11 ErrorEnvelope {error:{code,message,fields,request_id}}, §8 PageEnvelope {data,next_cursor,has_more}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Companies + sites contract tests</name>
  <read_first>
    - backend/internal/handler/foundations/handler_test.go (full pattern)
    - backend/internal/handler/org/companies_handler.go + companies_dto.go + service/org/companies_service.go (the code under test)
    - docs/api/E2-identity/openapi.yaml 1202–1698 + ClientCompany/Site schemas
  </read_first>
  <action>
Create `backend/internal/handler/org/companies_handler_test.go` (package org_test). Build a fakeCompanyRepo + fakeTxRunner and the real org Service + Handler. Table-driven tests:
- listClientCompanies 200: body has data[], next_cursor, has_more; item has keys id,name,address,leader_scope,status,has_leader,site_count,active_placement_count and status=="ACTIVE".
- getClientCompany 200 + 404 (fakeRepo returns domain.ErrNotFound).
- createClientCompany 201: Location header set; response is a ClientCompany; (assert site auto-provision is a service concern — assert SiteCount==1 in response if repo fake records it).
- createClientCompany 409 on duplicate (fakeRepo returns a unique-violation pgconn error → service maps to CONFLICT); assert body.error.code=="CONFLICT".
- updateClientCompany 200; deactivateClientCompany 200 + 409 already-inactive; reactivateClientCompany 200 + 409 already-active.
- listSites 200: item has geo (object or null), geofence_radius_m (int), geofence_active (bool, server-derived), is_primary (bool), status UPPERCASE.
- createSite 201; createSite 400 GEOFENCE_RADIUS_INVALID when geofence_radius_m=5000 → assert w.Code==400 && body.error.code=="GEOFENCE_RADIUS_INVALID".
- updateSite 200; updateSite with is_primary=true → assert response is_primary==true.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handler/org/ -run 'Compan|Site' -count=1 2>&1 | tail -5</automated>
  </verify>
  <done>Company+site contract tests compile and pass; geofence_active asserted as derived bool; GEOFENCE_RADIUS_INVALID is 400; dup is 409 CONFLICT; status UPPERCASE; PageEnvelope shape asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Service-lines + positions contract tests</name>
  <read_first>
    - backend/internal/handler/org/serviceline_handler.go + serviceline_dto.go + service/org/serviceline_service.go
    - docs/api/E2-identity/openapi.yaml 1704–2034 + ServiceLine/Position schemas
  </read_first>
  <action>
Create `backend/internal/handler/org/serviceline_handler_test.go`. fakeServiceLineRepo + real service/handler. Tests:
- listServiceLines 200: item keys id,name,status,position_count; status UPPERCASE.
- createServiceLine 201 (principal super_admin); createServiceLine 403 when principal is hr_admin (route RequireRole(super_admin)) — wire the route group with rbac.RequireRole(auth.RoleSuperAdmin) in the test router and inject hr_admin principal → assert 403.
- updateServiceLine 200; discontinueServiceLine 200; discontinueServiceLine 409 SERVICE_LINE_IN_USE when fakeRepo.CountActivePositionsForLine>0 → assert body.error.code.
- listPositionsInServiceLine 200: item keys id,service_line_id,name,alias,status.
- createPosition 201; createPosition 409 POSITION_IN_USE on duplicate; updatePosition 200; softDeletePosition 204 (assert w.Code==204 and empty body); softDeletePosition 404 not-found.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handler/org/ -run 'ServiceLine|Position' -count=1 2>&1 | tail -5</automated>
  </verify>
  <done>Service-line/position tests pass; super_admin-only create returns 403 for hr_admin; SERVICE_LINE_IN_USE/POSITION_IN_USE codes asserted; soft-delete 204; position_count present.</done>
</task>

<task type="auto">
  <name>Task 3: Master-data contract tests + full suite green</name>
  <read_first>
    - backend/internal/handler/org/masterdata_handler.go + masterdata_dto.go + service/org/masterdata_service.go
    - docs/api/E2-identity/openapi.yaml 2040–2509 + LeaveType/AttendanceCode/OvertimeRule schemas
  </read_first>
  <action>
Create `backend/internal/handler/org/masterdata_handler_test.go`. fakeMasterDataRepo + real service/handler. Tests per entity:
- listLeaveTypes 200: item keys id,name,code,description,default_annual_quota(number),is_annual(bool),requires_document(bool),color,status.
- listAttendanceCodes 200: item keys incl is_workday,is_paid,is_billable,needs_verification (all bool), color, status.
- listOvertimeRules 200: item keys incl service_line_id (null OR string), weekday_rate/restday_rate/holiday_rate (numbers, assert 1.5 round-trips), min_minutes(number), max_minutes_per_day(number), pre_approval_required(bool).
- create<Entity> 201 + 409 dup CONFLICT for each.
- createOvertimeRule 422 RULE_VIOLATION when min_minutes=20 → assert body.error.code=="RULE_VIOLATION" and body.error.fields.min_minutes present.
- update<Entity> 200; softDelete<Entity> 204 + 404 for each.

Then run the FULL org suite to confirm everything is green together.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handler/org/... -count=1 2>&1 | tail -8</automated>
  </verify>
  <done>Master-data tests pass; min_minutes<30 → 422 RULE_VIOLATION with field error; rates round-trip as numbers; nullable service_line_id; soft-delete 204; the entire `go test ./internal/handler/org/...` suite is green.</done>
</task>

</tasks>

<verification>
- `cd backend && go test ./... -count=1` passes (org contract tests + all prior).
- `go vet ./... && gofmt -l internal` clean.
</verification>

<success_criteria>
Every FE-used E2 org/master endpoint has a contract test asserting exact shape + status/error codes; the suite is green; drift between BE and the OpenAPI-generated FE client is now gated.
</success_criteria>

<output>
Create `.planning/phases/03-e2-org-master-data/03-05-SUMMARY.md` listing the endpoints covered and any spec/code mismatches found+fixed (raise as a note if the spec and code disagreed — spec wins).
</output>
