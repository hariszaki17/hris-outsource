---
phase: 03-e2-org-master-data
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/internal/domain/org.go
  - backend/internal/repository/org/companies_repo.go
  - backend/internal/service/org/companies_service.go
  - backend/internal/handler/org/companies_dto.go
  - backend/internal/handler/org/companies_handler.go
  - backend/internal/server/server.go
  - backend/cmd/api/main.go
  - backend/cmd/seed/seed.go
autonomous: true
requirements: [ORG-01, ORG-02]

must_haves:
  truths:
    - "HR can list/get/create/update client companies and deactivate/reactivate them against the real BE; responses match the ClientCompany schema."
    - "HR can list/create sites for a company and update a site including geofence (geo lat/lng + radius); geofence_active is server-derived from geo presence; geofence_radius_m out of 25–1000 returns 400 GEOFENCE_RADIUS_INVALID."
    - "Creating a company auto-provisions a primary Main Site (CC-1c); setting a site is_primary=true demotes the old primary atomically."
    - "Seed contains client company 'Plaza Senayan' with id SWP-CMP-0021 and a primary site, so the shift_leader persona scope resolves."
  artifacts:
    - path: "backend/internal/handler/org/companies_handler.go"
      provides: "company + site HTTP handlers"
      min_lines: 120
    - path: "backend/internal/service/org/companies_service.go"
      provides: "company/site business rules + audit + geofence validation"
      min_lines: 120
    - path: "backend/cmd/seed/seed.go"
      provides: "Plaza Senayan SWP-CMP-0021 + primary site seed"
      contains: "SWP-CMP-0021"
  key_links:
    - from: "backend/internal/server/server.go"
      to: "org companies handler"
      via: "r.Get(\"/client-companies\", ...) under authenticated group"
      pattern: "client-companies"
    - from: "companies_service.go"
      to: "audit.Record"
      via: "audit on every write inside txm.InTx"
      pattern: "audit\\.Record"
---

<objective>
Implement the client-companies + client-sites slice (ORG-01, ORG-02): domain types, repository over the 03-01 sqlc methods, service (business rules, geofence validation, audit), hand-written chi handlers, routes in server.go, deps wiring in cmd/api, and a seed extension adding Plaza Senayan (SWP-CMP-0021) + its primary site.

Purpose: Companies/sites are the spine of E2 and the shift_leader scope target. Geofence persistence (E5 dependency) and the auto-primary-site invariant live here.
Output: A new `org` package trio (domain/repo/service/handler) covering 8 endpoints, mounted under /api/v1, plus seed data.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/reference/backend-build-conventions.md
@.planning/phases/03-e2-org-master-data/03-CONTEXT.md
@.planning/phases/03-e2-org-master-data/03-01-SUMMARY.md   # sqlc method names from wave 1

<interfaces>
<!-- COPY the E1 foundations slice shape exactly. -->
Reference slice files (read these, mirror their structure):
- backend/internal/service/foundations/service.go  — Repository port interface + Service + txm.InTx + audit.Record + pageCursor{CreatedAt,ID}
- backend/internal/repository/foundations/repository.go — reads on pool, writes via r.q.WithTx(tx), mapErr(pgx.ErrNoRows→domain.ErrNotFound)
- backend/internal/handler/foundations/handler.go — decodeJSON(DisallowUnknownFields), httpx.WriteJSON/WriteError, httpx.PageResponse, DecodeCursor
- backend/internal/handler/foundations/dto.go — request/response structs + to<X>Response mappers
- backend/internal/server/server.go lines 73–94 — RBAC group + idempotency + chi ':' action routes
- backend/cmd/api/main.go lines 25,86,93 — repo→svc→handler construction + Deps field

apperr codes (from backend/internal/platform/apperr/apperr.go): apperr.Invalid(fields) (422), apperr.Conflict(code) (409), apperr.NotFound() (404), apperr.Rule(code, fields) (422). For the 400 GEOFENCE_RADIUS_INVALID use a 400 BadRequest variant — check apperr for the 400 constructor (apperr.BadRequest or similar); if only 422 exists, the spec example shows 400 for radius — match the spec status code.

RBAC (backend/internal/platform/rbac/rbac.go): rbac.RequireRole(roles...). Roles: auth.RoleSuperAdmin, auth.RoleHRAdmin, auth.RoleShiftLeader, auth.RoleAgent. Scope guard rbac.GuardCompany available for company_or_global reads.

Status mapping (Phase-2 decision): DB lowercase 'active'/'inactive'; DTO uppercases to ACTIVE/INACTIVE. Service lowercases incoming status filter.
</interfaces>

# Contract — match EXACTLY:
@docs/api/E2-identity/openapi.yaml   # /client-companies 1202–1477; /sites 1483–1698; ClientCompany 3111, ClientCompanyWriteRequest 3156, Site 3172, SiteWriteRequest 3220
@docs/api/CONVENTIONS.md             # §7 status, §8 cursor, §9 filtering, §11 errors, §16.1 audit, §17 RBAC, §18 pickers
@docs/epics/E2-identity/prds/client-company-directory.md   # CC-1..CC-5 rules + Gherkin
@docs/epics/E2-identity/prds/client-sites-geofence.md      # ST-1..ST-8 rules + geofence Gherkin
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain types + repository for companies & sites</name>
  <read_first>
    - backend/internal/domain/foundations.go (domain type style)
    - backend/internal/repository/foundations/repository.go (full file — mapping + WithTx + mapErr + null helpers to copy)
    - backend/internal/repository/sqlc/client_companies.sql.go + client_sites.sql.go (generated param/row names from 03-01)
    - docs/api/E2-identity/openapi.yaml 3111–3242 (ClientCompany + Site field names)
  </read_first>
  <action>
Create `backend/internal/domain/org.go` with structs `ClientCompany` (ID, Name, Address, LeaderScope, NPWP *string, PICName *string, Phone *string, Email *string, Status string, HasLeader bool, SiteCount int, ActivePlacementCount int, CreatedAt, UpdatedAt) and `Site` (ID, ClientCompanyID, Name, Code *string, Address, GeoLat *float64, GeoLng *float64, GeofenceRadiusM int, IsPrimary bool, PICName *string, Phone *string, Status string, ActivePlacementCount int, CreatedAt, UpdatedAt), plus filter structs `CompanyFilter` (Q, Status, ServiceLine, HasLeader, Limit, CursorCreatedAt, CursorID) and `SiteFilter` (Status, Limit, CursorCreatedAt, CursorID).

Create `backend/internal/repository/org/companies_repo.go` mirroring foundations/repository.go:
- struct Repository{pool *db.Pool; q *sqlcgen.Queries}; New(pool); compile-time `var _ svc.CompanyRepository = (*Repository)(nil)`.
- Implement: ListClientCompanies, GetCompanyByID, CreateCompany(tx, params), UpdateCompany(tx, params), SetCompanyStatus(tx,id,status), CountActiveSitesForCompany(id) → set SiteCount on list/get; ListSitesForCompany, GetSiteByID, CreateSite(tx, params), UpdateSite(tx, params), DemoteOtherPrimaries(tx, companyID, exceptID), SetSitePrimary(tx, id).
- Phase 3 has no placements/assignments: set HasLeader=false and ActivePlacementCount=0 in the mappers (TODO(Phase-5) comments). SiteCount comes from CountActiveSitesForCompany.
- Copy the mapErr / nullStr / derefStr helpers (or import a shared one if 03-03/03-04 also need them — keep per-package for now to avoid coupling).
  </action>
  <verify>
    <automated>cd backend && go build ./internal/domain/... ./internal/repository/org/... 2>&1 | head; test -f internal/domain/org.go && test -f internal/repository/org/companies_repo.go && grep -q "svc.CompanyRepository" internal/repository/org/companies_repo.go</automated>
  </verify>
  <done>domain/org.go and repository/org/companies_repo.go compile; repository satisfies the service port; HasLeader/ActivePlacementCount hardcoded with Phase-5 TODOs; SiteCount wired to CountActiveSitesForCompany.</done>
</task>

<task type="auto">
  <name>Task 2: Service (rules + geofence + audit) and handlers/DTOs for companies & sites</name>
  <read_first>
    - backend/internal/service/foundations/service.go (Service shape, InTx+audit, pageCursor, ClampLimit, status lowercasing)
    - backend/internal/handler/foundations/handler.go + dto.go (handler + DTO mapping + cursor decode)
    - docs/api/E2-identity/openapi.yaml 1202–1698 (all 8 ops: statuses, error codes, examples)
    - docs/epics/E2-identity/prds/client-company-directory.md (CC-2 duplicate name/npwp→409; CC-5 deactivate active-placement guard) + client-sites-geofence.md (ST-2 dup name→409; ST-8 geofence_active derived; radius 25–1000)
  </read_first>
  <action>
Create `backend/internal/service/org/companies_service.go`:
- Define ports `CompanyRepository` (the methods from Task 1) and `TxRunner` (InTx). Service{repo, txm, now}.
- ListClientCompanies(ctx, filter) → []domain.ClientCompany, *nextCursor — ClampLimit, fetch limit+1, lowercase status filter, encode cursor from last (CreatedAt,ID). Mirror foundations ListUsers exactly.
- GetClientCompany(ctx,id) → ErrNotFound→apperr.NotFound().
- CreateClientCompany(ctx, params): default leader_scope='company' if empty; in InTx → CreateCompany, then auto-provision primary Main Site (CC-1c): CreateSite with name "Main Site", is_primary=true, geofence_radius_m=100, no geo; audit.Record action create entity_type "client_company". On duplicate name/npwp the unique index errors → map to apperr.Conflict("CONFLICT"). Return company with SiteCount=1.
- UpdateClientCompany(ctx,id,params): load current (404 if missing); InTx UpdateCompany + audit before/after. Duplicate→409.
- DeactivateClientCompany(ctx,id,reason,force): load current (404); 409 CONFLICT if already inactive; CC-5: active-placement guard — Phase 3 has no placements so active_placement_count=0, guard never trips (add TODO(Phase-5) to return apperr.Conflict("COMPANY_HAS_ACTIVE_PLACEMENTS") when count>0 && !force). InTx SetCompanyStatus 'inactive' + audit.
- ReactivateClientCompany(ctx,id): 404; 409 if already active; InTx SetCompanyStatus 'active' + audit.
- ListSites(ctx, companyID, filter): verify company exists (404); list (primary first ordering from query) + cursor.
- CreateSite(ctx, companyID, params): validate geofence_radius_m in 25..1000 else GEOFENCE_RADIUS_INVALID (400 status per spec example); derive nothing for geo (store lat/lng if both present, else null). Unique name within company → 409. InTx CreateSite + audit. If is_primary requested, DemoteOtherPrimaries first.
- UpdateSite(ctx, siteId, params): load (404); validate radius if provided; if is_primary=true → DemoteOtherPrimaries(companyID, siteId) then SetSitePrimary in same tx; partial update; audit; dup name→409.

Create `backend/internal/handler/org/companies_dto.go`: request structs (createCompanyRequest, updateCompanyRequest, createSiteRequest, updateSiteRequest, reasonRequest) using pointers for optional fields; response structs (clientCompanyResponse, siteResponse) with snake_case json tags EXACTLY per schema (note: `geo` is nested object {lat,lng} or null; `geofence_active` is server-derived = GeoLat!=nil && GeoLng!=nil; status UPPERCASED). Mapper funcs toClientCompanyResponse / toSiteResponse.

Create `backend/internal/handler/org/companies_handler.go`: Handler{svc}; methods ListClientCompanies, GetClientCompany, CreateClientCompany, UpdateClientCompany, DeactivateClientCompany (reads `force` query bool), ReactivateClientCompany, ListSites, CreateSite, UpdateSite. Use chi.URLParam("client_company_id") / ("site_id"). Decode cursor via local pageCursor{CreatedAt `json:"c"`, ID `json:"i"`}. Create returns 201 + Location header. Copy helpers (decodeJSON, queryStringPtr, parseLimit) from foundations/handler.go (or a shared file in handler/org).
  </action>
  <verify>
    <automated>cd backend && go build ./internal/service/org/... ./internal/handler/org/... 2>&1 | head; grep -q "GEOFENCE_RADIUS_INVALID" internal/service/org/companies_service.go && grep -q "geofence_active" internal/handler/org/companies_dto.go && grep -q "Main Site" internal/service/org/companies_service.go && grep -q "audit.Record" internal/service/org/companies_service.go</automated>
  </verify>
  <done>org service + handlers + DTOs compile; geofence radius validation returns GEOFENCE_RADIUS_INVALID; auto Main Site on company create; geofence_active derived in DTO; every write calls audit.Record; status uppercased in responses.</done>
</task>

<task type="auto">
  <name>Task 3: Routes (server.go — COORDINATION POINT), deps wiring, seed Plaza Senayan</name>
  <read_first>
    - backend/internal/server/server.go (FULL — Deps struct + the authenticated RBAC group)
    - backend/cmd/api/main.go (repo→svc→handler construction + Deps literal)
    - backend/cmd/seed/seed.go (FULL — Seed function structure, sequential idempotent skip-if-exists inserts)
    - docs/api/E2-identity/openapi.yaml x-rbac per op (companies writes hr_admin+super_admin scope global; reads add shift_leader scope company_or_global; sites same)
  </read_first>
  <action>
COORDINATION POINT — server.go is also edited by 03-03 and 03-04. To minimize conflicts: add a SINGLE new dedicated route group block for the ORG companies+sites routes, placed immediately after the E1 foundations group (after line 94's closing). Do NOT modify the foundations group. 03-03 and 03-04 will append THEIR OWN separate group blocks after yours. Each group opens its own `r.Group(func(r chi.Router){ r.Use(rbac.RequireRole(...)); ... })`.

In server.go:
- Add `OrgCompanies *orghttp.Handler` to Deps (import `orghttp "github.com/hariszaki17/hris-outsource/backend/internal/handler/org"`). NOTE: 03-03/03-04 may reuse the same handler package or add OrgServiceLines/OrgMasterData — if they share the org package, a single Org field may suffice; since this plan runs first, add `OrgCompanies` and document in SUMMARY so siblings reuse `org` handler or add their own Deps field.
- Routes — reads allow shift_leader (company_or_global), writes are hr_admin+super_admin. Implement as: a reads group `RequireRole(SuperAdmin,HRAdmin,ShiftLeader)` with GET /client-companies, GET /client-companies/{client_company_id}, GET /client-companies/{client_company_id}/sites; and a writes group `RequireRole(SuperAdmin,HRAdmin)` with: `r.With(d.Idempotency.Handler).Post("/client-companies", ...)`, `Patch("/client-companies/{client_company_id}", ...)`, `r.With(d.Idempotency.Handler).Post("/client-companies/{client_company_id}:deactivate", ...)`, `r.With(d.Idempotency.Handler).Post("/client-companies/{client_company_id}:reactivate", ...)`, `r.With(d.Idempotency.Handler).Post("/client-companies/{client_company_id}/sites", ...)`, `Patch("/sites/{site_id}", ...)`.

In cmd/api/main.go: construct `orgCompaniesRepo := orgrepo.New(pool)`, `orgCompaniesSvc := orgsvc.NewService(orgCompaniesRepo, txm)`, `orgCompaniesHandler := orghttp.NewHandler(orgCompaniesSvc)`; add to Deps literal. (Use the same txm/pool already built for foundations.)

In cmd/seed/seed.go: after the personas loop, add an idempotent (skip-if-exists by id) insert of:
- client_companies row id='SWP-CMP-0021', name='Plaza Senayan', address set, leader_scope='company', status='active'. Use a direct INSERT (raw SQL via pool.Exec or a seed-only query) with explicit id 'SWP-CMP-0021' (NOT swp_next_id, because the shift_leader persona references this literal). Guard with `INSERT ... ON CONFLICT (id) DO NOTHING`.
- client_sites row: a primary Main Site for SWP-CMP-0021 (id via swp_next_id('SITE') or a literal 'SWP-SITE-0001'), name='Plaza Senayan Main', is_primary=true, geo_lat=-6.2253, geo_lng=106.7995, geofence_radius_m=100, status='active', ON CONFLICT DO NOTHING.
- Add a couple more companies (e.g. 'Mall Kelapa Gading') so list/pagination E2E has multiple rows.
Run `go build ./...`.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && grep -q "client-companies" internal/server/server.go && grep -q "sites/{site_id}" internal/server/server.go && grep -q "client-companies/{client_company_id}:reactivate" internal/server/server.go && grep -q "SWP-CMP-0021" cmd/seed/seed.go</automated>
  </verify>
  <done>Backend builds and vets clean; all 8 company/site routes mounted in a dedicated ORG group; deps wired in cmd/api; seed inserts Plaza Senayan SWP-CMP-0021 + a primary site idempotently.</done>
</task>

</tasks>

<verification>
- `cd backend && go build ./... && go vet ./... && gofmt -l internal cmd` clean.
- Manual smoke (optional): boot stack, `curl` GET /api/v1/client-companies with hr_admin token returns the seeded companies in PageEnvelope shape; POST a company returns 201 + a Main Site exists via GET /client-companies/{id}/sites.
- Contract tests in 03-05 assert exact shapes; E2E in 03-06 drives the screens.
</verification>

<success_criteria>
ORG-01 + ORG-02 endpoints implemented end-to-end (data layer→handler→route), geofence persists with derived geofence_active, auto Main Site on create, atomic primary reassignment, audit on every write, seed has Plaza Senayan SWP-CMP-0021.
</success_criteria>

<output>
After completion, create `.planning/phases/03-e2-org-master-data/03-02-SUMMARY.md`. CRITICAL: document the exact server.go ORG Deps field name(s) and the position where sibling groups should append, and whether 03-03/03-04 should reuse the `org` handler package or add their own — this is the coordination contract for the parallel wave-2 slices.
</output>
