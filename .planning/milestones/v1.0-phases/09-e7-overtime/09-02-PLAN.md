---
phase: 09-e7-overtime
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - backend/internal/repository/overtime/overtime_repo.go
  - backend/internal/repository/overtime/holiday_repo.go
  - backend/internal/repository/overtime/mapping.go
  - backend/internal/service/overtime/overtime_service.go
  - backend/internal/service/overtime/holiday_service.go
  - backend/internal/service/overtime/ports.go
  - backend/internal/service/overtime/helpers.go
  - backend/internal/handler/overtime/handler.go
  - backend/internal/handler/overtime/overtime_handler.go
  - backend/internal/handler/overtime/holiday_handler.go
  - backend/internal/handler/overtime/dto.go
  - backend/internal/server/server.go
  - backend/cmd/api/main.go
  - backend/cmd/seed/seed.go
autonomous: true
requirements: [OVT-01, OVT-02]
user_setup: []

must_haves:
  truths:
    - "An OT moves PENDING_AGENT_CONFIRM → :confirm → PENDING_L1 → :approve-l1 → PENDING_HR → :approve-final → APPROVED"
    - ":reject (reason required) → REJECTED and :withdraw → WITHDRAWN; acting on a terminal/wrong-state OT returns 409"
    - "A leader approving another company's OT gets 403 OUT_OF_SCOPE; approving their own OT gets 403 SELF_APPROVAL_FORBIDDEN"
    - "Bulk approve/reject returns {succeeded, failed[]} with per-id partial success"
    - "An OT below the rule's min_minutes returns 422 OT_BELOW_MIN with field errors"
    - "Holidays can be created (HOLIDAY_DATE_CLASH on dup date 409), updated, listed, and deleted (HOLIDAY_IN_USE 409 when referenced by APPROVED OT)"
  artifacts:
    - path: "backend/internal/service/overtime/overtime_service.go"
      provides: "OT workflow state machine + bulk partial-success + OT_BELOW_MIN + scope/self guards + day_type classification"
      contains: "SELF_APPROVAL_FORBIDDEN"
    - path: "backend/internal/service/overtime/holiday_service.go"
      provides: "holiday CRUD with clash/in-use guards"
      contains: "HOLIDAY_IN_USE"
    - path: "backend/internal/handler/overtime/overtime_handler.go"
      provides: "chi handlers for the 9 overtime endpoints"
      contains: "BulkApprove"
    - path: "backend/internal/server/server.go"
      provides: "E7 routes mounted under RequireRole"
      contains: "overtime:bulk-approve"
    - path: "backend/cmd/seed/seed.go"
      provides: "seeded OT records in each workflow state + holidays"
      contains: "seedOvertime"
  key_links:
    - from: "backend/internal/handler/overtime/handler.go"
      to: "GET /overtime/{id} response"
      via: "httpx data-envelope wrap (FE unwraps {data})"
      pattern: "dataResponse"
    - from: "backend/internal/service/overtime/overtime_service.go"
      to: "overtime_rules (E2)"
      via: "rule lookup for OT_BELOW_MIN + reference multiplier"
      pattern: "min_minutes|MinMinutes"
    - from: "backend/internal/service/overtime/overtime_service.go"
      to: "holidays calendar"
      via: "day_type classification (GetHolidayForDate)"
      pattern: "GetHolidayForDate"
---

<objective>
Implement the E7 overtime service + handlers: the two-level OT workflow state machine
(:confirm / :approve-l1 / :approve-final / :reject / :withdraw) with *ForUpdate guards +
409s, bulk approve/reject (partial success), OT_BELOW_MIN (422 using existing overtime_rules),
day_type classification from schedule + holiday calendar, holiday CRUD (HOLIDAY_DATE_CLASH /
HOLIDAY_IN_USE), GuardCompany scope + SELF_APPROVAL_FORBIDDEN, audit-in-tx + decision trail +
notify stub, and the seed. Mirrors Phase-8 leave (two-level approval) and Phase-7 attendance
(bulk partial-success) EXACTLY. This plan OWNS the shared server.go / main.go / seed.go edits.

Purpose: Make every E7 workflow + holiday operation work against the real BE, matching
`docs/api/E7-overtime/openapi.yaml` byte-for-byte.
Output: repository + service + handler packages, routes, wiring, seed fixtures.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-e7-overtime/09-CONTEXT.md
@.planning/reference/backend-build-conventions.md
@docs/api/E7-overtime/openapi.yaml
@docs/api/CONVENTIONS.md

<interfaces>
<!-- The exact analogs to copy. Phase-8 leave = closest mirror (two-level approval). -->

Phase-8 leave slice (COPY ITS SHAPE):
  backend/internal/service/leave/leave_service.go  (List scope-forcing, lockRequest ForUpdate, stateConflict, guardSelf, ApproveL1/ApproveFinal/Reject in InTx + audit-in-tx)
  backend/internal/service/leave/ports.go          (LeaveRepository / SchedulePort / TxRunner / Clock interfaces)
  backend/internal/service/leave/helpers.go         (clampLimit, encode/decode cursor, actorEmployeeID/actorRole, deref)
  backend/internal/handler/leave/handler.go         (decodeJSON/decodeOptionalJSON/csvParam/parseDateParam helpers)
  backend/internal/handler/leave/leave_handler.go   (chi handler method shape)
  backend/internal/repository/leave/leave_repo.go + mapping.go

Phase-7 attendance bulk partial-success (COPY ITS SHAPE):
  backend/internal/service/attendance/  — BulkVerify/BulkReject loop-single + apperr.As → {succeeded, failed} 200/422; idempotency owned by the router middleware.

Platform kernel:
  rbac.GuardCompany(ctx, companyID) → 403 OUT_OF_SCOPE ; rbac.RequireRole(...)
  apperr.Conflict() / apperr.ConflictWithDetails(code, details) (409) ; apperr.Rule(code) (422 default) ; apperr.Error{HTTPStatus:403, Code:"SELF_APPROVAL_FORBIDDEN"} struct literal (bypass statusForCode, like GEOFENCE_RADIUS_INVALID decision [03-02])
  audit.Record(ctx, tx, ...) in-tx ; jobs.Client.EnqueueTx notify stub ; httpx.WriteJSON / WriteError / PageResponse / EncodeCursor / DecodeCursor ; d.Idempotency.Handler

server.go LEAVE slice (mount AFTER "LEAVE slice end (08-02). Phase 8+ appends after this line."):
  // company_or_global group (RequireRole super_admin, hr_admin, shift_leader): reads + :confirm? + :approve-l1 + :reject + bulk
  // global group (RequireRole super_admin, hr_admin): :approve-final + holidays POST/PATCH/DELETE
  Action endpoints wrapped r.With(d.Idempotency.Handler).Post(...)

FE response-shape anchors (overtime-approvals-screen.tsx + overtime-rules-screen.tsx):
  - List response is read as `query.data?.data as { data?: Overtime[]; has_more?; next_cursor? }` → handler returns {data:[...], next_cursor, has_more} (httpx.PageResponse) at the TOP level (NOT double-wrapped) — the FE `query.data.data` is Orval's axios .data. Confirm the page handler writes PageResponse directly (matches Phase-8 list).
  - GET /overtime/{id} → FE detail unwraps `{data:<Overtime>}` (recurring Phase-8 fix). Handler MUST wrap single-object GET in dataResponse({...}) (httpx data-envelope) — same as every epic's detail handler. 09-04 applies the FE bare-fallback if needed.
  - Approve row → useApproveOvertimeFinal (HR) / useApproveOvertimeL1 (SL), body `{}` (empty/optional note). Reject → `{ reason }`. Bulk → `{ ids, note? }` / `{ ids, reason }`.
  - Holiday create/update body = HolidayWriteRequest {name, date, category, recurring} (applicable_service_lines optional). Delete confirm disabled when `in_use_by_overtime` true.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repository (overtime + holidays) + ports + service state machine</name>
  <files>backend/internal/repository/overtime/overtime_repo.go, backend/internal/repository/overtime/holiday_repo.go, backend/internal/repository/overtime/mapping.go, backend/internal/service/overtime/ports.go, backend/internal/service/overtime/helpers.go, backend/internal/service/overtime/overtime_service.go</files>
  <read_first>
    - backend/internal/repository/leave/leave_repo.go + mapping.go (port-impl over sqlc + row→domain mapping + pgx.ErrNoRows→domain.ErrNotFound + dates pgtype.Date↔time.Time + jsonb/text[] quirks decision [08-01])
    - backend/internal/service/leave/leave_service.go (lockRequest ForUpdate → stateConflict; ApproveL1/ApproveFinal/Reject InTx; GuardCompany; guardSelf; audit-in-tx; notify stub)
    - backend/internal/service/leave/ports.go (interface ports)
    - backend/db/queries/org/ overtime_rules queries (the rule source: min_minutes + weekday/restday/holiday_rate by day_type for OT_BELOW_MIN + reference multiplier)
    - docs/api/E7-overtime/openapi.yaml the 5 transition ops (confirm/approve-l1/approve-final/reject/withdraw) + their 403/409/422 semantics
  </read_first>
  <action>
    Create `backend/internal/repository/overtime/overtime_repo.go`, `holiday_repo.go`, `mapping.go` implementing the service ports over the 09-01 sqlc queries. Map rows→`domain/overtime` types (pgtype.Date→time.Time, numeric→*float64 for reference_multiplier, text[]→[]string for applicable_service_lines, pgx.ErrNoRows→domain.ErrNotFound). Writes take `pgx.Tx`. Add a rule-lookup repo method `FindOvertimeRule(ctx, dayType, serviceLineID *string) (rule, error)` reusing the EXISTING `db/queries/org` overtime_rules queries (do NOT add new rule CRUD) — returns min_minutes + the per-tier rate for the multiplier.

    Create `backend/internal/service/overtime/ports.go`: `OvertimeRepository`, `HolidayRepository`, `RuleRepository` (the overtime_rules read port), `SchedulePort` (read scheduled shift for day_type WORKDAY/RESTDAY classification — reuse the scheduling repo like leave's SchedulePort), `TxRunner`, `Clock` interfaces.

    Create `backend/internal/service/overtime/helpers.go`: clampLimit, encode/decode overtime cursor (created_at DESC, id) + holiday cursor (holiday_date ASC, id), actorEmployeeID/actorRole(ctx), deref, and `stateConflict(status)` → `apperr.Conflict()` with a message naming the current state (mirror leave stateConflict; the openapi maps wrong-state to 409 CONFLICT).

    Create `backend/internal/service/overtime/overtime_service.go`:
    - `List(ctx, filter)` — leader scope forced to own company (explicit cross-company company_id → 403 OUT_OF_SCOPE; mirror leave List); agent sees own (employee_id forced); HR/super global. Server filters out-of-scope rows; cursor page. Each row carries the computed `calculation` block (worked/counted/min_threshold/skipped + tier_breakdown built from day_type + reference_multiplier + overtime_rule_id; `supersedes` null for single-tier). Approvals omitted on list per openapi.
    - `Get(ctx, id)` — GuardCompany (cross-scope → 404 hide), attach Approvals timeline (ListOvertimeApprovals) + recomputed calculation.
    - `Confirm(ctx, id, note)` — InTx: lock ForUpdate; require status == PENDING_AGENT_CONFIRM else stateConflict (409); only the OT's own agent may confirm (else 403 — but web confirm flow is HR/leader-triggered on seeded candidates per CONTEXT; honor openapi x-rbac agent-scope by checking actor == employee, return apperr.Forbidden() with code if mismatch — document the seam). Transition → PENDING_L1. Append optional note. audit.Record + notify stub. Return updated OT.
    - `ApproveL1(ctx, id, note)` — InTx: lock; require PENDING_L1 else 409; `rbac.GuardCompany(ctx, rec.CompanyID)` → 403 OUT_OF_SCOPE; `guardSelf` → if actor's employee_id == rec.EmployeeID return `apperr.Error{HTTPStatus:403, Code:"SELF_APPROVAL_FORBIDDEN"}` (struct literal, bypasses statusForCode). Transition → PENDING_HR. InsertOvertimeApproval(level=1, decision=APPROVED, approver, note). audit + notify. Return OT with fresh calculation.
    - `ApproveFinal(ctx, id, note, isOverride)` — InTx: lock; require PENDING_HR (or, when isOverride, allow PENDING_L1 per openapi OA-8 bypass) else 409; if isOverride && note=="" → `apperr.Rule("OVERRIDE_REASON_REQUIRED")` (422). Transition → APPROVED. InsertOvertimeApproval(level=2, decision=APPROVED or OVERRIDE_APPROVED). audit + notify.
    - `Reject(ctx, id, reason)` — reason required (handler validates minLength 5 / else 400); InTx: lock; require PENDING_L1 or PENDING_HR else 409; GuardCompany; transition → REJECTED. InsertOvertimeApproval(decision=REJECTED, reason). audit + notify.
    - `Withdraw(ctx, id)` — InTx: lock; require PENDING_AGENT_CONFIRM or PENDING_L1 else 409 (per openapi: not withdrawable when APPROVED/REJECTED/WITHDRAWN); transition → WITHDRAWN. audit + notify. (No body, 204.)
    - `BulkApprove(ctx, ids, note)` / `BulkReject(ctx, ids, reason)` — loop each id calling the single ApproveL1/ApproveFinal (by caller role) / Reject in its OWN tx (Phase-7 per-item own-tx atomicity); collect `{succeeded:[], failed:[{id, error}]}` via apperr.As. Return 200 if ≥1 succeeded else the handler returns 422 (Phase-7 bulk semantics). SELF_APPROVAL_FORBIDDEN / OUT_OF_SCOPE / state-409 land in failed[].
    - Day_type classification helper `classifyDayType(ctx, employeeID, workDate, serviceLineID)`: GetHolidayForDate → HOLIDAY (set holiday_id) ; else SchedulePort scheduled-shift exists on work_date → WORKDAY ; else RESTDAY. Apply TierPrecedence. Used at record/seed time (CONTEXT discretion: compute at record time). Also `enforceMinMinutes`: look up the rule (FindOvertimeRule by day_type+line); if counted_minutes < rule.min_minutes → `apperr.Rule("OT_BELOW_MIN").WithFields(map[string]string{"counted_minutes": "...", "min_minutes": "..."})` (422 with field errors, INV-5). Wire min check into Confirm and into the seed validation path; the openapi also returns OT_BELOW_MIN on the create path (out of web scope) — expose enforceMinMinutes as an exported seam for 09-03 contract tests.
  </action>
  <verify>
    <automated>cd backend && grep -q "SELF_APPROVAL_FORBIDDEN" internal/service/overtime/overtime_service.go && grep -q "OT_BELOW_MIN" internal/service/overtime/overtime_service.go && grep -q "func.*ApproveL1" internal/service/overtime/overtime_service.go && grep -q "func.*ApproveFinal" internal/service/overtime/overtime_service.go && grep -q "func.*Withdraw" internal/service/overtime/overtime_service.go && grep -qiE "GuardCompany" internal/service/overtime/overtime_service.go && grep -q "GetHolidayForDate" internal/service/overtime/*.go && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `overtime_service.go` contains `SELF_APPROVAL_FORBIDDEN`, `OT_BELOW_MIN`, `ApproveL1`, `ApproveFinal`, `Reject`, `Withdraw`, `Confirm`, and a bulk method producing `{succeeded, failed}`.
    - Service calls `rbac.GuardCompany` and `GetHolidayForDate` (day_type classification).
    - Reference multiplier read from overtime_rules but NEVER applied to a money figure (INV-2).
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <done>The two-level state machine, scope+self guards, OT_BELOW_MIN, bulk partial-success, and day_type classification are implemented and compile.</done>
</task>

<task type="auto">
  <name>Task 2: Holiday service + handlers (overtime + holidays) + DTO</name>
  <files>backend/internal/service/overtime/holiday_service.go, backend/internal/handler/overtime/handler.go, backend/internal/handler/overtime/overtime_handler.go, backend/internal/handler/overtime/holiday_handler.go, backend/internal/handler/overtime/dto.go</files>
  <read_first>
    - backend/internal/service/leave/quota_service.go (CRUD-ish service with adjust + audit-in-tx + apperr codes)
    - backend/internal/handler/leave/leave_handler.go + quota_handler.go + dto.go (chi method shape; row→DTO mapping; httpx.WriteJSON/PageResponse/WriteError; dataResponse single-object wrap)
    - backend/internal/handler/overtime/... (this plan's Task 1 service)
    - docs/api/E7-overtime/openapi.yaml /holidays CRUD + schemas.Holiday/HolidayWriteRequest + BulkResult + Overtime DTO (required-nullable fields → pointer WITHOUT omitempty to serialize JSON null, decision [07-02])
    - frontend/apps/web/src/features/e7-overtime/holiday-overlays.tsx + overtime-rules-screen.tsx (the consumed Holiday + HolidayPage shapes + in_use_by_overtime)
  </read_first>
  <action>
    Create `backend/internal/service/overtime/holiday_service.go`:
    - `List(ctx, filter)` — cursor page (holiday_date ASC); each Holiday's `in_use_by_overtime` computed via CountOvertimeUsingHoliday > 0.
    - `Get(ctx, id)`.
    - `Create(ctx, req)` — validate name minLength 2/maxLength 120 + date present (else 400/422 field errors); pre-check GetHolidayByDateCategory → if a live holiday exists on (date, category) return `apperr.Conflict()` with code `HOLIDAY_DATE_CLASH`; InsertHoliday (backstop the unique index 23505 → same 409). audit + notify. Return Holiday.
    - `Update(ctx, id, req)` — partial; if date/category changes and clashes → HOLIDAY_DATE_CLASH 409. audit.
    - `Delete(ctx, id)` — if CountOvertimeUsingHoliday(id) > 0 → `apperr.Conflict()` code `HOLIDAY_IN_USE` (409); else SoftDeleteHoliday. audit. (204.)

    Create `backend/internal/handler/overtime/handler.go` (shared helpers: decodeJSON/decodeOptionalJSON/csvParam/parseDateParam/strPtrParam — copy from leave handler.go), `overtime_handler.go`, `holiday_handler.go`, `dto.go`.
    - `dto.go`: `toOvertimeResponse(dom.Overtime)` and `toHolidayResponse(dom.Holiday)` producing the EXACT openapi shapes. Overtime: employee {id,name}, company {id,name}, placement_id, attendance_id (pointer no-omitempty → null), work_date (YYYY-MM-DD), planned_/actual_ times (pointer no-omitempty), cross_midnight, source, status, tier_indicator (= day_type), flagged_no_preapproval, reason (no-omitempty), calculation {worked_minutes, counted_minutes, min_minutes_threshold, skipped_too_short, tier_breakdown:[{tier, minutes, multiplier, overtime_rule_id, supersedes(null)}]}, approvals (only on GET detail), created_at, updated_at. Holiday: id, name, date (map holiday_date), category, recurring, applicable_service_lines, in_use_by_overtime, created_at, updated_at.
    - `overtime_handler.go`: `ListOvertime` (parse cursor/limit/employee_id/company_id/status/status__in(csv)/work_date__gte/lte/tier/source/flagged_no_preapproval → httpx.PageResponse, write {data,next_cursor,has_more} directly), `GetOvertime` (dataResponse wrap → {data:<Overtime>}), `Confirm`, `ApproveL1`, `ApproveFinal` (decode optional {note, is_override}), `Reject` (decode {reason}, validate minLength 5 else 400), `Withdraw` (204), `BulkApprove` (decode {ids, note}; 200 BulkResult if ≥1 ok else 422), `BulkReject` (decode {ids, reason}).
    - `holiday_handler.go`: `ListHolidays`, `GetHoliday`, `CreateHoliday` (201 + Location), `UpdateHoliday` (200), `DeleteHoliday` (204). Errors via httpx.WriteError.
  </action>
  <verify>
    <automated>cd backend && grep -q "HOLIDAY_IN_USE" internal/service/overtime/holiday_service.go && grep -q "HOLIDAY_DATE_CLASH" internal/service/overtime/holiday_service.go && grep -q "func.*BulkApprove" internal/handler/overtime/overtime_handler.go && grep -q "tier_indicator\|TierIndicator" internal/handler/overtime/dto.go && grep -q "in_use_by_overtime\|InUseByOvertime" internal/handler/overtime/dto.go && grep -q "dataResponse\|data\":" internal/handler/overtime/overtime_handler.go && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `holiday_service.go` contains `HOLIDAY_DATE_CLASH` and `HOLIDAY_IN_USE`.
    - `overtime_handler.go` contains `BulkApprove`; `GetOvertime` wraps the single object in a `{data}` envelope.
    - `dto.go` emits `tier_indicator`, `calculation` with `tier_breakdown`, and Holiday `in_use_by_overtime`; required-nullable fields are pointers without omitempty (serialize JSON null).
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <done>Holiday CRUD + clash/in-use guards and all 9 overtime handlers exist; DTOs match openapi byte-for-byte; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Routes (server.go) + wiring (main.go) + seed</name>
  <files>backend/internal/server/server.go, backend/cmd/api/main.go, backend/cmd/seed/seed.go</files>
  <read_first>
    - backend/internal/server/server.go LEAVE slice block (lines ~382-415: two RequireRole groups + r.With(d.Idempotency.Handler).Post action wrapping + the "LEAVE slice end (08-02). Phase 8+ appends after this line." marker) and the Deps struct
    - backend/cmd/api/main.go (where leave repo/service/handler are constructed + assigned to Deps)
    - backend/cmd/seed/seed.go (seedLeave fixture pattern: explicit SWP-* ids, ON CONFLICT DO NOTHING, the SWP-PL-5001..5004 placements + SWP-CMP-0021/0022 + persona employees; TZ note: clearly-in-range Asia/Jakarta dates)
    - docs/api/E7-overtime/openapi.yaml x-rbac per op (confirm: agent; approve-l1/reject/bulk: shift_leader+hr+super company_or_global; approve-final + holidays write: hr+super global; reads + holidays read: all roles)
  </read_first>
  <action>
    Edit `backend/internal/server/server.go`: AFTER the "LEAVE slice end (08-02)" marker add an OVERTIME slice with the same two-group pattern + a reads group:
    - Reads + L1 + reject + bulk + confirm + holidays-read group `RequireRole(super_admin, hr_admin, shift_leader)` (agent reads are mobile — web scope is these three; confirm's agent-only check is enforced in-service):
      `r.Get("/overtime", d.Overtime.ListOvertime)`, `r.Get("/overtime/{id}", d.Overtime.GetOvertime)`, `r.Get("/holidays", d.Overtime.ListHolidays)`,
      `r.With(d.Idempotency.Handler).Post("/overtime/{id}:confirm", d.Overtime.Confirm)`,
      `r.With(d.Idempotency.Handler).Post("/overtime/{id}:approve-l1", d.Overtime.ApproveL1)`,
      `r.With(d.Idempotency.Handler).Post("/overtime/{id}:reject", d.Overtime.Reject)`,
      `r.With(d.Idempotency.Handler).Post("/overtime/{id}:withdraw", d.Overtime.Withdraw)`,
      `r.With(d.Idempotency.Handler).Post("/overtime:bulk-approve", d.Overtime.BulkApprove)`,
      `r.With(d.Idempotency.Handler).Post("/overtime:bulk-reject", d.Overtime.BulkReject)`.
    - Final + holidays-write group `RequireRole(super_admin, hr_admin)`:
      `r.With(d.Idempotency.Handler).Post("/overtime/{id}:approve-final", d.Overtime.ApproveFinal)`,
      `r.With(d.Idempotency.Handler).Post("/holidays", d.Overtime.CreateHoliday)`,
      `r.With(d.Idempotency.Handler).Patch("/holidays/{id}", d.Overtime.UpdateHoliday)`,
      `r.Delete("/holidays/{id}", d.Overtime.DeleteHoliday)`.
    Add `Overtime *overtimehttp.Handler` to the Deps struct + an "OVERTIME slice end (09-02). Phase 9+ appends after this line." marker.

    Edit `backend/cmd/api/main.go`: construct overtime repo (overtime + holiday + rule + schedule ports) → overtime service + holiday service → overtime handler; assign to Deps.Overtime. Mirror the leave construction block.

    Edit `backend/cmd/seed/seed.go`: add `seedOvertime()` + `seedHolidays()` (call after seedLeave). Seed per CONTEXT, using SWP-PL-5001..5004 placements + persona employees + SWP-CMP-0021 (Rudi's company) / SWP-CMP-0022. Explicit ids, ON CONFLICT (id) DO NOTHING. Clearly-in-range Asia/Jakarta dates. Rows:
    - `SWP-OT-30001` PENDING_AGENT_CONFIRM AUTO_DETECTED @ CMP-0021 (confirm target).
    - `SWP-OT-30002` PENDING_L1 @ CMP-0021 (Rudi leader L1 target), day_type WORKDAY.
    - `SWP-OT-30003` PENDING_HR @ CMP-0021 (HR final target).
    - `SWP-OT-30004` PENDING_L1 belonging to Rudi's OWN employee record (SELF_APPROVAL_FORBIDDEN target).
    - `SWP-OT-30005` PENDING_L1 @ CMP-0022 (OUT_OF_SCOPE target for Rudi).
    - `SWP-OT-30006` PENDING_L1 with counted_minutes < 30 / skipped_too_short (OT_BELOW_MIN target).
    - `SWP-OT-30007` APPROVED + `SWP-OT-30008` REJECTED (terminal list-filter rows).
    - `SWP-OT-30009` day_type HOLIDAY referencing the seeded holiday `SWP-HOL-9001` + status APPROVED (HOLIDAY_IN_USE source) ; `SWP-OT-30010` day_type RESTDAY.
    Holidays: `SWP-HOL-9001` (referenced by SWP-OT-30009 APPROVED → in_use, delete blocked) + `SWP-HOL-9002` (free, deletable). Both NATIONAL, clearly-in-range dates.
    Run `make gen && go build ./... && go vet ./...`.
  </action>
  <verify>
    <automated>cd backend && grep -q 'overtime:bulk-approve' internal/server/server.go && grep -q 'overtime/{id}:approve-final' internal/server/server.go && grep -q '"/holidays"' internal/server/server.go && grep -q "d.Overtime" cmd/api/main.go && grep -q "seedOvertime\|seedHolidays" cmd/seed/seed.go && grep -q "SWP-OT-30004" cmd/seed/seed.go && grep -q "SWP-HOL-9001" cmd/seed/seed.go && go build ./... && go vet ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `server.go` registers `/overtime`, `/overtime/{id}`, the 5 transition actions, `:bulk-approve`/`:bulk-reject`, and `/holidays` CRUD under the correct RequireRole groups, with action endpoints idempotency-wrapped.
    - `main.go` constructs and assigns `Deps.Overtime`.
    - `seed.go` seeds `seedOvertime`/`seedHolidays` incl. `SWP-OT-30004` (self), `SWP-OT-30005` (out-of-scope), `SWP-OT-30006` (below-min), `SWP-HOL-9001` (in-use), `SWP-HOL-9002` (free).
    - `go build ./... && go vet ./...` exit 0.
  </acceptance_criteria>
  <done>E7 routes mounted, wiring constructed, seed plants targets for every E2E scenario; build + vet clean.</done>
</task>

</tasks>

<verification>
- `go build ./... && go vet ./... && gofmt -l backend/internal/{service,handler,repository}/overtime` clean.
- `grep -c "Idempotency.Handler" backend/internal/server/server.go` increases (action endpoints wrapped).
- Manual route sanity: every one of the 13 FE-used E7 endpoints maps to a handler in server.go (GET /overtime, GET /overtime/{id}, :confirm, :approve-l1, :approve-final, :reject, :withdraw, :bulk-approve, :bulk-reject, GET/POST/PATCH/DELETE holidays). No :auto-detect, no POST /overtime create (out of scope).
</verification>

<success_criteria>
The OT two-level workflow (confirm → L1 → final, reject, withdraw) works with *ForUpdate state guards (409 on terminal/wrong-state), bulk approve/reject reports partial success, OT_BELOW_MIN returns 422 with field errors from the existing overtime_rules, day_type is classified from schedule + holiday calendar, holiday CRUD enforces HOLIDAY_DATE_CLASH + HOLIDAY_IN_USE, GuardCompany + SELF_APPROVAL_FORBIDDEN are enforced, every write audits in-tx + stubs notify, and the seed plants a target for each scenario. Matches openapi byte-for-byte.
</success_criteria>

<output>
After completion, create `.planning/phases/09-e7-overtime/09-02-SUMMARY.md`.
</output>
