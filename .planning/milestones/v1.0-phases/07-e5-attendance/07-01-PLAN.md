---
phase: 07-e5-attendance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/db/migrations/00026_attendance.sql
  - backend/db/migrations/00027_attendance_corrections.sql
  - backend/db/queries/attendance/attendance.sql
  - backend/db/queries/attendance/corrections.sql
  - backend/internal/domain/attendance/attendance.go
  - backend/internal/domain/attendance/correction.go
autonomous: true
requirements: [ATT-01, ATT-02]
must_haves:
  truths:
    - "An `attendance` table exists with the seeded exception flags (is_late, in_geofence_in/out, auto_closed) and verification_status + status enums, FK to schedule_entries/placements/employees."
    - "An `attendance_corrections` table exists with status enum, FK to attendance, proposed_* fields, original_snapshot jsonb, and the correction window timestamp."
    - "sqlc-generated code compiles (`make gen` clean, `go build ./...` exits 0) with list (cursor) + get + state-transition queries for both tables."
  artifacts:
    - path: "backend/db/migrations/00026_attendance.sql"
      provides: "attendance records table + indexes"
      contains: "verification_status"
    - path: "backend/db/migrations/00027_attendance_corrections.sql"
      provides: "attendance_corrections table"
      contains: "original_snapshot"
    - path: "backend/db/queries/attendance/attendance.sql"
      provides: "attendance list/get/verify/reject sqlc queries"
      contains: "ListAttendance"
    - path: "backend/db/queries/attendance/corrections.sql"
      provides: "corrections list/get/approve/reject sqlc queries"
      contains: "ListCorrections"
    - path: "backend/internal/domain/attendance/attendance.go"
      provides: "domain Attendance + enums + GeofenceCheck"
      contains: "type Attendance"
  key_links:
    - from: "backend/db/migrations/00026_attendance.sql"
      to: "swp_next_id('ATT')"
      via: "id column DEFAULT"
      pattern: "swp_next_id\\('ATT'\\)"
    - from: "backend/db/queries/attendance/attendance.sql"
      to: "internal/repository/sqlc"
      via: "make gen"
      pattern: "-- name: ListAttendance"
---

<objective>
Lay the E5 Attendance data layer: two goose migrations (`attendance`, `attendance_corrections`)
with FKs to the existing Phase-6 `schedule_entries`, Phase-5 `placements`, and `employees`
tables; sqlc query files; and `make gen` to produce the typed repository code. Plus the
`internal/domain/attendance` types the service/handler in 07-02 build against.

Purpose: Data layer + contracts must exist before services. This is the interface-first wave —
07-02 implements against the domain types and sqlc queries created here.
Output: migrations 00026/00027, `db/queries/attendance/*.sql`, generated sqlc, domain types.
Scope = the 10 FE-used E5 endpoints ONLY. Clock-in/out is mobile and OUT OF SCOPE: geofence
and lateness are STORED COLUMNS on seeded records (no clock pipeline, no Haversine compute).
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-e5-attendance/07-CONTEXT.md
@.planning/reference/backend-build-conventions.md
@docs/api/E5-attendance/openapi.yaml
@docs/epics/E5-attendance/FEATURE.md

<interfaces>
<!-- Anchors extracted from the codebase. Executor: use these directly. -->

ids.go ALREADY has the prefixes — DO NOT touch ids.go:
  Attendance        Prefix = "ATT"   // SWP-ATT-*
  AttendanceCorrect Prefix = "COR"   // SWP-COR-*

Migration pattern (mirror backend/db/migrations/00024_schedule_entries.sql + 00020_placements.sql):
  - goose `-- +goose Up` / `-- +goose Down`.
  - `id text PRIMARY KEY DEFAULT ('SWP-ATT-' || swp_next_id('ATT'))` (Phase-5 column-DEFAULT
    allocation, decision [05-01]; seed/INSERT omits id to let DEFAULT fire, OR supplies explicit id).
  - soft-delete `deleted_at timestamptz`.
  - time columns that the FE renders as instants are `timestamptz` (check_in_at, shift_start_at…);
    FE does `new Date(x).toLocaleString('id-ID',{timeZone:'Asia/Jakarta'})`.
  - Phase-6 used partial unique indexes for INV backstops (schedule_entries_active_agent_date_uq).

sqlc query pattern (mirror backend/db/queries/scheduling/schedule_entries.sql):
  - `-- name: X :one|:many|:exec` annotations; cursor lists ORDER BY (sort_col, id) with a
    keyset WHERE clause; `make gen` writes internal/repository/sqlc (NEVER hand-edit).

Domain pattern: there is no internal/domain/scheduling dir (scheduling kept types in repo/service);
Phase-5 placement HAS internal/domain types. Create internal/domain/attendance/ for E5 (the FE
+ openapi shapes are rich enough to warrant a domain package; 07-02 maps rows→domain→DTO).
</interfaces>

<openapi_anchors>
docs/api/E5-attendance/openapi.yaml — the byte-for-byte contract:
  - schema `Attendance` (lines ~1300-1449): required id, employee_id, placement_id, company_id,
    service_line, check_in_at, lat_in, lng_in, status, verification_status, flags, wfo,
    auto_closed, created_at, updated_at. Optional/nullable: employee_name, company_name,
    schedule_id, attendance_code_id, shift_start_at, shift_end_at, check_out_at, lat_out, lng_out,
    photo_in_id, photo_out_id, late_minutes, worked_minutes, geofence_in, geofence_out,
    verified_by, verified_at, rejected_by, rejected_at, reject_reason, last_correction_id.
  - enum AttendanceStatus: PRESENT, LATE, INCOMPLETE, ABSENT, ON_LEAVE.
  - enum VerificationStatus: AUTO_APPROVED, PENDING, VERIFIED, REJECTED, ESCALATED.
  - enum AttendanceFlag: LATE, EARLY, OUTSIDE_GEOFENCE, UNSCHEDULED, ESCALATED, CORRECTED,
    AUTO_CLOSED, ABSENT, NEEDS_CODE_VERIFICATION.
  - schema GeofenceCheck: { inside bool, distance_m int, radius_m int }.
  - schema `Correction` (lines ~1636-1713): required id, attendance_id, requester_id, type, reason,
    status, original_snapshot, created_at, updated_at. Optional: proposed_check_in_at,
    proposed_check_out_at, proposed_attendance_code_id, evidence_file_id, decided_by, decided_at,
    reject_reason, diff[].
  - enum CorrectionType: CHECK_IN, CHECK_OUT, CODE, OTHER.
  - enum CorrectionStatus: PENDING, APPROVED, APPLIED, REJECTED, CANCELLED.
</openapi_anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations 00026 (attendance) + 00027 (attendance_corrections)</name>
  <files>backend/db/migrations/00026_attendance.sql, backend/db/migrations/00027_attendance_corrections.sql</files>
  <read_first>
    - backend/db/migrations/00024_schedule_entries.sql + 00020_placements.sql (goose + swp_next_id DEFAULT + partial-index pattern)
    - docs/api/E5-attendance/openapi.yaml schema `Attendance` + `Correction` (column set, enums)
    - backend/internal/platform/ids/ids.go (confirm ATT + COR prefixes already present — do NOT edit)
  </read_first>
  <action>
    Create `00026_attendance.sql` (goose Up/Down). Table `attendance`:
      - `id text PRIMARY KEY DEFAULT ('SWP-ATT-' || swp_next_id('ATT'))`
      - `employee_id text NOT NULL REFERENCES employees(id)`
      - `placement_id text NOT NULL REFERENCES placements(id)`
      - `schedule_id text REFERENCES schedule_entries(id)`  (NULL = unscheduled)
      - `company_id text NOT NULL REFERENCES client_companies(id)`
      - `service_line text NOT NULL`  (facility_services|building_management|parking)
      - `attendance_code_id text REFERENCES attendance_codes(id)`
      - `shift_start_at timestamptz`, `shift_end_at timestamptz` (nullable; from E4 schedule)
      - `check_in_at timestamptz NOT NULL`, `check_out_at timestamptz`
      - `lat_in double precision NOT NULL`, `lng_in double precision NOT NULL`
      - `lat_out double precision`, `lng_out double precision`
      - `photo_in_id text`, `photo_out_id text`
      - `wfo boolean NOT NULL DEFAULT true`
      - `is_late boolean NOT NULL DEFAULT false`, `late_minutes integer NOT NULL DEFAULT 0`
      - `worked_minutes integer`
      - `auto_closed boolean NOT NULL DEFAULT false`
      - geofence as STORED columns (no Haversine at runtime): `in_geofence boolean`,
        `in_distance_m integer`, `out_geofence boolean`, `out_distance_m integer`,
        `geofence_radius_m integer NOT NULL DEFAULT 100`
      - `status text NOT NULL DEFAULT 'PRESENT'`  (PRESENT|LATE|INCOMPLETE|ABSENT|ON_LEAVE)
      - `verification_status text NOT NULL DEFAULT 'PENDING'`  (AUTO_APPROVED|PENDING|VERIFIED|REJECTED|ESCALATED)
      - `flags text[] NOT NULL DEFAULT '{}'`  (AttendanceFlag values)
      - `verified_by text`, `verified_at timestamptz`
      - `rejected_by text`, `rejected_at timestamptz`, `reject_reason text`
      - `last_correction_id text`
      - `created_at timestamptz NOT NULL DEFAULT now()`, `updated_at timestamptz NOT NULL DEFAULT now()`
      - `deleted_at timestamptz`
      Indexes: `CREATE INDEX attendance_company_checkin_idx ON attendance (company_id, check_in_at DESC) WHERE deleted_at IS NULL;`
      and `CREATE INDEX attendance_vstatus_idx ON attendance (verification_status) WHERE deleted_at IS NULL;`
      Down: `DROP TABLE attendance;`

    Create `00027_attendance_corrections.sql` (goose Up/Down). Table `attendance_corrections`:
      - `id text PRIMARY KEY DEFAULT ('SWP-COR-' || swp_next_id('COR'))`
      - `attendance_id text NOT NULL REFERENCES attendance(id)`
      - `requester_id text NOT NULL REFERENCES employees(id)`
      - `company_id text NOT NULL REFERENCES client_companies(id)`  (denormalized from attendance for leader-scope queries)
      - `type text NOT NULL`  (CHECK_IN|CHECK_OUT|CODE|OTHER)
      - `proposed_check_in_at timestamptz`, `proposed_check_out_at timestamptz`
      - `proposed_attendance_code_id text REFERENCES attendance_codes(id)`
      - `reason text NOT NULL`
      - `evidence_file_id text`
      - `status text NOT NULL DEFAULT 'PENDING'`  (PENDING|APPROVED|APPLIED|REJECTED|CANCELLED)
      - `decided_by text`, `decided_at timestamptz`, `reject_reason text`
      - `original_snapshot jsonb NOT NULL DEFAULT '{}'::jsonb`
      - `attendance_shift_date date NOT NULL`  (basis for OUTSIDE_CORRECTION_WINDOW 7-day check)
      - `created_at timestamptz NOT NULL DEFAULT now()`, `updated_at timestamptz NOT NULL DEFAULT now()`
      - `deleted_at timestamptz`
      Backstop for CORRECTION_ALREADY_PENDING (one open correction per attendance):
        `CREATE UNIQUE INDEX corrections_one_pending_per_attendance_uq ON attendance_corrections (attendance_id) WHERE status = 'PENDING' AND deleted_at IS NULL;`
      Index: `CREATE INDEX corrections_company_created_idx ON attendance_corrections (company_id, created_at DESC) WHERE deleted_at IS NULL;`
      Down: `DROP TABLE attendance_corrections;`
  </action>
  <verify>
    <automated>cd backend && goose -dir db/migrations validate 2>/dev/null; grep -q "swp_next_id('ATT')" db/migrations/00026_attendance.sql && grep -q "verification_status" db/migrations/00026_attendance.sql && grep -q "original_snapshot" db/migrations/00027_attendance_corrections.sql && grep -q "corrections_one_pending_per_attendance_uq" db/migrations/00027_attendance_corrections.sql && echo MIGRATIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `backend/db/migrations/00026_attendance.sql` exists and contains `swp_next_id('ATT')`, `verification_status`, `flags text[]`, `auto_closed`, `is_late`, `in_geofence`.
    - `backend/db/migrations/00027_attendance_corrections.sql` exists and contains `original_snapshot`, `corrections_one_pending_per_attendance_uq`, `attendance_shift_date`.
    - Both have `-- +goose Up` and `-- +goose Down`.
    - ids.go NOT modified (`git diff --quiet backend/internal/platform/ids/ids.go` exits 0).
  </acceptance_criteria>
  <done>Both migrations created with FKs, enums-as-text, partial unique index for one-pending-correction backstop, and goose Up/Down; ids.go untouched.</done>
</task>

<task type="auto">
  <name>Task 2: sqlc queries + make gen + domain types</name>
  <files>backend/db/queries/attendance/attendance.sql, backend/db/queries/attendance/corrections.sql, backend/internal/domain/attendance/attendance.go, backend/internal/domain/attendance/correction.go</files>
  <read_first>
    - backend/db/queries/scheduling/schedule_entries.sql (cursor list + :one/:exec annotation pattern)
    - backend/internal/domain (placement domain types — naming/structure to mirror)
    - docs/api/E5-attendance/openapi.yaml schemas Attendance/Correction/GeofenceCheck (field set)
  </read_first>
  <action>
    Create `db/queries/attendance/attendance.sql` with sqlc-annotated queries (cursor keyset on
    (check_in_at DESC, id)):
      - `-- name: ListAttendance :many` — filters: company_id (nullable narg), employee_id,
        service_line, verification_status (= ANY array narg), status (= ANY), date_from/date_to on
        shift date basis, exceptions flag; WHERE deleted_at IS NULL; keyset cursor; LIMIT.
        SELECT the columns + LEFT JOIN employees for employee_name, client_companies for company_name.
      - `-- name: GetAttendance :one` — by id, deleted_at IS NULL.
      - `-- name: VerifyAttendance :one` — UPDATE set verification_status='VERIFIED', verified_by,
        verified_at=now(), updated_at=now() WHERE id AND verification_status IN ('PENDING','ESCALATED')
        RETURNING * (zero rows ⇒ terminal-state 409 in service).
      - `-- name: RejectAttendance :one` — UPDATE set verification_status='REJECTED', rejected_by,
        rejected_at=now(), reject_reason, updated_at=now() WHERE id AND verification_status IN
        ('PENDING','ESCALATED') RETURNING *.
      - `-- name: GetAttendanceForUpdate :one` — SELECT ... WHERE id AND deleted_at IS NULL FOR UPDATE
        (used by bulk + correction-apply to read company_id/employee_id for scope + state guards).
      - `-- name: ApplyCorrectionToAttendance :one` — UPDATE check_in_at/check_out_at/attendance_code_id
        with COALESCE(narg, existing), append 'CORRECTED' to flags, set last_correction_id,
        updated_at=now() WHERE id RETURNING *.

    Create `db/queries/attendance/corrections.sql`:
      - `-- name: ListCorrections :many` — filters: company_id (narg), employee_id (=requester_id narg),
        status (= ANY array), type (= ANY array), date_from/date_to; keyset on (created_at DESC, id); LIMIT.
      - `-- name: GetCorrection :one` — by id.
      - `-- name: GetCorrectionForUpdate :one` — by id FOR UPDATE.
      - `-- name: ApproveCorrection :one` — UPDATE set status='APPLIED', decided_by, decided_at=now(),
        updated_at=now() WHERE id AND status='PENDING' RETURNING * (zero rows ⇒ 409).
      - `-- name: RejectCorrection :one` — UPDATE set status='REJECTED', decided_by, decided_at=now(),
        reject_reason, updated_at=now() WHERE id AND status='PENDING' RETURNING *.

    Run `make gen` from backend/ to generate sqlc into internal/repository/sqlc (do NOT hand-edit output).

    Create `internal/domain/attendance/attendance.go`:
      - `type AttendanceStatus string` + consts (PRESENT, LATE, INCOMPLETE, ABSENT, ON_LEAVE).
      - `type VerificationStatus string` + consts (AUTO_APPROVED, PENDING, VERIFIED, REJECTED, ESCALATED).
      - `type Flag string` + consts (LATE, EARLY, OUTSIDE_GEOFENCE, UNSCHEDULED, ESCALATED, CORRECTED, AUTO_CLOSED, ABSENT, NEEDS_CODE_VERIFICATION).
      - `type GeofenceCheck struct { Inside bool; DistanceM int; RadiusM int }`.
      - `type Attendance struct { ... }` with all openapi fields (pointers for nullable: SchedulePtr,
        CheckOutAt, LatOut, LngOut, ShiftStartAt, ShiftEndAt, WorkedMinutes, VerifiedBy/At,
        RejectedBy/At, RejectReason, LastCorrectionID, GeofenceIn/Out *GeofenceCheck, EmployeeName,
        CompanyName, AttendanceCodeID). Flags []Flag.
    Create `internal/domain/attendance/correction.go`:
      - `type CorrectionType string` (CHECK_IN, CHECK_OUT, CODE, OTHER).
      - `type CorrectionStatus string` (PENDING, APPROVED, APPLIED, REJECTED, CANCELLED).
      - `type DiffRow struct { Field string; Before any; After any }`.
      - `type Correction struct { ... }` with all openapi fields (OriginalSnapshot map[string]any,
        Diff []DiffRow, proposed_* pointers, DecidedBy/At, RejectReason pointers).
  </action>
  <verify>
    <automated>cd backend && make gen && go build ./... && go vet ./... && grep -q "ListAttendance" db/queries/attendance/attendance.sql && grep -q "ListCorrections" db/queries/attendance/corrections.sql && grep -q "type Attendance struct" internal/domain/attendance/attendance.go && grep -q "type Correction struct" internal/domain/attendance/correction.go && echo GEN_BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `db/queries/attendance/attendance.sql` contains `-- name: ListAttendance`, `-- name: VerifyAttendance`, `-- name: RejectAttendance`, `-- name: GetAttendanceForUpdate`, `-- name: ApplyCorrectionToAttendance`.
    - `db/queries/attendance/corrections.sql` contains `-- name: ListCorrections`, `-- name: ApproveCorrection`, `-- name: RejectCorrection`, `-- name: GetCorrectionForUpdate`.
    - `make gen` regenerates without error; `go build ./...` and `go vet ./...` exit 0.
    - `internal/domain/attendance/attendance.go` exports `Attendance`, `VerificationStatus`, `GeofenceCheck`; `correction.go` exports `Correction`, `CorrectionStatus`, `CorrectionType`.
  </acceptance_criteria>
  <done>sqlc queries for both tables written, `make gen` clean, build+vet green, and domain types match the openapi Attendance/Correction shapes (nullable fields are pointers).</done>
</task>

</tasks>

<verification>
- `make gen && go build ./... && go vet ./...` all exit 0.
- Migrations 00026 + 00027 present with goose Up/Down; ATT/COR DEFAULT id allocation; one-pending-correction partial unique index.
- sqlc query files contain the named queries listed above.
- Domain package `internal/domain/attendance` exports Attendance + Correction + enums for 07-02.
- ids.go unchanged.
</verification>

<success_criteria>
The E5 data layer compiles and is ready for the 07-02 service/handler slice to build against:
tables, FKs, indexes, sqlc, and domain types exist; geofence/lateness are stored columns (no clock
pipeline). No endpoints, services, or seed yet — those are 07-02.
</success_criteria>

<output>
After completion, create `.planning/phases/07-e5-attendance/07-01-SUMMARY.md`.
</output>
