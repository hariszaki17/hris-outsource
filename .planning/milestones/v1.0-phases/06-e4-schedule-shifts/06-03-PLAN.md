---
phase: 06-e4-schedule-shifts
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - backend/internal/handler/scheduling/shift_master_handler_test.go
  - backend/internal/handler/scheduling/schedule_handler_test.go
  - backend/internal/handler/scheduling/scheduling_testkit_test.go
autonomous: true
requirements: [SCH-01, SCH-02, SCH-03]
user_setup: []

must_haves:
  truths:
    - "every E4 conflict code is asserted against the openapi shape: DOUBLE_SHIFT, OUTSIDE_PLACEMENT_PERIOD, SHIFT_OVER_LEAVE, OUT_OF_SCOPE, SHIFT_NOT_FOR_SERVICE_LINE, SHIFT_DEACTIVATED"
    - "bulk-apply partial-success envelope is asserted (mixed succeeded/failed, 200; all-failed → 422)"
    - "leader-scope 403 OUT_OF_SCOPE is contract-tested"
    - "list endpoints assert the exact cursor/envelope shapes ({data,next_cursor,has_more} for shift-masters; {data,warnings} for schedule)"
    - "go test ./internal/handler/scheduling/... exits 0 (drift gate)"
  artifacts:
    - path: "backend/internal/handler/scheduling/scheduling_testkit_test.go"
      provides: "in-memory fake repos + fakeTx + httptest harness mirroring placement contract tests"
      contains: "fakeTx"
    - path: "backend/internal/handler/scheduling/schedule_handler_test.go"
      provides: "conflict-code + bulk-apply + scope contract tests"
      contains: "SHIFT_OVER_LEAVE"
    - path: "backend/internal/handler/scheduling/shift_master_handler_test.go"
      provides: "shift-master CRUD + DUPLICATE_NAME + BREAK_OUTSIDE_WINDOW + ALREADY_INACTIVE tests"
      contains: "BREAK_OUTSIDE_WINDOW"
  key_links:
    - from: "contract tests"
      to: "docs/api/E4-shift-scheduling/openapi.yaml examples"
      via: "asserted response shapes/codes/details"
      pattern: "error.*code"
---

<objective>
Go contract tests for the E4 handlers — the drift gate that replaces server codegen. Assert
that every endpoint's response matches the openapi shapes/codes/details EXACTLY: all six
conflict codes, the bulk-apply partial-success envelope, the leader-scope 403, and the
cursor/list envelopes. Mirror the Phase-5 placement contract-test harness (in-memory fake
repo + fakeTx + httptest), no testcontainers.

Purpose: lock the BE response shapes so the Orval-generated FE client never drifts.
Output: three test files in `internal/handler/scheduling/`. `go test ./internal/handler/scheduling/...` green.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-e4-schedule-shifts/06-CONTEXT.md
@docs/api/E4-shift-scheduling/openapi.yaml

<interfaces>
<!-- Phase-5 contract-test harness to mirror EXACTLY: -->
From backend/internal/handler/placement/placements_handler_test.go:
```go
type fakeTx struct{}  // only Exec is a no-op; other pgx.Tx methods panic
type fakeTxRunner struct{} // InTx(ctx, fn) → fn(&fakeTx{})
func decodeBody(t, rr) map[string]any
type fakePlacementRepo struct { ... }  // in-memory svc.Repository impl
// httptest.NewRecorder + handler.ServeHTTP; principal injected via a mutable harness field
```

<!-- 06-02 SUMMARY (read it) will list the seeded fixture ids/dates + the conflict-engine -->
<!-- ordering + each code's HTTP status. Anchor test expectations on that summary + the openapi. -->
</interfaces>

<!-- Expected status per code (assert exactly): -->
<!--   OUT_OF_SCOPE 403 · OUTSIDE_PLACEMENT_PERIOD 422 · SHIFT_DEACTIVATED 422 -->
<!--   SHIFT_NOT_FOR_SERVICE_LINE 422 · SHIFT_OVER_LEAVE 409 · DOUBLE_SHIFT 409 -->
<!--   DUPLICATE_NAME 409 · BREAK_OUTSIDE_WINDOW 422 · ALREADY_INACTIVE 409 -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test harness (fakes) + shift-master contract tests</name>
  <files>backend/internal/handler/scheduling/scheduling_testkit_test.go, backend/internal/handler/scheduling/shift_master_handler_test.go</files>
  <read_first>
    - backend/internal/handler/placement/placements_handler_test.go (fakeTx, fakeTxRunner, decodeBody, fakeRepo wiring, principal injection, httptest pattern)
    - .planning/phases/06-e4-schedule-shifts/06-02-SUMMARY.md (the actual service/repo interfaces + DTO field names + seeded fixtures to assert against)
    - backend/internal/handler/scheduling/shift_master_handler.go + shift_master_dto.go (from 06-02 — the surface under test)
    - docs/api/E4-shift-scheduling/openapi.yaml ShiftMaster (1009-1090), createShiftMaster 409/422 (205-237), deactivate 409 (325-336)
  </read_first>
  <action>
    Create `scheduling_testkit_test.go` (shared test plumbing) and `shift_master_handler_test.go`.

    **scheduling_testkit_test.go**: copy the Phase-5 `fakeTx`, `fakeTxRunner`, `decodeBody` verbatim (adjust package to `scheduling`). Add in-memory fake repos implementing the 06-02 service repository ports:
    - `fakeShiftMasterRepo` — map[id]domain.ShiftMaster; List/Get/Create (allocates SWP-SHF-test ids)/Update/SetActive/GetForUpdate; a `nameIndex` to raise the DUPLICATE_NAME sentinel on collision.
    - `fakeScheduleRepo` — backs all schedule queries: in-memory entries, placements (by employee→{company,svc_line,start,end}), shift masters, and an `approvedLeave` map[employee+date]→{lrID,type} so the over-leave branch fires. Implements FindLiveEntryForAgentDate / FindActivePlacementForAgentDate / FindApprovedLeaveForAgentDate / GetShiftMaster / Create / Update / SoftDelete / ListSchedule / GetForUpdate.
    - A `newHarness(t, principalRole, leaderCompanyID)` building the real scheduling.Service + scheduling.Handler over the fakes, with a mutable principal (mirror Phase-2 "Dynamic principal injection" — leader scope read from ctx). Provide a helper `do(t, method, path, body, idempotencyKey)` → *httptest.ResponseRecorder.

    **shift_master_handler_test.go** — table/sub-tests:
    - `TestListShiftMasters_Envelope`: GET /shift-masters → 200, body has keys `data`,`next_cursor`,`has_more`; each item has `status` ∈ {ACTIVE,INACTIVE}, `break_minutes`, `in_use_count`, `cross_midnight`.
    - `TestCreateShiftMaster_CrossMidnightDerived`: POST with end_time<=start_time → 201, response `cross_midnight=true`, `status=ACTIVE`.
    - `TestCreateShiftMaster_DuplicateName`: second create same name → 409 `DUPLICATE_NAME`.
    - `TestCreateShiftMaster_BreakOutsideWindow`: break outside [start,end] → 422 `BREAK_OUTSIDE_WINDOW` with fields.break_start.
    - `TestDeactivateReactivate`: deactivate active → 200 status=INACTIVE; deactivate again → 409 `ALREADY_INACTIVE`; reactivate → 200 status=ACTIVE.
    - `TestShiftMasterWrites_RBAC`: leader (shift_leader) POST /shift-masters → 403 (writes are super/hr only).
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handler/scheduling/ -run 'ShiftMaster' -count=1 && grep -q "DUPLICATE_NAME" internal/handler/scheduling/shift_master_handler_test.go && grep -q "BREAK_OUTSIDE_WINDOW" internal/handler/scheduling/shift_master_handler_test.go && grep -q "ALREADY_INACTIVE" internal/handler/scheduling/shift_master_handler_test.go && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - scheduling_testkit_test.go provides fakeTx/fakeTxRunner/decodeBody + fakeShiftMasterRepo + fakeScheduleRepo (with approvedLeave map) + newHarness
    - shift_master_handler_test.go asserts the list envelope keys, cross_midnight derivation, DUPLICATE_NAME (409), BREAK_OUTSIDE_WINDOW (422), ALREADY_INACTIVE (409), and leader-write 403
    - `go test ./internal/handler/scheduling/ -run ShiftMaster` exits 0
  </acceptance_criteria>
  <done>in-memory fakeTx/fakeTxRunner/decodeBody + fakeShiftMasterRepo + fakeScheduleRepo (with approvedLeave map) + newHarness exist; shift-master tests assert the list envelope, cross_midnight derivation, DUPLICATE_NAME 409, BREAK_OUTSIDE_WINDOW 422, ALREADY_INACTIVE 409, leader-write 403; go test -run ShiftMaster exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Schedule conflict + bulk-apply + scope contract tests</name>
  <files>backend/internal/handler/scheduling/schedule_handler_test.go</files>
  <read_first>
    - backend/internal/handler/scheduling/schedule_handler.go + schedule_dto.go (from 06-02 — surface under test, esp. the bulkApplyResult nesting `failed[].error.{code,message,details}`)
    - .planning/phases/06-e4-schedule-shifts/06-02-SUMMARY.md (conflict ordering + each code's status + details fields)
    - docs/api/E4-shift-scheduling/openapi.yaml: POST /schedule examples (557-613), bulk-apply partial example (818-840), check (855-909), 403 OUT_OF_SCOPE (537-549), ScheduleEntry (1128-1230)
  </read_first>
  <action>
    Create `schedule_handler_test.go`. Seed the fake repo per test so each conflict branch fires. Sub-tests (one per code + the envelopes):

    Happy path:
    - `TestCreateScheduleEntry_Success`: agent with active placement covering the date, valid active master, no leave, no existing entry → 201, response is a ScheduleEntry with `status=SCHEDULED`, `work_date` "YYYY-MM-DD", `placement_id` set, `company_id` derived, `warnings:[]`.
    - `TestListSchedule_Envelope`: GET /schedule?company_id&start_date&end_date → 200, body has `data` (array) + `warnings` (array, present even if empty).

    Conflict codes (assert status + error.code + error.details where the openapi populates them):
    - `TestConflict_OutsidePlacementPeriod`: date outside window → 422 `OUTSIDE_PLACEMENT_PERIOD`, details has placement_id/placement_start/placement_end (when a placement exists).
    - `TestConflict_ShiftDeactivated`: master is_active=false → 422 `SHIFT_DEACTIVATED`.
    - `TestConflict_ShiftNotForServiceLine`: master.service_line_id != placement.service_line_id → 422 `SHIFT_NOT_FOR_SERVICE_LINE`.
    - `TestConflict_ShiftOverLeave`: approvedLeave map has the (employee,date) → 409 `SHIFT_OVER_LEAVE`, details.leave_request_id + details.leave_type.
    - `TestConflict_DoubleShift`: pre-existing live entry, force_replace=false → 409 `DOUBLE_SHIFT`, details.existing_entry_id + details.existing_shift_name.
    - `TestForceReplace_Succeeds`: same as above but force_replace=true → 201, response status=MODIFIED, replaced_entry_id set.
    - `TestScope_OutOfScope`: leader principal scoped to CMP-A, target agent placed at CMP-B → 403 `OUT_OF_SCOPE`.

    Check (dry-run):
    - `TestCheck_SingleNoWrite`: POST /schedule:check kind=single with a conflicting cell → 200 with `failed[0].error.code` set and the repo's entry count UNCHANGED (no persistence).

    Bulk-apply partial success:
    - `TestBulkApply_PartialSuccess`: a date range × employees where some cells pass and >=1 fails (e.g. one date hits SHIFT_OVER_LEAVE via the approvedLeave map) → 200, `succeeded` non-empty with {id,employee_id,date,status}, `failed` non-empty with {employee_id,date,error:{code,message,details}}.
    - `TestBulkApply_AllFailed_422`: every cell conflicts → 422, same BulkApplyResult body shape (succeeded empty, failed populated).
    - `TestBulkApply_WeekdaysMask`: weekdays_mask filters which dates are attempted (assert succeeded+failed count == in-mask date count × employees).

    Delete:
    - `TestDelete_204`: DELETE /schedule/{id} for a live entry by hr_admin → 204, entry soft-deleted.
    - `TestDelete_LeaderPastDate_403`: leader deleting a past-date entry → 403 (C-5 restriction).

    Use `decodeBody` + assert `body["error"].(map)["code"]` and nested `details`. For bulk, assert array element nesting matches the FE-consumed shape (`failed[i].error.code`).
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handler/scheduling/ -count=1 && for c in OUTSIDE_PLACEMENT_PERIOD SHIFT_DEACTIVATED SHIFT_NOT_FOR_SERVICE_LINE SHIFT_OVER_LEAVE DOUBLE_SHIFT OUT_OF_SCOPE; do grep -q "$c" internal/handler/scheduling/schedule_handler_test.go || { echo "MISSING $c"; exit 1; }; done && grep -q "PartialSuccess\|partial" internal/handler/scheduling/schedule_handler_test.go && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - schedule_handler_test.go has a sub-test for EACH of the six conflict codes asserting status + error.code + (where applicable) error.details fields
    - bulk-apply tests assert: partial success → 200 with non-empty succeeded AND failed; all-failed → 422; weekdays_mask filtering
    - :check test asserts no persistence (repo entry count unchanged)
    - leader-scope OUT_OF_SCOPE (403) and force_replace (201 + MODIFIED + replaced_entry_id) covered
    - `go test ./internal/handler/scheduling/...` exits 0
  </acceptance_criteria>
  <done>a sub-test per conflict code (DOUBLE_SHIFT, OUTSIDE_PLACEMENT_PERIOD, SHIFT_OVER_LEAVE, OUT_OF_SCOPE, SHIFT_NOT_FOR_SERVICE_LINE, SHIFT_DEACTIVATED) asserting status+code+details; bulk-apply partial-success (200) + all-failed (422) + weekdays_mask; :check no-side-effect; force_replace 201 MODIFIED; DELETE 204 + leader past-date 403; go test ./internal/handler/scheduling/... exits 0.</done>
</task>

</tasks>

<verification>
- `cd backend && go test ./internal/handler/scheduling/... -count=1` green.
- Every conflict code + the bulk-apply partial-success envelope + leader-scope 403 asserted.
- List envelopes match openapi ({data,next_cursor,has_more} / {data,warnings}).
- `go vet ./...` clean.
</verification>

<success_criteria>
- Contract tests are the drift gate: BE response shapes locked to the openapi.
- All conflict codes honestly exercised (over-leave via the fake approvedLeave map).
- Bulk-apply 200/422 + partial-success structure proven.
</success_criteria>

<output>
After completion, create `.planning/phases/06-e4-schedule-shifts/06-03-SUMMARY.md` documenting:
- the codes/envelopes covered, any openapi-vs-impl mismatch found + how resolved,
- the fake-repo seeding pattern 06-04 can reference,
- any divergence + reason.
</output>
