---
phase: 10-e8-payroll
plan: 04
type: execute
wave: 4
depends_on: ["10-02", "10-03"]
files_modified:
  - frontend/apps/web/src/features/e8-payroll/payslip-archive-screen.tsx
  - frontend/apps/web/src/features/e8-payroll/payslip-detail-screen.tsx
  - frontend/apps/web/src/features/e8-payroll/payslip-detail-route.tsx
  - frontend/apps/web/src/features/e8-payroll/audit-note-drawer.tsx
  - frontend/e2e/lib/e8-helpers.ts
  - frontend/e2e/lib/backend.ts
  - frontend/e2e/lib/reset-db.ts
  - frontend/e2e/.env.e2e
  - frontend/e2e/tests/e8/archive.spec.ts
  - frontend/e2e/tests/e8/detail.spec.ts
  - frontend/e2e/tests/e8/audit-notes.spec.ts
  - frontend/e2e/tests/e8/export.spec.ts
  - frontend/e2e/tests/e8/rbac.spec.ts
autonomous: true
requirements: [PAY-01, PAY-02]
user_setup: []

must_haves:
  truths:
    - "The e8-payroll screens render real BE data with MSW off (archive list + filters, detail breakdown, audit-note drawer)"
    - "A DECRYPT_FAIL payslip renders the decrypt-fail state (200, money placeholders, banner) — not an error page"
    - "Creating an audit note appears in the list; the export trigger returns 202 + a job id AND the River worker completes the export_jobs row to DONE (asserted via a DB poll)"
    - "agent / shift_leader cannot reach the web payroll archive (403)"
  artifacts:
    - path: "frontend/e2e/tests/e8/export.spec.ts"
      provides: "export 202 + worker-completes-job (export_jobs → DONE via DB poll) E2E"
      contains: "DONE"
    - path: "frontend/e2e/tests/e8/audit-notes.spec.ts"
      provides: "audit-note list + create E2E"
      contains: "audit"
    - path: "frontend/e2e/tests/e8/rbac.spec.ts"
      provides: "agent/shift_leader 403 on payroll archive E2E"
      contains: "403"
    - path: "frontend/e2e/lib/backend.ts"
      provides: "River worker (cmd/worker) booted in the harness so the export job completes"
      contains: "worker"
    - path: "frontend/e2e/lib/reset-db.ts"
      provides: "payroll + export_jobs added to TRUNCATE (River internal tables untouched)"
      contains: "payslips"
  key_links:
    - from: "frontend/e2e/tests/e8/export.spec.ts"
      to: "export_jobs table (real River worker)"
      via: "pollExportJob DB helper asserts status → DONE"
      pattern: "pollExportJob|export_jobs"
    - from: "frontend/e2e/lib/backend.ts"
      to: "cmd/worker process"
      via: "spawn go run ./cmd/worker (PAYROLL_ENCRYPTION_KEY + DATABASE_URL env)"
      pattern: "cmd/worker"
    - from: "payslip-detail-screen.tsx"
      to: "BE GET /payslips/{id} {data} envelope"
      via: "query.data?.data unwrap (already present) + decrypt-fail state"
      pattern: "query.data?.data"
---

<objective>
Wire the e8-payroll screens off MSW to the real Go BE and prove every E8 Gherkin AC with exhaustive
full-stack Playwright E2E under a NEW `frontend/e2e/tests/e8/`. Selectors are derived from the REAL
e8-payroll components (archive/detail/audit-note-drawer/export), not invented. The HEADLINE is the
export: trigger → assert 202 + a job id → wait for the REAL River worker to flip the `export_jobs`
row to DONE (DB poll helper, since E8 has no FE job-status hook). This plan ALSO boots the River
worker in the harness (it currently runs only the API — confirmed) and threads the payroll encryption
key so the seed's ciphertext decrypts. Add payroll + export_jobs to reset-db TRUNCATE (without
breaking River's own tables).

Purpose: Close Phase 10 — E8 payroll works end-to-end against the real backend, including a real
async export completed by the worker.
Output: FE wiring fixes (if any), e8-helpers (+ export_jobs DB poll), harness worker boot, reset-db
edit, 5 E2E specs green headless.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-e8-payroll/10-CONTEXT.md
@.planning/reference/e2e-harness-spec.md
@docs/api/E8-payroll/openapi.yaml
@frontend/apps/web/src/features/e8-payroll/payslip-archive-screen.tsx
@frontend/apps/web/src/features/e8-payroll/payslip-detail-screen.tsx
@frontend/apps/web/src/features/e8-payroll/audit-note-drawer.tsx
@frontend/apps/web/src/features/e8-payroll/payroll-export.tsx

<interfaces>
<!-- REAL component selectors + harness facts (anchor E2E here — NOT assumptions). -->

payslip-archive-screen.tsx (route /payroll, HR/super only):
  - Reads `query.data?.data as PayslipListResponse` → BE list {data, has_more, next_cursor, meta?} envelope (handler returns it at top level). Default filter year = CURRENT_YEAR (note: SEED periods are 2025-11/2025-12 — a 2026 default year shows EMPTY; the spec must SELECT year 2025 or reset the year filter to see seeded rows).
  - SearchField placeholder t('archive.searchPlaceholder') drives `employee_id` (exact match). FilterSelect aria t('archive.filterYear')/filterMonth/filterStatus. Reset button t('common.resetFilters') (visible when filters active).
  - DataTable rows are div.border-b (decision [02-01]); per-row "Lihat detail" button t('archive.viewDetail') → onRowClick(id). Status cell = StatusBadge (FINAL neutral, DECRYPT_FAIL warn) with t('status.FINAL')/t('status.DECRYPT_FAIL').
  - Confidentiality banner (lock icon) t('archive.confBanner'). RBAC client-gate: agent/shift_leader → EmptyState no-permission (BUT the real gate is the BE 403 — assert the BE 403 via apiAs too).

payslip-detail-screen.tsx (route /payroll/$payslipId):
  - `useGetPayslip(id)` → reads `query.data?.data as Payslip` (already unwraps {data}). `useListPayslipAuditNotes(id)` for the inline notes section.
  - isDecryptFail = payslip.decrypt_fail || status===DECRYPT_FAIL → Banner tone bad t('detail.decryptFailTitle'); money via formatMoney(null)→"—"; DecryptPlaceholder for empty earnings/deductions/benefits; "FINAL · Read-only" vs decrypt-fail pill.
  - "Ekspor" button t('detail.export') hidden on decrypt-fail rows (onExportClick prop). "Tambah Catatan" / audit-notes section (onAddNote prop opens the drawer). InfoRow shows source `{system} #{source_id}`.

audit-note-drawer.tsx (open when open && payslipId):
  - List via useListPayslipAuditNotes reads `query.data?.data as PayslipAuditNoteListResponse`. Append form: #audit-note-text textarea (RHF+Zod min 8 / max 1000, noValidate present), submit "Simpan"/t('auditNotes.submit') → useCreatePayslipAuditNote({ id, data:{ text }}) → 201; success toast t('auditNotes.saveSuccess'); list refetches. NOTE: FE Zod min is 8 chars — E2E note text MUST be ≥ 8 chars.

payroll-export.tsx:
  - PayrollExportButton → useExportPayslips({ data:{ period?|year?, employee_ids?, format:'XLSX', confidential:true }}); on success reads `res.data as PayslipExportJob`, shows a QUEUED toast t('export.queuedToastTitle') + the ExportJobCard (status QUEUED→RUNNING→DONE→FAILED). The FE does NOT poll job status (no E8 job-status hook) — the E2E asserts completion via the DB, not the FE card.
  - The archive title band has an "Ekspor (RAHASIA)" entry-point; the detail screen has the per-slip "Ekspor" button. Pick whichever surface actually mounts PayrollExportButton (inspect during the task); if neither wires it, the spec drives the export via apiAs POST /payslips:export and STILL asserts the worker completes the job — the worker-completion proof is the headline, not the button.

Harness facts (CONFIRMED):
  - frontend/e2e/lib/backend.ts boots ONLY `go run ./cmd/api` (step 7) — it does NOT spawn the worker. River migrations ARE applied (step 4, river migrate-up). So 10-04 MUST spawn `go run ./cmd/worker` so the PayslipExportArgs job is processed.
  - frontend/e2e/lib/reset-db.ts TRUNCATE_TABLES list (most-dependent-first); add payroll + export_jobs. DO NOT truncate River's own tables (river_job, river_*) — leaving them lets the worker keep running; TRUNCATE the app tables only.
  - frontend/e2e/lib/personas.ts: PERSONAS.hrAdmin (Sari Hadi = SWP-EMP-9001), shiftLeader (Rudi @ Plaza Senayan), superAdmin, agent.
  - DB poll: reset-db.ts already uses `pg` Client with the .env.e2e DATABASE_URL — copy that connection pattern for the export_jobs poll helper.

E2E patterns (COPY from e7):
  frontend/e2e/lib/e7-helpers.ts (apiAs token call + errorCode + waitForToken re-exported from e5-helpers; PERSONAS.*; .js imports; DataTable rows div.border-b filter)

Seed fixtures (from 10-02): SWP-PS-90121 (EMP-1042 Budi, 2025-12, FINAL, full breakdown), SWP-PS-90122
  (EMP-1108, 2025-12, FINAL), SWP-PS-90123 (EMP-2891, 2025-11, FINAL), SWP-PS-90119 (EMP-1108, 2025-12,
  DECRYPT_FAIL garbage ciphertext) + audit notes SWP-PS-90119-NOTE-1/2 (author Sari Hadi). Periods 2025-11/2025-12.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FE wiring fixes + harness worker boot + reset-db + e8-helpers (export_jobs poll)</name>
  <files>frontend/apps/web/src/features/e8-payroll/payslip-archive-screen.tsx, frontend/apps/web/src/features/e8-payroll/payslip-detail-screen.tsx, frontend/apps/web/src/features/e8-payroll/audit-note-drawer.tsx, frontend/e2e/lib/backend.ts, frontend/e2e/lib/reset-db.ts, frontend/e2e/.env.e2e, frontend/e2e/lib/e8-helpers.ts</files>
  <read_first>
    - frontend/apps/web/src/features/e8-payroll/{payslip-archive-screen,payslip-detail-screen,audit-note-drawer,payroll-export}.tsx (find any place reading `query.data?.data` that needs the bare-fallback unwrap, and any classifyError mismatch — apply the Phase-8 leave-detail fix ONLY where the real BE shape breaks the screen)
    - frontend/e2e/lib/backend.ts (the spawn('go', ['run','./cmd/api']) block at step 7 + the detached process-group teardown — mirror it for cmd/worker; the testEnv carries DATABASE_URL + must also carry PAYROLL_ENCRYPTION_KEY)
    - frontend/e2e/lib/reset-db.ts (TRUNCATE_TABLES ordering + the pg Client connection pattern to copy for the poll helper)
    - frontend/e2e/.env.e2e (add PAYROLL_ENCRYPTION_KEY)
    - frontend/e2e/lib/e7-helpers.ts (helper shape to mirror)
  </read_first>
  <action>
    1) FE fixes (minimal): run the FE against the real BE (MSW off) and fix ONLY components that misread
       the BE shape. The archive reads `query.data?.data as PayslipListResponse` and the detail already
       unwraps `query.data?.data as Payslip` + the drawer `query.data?.data as PayslipAuditNoteListResponse`
       — these match the handler envelopes (top-level page for list, {data} for detail). Apply the bare-fallback
       `?? (resp?.data as T)` only if a screen renders blank against the real BE. If a toast classifyError
       mismatches a Bahasa message, branch on `ApiError.code` (Phase-8 precedent). Keep edits surgical.

    2) Harness worker boot — edit `frontend/e2e/lib/backend.ts`: after the API spawn (step 7), spawn the
       River worker `go run ./cmd/worker` with the SAME testEnv (must include DATABASE_URL + PAYROLL_ENCRYPTION_KEY),
       in its OWN detached process group (mirror the API spawn + the freePort/process-group-kill teardown so a
       stale worker is reaped — decision [07-04]). Keep a module-level `workerProcess` handle; stopBackend()
       SIGTERMs `-workerProcess.pid` too. Add a brief comment: the worker processes the PayslipExportArgs job
       so the export completes (success criterion 2). The River queue tables already exist (step 4 migrate-up).

    3) Env — edit `frontend/e2e/.env.e2e`: add `PAYROLL_ENCRYPTION_KEY=<a deterministic base64 of 32 bytes>`
       (e.g. base64 of 32 'A' bytes). The SAME key is read by `go run ./cmd/seed` (seeds ciphertext), the API
       (decrypts), and the worker — so FINAL rows decrypt and the SWP-PS-90119 garbage row fails. Confirm the
       backend.ts testEnv passes this var to ALL THREE of seed (step 5), API (step 7), worker (new).

    4) reset-db — edit `frontend/e2e/lib/reset-db.ts`: add to TRUNCATE_TABLES (most-dependent-first, BEFORE
       placements/employees): `'payslip_audit_notes'`, `'payslip_benefits'`, `'payslip_components'`, `'payslips'`,
       and `'export_jobs'`. Document: TRUNCATE the APP tables only — do NOT touch River's `river_job`/internal
       tables (truncating them would break the running worker's queue). The seed re-applies the SWP-PS-* fixtures
       (ON CONFLICT DO NOTHING).

    5) Create `frontend/e2e/lib/e8-helpers.ts` mirroring e7-helpers.ts: re-export apiAs/errorCode/waitForToken
       from e5-helpers; export seeded fixture ids
       (`PS = { final:'SWP-PS-90121', final2:'SWP-PS-90122', final3:'SWP-PS-90123', decryptFail:'SWP-PS-90119' }`,
       `PS_NOTE = { one:'SWP-PS-90119-NOTE-1', two:'SWP-PS-90119-NOTE-2' }`); a `payslipRow(page, text)` locator
       (div.border-b filtered by employee/period text); and the KEY helper
       `pollExportJob(jobId, { timeoutMs=15000 }) : Promise<{status:string, row_count?:number}>` — opens a `pg`
       Client on the .env.e2e DATABASE_URL (copy reset-db's getDbUrl + Client pattern), polls
       `SELECT status, row_count FROM export_jobs WHERE id=$1` every 250ms until status IN ('DONE','FAILED') or
       timeout, returns the row. This is how the E2E proves the worker completed the job (no FE job-status hook).
  </action>
  <verify>
    <automated>cd frontend && grep -q "cmd/worker" e2e/lib/backend.ts && grep -q "PAYROLL_ENCRYPTION_KEY" e2e/.env.e2e && grep -q "payslips" e2e/lib/reset-db.ts && grep -q "export_jobs" e2e/lib/reset-db.ts && grep -q "pollExportJob" e2e/lib/e8-helpers.ts && grep -q "SWP-PS-90119" e2e/lib/e8-helpers.ts && pnpm --filter @swp/web build && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `backend.ts` spawns `go run ./cmd/worker` (detached, same testEnv incl. PAYROLL_ENCRYPTION_KEY) and reaps it on teardown.
    - `.env.e2e` defines PAYROLL_ENCRYPTION_KEY (a valid base64 32-byte key) passed to seed + API + worker.
    - `reset-db.ts` TRUNCATE list contains payslips/payslip_components/payslip_benefits/payslip_audit_notes/export_jobs (app tables only; River tables untouched).
    - `e8-helpers.ts` exports the PS/PS_NOTE fixture maps + `payslipRow` + `pollExportJob` (export_jobs DB poll → DONE/FAILED) + re-exports apiAs/errorCode/waitForToken.
    - Any FE fix is the minimal {data}-fallback / classifyError-by-code change; `pnpm --filter @swp/web build` succeeds.
  </acceptance_criteria>
  <done>FE reads the real BE shapes; the worker runs in the harness; reset-db + e8-helpers (with the export_jobs poll) ready for the specs.</done>
</task>

<task type="auto">
  <name>Task 2: Archive + detail + audit-notes E2E specs</name>
  <files>frontend/e2e/tests/e8/archive.spec.ts, frontend/e2e/tests/e8/detail.spec.ts, frontend/e2e/tests/e8/audit-notes.spec.ts</files>
  <read_first>
    - frontend/e2e/tests/e7/approvals.spec.ts (the loginAs → goto → waitForToken → drive UI / apiAs pattern; DataTable div.border-b row assertions)
    - frontend/apps/web/src/features/e8-payroll/{payslip-archive-screen,payslip-detail-screen,audit-note-drawer}.tsx (the REAL selectors)
    - docs/api/E8-payroll/openapi.yaml getPayslip (FINAL + DECRYPT_FAIL) + audit-notes list/create
  </read_first>
  <action>
    Create `frontend/e2e/tests/e8/archive.spec.ts` — one test() per Gherkin AC, beforeEach resetDb(), HR persona:
    - ARCH-list: HR /payroll — SELECT year 2025 (seed periods are 2025) → the FINAL rows (SWP-PS-90121/90122/90123) render in the DataTable (div.border-b); each shows employee, period, take-home money string, FINAL status badge.
    - ARCH-decrypt-fail-row: the SWP-PS-90119 row renders with the DECRYPT_FAIL status badge (warn) + "—" money placeholders, at 200 (the page is NOT an error) — assert the row is present alongside the FINAL rows.
    - ARCH-filter: filter by status=DECRYPT_FAIL → only the 90119 row; reset → all; filter by period 2025-11 → only 90123; filter by employee_id SWP-EMP-1042 (SearchField) → only 90121.
    - ARCH-empty: filter to a year with no payslips (e.g. 2099 via apiAs or the year select if available) → the no-history empty state (meta.code MISSING_PAYROLL_HISTORY drives EmptyState fresh). (If the year select tops out below 2099, assert via apiAs GET /payslips?year=2099 → data:[] + meta.code.)

    Create `frontend/e2e/tests/e8/detail.spec.ts` (HR):
    - DETAIL-final: open SWP-PS-90121 detail → header (employee, period, working_days), earnings (Gaji Pokok etc.), deductions (BPJS/PPh 21), benefits, take-home card with the money string, source "lumen_swp #44218" — all decrypted from the real ciphertext.
    - DETAIL-decrypt-fail: open SWP-PS-90119 → the decrypt-fail Banner (t('detail.decryptFailTitle')); money shows "—"; earnings/deductions/benefits show the DecryptPlaceholder; the "Ekspor" button is hidden (decrypt-fail) — at 200, not an error page.

    Create `frontend/e2e/tests/e8/audit-notes.spec.ts` (HR):
    - NOTES-list: open the audit-note drawer (or the detail notes section) for SWP-PS-90119 → the two seeded notes (SWP-PS-90119-NOTE-1/2) render oldest-first with author "Sari Hadi".
    - NOTES-create: open the drawer for SWP-PS-90121, type a note (≥ 8 chars per the Zod min), "Simpan" → success toast; the new note appears in the list (refetch). Assert via the UI list AND apiAs GET /payslips/SWP-PS-90121/audit-notes shows the new note with a `SWP-PS-90121-NOTE-1` composite id.
    - NOTES-validation: a < 8-char note is blocked client-side (submit disabled / inline error) — assert the Zod min message; (the BE also enforces ≥1 but the FE min is 8).
  </action>
  <verify>
    <automated>cd frontend && grep -qE "DECRYPT_FAIL|decrypt" e2e/tests/e8/archive.spec.ts && grep -qE "DECRYPT_FAIL|decrypt|Ekspor|placeholder" e2e/tests/e8/detail.spec.ts && grep -qE "Simpan|audit|NOTE-" e2e/tests/e8/audit-notes.spec.ts && pnpm --filter @swp/e2e exec playwright test tests/e8/archive.spec.ts tests/e8/detail.spec.ts tests/e8/audit-notes.spec.ts --reporter=line && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `archive.spec.ts` covers the FINAL list (year 2025), the DECRYPT_FAIL row rendering at 200, status/period/employee filters, and the empty/missing-history state.
    - `detail.spec.ts` covers the FINAL full breakdown (decrypted) + the DECRYPT_FAIL state (banner, "—" money, placeholders, no export button).
    - `audit-notes.spec.ts` covers listing the seeded notes, creating one (≥8 chars) that appears in the list + the composite NOTE id via apiAs, and the < 8-char validation block.
    - The three specs run green headless against the real stack.
  </acceptance_criteria>
  <done>Archive + detail + audit-notes specs green vs real FE↔Go↔ephemeral Postgres; DECRYPT_FAIL renders at 200.</done>
</task>

<task type="auto">
  <name>Task 3: Export (worker-completes-job) + RBAC E2E + full-suite regression</name>
  <files>frontend/e2e/tests/e8/export.spec.ts, frontend/e2e/tests/e8/rbac.spec.ts</files>
  <read_first>
    - frontend/apps/web/src/features/e8-payroll/payroll-export.tsx (the PayrollExportButton + useExportPayslips surface + which screen mounts it)
    - frontend/e2e/lib/e8-helpers.ts (Task 1 — apiAs + pollExportJob + PS ids)
    - frontend/e2e/tests/e7/scope-negatives.spec.ts (the apiAs real-403 pattern to copy)
    - docs/api/E8-payroll/openapi.yaml exportPayslips (202 + PayslipExportJob + EXPORT_TOO_LARGE) + the Forbidden response
  </read_first>
  <action>
    Create `frontend/e2e/tests/e8/export.spec.ts` (HR persona) — THE HEADLINE:
    - EXPORT-worker-completes: trigger an export for period 2025-12 — via the real PayrollExportButton if it
      is mounted (assert the QUEUED toast + the ExportJobCard renders), AND/OR via apiAs POST /payslips:export
      {period:"2025-12", format:"XLSX", confidential:true} → assert 202 + the body has an `id` matching
      `^SWP-EXP-\d+$` + status "QUEUED" + confidential true. THEN call `pollExportJob(jobId)` → assert the
      `export_jobs` row transitions to **DONE** (the REAL River worker processed it; NOT mocked) with a
      row_count > 0. This is success criterion 2.
    - EXPORT-confidential-lock: POST {period:"2025-12", confidential:false} via apiAs → the 202 body reflects
      confidential:**true** (server-forced).
    - EXPORT-too-large: apiAs POST with a scope that exceeds the threshold (if the seed can't reach 50k rows,
      assert the guard via apiAs against a request the BE rejects — OR document that EXPORT_TOO_LARGE is fully
      covered by the 10-03 contract test and assert here only the happy 202+DONE; prefer driving it honestly if
      a small configurable threshold path exists, else cite the contract-test coverage in a comment).
    - EXPORT-no-scope: apiAs POST {} (no period/year) → 422.

    Create `frontend/e2e/tests/e8/rbac.spec.ts`:
    - RBAC-agent: loginAs PERSONAS.agent → /payroll renders the no-permission EmptyState; AND apiAs (agent token)
      GET /payslips → 403 (the real BE gate).
    - RBAC-leader: loginAs PERSONAS.shiftLeader → /payroll no-permission; apiAs GET /payslips → 403; apiAs
      POST /payslips:export → 403; apiAs GET /payslips/SWP-PS-90121/audit-notes → 403.
    - RBAC-hr-allowed: HR apiAs GET /payslips → 200 (the positive control).

    Run the FULL e1–e8 suite headless to confirm zero regressions; commit specs under
    `frontend/e2e/tests/e8/`.
  </action>
  <verify>
    <automated>cd frontend && grep -q "pollExportJob" e2e/tests/e8/export.spec.ts && grep -q "DONE" e2e/tests/e8/export.spec.ts && grep -qE "SWP-EXP|:export" e2e/tests/e8/export.spec.ts && grep -q "403" e2e/tests/e8/rbac.spec.ts && pnpm --filter @swp/e2e exec playwright test tests/e8 --reporter=line && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `export.spec.ts` asserts 202 + a `SWP-EXP-…` job id AND `pollExportJob` → export_jobs.status == DONE (real worker, row_count > 0); plus the confidential-true lock and the no-scope 422.
    - `rbac.spec.ts` asserts agent + shift_leader get the no-permission UI AND a real BE 403 on /payslips (+ export + audit-notes), with an HR 200 positive control.
    - All e8 specs green; the full e1–e8 headless suite passes with zero failures.
  </acceptance_criteria>
  <done>Export proves worker completion via the export_jobs DB poll; RBAC 403 enforced; no regression across e1–e8; Phase 10 closes PAY-01/PAY-02.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm --filter @swp/e2e exec playwright test tests/e8 --reporter=line` → all e8 specs pass headless.
- Full suite `pnpm --filter @swp/e2e exec playwright test --reporter=line` → 0 failures (no regression in e1–e7).
- Every one of the 5 FE-used E8 endpoints is exercised by a spec or apiAs call; no out-of-scope endpoint (no PDF, no forward-export, no payslip write/405). The export proof is the export_jobs row reaching DONE via the REAL worker, not a 202-only assertion.
</verification>

<success_criteria>
The e8-payroll screens drive the real Go BE green: the archive list + filters, the DECRYPT_FAIL row
at 200, the detail breakdown (decrypted) + decrypt-fail state, audit-note list + create, the export
(202 + job id THEN the River worker flips export_jobs → DONE proven via a DB poll), and RBAC 403 for
agent/shift_leader are all asserted via the REAL component selectors / apiAs against an ephemeral
Postgres, headless, with the worker actually running. The full e1–e8 suite is green. Closes
PAY-01 / PAY-02.
</success_criteria>

<output>
After completion, create `.planning/phases/10-e8-payroll/10-04-SUMMARY.md`.
</output>
