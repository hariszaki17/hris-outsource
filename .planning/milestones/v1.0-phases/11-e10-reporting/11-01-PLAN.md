---
phase: 11-e10-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/db/migrations/00035_notifications.sql
  - backend/db/migrations/00036_export_jobs_generalize.sql
  - backend/db/queries/reporting/notifications.sql
  - backend/db/queries/reporting/exports.sql
  - backend/db/queries/reporting/dashboard.sql
  - backend/db/queries/reporting/billable.sql
  - backend/internal/domain/reporting/notification.go
  - backend/internal/domain/reporting/export.go
  - backend/internal/domain/reporting/dashboard.go
  - backend/internal/domain/reporting/billable.go
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, RPT-04]
user_setup: []

must_haves:
  truths:
    - "A notifications table exists with recipient_id, kind, title, body, deep_link (epic/entity_id/path), actor (id/label), is_critical, read_at, created_at; id allocated via DEFAULT 'SWP-NTF-'||swp_next_id('NTF')"
    - "The Phase-10 export_jobs table is generalized: status CHECK adds CANCELLED, format CHECK adds EXCEL, and report_type / filters(jsonb) / audit_log_entry_id / progress_percent / expires_at columns are added — the PAYSLIP_EXPORT path keeps working unchanged"
    - "sqlc generates typed queries for notifications (list cursor + unread filter, mark-read, mark-all-read, insert), exports (insert/get/cancel/update-status), dashboard pending-count aggregations, and billable aggregation over verified attendance"
    - "make gen succeeds and the new reporting sqlc package compiles"
  artifacts:
    - path: "backend/db/migrations/00035_notifications.sql"
      provides: "notifications table + index on (recipient_id, created_at DESC) and partial unread index"
      contains: "read_at"
    - path: "backend/db/migrations/00036_export_jobs_generalize.sql"
      provides: "ALTER export_jobs: CANCELLED status, EXCEL format, report_type/filters/audit_log_entry_id/progress_percent/expires_at columns"
      contains: "CANCELLED"
    - path: "backend/db/queries/reporting/notifications.sql"
      provides: "ListNotifications/InsertNotification/MarkNotificationRead/MarkAllNotificationsRead/GetNotification sqlc queries"
      contains: "MarkAllNotificationsRead"
    - path: "backend/db/queries/reporting/exports.sql"
      provides: "InsertExportJobGeneric/GetExportJob/UpdateExportJobStatus/CancelExportJob sqlc queries (report_type + filters)"
      contains: "report_type"
    - path: "backend/db/queries/reporting/dashboard.sql"
      provides: "pending-count aggregations: attendance PENDING_VERIFY, leave PENDING_L1/HR, OT PENDING, placements expiring 30d, today clock-in/late/absent"
      contains: "count"
    - path: "backend/db/queries/reporting/billable.sql"
      provides: "billable aggregation over verified attendance on billable codes joined to placements"
      contains: "is_verified"
    - path: "backend/internal/domain/reporting/notification.go"
      provides: "Notification + DeepLink + Actor + NotificationKind domain types"
      contains: "DeepLink"
    - path: "backend/internal/domain/reporting/export.go"
      provides: "ExportJob domain type with ReportType/ExportStatus/ExportFormat + filters map"
      contains: "ExportStatus"
  key_links:
    - from: "backend/db/migrations/00036_export_jobs_generalize.sql"
      to: "backend/db/migrations/00034_export_jobs.sql"
      via: "ALTER TABLE export_jobs (reuse the Phase-10 table, do not recreate)"
      pattern: "ALTER TABLE export_jobs"
    - from: "backend/db/queries/reporting/notifications.sql"
      to: "swp_next_id('NTF')"
      via: "INSERT relies on the column DEFAULT for id allocation"
      pattern: "swp_next_id"
---

<objective>
Lay the E10 data foundation: a new `notifications` table, a generalization of the Phase-10
`export_jobs` table so it can carry any report_type (not just PAYSLIP_EXPORT), and the sqlc
queries + domain types that 11-02 / 11-02b build their services on. Read-only aggregation
queries (dashboard counts, billable report) live here too. This is a data-layer-only plan —
no services, handlers, or routes. Mirror the Phase-10 10-01 slice EXACTLY (migration → sqlc →
domain → `make gen`).

Purpose: give the wave-2 service plans typed queries + domain shapes so they never hand-roll SQL.
Output: 2 migrations, 4 sqlc query files in a new `reporting/` dir, 4 domain files, regenerated sqlc.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-e10-reporting/11-CONTEXT.md
@.planning/reference/backend-build-conventions.md

<interfaces>
<!-- The export_jobs table to GENERALIZE (Phase-10, do NOT recreate): -->
From backend/db/migrations/00034_export_jobs.sql:
  id text PK DEFAULT ('SWP-EXP-'||swp_next_id('EXP'));
  kind text DEFAULT 'PAYSLIP_EXPORT';
  status text CHECK (status IN ('QUEUED','RUNNING','DONE','FAILED'));   -- 11-01 adds 'CANCELLED'
  format text CHECK (format IN ('XLSX'));                              -- 11-01 adds 'EXCEL'
  confidential bool DEFAULT true; requested_by_id text; requested_by_name text;
  scope_period text; scope_year int; scope_employee_ids text[];
  row_count int; artifact_ref text; error_message text;
  requested_at/started_at/completed_at timestamptz;

<!-- ids.go ALREADY has both prefixes — DO NOT touch ids.go: -->
From backend/internal/platform/ids/ids.go:
  Notification Prefix = "NTF"   // SWP-NTF-*
  ExportJob    Prefix = "EXP"   // SWP-EXP-*

<!-- The Notification wire shape the FE + contract require (openapi E10 components.schemas.Notification): -->
  { id, kind(NotificationKind enum), title, body, read_at(nullable), created_at,
    deep_link:{epic(E2..E8), entity_id(nullable), path}, actor:{id(nullable), label}, is_critical }

<!-- ExportJob wire shape (openapi components.schemas.ExportJob) — status enum is
     QUEUED/PROCESSING/COMPLETED/FAILED/CANCELLED on the WIRE (FE e10-shared.tsx maps these);
     the DB keeps RUNNING/DONE — the service maps RUNNING<->PROCESSING, DONE<->COMPLETED at the DTO boundary (11-02b). -->
  { id, report_type(ReportType), status(ExportStatus), format(ExportFormat),
    confidential, progress_percent(nullable), filename(nullable), size_bytes(nullable),
    file_url(nullable), error:{code,message}(nullable), filters(object),
    audit_log_entry_id, requester_id, requested_at, completed_at(nullable), expires_at(nullable) }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 00035 — notifications table</name>
  <read_first>
    - backend/db/migrations/00034_export_jobs.sql (the goose Up/Down + swp_next_id DEFAULT pattern to mirror)
    - backend/db/migrations/00028_leave_requests.sql or any 0002*/0003* migration (FK + index conventions, timestamptz now())
    - docs/api/E10-reporting/openapi.yaml components.schemas.Notification + NotificationKind + DeepLink (the exact columns/enums)
    - backend/internal/platform/ids/ids.go (confirm Notification="NTF" — do NOT edit)
  </read_first>
  <action>
    Create backend/db/migrations/00035_notifications.sql with a goose Up/Down. CREATE TABLE notifications:
      id text PRIMARY KEY DEFAULT ('SWP-NTF-' || swp_next_id('NTF')),
      recipient_id text NOT NULL,                          -- SWP-USR-* or SWP-EMP-* (the dispatch helper resolves)
      kind text NOT NULL,                                  -- NotificationKind enum string (no CHECK — forward-compat with new kinds)
      title text NOT NULL,
      body text NOT NULL,
      deep_link_epic text,                                 -- 'E2'..'E8' (nullable; some notifs have no entity)
      deep_link_entity_id text,                            -- SWP-LR-* etc (nullable)
      deep_link_path text NOT NULL DEFAULT '',             -- client route path
      actor_id text,                                       -- SWP-USR-* (nullable = system actor)
      actor_label text NOT NULL DEFAULT 'system',
      is_critical boolean NOT NULL DEFAULT false,
      read_at timestamptz,                                 -- null = unread
      created_at timestamptz NOT NULL DEFAULT now();
    Indexes:
      CREATE INDEX notifications_recipient_created_idx ON notifications (recipient_id, created_at DESC, id DESC);   -- cursor list, newest-first
      CREATE INDEX notifications_unread_idx ON notifications (recipient_id) WHERE read_at IS NULL;                 -- unread filter + count
    Down: DROP TABLE IF EXISTS notifications.
    Match the comment style of 00034 (a header block explaining the table + the swp_next_id DEFAULT).
  </action>
  <verify>
    <automated>cd backend && grep -q "read_at" db/migrations/00035_notifications.sql && grep -q "swp_next_id('NTF')" db/migrations/00035_notifications.sql && grep -q "notifications_recipient_created_idx" db/migrations/00035_notifications.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File backend/db/migrations/00035_notifications.sql exists with `-- +goose Up` and `-- +goose Down`.
    - notifications table has columns: recipient_id, kind, title, body, deep_link_epic, deep_link_entity_id, deep_link_path, actor_id, actor_label, is_critical, read_at, created_at.
    - id column DEFAULT is `('SWP-NTF-' || swp_next_id('NTF'))`.
    - Two indexes present: notifications_recipient_created_idx and notifications_unread_idx (partial WHERE read_at IS NULL).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migration 00036 — generalize export_jobs (reuse, never recreate)</name>
  <read_first>
    - backend/db/migrations/00034_export_jobs.sql (the EXISTING table + its status/format CHECK constraints to ALTER)
    - docs/api/E10-reporting/openapi.yaml components.schemas.ExportJob + ReportType + ExportStatus + ExportFormat (target wire fields)
    - backend/internal/service/payroll/export_service.go (the PAYSLIP path that MUST keep working — new columns must be nullable / defaulted)
  </read_first>
  <action>
    Create backend/db/migrations/00036_export_jobs_generalize.sql (goose Up/Down) that ALTERs the EXISTING export_jobs table (do NOT DROP/CREATE — the Phase-10 PAYSLIP_EXPORT path depends on it):
      Up:
        ALTER TABLE export_jobs DROP CONSTRAINT IF EXISTS export_jobs_status_check;
        ALTER TABLE export_jobs ADD CONSTRAINT export_jobs_status_check CHECK (status IN ('QUEUED','RUNNING','DONE','FAILED','CANCELLED'));
        ALTER TABLE export_jobs DROP CONSTRAINT IF EXISTS export_jobs_format_check;
        ALTER TABLE export_jobs ADD CONSTRAINT export_jobs_format_check CHECK (format IN ('XLSX','EXCEL'));
        ALTER TABLE export_jobs ADD COLUMN IF NOT EXISTS report_type text NOT NULL DEFAULT 'PAYSLIPS';   -- ReportType; default keeps Phase-10 rows valid
        ALTER TABLE export_jobs ADD COLUMN IF NOT EXISTS filters jsonb NOT NULL DEFAULT '{}'::jsonb;     -- echoed filter set
        ALTER TABLE export_jobs ADD COLUMN IF NOT EXISTS audit_log_entry_id text;                        -- INV-5 traceability
        ALTER TABLE export_jobs ADD COLUMN IF NOT EXISTS progress_percent integer;                       -- worker progress (nullable)
        ALTER TABLE export_jobs ADD COLUMN IF NOT EXISTS expires_at timestamptz;                          -- file retention
      Down: reverse the ADD COLUMNs and restore the original CHECK constraints (status IN QUEUED/RUNNING/DONE/FAILED, format IN XLSX).
    NOTE in a comment: the WIRE status enum is QUEUED/PROCESSING/COMPLETED/FAILED/CANCELLED; the DB keeps RUNNING/DONE and the 11-02b service maps RUNNING<->PROCESSING and DONE<->COMPLETED at the DTO boundary. confidential stays as-is (Phase-10 forces true for payslips; generic exports default it per request).
  </action>
  <verify>
    <automated>cd backend && grep -q "CANCELLED" db/migrations/00036_export_jobs_generalize.sql && grep -q "report_type" db/migrations/00036_export_jobs_generalize.sql && grep -q "ALTER TABLE export_jobs" db/migrations/00036_export_jobs_generalize.sql && ! grep -q "CREATE TABLE export_jobs" db/migrations/00036_export_jobs_generalize.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Migration ALTERs export_jobs (no CREATE TABLE / DROP TABLE of export_jobs in the Up).
    - status CHECK now includes CANCELLED; format CHECK now includes EXCEL.
    - Columns added: report_type, filters (jsonb), audit_log_entry_id, progress_percent, expires_at.
    - All new columns are nullable or defaulted so the existing Phase-10 PAYSLIP rows + InsertExportJob path stay valid.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: sqlc queries (reporting/) + domain types + make gen</name>
  <read_first>
    - backend/db/queries/payroll/*.sql or backend/db/queries/foundations/*.sql (sqlc :many/:one/:exec + cursor keyset pattern with (created_at,id) < (@cursor_*) — copy the exact keyset shape used by the audit-log or payslip list)
    - backend/internal/domain/payroll/*.go (domain struct + enum const pattern to mirror for reporting domain)
    - backend/db/queries/attendance/*.sql + backend/db/queries/leave/*.sql + backend/db/queries/overtime/*.sql (the verified-attendance / pending-status columns the dashboard + billable aggregations read: attendance.verification_status, is_verified, leave_requests.status, overtime.status, placements end_date)
    - docs/api/E10-reporting/openapi.yaml (BillableReport / Dashboard pending counts — the exact aggregates)
    - backend/Makefile + backend/sqlc.yaml (confirm db/queries/* glob picks up the new reporting/ subdir; make gen target)
  </read_first>
  <action>
    Create the new sqlc query dir backend/db/queries/reporting/ with FOUR files:

    1) notifications.sql:
       - ListNotifications :many — WHERE recipient_id=@recipient AND (@read_state filter: 'ALL' no-op, 'UNREAD' read_at IS NULL, 'READ' read_at IS NOT NULL) AND (@kind filter optional) AND keyset (created_at,id) < (@cursor_created_at,@cursor_id) ORDER BY created_at DESC, id DESC LIMIT @lim. (Use sqlc.narg for nullable cursor + kind; mirror the audit-log keyset.)
       - GetNotification :one — by id + recipient_id (scope=self).
       - InsertNotification :one — recipient_id, kind, title, body, deep_link_* , actor_id, actor_label, is_critical (id via DEFAULT) RETURNING *.  (called by the worker in 11-02.)
       - MarkNotificationRead :one — UPDATE SET read_at = COALESCE(read_at, now()) WHERE id=@id AND recipient_id=@recipient RETURNING * (no-op if already read).
       - MarkAllNotificationsRead :execrows — UPDATE SET read_at=now() WHERE recipient_id=@recipient AND read_at IS NULL AND (@before::timestamptz IS NULL OR created_at < @before).
       - CountUnreadNotifications :one — COUNT(*) WHERE recipient_id=@recipient AND read_at IS NULL.

    2) exports.sql:
       - InsertExportJobGeneric :one — report_type, format, confidential, filters(jsonb), requested_by_id, requested_by_name, audit_log_entry_id, status default QUEUED RETURNING *.
       - GetExportJob :one — by id (+ requester scope handled in service).
       - UpdateExportJobStatusGeneric :one — SET status, progress_percent, row_count, artifact_ref, filename:=artifact_ref, error_message, expires_at, started_at, completed_at WHERE id RETURNING *.
       - CancelExportJob :one — UPDATE SET status='CANCELLED', completed_at=now() WHERE id=@id AND status IN ('QUEUED','RUNNING') RETURNING * (no-op-safe: service re-reads if 0 rows).
       (Keep the Phase-10 export_repo queries untouched; these are NEW generic queries.)

    3) dashboard.sql (read-only aggregations, scope params passed by service):
       - CountPendingAttendanceVerify :one — attendance WHERE verification_status='PENDING_VERIFY' AND (@company::text IS NULL OR company_id=@company).
       - CountPendingLeaveApprove :one — leave_requests WHERE status IN ('PENDING_L1','PENDING_HR') AND (@company IS NULL OR company_id=@company).
       - CountPendingOtApprove :one — overtime WHERE status='PENDING' AND (@company IS NULL OR company_id=@company).
       - CountExpiringPlacements30d :one — placements WHERE lifecycle_status IN ('ACTIVE','EXPIRING') AND end_date <= (@today::date + 30) AND end_date >= @today.
       - LeaderTodayStatus :one — for a company+today: shifts_total (schedule_entries count), clocked_in/late_count/absent_count derived from attendance verification/late flags, pending_verifications. (Use COALESCE to 0; match LeaderDashboardToday fields.)
       - CountActivePlacements / CountActiveCompanies :one — for HR KPI cards.

    4) billable.sql:
       - BillableAggregate :many — over attendance a JOIN placements p JOIN attendance_codes ac WHERE a.verification_status='VERIFIED' AND ac.is_billable=true AND a.attendance_shift_date BETWEEN @start AND @end AND (@company IS NULL OR p.company_id=@company) AND (@service_line IS NULL OR p.service_line_id=@service_line) GROUP BY (@group_by → employee_id / shift_date / shift_master_id); emit group_key, group_label, company_id, company_name, service_line_id, service_line_name, worked_hours, billable_hours, payable_hours, verified_record_count. (If the schema lacks an hours column, compute hours from check_in/check_out timestamps — document the formula; faithful stand-in is acceptable but must be derived from real rows, not constants.)
       - BillablePendingSummary :one — pending_records + pending_hours_estimate over the SAME filter but verification_status != 'VERIFIED'.
       Provide three group_by variants OR a single parameterized query with a @group_by CASE — Claude's discretion; document which.

    Then create the reporting domain package backend/internal/domain/reporting/ with:
       - notification.go: Notification{ID,Kind,Title,Body,ReadAt *time.Time,CreatedAt,DeepLink{Epic,EntityID *string,Path},Actor{ID *string,Label},IsCritical} + NotificationKind string consts for all 19 openapi kinds.
       - export.go: ExportJob{ID,ReportType,Status,Format,Confidential,ProgressPercent *int,Filename *string,SizeBytes *int64,FileURL *string,ErrCode/ErrMessage *string,Filters map[string]any,AuditLogEntryID,RequesterID,RequestedAt,CompletedAt/ExpiresAt *time.Time} + ReportType/ExportStatus/ExportFormat consts. ExportStatus consts: StatusQueued/StatusRunning/StatusDone/StatusFailed/StatusCancelled (DB values) — DTO mapping to wire (PROCESSING/COMPLETED) lives in 11-02b.
       - dashboard.go: HrDashboard/LeaderDashboard/AgentDashboard structs + ApprovalInboxRow{Kind,Label,Count,DeepLink} matching the openapi required fields.
       - billable.go: BillableReport{GeneratedAt,Filters,Summary,PendingSummary,Rows[]} + BillableReportRow matching openapi.

    Run `cd backend && make gen` and fix any sqlc errors. Verify the generated reporting package compiles.
  </action>
  <verify>
    <automated>cd backend && make gen && go build ./... && ls db/queries/reporting/notifications.sql db/queries/reporting/exports.sql db/queries/reporting/dashboard.sql db/queries/reporting/billable.sql internal/domain/reporting/notification.go && grep -q "MarkAllNotificationsRead" db/queries/reporting/notifications.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `make gen` exits 0 and `go build ./...` exits 0 (generated reporting sqlc package compiles).
    - 4 query files exist under backend/db/queries/reporting/.
    - notifications.sql contains ListNotifications, InsertNotification, MarkNotificationRead, MarkAllNotificationsRead, CountUnreadNotifications.
    - exports.sql contains InsertExportJobGeneric, GetExportJob, UpdateExportJobStatusGeneric, CancelExportJob.
    - billable.sql filters on verified attendance (is_verified / verification_status='VERIFIED') and is_billable codes.
    - backend/internal/domain/reporting/ has notification.go, export.go, dashboard.go, billable.go with the openapi-shaped structs + NotificationKind/ExportStatus consts.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd backend && make gen && go build ./...` exits 0.
- Both migrations apply cleanly (goose Up then Down round-trips) against a fresh DB; the Phase-10 export_jobs PAYSLIP path is unaffected (00034 still owns the base table).
- ids.go is NOT modified (NTF + EXP prefixes already exist).
- No service / handler / route / seed edits in this plan (data layer only).
</verification>

<success_criteria>
- notifications table + generalized export_jobs exist; sqlc queries + reporting domain compile.
- Wave-2 plans (11-02, 11-02b) can build services against these typed queries with zero hand-rolled SQL.
</success_criteria>

<output>
After completion, create `.planning/phases/11-e10-reporting/11-01-SUMMARY.md`.
</output>
