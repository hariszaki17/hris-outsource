---
phase: 11-e10-reporting
plan: 03
type: execute
wave: 4
depends_on: ["11-02", "11-02b"]
files_modified:
  - backend/internal/handler/reporting/reporting_testkit_test.go
  - backend/internal/handler/reporting/notification_handler_test.go
  - backend/internal/handler/reporting/dashboard_handler_test.go
  - backend/internal/handler/reporting/billable_handler_test.go
  - backend/internal/handler/reporting/export_handler_test.go
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, RPT-04]
user_setup: []

must_haves:
  truths:
    - "Contract tests assert GET /notifications cursor envelope + read_state/kind filters, :mark-read {data:Notification} with read_at flipped, :mark-all-read {marked_count}, scope=self 404 on non-owned id"
    - "Contract tests assert /dashboards/me role-shaped payloads (HrDashboard vs LeaderDashboard vs AgentDashboard) with required fields + pending_approvals_panel deep_link paths"
    - "Contract tests assert /reports/attendance-billable summary+pending_summary+rows shape, OUT_OF_SCOPE 403 for leader cross-company, REPORT_PERIOD_TOO_WIDE 422"
    - "Contract tests assert POST /exports 202 + transactional-outbox enqueue (one ReportExportArgs) + EXPORT_FORMAT_UNSUPPORTED 422 (PDF) + EXPORT_TOO_LARGE 422 + RATE_LIMITED_EXPORTS 429; GET /exports/{id} wire status (PROCESSING/COMPLETED); :cancel → CANCELLED; RBAC 403"
    - "go test ./internal/handler/reporting/... exits 0 — the drift gate replaces server codegen"
  artifacts:
    - path: "backend/internal/handler/reporting/reporting_testkit_test.go"
      provides: "newHarness(role,company,employee) on chi with mutable-principal middleware + fake repos + fake jobs (recording EnqueueTx) — mirrors the Phase-10 payroll testkit"
      contains: "newHarness"
    - path: "backend/internal/handler/reporting/export_handler_test.go"
      provides: "export 202 + recording fakeJobs (one ReportExportArgs) + format/size/throttle codes + cancel + status mapping"
      contains: "EXPORT_FORMAT_UNSUPPORTED"
    - path: "backend/internal/handler/reporting/notification_handler_test.go"
      provides: "list cursor + read_state filter + mark-read + mark-all-read marked_count + scope 404"
      contains: "marked_count"
  key_links:
    - from: "backend/internal/handler/reporting/export_handler_test.go"
      to: "the real ExportService + handler over fakes"
      via: "recording fakeJobs asserts EnqueueTx received exactly one ReportExportArgs with the job id"
      pattern: "ReportExportArgs"
    - from: "backend/internal/handler/reporting/reporting_testkit_test.go"
      to: "server.go router position"
      via: "stubIdempotency at the same router position + mutable-principal closure middleware"
      pattern: "RequireRole|principal"
---

<objective>
The E10 drift gate: Go contract tests over the REAL reporting services + handlers (via in-memory fake
repos + a recording fakeJobs + a mutable-principal chi harness), asserting the wire shapes match
docs/api/E10-reporting/openapi.yaml byte-for-byte for all 8 FE-used ops + the export error codes +
RBAC + cursor envelopes. Copy the Phase-10 payroll testkit harness EXACTLY (the established pattern —
fakeTx, fakeTxRunner, fake repos, recording fakeJobs, newHarness(role,...) on chi, stubIdempotency at
the server.go router position).

Purpose: prove the BE matches the contract the FE client is generated from (no server codegen).
Output: 1 testkit + 4 handler test files; `go test ./internal/handler/reporting/...` green.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-e10-reporting/11-CONTEXT.md
@.planning/phases/11-e10-reporting/11-02-PLAN.md
@.planning/phases/11-e10-reporting/11-02b-PLAN.md
@.planning/reference/backend-build-conventions.md
@.planning/reference/e2e-harness-spec.md

<interfaces>
<!-- The Phase-10 testkit to copy (the canonical drift-gate harness): -->
From backend/internal/handler/payroll/*_test.go:
  fakeTx (Exec no-op for audit-in-tx) + fakeTxRunner + in-memory fake repos over the REAL services+handler
  newHarness(role, company, employee) mounting services+handler on chi with a mutable-principal closure middleware
  recording fakeJobs implementing the svc Jobs seam → asserts one PayslipExportArgs (mirror → ReportExportArgs)
  stubIdempotency middleware at the same router position as server.go
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reporting testkit + notifications + dashboard + billable contract tests</name>
  <read_first>
    - backend/internal/handler/payroll/*_test.go (the FULL testkit: fakeTx, fakeTxRunner, fake repos, newHarness, decodeBody snapshot, RequireRole mount) — copy structure verbatim
    - backend/internal/handler/reporting/*.go (the REAL handlers + DTOs to test) + backend/internal/service/reporting/*.go (service signatures + the ports the fakes implement)
    - docs/api/E10-reporting/openapi.yaml examples: Notification (inbox), HrDashboard/LeaderDashboard/AgentDashboard, BillableReport (hrMonth + emptyAfterFilters), the OUT_OF_SCOPE/REPORT_PERIOD_TOO_WIDE envelopes
  </read_first>
  <action>
    reporting_testkit_test.go: build newHarness(role, companyID, employeeID) exactly like the payroll testkit — fakeTx + fakeTxRunner + in-memory fake repos (fakeNotificationRepo with a seeded set keyed by recipient; fakeDashboardRepo returning configurable counts; fakeBillableRepo returning configurable aggregate rows) over the REAL reporting services+handler, mounted on chi with the mutable-principal closure middleware + stubIdempotency at the server.go router position. Provide a recording fakeJobs (used by Task 2's export tests).

    notification_handler_test.go:
      - GET /notifications → 200 cursor envelope {data:[...], has_more, next_cursor}; assert read_state=UNREAD returns only read_at==null rows; kind filter narrows; scope=self (a notification for another recipient is NOT returned).
      - POST /notifications/{id}:mark-read → 200 {data:Notification} with read_at non-null; second call (already read) → 200 read_at preserved; non-owned id → 404 NOT_FOUND.
      - POST /notifications:mark-all-read → 200 {marked_count: N} (N = the unread count); after, list UNREAD is empty.
    dashboard_handler_test.go:
      - GET /dashboards/me as hr_admin → HrDashboard (role hr_admin, role_label "HR Admin", kpis + pending_approvals_panel with deep_link.path matching openapi); as super_admin → role super_admin + role_label "Super Admin", same shape; as shift_leader → LeaderDashboard (company scoped, today + pending_counts + schedule_alerts); as agent → AgentDashboard.
    billable_handler_test.go:
      - GET /reports/attendance-billable (hr) → 200 BillableReport with summary + pending_summary{pending_records,pending_hours_estimate,note} + rows[] matching BillableReportRow fields; verification_rate_pct null when zero records (emptyAfterFilters example).
      - leader requesting company != own → 403 OUT_OF_SCOPE.
      - period range > 1 year → 422 REPORT_PERIOD_TOO_WIDE with fields.period_end.

    `cd backend && go test ./internal/handler/reporting/... -run 'Notification|Dashboard|Billable' 2>&1 | tail -10`.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handler/reporting/... -run 'Notification|Dashboard|Billable' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - reporting_testkit_test.go has newHarness(role, company, employee) + fake repos + a recording fakeJobs, mirroring the payroll testkit.
    - Notifications tests assert cursor envelope, read_state/kind filters, mark-read read_at flip + no-op, mark-all-read marked_count, scope-self 404.
    - Dashboard tests assert all 4 role shapes + role_label difference + deep_link paths.
    - Billable tests assert summary+pending_summary+rows, OUT_OF_SCOPE 403, REPORT_PERIOD_TOO_WIDE 422.
    - The targeted `go test` run is green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Export framework contract tests (202 + outbox + error codes + status mapping)</name>
  <read_first>
    - backend/internal/handler/payroll/export_handler_test.go (the Phase-10 export contract test: 202 + recording fakeJobs assert one PayslipExportArgs + EXPORT_TOO_LARGE 422 — mirror for the generic export)
    - backend/internal/handler/reporting/export_handler.go + backend/internal/service/reporting/export_service.go (the real code under test + the DB→wire status mapping)
    - docs/api/E10-reporting/openapi.yaml /exports POST (202 ExportJob example, EXPORT_FORMAT_UNSUPPORTED/EXPORT_TOO_LARGE 422 examples, RATE_LIMITED_EXPORTS 429), /exports/{id} (processing/completed examples), :cancel (cancelled example)
  </read_first>
  <action>
    export_handler_test.go (using the testkit harness + recording fakeJobs):
      - POST /exports {report_type:ATTENDANCE_BILLABLE, format:EXCEL, filters:{period_start,period_end}} → 202; body is the ExportJob (status QUEUED); assert the recording fakeJobs received EXACTLY one ReportExportArgs whose JobID == the returned job id (transactional-outbox proof).
      - POST /exports format=PDF → 422 EXPORT_FORMAT_UNSUPPORTED (fields.format present).
      - POST /exports with a scope that the fakeBillableRepo reports as oversized → 422 EXPORT_TOO_LARGE (fields.period_end).
      - POST /exports tripping the throttle (configure the fake to report many recent exports) → 429 RATE_LIMITED_EXPORTS.
      - GET /exports/{id}: seed the fake export repo with a DB status RUNNING → assert wire status PROCESSING; DB DONE → wire COMPLETED (+ file_url/filename/size_bytes populated); non-owner caller → 404.
      - POST /exports/{id}:cancel on a QUEUED job → 200 status CANCELLED; on an already-terminal job → 200 no-op (status unchanged).
      - RBAC: agent → 403 on POST /exports (x-rbac excludes agent).

    `cd backend && go test ./internal/handler/reporting/... 2>&1 | tail -10` (full package green).
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handler/reporting/... 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - export_handler_test.go asserts 202 + exactly-one ReportExportArgs via recording fakeJobs (outbox proof).
    - EXPORT_FORMAT_UNSUPPORTED 422 (PDF), EXPORT_TOO_LARGE 422, RATE_LIMITED_EXPORTS 429 all asserted with the right code.
    - GET /exports/{id} status mapping asserted: DB RUNNING→PROCESSING, DB DONE→COMPLETED; non-owner → 404.
    - :cancel → CANCELLED (+ no-op on terminal); agent POST /exports → 403.
    - `go test ./internal/handler/reporting/...` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd backend && go test ./internal/handler/reporting/...` exits 0.
- All 8 FE-used E10 ops + the export error codes + RBAC + cursor envelopes asserted against the openapi byte-for-byte.
- The harness mirrors the Phase-10 payroll testkit (drift gate, no server codegen).
</verification>

<success_criteria>
- The E10 BE provably matches the contract the FE client is generated from; the contract tests are the regression gate for the FE wiring in 11-04.
</success_criteria>

<output>
After completion, create `.planning/phases/11-e10-reporting/11-03-SUMMARY.md`.
</output>
