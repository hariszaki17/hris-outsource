---
phase: 11-e10-reporting
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - backend/internal/repository/reporting/notification_repo.go
  - backend/internal/service/reporting/notification_service.go
  - backend/internal/service/reporting/ports.go
  - backend/internal/handler/reporting/notification_handler.go
  - backend/internal/handler/reporting/dto.go
  - backend/internal/platform/jobs/notify.go
  - backend/internal/platform/jobs/jobs.go
  - backend/internal/server/server.go
  - backend/cmd/api/main.go
  - backend/cmd/seed/seed.go
  - backend/internal/service/leave/leave_service.go
  - backend/internal/service/overtime/overtime_service.go
  - backend/internal/service/attendance/attendance_service.go
autonomous: true
requirements: [RPT-02]
user_setup: []

must_haves:
  truths:
    - "GET /notifications lists the caller's own notifications (cursor, newest-first, read_state UNREAD/READ/ALL filter, optional kind), scope=self — a user only ever sees their own rows"
    - "POST /notifications/{id}:mark-read flips read_at null→now and returns the updated Notification (no-op 200 if already read); POST /notifications:mark-all-read marks all unread for the caller and returns marked_count"
    - "The NotificationWorker.Work is UN-STUBBED — it INSERTs a real notifications row (resolving recipient + title/body/deep_link from NotificationArgs) instead of only logging"
    - "A reusable notify.Dispatch helper EnqueueTx's a NotificationArgs in the caller's tx (transactional outbox) — callable from any service inside its InTx closure"
    - "The prior-phase TODO(Phase-11) dispatch points in leave (approve-final/reject), overtime (approve-final/reject), and attendance (verify/reject) call notify.Dispatch — so auto-dispatched notifications are REAL; existing tests stay green (additive only)"
    - "Seed inserts a handful of notifications (mixed read/unread, across kinds) for the seeded personas so the list + mark-read flows render"
  artifacts:
    - path: "backend/internal/platform/jobs/notify.go"
      provides: "un-stubbed NotificationWorker.Work that inserts a notifications row + a notify.Dispatch helper"
      contains: "InsertNotification"
    - path: "backend/internal/service/reporting/notification_service.go"
      provides: "List (cursor + read_state + kind), MarkRead (self), MarkAllRead (self) with scope=self"
      contains: "MarkAllRead"
    - path: "backend/internal/handler/reporting/notification_handler.go"
      provides: "GET /notifications + :mark-read + :mark-all-read handlers (data-envelope + cursor envelope)"
      contains: "mark-all-read"
    - path: "backend/internal/service/leave/leave_service.go"
      provides: "leave approve-final/reject enqueue real notifications via notify.Dispatch"
      contains: "Dispatch"
    - path: "backend/internal/service/overtime/overtime_service.go"
      provides: "OT approve-final/reject enqueue real notifications"
      contains: "Dispatch"
    - path: "backend/internal/service/attendance/attendance_service.go"
      provides: "attendance verify/reject enqueue real notifications"
      contains: "Dispatch"
    - path: "backend/cmd/seed/seed.go"
      provides: "seedNotifications fixtures (mixed read/unread across kinds for personas)"
      contains: "seedNotifications"
  key_links:
    - from: "backend/internal/service/leave/leave_service.go"
      to: "backend/internal/platform/jobs (River → notifications row)"
      via: "notify.Dispatch(ctx, tx, NotificationArgs{...}) inside the approve/reject InTx"
      pattern: "Dispatch"
    - from: "backend/internal/platform/jobs/notify.go"
      to: "notifications table"
      via: "NotificationWorker.Work → InsertNotification"
      pattern: "InsertNotification"
    - from: "backend/internal/handler/reporting/notification_handler.go"
      to: "GET /notifications response"
      via: "cursor envelope {data:[],has_more,next_cursor}"
      pattern: "has_more|next_cursor"
---

<objective>
The milestone loop-closer. Deliver the notifications surface (list / mark-read / mark-all-read,
scope=self) AND make auto-dispatched notifications REAL: un-stub the River NotificationWorker so it
inserts a notifications row, add a reusable notify.Dispatch transactional-outbox helper, and
retro-wire the prior-phase TODO(Phase-11) dispatch points (leave, overtime, attendance) to enqueue
real notifications inside their existing write tx. Extend the seed with a handful of notifications.

This plan OWNS jobs.go + notify.go + the notifications routes in server.go + main.go wiring + the
prior-service retro-wire edits + seed.go notifications. 11-02b (dashboard/report/exports) appends
AFTER this plan's E10 route block. Mirror the Phase-10 slice (service → handler → routes under
RequireRole → seed). Match docs/api/E10-reporting/openapi.yaml byte-for-byte for the 3 notification ops.

Purpose: success-criterion 2 — notifications list + mark-read + auto-dispatched-from-earlier-phases.
Output: notifications service/handler/routes, un-stubbed worker + dispatch helper, retro-wire, seed.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-e10-reporting/11-CONTEXT.md
@.planning/phases/11-e10-reporting/11-01-PLAN.md
@.planning/reference/backend-build-conventions.md

<interfaces>
<!-- River outbox seam ALREADY wired (Phase-10) — reuse verbatim: -->
From backend/internal/platform/jobs/jobs.go:
  func (c *Client) EnqueueTx(ctx, tx pgx.Tx, args river.JobArgs) error
  func registerWorkers(w *river.Workers)
  func NewWorkerClient(pool *db.Pool)   // PayslipExportWorker is registered WITH the pool here; the un-stubbed NotificationWorker also needs the pool → move its AddWorker here

From backend/internal/platform/jobs/notify.go (the STUB to un-stub):
  type NotificationArgs struct { Event, RecipientID, EntityType, EntityID string; Data map[string]string }
  func (NotificationArgs) Kind() string { return "notification.dispatch" }
  func (w *NotificationWorker) Work(ctx, job) error   // currently only slog.Info — make it INSERT

<!-- apperr helpers: -->
From backend/internal/platform/apperr:
  Forbidden() 403; NotFound() 404; Invalid(fields) 400; Internal(err) 500

<!-- The prior-service TODO(Phase-11) retro-wire SITES (exact lines from grep, inside each InTx closure): -->
  leave_service.go:289  (approve-final) "leave approved"  → recipient = submitter (rec.EmployeeID)
  leave_service.go:350  (reject)        "leave rejected"  → recipient = submitter
  overtime_service.go:312 (approve-final) → submitter
  overtime_service.go:356 (reject)        → submitter
  attendance_service.go:186 (verify)      → record owner (employee)
  attendance_service.go:227 (reject)      → record owner
  (approve-l1 / next-stage points at leave:141, overtime:264 are OPTIONAL — wire if a clean single recipient exists, else leave the comment as a documented stub.)

<!-- Notification wire shape (openapi Notification) + mark-all-read response (openapi: {marked_count}). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-stub NotificationWorker + notify.Dispatch helper + register with pool</name>
  <read_first>
    - backend/internal/platform/jobs/notify.go (the stub Work + NotificationArgs)
    - backend/internal/platform/jobs/jobs.go (EnqueueTx + NewWorkerClient where PayslipExportWorker is registered WITH the pool — copy that pattern for NotificationWorker)
    - backend/internal/platform/jobs/payslip_export.go (NewPayslipExportWorker(pool) + how the worker opens a tx from the pool and updates a table — mirror for the notification insert)
    - backend/internal/repository/reporting/notification_repo.go (created in Task 2 — InsertNotification signature; to avoid an import cycle define a tiny InsertNotification interface or func in jobs/)
    - docs/api/E10-reporting/openapi.yaml NotificationKind enum + DeepLink (kind→epic/path used to build the row)
  </read_first>
  <action>
    1) Extend NotificationArgs (notify.go) with the fields the worker needs to write a COMPLETE row without a second query: add Title, Body string; Kind string (NotificationKind value; Event may mirror it for back-compat); DeepLinkEpic, DeepLinkEntityID, DeepLinkPath string; ActorID, ActorLabel string; IsCritical bool. Keep Kind() returning "notification.dispatch".

    2) Add a reusable dispatch helper in notify.go:
         func Dispatch(ctx context.Context, c *Client, tx pgx.Tx, args NotificationArgs) error { return c.EnqueueTx(ctx, tx, args) }
       (If a thinner seam reads cleaner, expose a Dispatcher interface { Dispatch(ctx, tx, NotificationArgs) error } implemented by *Client and inject it; Claude's discretion. The service CALL SITE must read `notify.Dispatch(` or `<seam>.Dispatch(`.)

    3) Un-stub NotificationWorker: NewNotificationWorker(pool *db.Pool) (mirror NewPayslipExportWorker). In Work(): open a tx/conn from the pool and INSERT a notifications row via the reporting repo's InsertNotification — recipient_id=args.RecipientID, kind=args.Kind (fallback args.Event), title=args.Title, body=args.Body, deep_link_* from args, actor_id/label from args, is_critical=args.IsCritical, created_at=now(). Keep the slog.Info line. On error return it so River backs off (the originating action already committed — never blocks it).

    4) jobs.go: MOVE the NotificationWorker registration from registerWorkers into NewWorkerClient (next to PayslipExportWorker) via river.AddWorker(workers, NewNotificationWorker(pool)) since it now needs the pool. Keep registerWorkers (may become empty) for pool-less workers.

    `cd backend && go build ./...`.
  </action>
  <verify>
    <automated>cd backend && grep -q "func Dispatch" internal/platform/jobs/notify.go && grep -q "InsertNotification" internal/platform/jobs/notify.go && grep -q "NewNotificationWorker(pool)" internal/platform/jobs/jobs.go && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - notify.go: NotificationWorker.Work calls InsertNotification (no longer a no-op slog-only stub).
    - notify.go contains `func Dispatch(` (or a Dispatcher seam with a Dispatch method) wrapping EnqueueTx.
    - NotificationArgs carries Title, Body, Kind, DeepLink*, Actor*, IsCritical.
    - jobs.go registers NewNotificationWorker(pool) inside NewWorkerClient (with the pool, like PayslipExportWorker).
    - `go build ./...` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Notifications repo + service + handler + routes + main.go</name>
  <read_first>
    - backend/db/queries/reporting/notifications.sql (the 11-01 sqlc queries: ListNotifications/GetNotification/InsertNotification/MarkNotificationRead/MarkAllNotificationsRead/CountUnreadNotifications)
    - backend/internal/repository/payroll/*.go (repo struct wrapping *db.Queries + sqlc-row→domain mapping — mirror)
    - backend/internal/service/payroll/payslip_service.go (service struct, actorUserID/actorEmployeeID helpers, cursor decode/encode, data-envelope) + backend/internal/platform/rbac (scope=self)
    - backend/internal/handler/payroll/handler.go + payslip_handler.go (decodeJSON, httpx.WriteJSON, cursor envelope, data envelope, apperr flow)
    - backend/internal/server/server.go (authenticated group + the "PAYROLL slice end" marker to append AFTER; RequireRole + d.Idempotency.Handler; Deps struct)
    - backend/cmd/api/main.go (how payroll services/Handler are constructed + added to server.Deps; the jobs.Client construction)
    - docs/api/E10-reporting/openapi.yaml /notifications (cursor/limit/read_state/kind), /{id}:mark-read, :mark-all-read (before_timestamp body, {marked_count}), x-rbac roles [all 4] scope self
  </read_first>
  <action>
    Repo (backend/internal/repository/reporting/notification_repo.go): wrap *db.Queries; methods List(ctx, recipientIDs, readState, kind, cursor, limit), Get, InsertNotification (export via a small interface so jobs/ can call it without an import cycle), MarkRead, MarkAllRead(before *time.Time) → rowsAffected, CountUnread. Map sqlc rows → domain.Notification (nullable read_at/actor_id/entity_id → pointers; build DeepLink + Actor).

    Service (backend/internal/service/reporting/notification_service.go + ports.go):
      - recipient resolution: a notification dispatched to the submitter's SWP-EMP-* must be visible to that logged-in user, while system/HR notifications target SWP-USR-*. So List filters recipient_id IN (actorUserID(ctx), actorEmployeeID(ctx)) — never other users' rows (scope=self honest). Pass both ids to the repo.
      - List(ctx, params) → ([]Notification, nextCursor, hasMore); opaque cursor (created_at,id) like the audit-log list.
      - MarkRead(ctx, id) → Notification: WHERE recipient match; 0 rows → 404 NOT_FOUND. No-op 200 if already read (COALESCE preserves read_at).
      - MarkAllRead(ctx, before *time.Time) → markedCount int.

    Handler (backend/internal/handler/reporting/notification_handler.go + dto.go):
      - GET /notifications → cursor envelope {data:[NotificationDTO], has_more, next_cursor}. DTO matches openapi Notification: read_at pointer WITHOUT omitempty (serializes null when unread); deep_link + actor always objects.
      - POST /notifications/{notification_id}:mark-read → {data: NotificationDTO}.
      - POST /notifications:mark-all-read → {marked_count: N} (openapi key). Optional before_timestamp from body. (The FE reads res.data.marked today — 11-04 fixes the FE to read marked_count; return marked_count per contract.)
      - Both action endpoints require Idempotency-Key (d.Idempotency.Handler).

    Routes (server.go) — append AFTER the "PAYROLL slice end" marker:
      // E10 REPORTING slice — NOTIFICATIONS (11-02). 11-02b appends dashboard/report/exports AFTER this block.
      r.Group(func(r chi.Router) {
        r.Use(rbac.RequireRole(auth.RoleSuperAdmin, auth.RoleHRAdmin, auth.RoleShiftLeader, auth.RoleAgent))
        r.Get("/notifications", d.Reporting.ListNotifications)
        r.With(d.Idempotency.Handler).Post("/notifications/{notification_id}:mark-read", d.Reporting.MarkNotificationRead)
        r.With(d.Idempotency.Handler).Post("/notifications:mark-all-read", d.Reporting.MarkAllNotificationsRead)
      })
    Add `Reporting *reportinghttp.Handler` to server.Deps (11-02b extends the same Handler with dashboard/report/export methods).

    main.go: construct the reporting repo + notification service + reporting Handler; add to Deps.

    `cd backend && go build ./...`.
  </action>
  <verify>
    <automated>cd backend && go build ./... && grep -q 'r.Get("/notifications"' internal/server/server.go && grep -q 'mark-all-read' internal/server/server.go && grep -q "marked_count" internal/handler/reporting/notification_handler.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - server.go has GET /notifications, POST /notifications/{notification_id}:mark-read, POST /notifications:mark-all-read under RequireRole(all 4 roles); action endpoints wrapped with Idempotency.Handler.
    - List returns a cursor envelope (has_more + next_cursor); read_state UNREAD/READ/ALL + optional kind honored.
    - mark-read returns the updated Notification in a {data} envelope; mark-all-read returns {marked_count}.
    - scope=self: a caller never sees another recipient's notifications; mark-read on a non-owned id → 404.
    - `go build ./...` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Retro-wire prior-phase dispatch points (leave, OT, attendance) + seed notifications</name>
  <read_first>
    - backend/internal/service/leave/leave_service.go ~289 (approve-final) / ~350 (reject) — TODO(Phase-11) inside each InTx; rec.EmployeeID / rec.CompanyID / actor in scope
    - backend/internal/service/overtime/overtime_service.go ~312 (approve-final) / ~356 (reject) — same shape
    - backend/internal/service/attendance/attendance_service.go ~186 (verify) / ~227 (reject) — record owner in scope
    - backend/internal/platform/jobs/notify.go (notify.Dispatch signature + NotificationArgs fields from Task 1)
    - each service struct + ports.go (grep `jobs`/`Jobs` to see if it already holds the River seam; if not, add it to the struct + constructor + main.go wiring — additive)
    - backend/cmd/seed/seed.go Seed() (seedLeave/seedOvertime ordering + persona ids SWP-USR-*/SWP-EMP-* + SWP-CMP-0021) + docs/api/E10-reporting/openapi.yaml NotificationKind (kind strings)
  </read_first>
  <action>
    Retro-wire — replace each TODO(Phase-11) comment with a real notify.Dispatch call inside the SAME tx (additive; do NOT change status transitions, audit, or return values):

    LEAVE (leave_service.go):
      - approve-final (~289): Dispatch kind=LEAVE_APPROVED, recipient=submitter (rec.EmployeeID), title="Cuti disetujui", body summarizing the dates, deep_link {epic:"E6", entity_id:id, path:"/leave-requests/"+id}, actor={actorUserID, label}, is_critical=true.
      - reject (~350): kind=LEAVE_REJECTED, recipient=rec.EmployeeID, title="Cuti ditolak", body=reason, deep_link E6.
    OVERTIME (overtime_service.go):
      - approve-final (~312): kind=OT_APPROVED, recipient=submitter, deep_link {epic:"E7", path:"/overtime/"+id}.
      - reject (~356): kind=OT_REJECTED, recipient=submitter, deep_link E7.
    ATTENDANCE (attendance_service.go):
      - verify (~186): Dispatch to the record's employee (owner) with the nearest honest kind (ATTENDANCE_VERIFY_NEEDED is the catalog's attendance kind — DOCUMENT this choice in a comment), title/body reflecting "kehadiran diverifikasi", deep_link {epic:"E5", path:"/attendance/"+id}.
      - reject (~227): same, to the owner, body reflecting rejection.

    For EVERY dispatched notification set Title/Body/DeepLink*/Actor*/IsCritical so the worker writes a complete row. Keep each call inside the existing InTx (rolled-back action never notifies — transactional outbox).

    MANDATORY coverage (E2E targets): leave approve-final + reject, OT approve-final + reject, attendance verify + reject MUST call notify.Dispatch. The l1/next-stage points (leave:141, overtime:264) are OPTIONAL — wire if a clean single recipient exists; otherwise leave `// Phase-11 stub: <reason>` and list them in the SUMMARY under "dispatch points left as stub".

    SEED (seed.go): add seedNotifications(ctx, pool) called in Seed() AFTER seedLeave/seedOvertime/seedPayroll. Insert ~6 notifications with EXPLICIT ids SWP-NTF-90001..90006 (ON CONFLICT (id) DO NOTHING for deterministic E2E + idempotency): mix read (read_at set) and unread (read_at null) across kinds (LEAVE_APPROVED, OT_APPROVED, ATTENDANCE_VERIFY_NEEDED, LEAVE_REQUEST_SUBMITTED, PLACEMENT_EXPIRING); recipient_id covering BOTH the HR persona SWP-USR-* AND the agent persona SWP-EMP-* so both list views render. created_at clearly-in-range Asia/Jakarta (now() and now()-interval '2 hours'/'1 day'). Seeding is ONLY for list rendering — the auto-dispatch proof is driven by a REAL action in 11-04.

    Build + keep existing tests green: `cd backend && go build ./... && go test ./internal/service/... 2>&1 | tail -3`.
  </action>
  <verify>
    <automated>cd backend && grep -lq "Dispatch" internal/service/leave/leave_service.go && grep -lq "Dispatch" internal/service/overtime/overtime_service.go && grep -lq "Dispatch" internal/service/attendance/attendance_service.go && grep -q "seedNotifications" cmd/seed/seed.go && go build ./... && go test ./internal/service/... 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - leave_service.go, overtime_service.go, attendance_service.go each contain a notify.Dispatch (or seam.Dispatch) call replacing the relevant TODO(Phase-11) comment.
    - Mandatory points wired: leave approve-final + reject, OT approve-final + reject, attendance verify + reject.
    - Each dispatch is inside the existing InTx closure (not after commit) and sets a complete NotificationArgs (title/body/deep_link/actor/is_critical).
    - seed.go has seedNotifications inserting ~6 fixtures (mixed read/unread, explicit SWP-NTF-9000x ids, recipients across SWP-USR-* + SWP-EMP-*).
    - `go build ./...` exits 0 and `go test ./internal/service/...` stays green (no regressions).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd backend && go build ./...` exits 0; `go test ./internal/service/...` green (additive retro-wire breaks nothing).
- NotificationWorker.Work inserts a real row; notify.Dispatch exists and is called from leave/OT/attendance.
- Notifications routes mounted under RequireRole(all 4 roles); scope=self enforced.
- Seed produces a renderable notification list for both HR + agent personas.
- This plan owns the shared server.go/main.go/jobs.go/notify.go/seed.go + prior-service edits; 11-02b appends after the NOTIFICATIONS route block.
</verification>

<success_criteria>
- Notifications list + mark-read + mark-all-read work against the real BE; a real prior-phase action (leave/OT approve, attendance verify) genuinely produces a notifications row via the un-stubbed worker.
</success_criteria>

<output>
After completion, create `.planning/phases/11-e10-reporting/11-02-SUMMARY.md` (list any dispatch points left as a documented stub).
</output>
