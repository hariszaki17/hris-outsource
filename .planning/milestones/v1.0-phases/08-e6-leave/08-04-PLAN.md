---
phase: 08-e6-leave
plan: 04
type: execute
wave: 4
depends_on: ["08-02", "08-03"]
files_modified:
  - frontend/apps/web/src/features/e6-leave/leave-approvals-screen.tsx
  - frontend/apps/web/src/features/e6-leave/leave-detail-screen.tsx
  - frontend/apps/web/src/features/e6-leave/leave-quotas-screen.tsx
  - frontend/apps/web/src/features/e6-leave/leave-calendar-screen.tsx
  - frontend/e2e/lib/e6-helpers.ts
  - frontend/e2e/lib/reset-db.ts
  - frontend/e2e/tests/e6/approvals.spec.ts
  - frontend/e2e/tests/e6/quotas.spec.ts
  - frontend/e2e/tests/e6/calendar.spec.ts
  - frontend/e2e/tests/e6/scope-negatives.spec.ts
  - frontend/e2e/tests/e6/inv3-loop-closer.spec.ts
autonomous: true
requirements: [LVE-01, LVE-02, LVE-03]

must_haves:
  truths:
    - "The real e6-leave screens drive the real Go BE (MSW off): approvals L1→final→approved + override + reject, quota list/adjust/bulk-grant, calendar render + show_pending"
    - "QUOTA_EXCEEDED/balance-recheck → the override modal path; leader cross-company :approve-l1 → 403"
    - "The INV-3 loop-closer is asserted end-to-end: approving a leave overlapping a seeded E4 schedule entry cancels it (E6 detail schedule_impact surfaces new_status='LEAVE'; the E4 GET /schedule surfaces the DB-backed status='CANCELLED_BY_LEAVE') AND a fresh E4 schedule attempt that day now hits SHIFT_OVER_LEAVE from the real approved_leave_days row"
    - "pnpm e2e is green headless with no e1..e5 regressions"
  artifacts:
    - path: "frontend/e2e/tests/e6/approvals.spec.ts"
      provides: "L1→final→approved + override + reject specs against real stack"
      contains: "approve"
    - path: "frontend/e2e/tests/e6/inv3-loop-closer.spec.ts"
      provides: "the loop-closer assertion (schedule cancelled + SHIFT_OVER_LEAVE from real source)"
      contains: "SHIFT_OVER_LEAVE"
    - path: "frontend/e2e/lib/reset-db.ts"
      provides: "leave tables added to TRUNCATE + approved_leave_days/schedule_entries reset between tests"
      contains: "leave_requests"
  key_links:
    - from: "frontend/apps/web/src/features/e6-leave/*"
      to: "real Go API /leave-* endpoints"
      via: "@swp/api-client e6 hooks (MSW off)"
      pattern: "useApproveLeaveRequest|useListLeaveQuotas|useGetLeaveCalendar"
    - from: "frontend/e2e/tests/e6/inv3-loop-closer.spec.ts"
      to: "approved_leave_days (real source)"
      via: "approve leave → E4 schedule:check returns SHIFT_OVER_LEAVE"
      pattern: "SHIFT_OVER_LEAVE"
---

<objective>
Wire the four E6 screens off MSW to the real BE and prove the whole epic with exhaustive full-stack
Playwright E2E under NEW `frontend/e2e/tests/e6/`, including the INV-3 loop-closer assertion. Mirror
the Phase-7 E5 E2E plan (07-04): real FE ↔ real Go API ↔ ephemeral Postgres, selectors anchored on
the REAL e6-leave components (read in this plan's context), `apiAs`/`waitForToken` helpers, and
`reset-db.ts` extended to truncate the leave tables + reset approved_leave_days/schedule_entries.

Purpose: close LVE-01/02/03 — every E6 screen works against the real backend, and the Phase-6
over-leave conflict now fires from the production leave source (loop closed).
Output: FE wiring fixes (if any), e6-helpers, reset-db edit, 5 E2E specs.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-e6-leave/08-CONTEXT.md
@.planning/phases/08-e6-leave/08-02-SUMMARY.md
@.planning/reference/e2e-harness-spec.md
@docs/api/E6-leave/openapi.yaml

<interfaces>
<!-- REAL e6-leave component selectors (read the .tsx files; these are the anchor points): -->
Routes: /leave (approvals queue), /leave/$leaveRequestId (detail), /leave/quotas, /leave/calendar.

leave-approvals-screen.tsx: DataTable rows = div.border-b (Phase-2 decision); per-row "Review" link
  → Link to="/leave/$leaveRequestId". Default status filter HR=PENDING_HR, SL=PENDING_L1.
  Filters: SearchField (q), FilterSelect status/company/type. Agent column renders employee_name.
leave-detail-screen.tsx: action buttons — t('detail.actionForward') (SL L1), t('detail.actionApprove')
  (HR final), t('detail.actionReject') opens RejectLeaveModal (#reject-reason textarea, min5,
  confirm t('reject.confirm')). Over-balance: button becomes t('detail.actionOverride') and/or
  BalanceChangedModal opens (#override-reason textarea, min10, confirm t('override.confirm')); the
  onError path opens the override modal when classifyError message includes "BALANCE". Timeline =
  <ol> entries; status badge via leaveStatusTone; noLeaderBadge for routing.no_leader.
  schedule_impact[] section (t('detail.sectionScheduleImpact')): each affected entry renders its
  new_status — the E6 contract value is 'LEAVE' (openapi ScheduleImpactEntry.new_status enum
  [LEAVE, UNASSIGNED]); the underlying E4 row's DB/grid status is 'CANCELLED_BY_LEAVE'.
leave-quotas-screen.tsx: route /leave/quotas. Table cols employee/leave_type/total/used/pending/remaining;
  per-row "Adjust" button (aria t('actions.adjustAriaLabel',{name})) opens AdjustQuotaModal (#delta number,
  #reason textarea, save t('adjust.saveBtn')). Bulk-grant: header t('actions.bulkGrant') opens BulkGrantModal
  (form→preview→apply; t('bulkGrant.previewBtn') then t('bulkGrant.applyBtn')). Field errors via applyFieldErrors.
leave-calendar-screen.tsx: route /leave/calendar. Month nav prev/next (aria t('prevMonth')/t('nextMonth'));
  show_pending Toggle (role=switch, aria t('showPendingAria')); DayCell renders Avatar + employee_name +
  StatusBadge(leave_type_code); clash cell bg-warn-bg + t('clash'); pending entries border-dashed.
  HR uses ClientCompanyPicker; SL company filter disabled.

<!-- E4 vs E6 status-value boundary (CRITICAL for the INV-3 assertions): -->
  E4 GET /schedule → ScheduleEntry.status enum [SCHEDULED, CANCELLED_BY_LEAVE, MODIFIED] — a cancelled
    entry surfaces 'CANCELLED_BY_LEAVE' (this is the DB value; migration 00024 CHECK).
  E6 GET /leave-requests/{id} → schedule_impact[].new_status enum [LEAVE, UNASSIGNED] — the same cancel
    surfaces 'LEAVE' here. So: assert 'LEAVE' on the E6 detail schedule_impact, 'CANCELLED_BY_LEAVE' on the
    E4 grid / GET /schedule. There is NO raw DB row with status='LEAVE' to assert.

<!-- Seeded E6 fixtures (from 08-02 seedLeave) the specs target: -->
SWP-LR-8001 Dewi@CMP-0021 PENDING_L1 (Rudi L1 target)
SWP-LR-8002 Dewi@CMP-0021 PENDING_HR leader-approved (HR final target)
SWP-LR-8003 Budi@CMP-0022 PENDING_HR over-balance (BALANCE_RECHECK/override target; no_leader)
SWP-LR-8004 Budi@CMP-0022 PENDING_L1 (Rudi cross-company OUT_OF_SCOPE target)
SWP-LR-8005 APPROVED, SWP-LR-8006 REJECTED (list filters + calendar)
SWP-LR-8007 Dewi@CMP-0021 PENDING_HR, overlaps SWP-SCH-6002 (monday+2) — the INV-3 loop-closer target
Quotas: Dewi remaining 8; Budi remaining 1 (drives QUOTA_EXCEEDED/override).
Personas: PERSONAS.hrAdmin (Sari), PERSONAS.shiftLeader (Rudi @ Plaza Senayan / CMP-0021).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FE wiring (MSW off) + e6-helpers + reset-db leave tables</name>
  <files>frontend/apps/web/src/features/e6-leave/leave-approvals-screen.tsx, frontend/apps/web/src/features/e6-leave/leave-detail-screen.tsx, frontend/apps/web/src/features/e6-leave/leave-quotas-screen.tsx, frontend/apps/web/src/features/e6-leave/leave-calendar-screen.tsx, frontend/e2e/lib/e6-helpers.ts, frontend/e2e/lib/reset-db.ts</files>
  <read_first>
    - frontend/apps/web/src/features/e6-leave/{leave-approvals-screen,leave-detail-screen,leave-quotas-screen,leave-calendar-screen,leave-overlays}.tsx (the REAL screens — confirm response-shape access matches BE: list `.data.data`/`has_more`/`next_cursor`; detail `.data` narrowed to LeaveRequest; quotas `.data.data`; calendar `.data.entries`/`.clashes`)
    - frontend/packages/api-client/src/e6.ts + the generated e6 model (the 10 hooks + response types)
    - frontend/e2e/lib/e5-helpers.ts (apiAs/errorCode/errorDetails/waitForToken re-export pattern) + e4-helpers.ts (waitForToken) + e3-helpers.ts (apiAs/API_BASE)
    - frontend/e2e/lib/reset-db.ts (TRUNCATE_TABLES list + FK order — leave tables FK to employees/leave_types/placements; approved_leave_days already listed)
    - .planning/reference/backend-build-conventions.md (FE consumes error.details / classifyError, NOT conflict_details)
  </read_first>
  <action>
    Run the FE against the real BE (VITE_ENABLE_MSW=false) and fix any response-shape mismatches between the built
    screens and the real BE responses (the screens were built off MSW assumptions). Audit points (fix only if the
    real BE shape differs from what the screen reads):
      - approvals list: confirm `query.data?.data` → `{data,next_cursor,has_more}` access matches BE PageResponse.
      - detail: confirm narrowing `raw && 'id' in raw && 'status' in raw` works against the real LeaveRequest;
        confirm the balance-recheck error path: BE returns 422 BALANCE_RECHECK_FAILED — ensure classifyError yields
        a message containing "BALANCE" (or adjust the onError check to test error.code) so the override modal opens.
        Document the fix (mirrors the Phase-5/6 error.details precedent).
      - quotas: confirm `.data.data` + has_more/next_cursor; adjust + bulk-grant field errors via applyFieldErrors.
      - calendar: confirm `.data.entries`/`.clashes`; show_pending param wired.
    Keep changes minimal + surgical; do NOT redesign. Cite each fix in the SUMMARY.

    **e6-helpers.ts** — mirror e5-helpers: re-export apiAs/errorCode/errorDetails/API_BASE/waitForToken; export seeded
    LR/LQ id constants (LR.l1Target='SWP-LR-8001', LR.finalTarget='SWP-LR-8002', LR.overrideTarget='SWP-LR-8003',
    LR.outOfScope='SWP-LR-8004', LR.approved='SWP-LR-8005', LR.rejected='SWP-LR-8006', LR.inv3Overlap='SWP-LR-8007');
    helpers: `leaveRow(page, idOrName)` (div.border-b filter), `openLeaveDetail(page, id)` (goto /leave/$id + waitForToken),
    `scheduleCheckOverLeave(token, employeeId, date)` (POST /schedule:check → returns the conflict code), and
    `mondayPlus(n)` (Asia/Jakarta monday-of-week + n days, matching the seed's date math).

    **reset-db.ts** — add the leave tables to TRUNCATE_TABLES in correct FK order: `leave_approvals` (FK leave_requests),
    `leave_requests` (FK employees/leave_types/placements/client_companies), `leave_quotas` (FK employees/leave_types) —
    placed BEFORE placements/employees, AFTER attendance (mirror the existing Phase-6/7 ordering comment). Keep
    `approved_leave_days` + `schedule_entries` in the list (already there) so the INV-3 write-through resets between
    tests (test-approved leaves must not leak; seed re-applies the fixtures).
  </action>
  <verify>
    <automated>cd frontend && grep -q "leave_requests" e2e/lib/reset-db.ts && grep -q "leave_approvals" e2e/lib/reset-db.ts && grep -q "leave_quotas" e2e/lib/reset-db.ts && grep -q "SWP-LR-8007" e2e/lib/e6-helpers.ts && pnpm --filter @swp/web build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `reset-db.ts` TRUNCATE_TABLES contains `leave_approvals`, `leave_requests`, `leave_quotas` in correct FK order (before placements/employees); approved_leave_days + schedule_entries still present
    - `e6-helpers.ts` exports the LR.* id constants (incl. SWP-LR-8007) + leaveRow/openLeaveDetail/scheduleCheckOverLeave/mondayPlus and re-exports apiAs/waitForToken
    - `pnpm --filter @swp/web build` succeeds (FE wiring fixes compile); any response-shape fix is documented
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Approvals + quotas + calendar + scope E2E specs</name>
  <files>frontend/e2e/tests/e6/approvals.spec.ts, frontend/e2e/tests/e6/quotas.spec.ts, frontend/e2e/tests/e6/calendar.spec.ts, frontend/e2e/tests/e6/scope-negatives.spec.ts</files>
  <read_first>
    - frontend/e2e/tests/e5/*.spec.ts (07-04 spec structure: test.beforeEach(resetDb); loginAs(page, PERSONAS.*); page.goto + waitForToken; div.border-b row locators; toast assertions; apiAs for deterministic real-409 probes)
    - frontend/e2e/lib/fixtures.ts + personas.ts (loginAs, PERSONAS.hrAdmin/shiftLeader) + e6-helpers.ts (Task 1)
    - frontend/apps/web/src/features/e6-leave/{leave-approvals-screen,leave-detail-screen,leave-quotas-screen,leave-calendar-screen,leave-overlays}.tsx (REAL button text/aria/ids — t() keys; do NOT invent selectors)
    - docs/epics/E6-leave/prds/leave-approval.md + leave-quota-balances.md + leave-calendar-views.md (Gherkin AC → one test() each)
  </read_first>
  <action>
    All specs: `test.beforeEach(resetDb)`; viewport 1600x1000.

    **approvals.spec.ts** (LVE-01):
      - L1→final happy path: loginAs shiftLeader → openLeaveDetail(LR.l1Target=8001) → click t('detail.actionForward')
        → success toast, status now PENDING_HR (re-query/refetch). Then loginAs hrAdmin → openLeaveDetail(8001 or 8002)
        → click t('detail.actionApprove') → success toast, status APPROVED, timeline shows L1+HR APPROVED entries.
      - HR final on leader-approved 8002 directly → APPROVED.
      - reject path: hrAdmin → detail(8002) → t('detail.actionReject') → fill #reject-reason (>=5) → confirm → REJECTED;
        also assert reject with <5 chars shows the min-length error and does not submit.
      - override path: hrAdmin → detail(LR.overrideTarget=8003, over-balance) → clicking approve opens BalanceChangedModal
        (or actionOverride button) → fill #override-reason (>=10) → confirm t('override.confirm') → APPROVED, timeline
        shows OVERRIDE_APPROVED entry. Assert <10-char override reason is blocked.
      - list: hrAdmin /leave default shows PENDING_HR rows (8002/8003/8007); status filter to APPROVED shows 8005.
      - no-leader: detail(8003) renders the no-leader badge (routing.no_leader true, CMP-0022 has no leader).
    **quotas.spec.ts** (LVE-02):
      - list: hrAdmin /leave/quotas shows Dewi + Budi rows with total/used/pending/remaining (Dewi remaining 8, Budi 1).
      - adjust happy: open Adjust on Dewi → #delta=2, #reason(>=5) → save → success toast, remaining updates to 10.
      - adjust refuse: open Adjust on Budi → #delta negative so total<used → 422 RULE_VIOLATION field error rendered (fields.delta), modal stays open.
      - QUOTA_EXCEEDED / balance recheck surfaced: drive the over-balance HR final on 8003 (remaining 1, 3 days) via the
        detail screen → the override modal/CTA appears (the FE balance-recheck path); assert the override then succeeds.
      - bulk-grant: click t('actions.bulkGrant') → fill form → t('bulkGrant.previewBtn') → preview shows total_affected +
        any failed count → t('bulkGrant.applyBtn') → success toast (partial success tolerated).
    **calendar.spec.ts** (LVE-03):
      - hrAdmin /leave/calendar renders the month grid; an APPROVED entry (8005) appears as a day cell with employee name.
      - show_pending toggle OFF → only approved; toggle ON (getByRole('switch')) → pending entries (8002/8003/8007) appear
        (border-dashed / neutral badge). Navigate months to the seeded leave dates (mondayPlus) so entries are visible.
    **scope-negatives.spec.ts** (LVE-01 RBAC):
      - shiftLeader (Rudi @ CMP-0021) cannot :approve-l1 a CMP-0022 request: via apiAs(shiftLeader) POST
        /leave-requests/SWP-LR-8004:approve-l1 → assert 403 OUT_OF_SCOPE (real envelope). Optionally also assert the
        UI: SL queue (/leave) does not list 8004 (cross-company hidden).
      - self-approve guard: a leader approving their own request → 403 (drive via apiAs if a self-target is seeded;
        else assert the contract path is covered in 08-03 and skip with a documented note).
  </action>
  <verify>
    <automated>cd frontend && pnpm e2e -- e6/approvals.spec.ts e6/quotas.spec.ts e6/calendar.spec.ts e6/scope-negatives.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `approvals.spec.ts` green: L1→final→APPROVED, reject (+min-length negative), override (+min-length negative), list filters, no-leader badge
    - `quotas.spec.ts` green: list remaining, adjust happy + refuse-total<used (422 field error), bulk-grant preview→apply, balance-recheck→override
    - `calendar.spec.ts` green: month render + show_pending toggle (approved-only vs incl. pending)
    - `scope-negatives.spec.ts` green: leader cross-company :approve-l1 → 403 OUT_OF_SCOPE
    - selectors are from the REAL e6-leave components (no invented test ids)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: INV-3 loop-closer E2E (the proof)</name>
  <files>frontend/e2e/tests/e6/inv3-loop-closer.spec.ts</files>
  <read_first>
    - frontend/e2e/tests/e4/*.spec.ts (06-04: how the schedule grid + :check conflict is driven; the SHIFT_OVER_LEAVE assertion against approved_leave_days, including apiAs schedule:check probe + errorDetails; how GET /schedule surfaces ScheduleEntry.status='CANCELLED_BY_LEAVE')
    - frontend/e2e/lib/e6-helpers.ts (Task 1: scheduleCheckOverLeave, mondayPlus, LR.inv3Overlap=8007, openLeaveDetail) + e4-helpers.ts
    - backend/cmd/seed/seed.go seedScheduling + seedLeave (SWP-SCH-6002 = Dewi/EMP-3001 on monday+2; SWP-LR-8007 = Dewi PENDING_HR overlapping monday+2)
    - docs/api/E6-leave/openapi.yaml (ScheduleImpactEntry.new_status enum [LEAVE, UNASSIGNED]) + docs/api/E4-shift-scheduling/openapi.yaml (ScheduleEntry.status enum [SCHEDULED, CANCELLED_BY_LEAVE, MODIFIED]) — the two boundary values for the same cancel
    - docs/epics/E6-leave/prds/leave-schedule-integration.md (LI-1/LI-2 — the cancel + suppress side effects)
  </read_first>
  <action>
    One spec, the loop-closer proof (LVE-01 + the Phase-6 hand-off):
      Setup: resetDb. The seed plants SWP-SCH-6002 (Dewi EMP-3001 SCHEDULED on monday+2) and SWP-LR-8007 (Dewi
      PENDING_HR leave on monday+2 at CMP-0021).
      Step 1 (pre-condition): loginAs hrAdmin, optionally assert via apiAs that a fresh E4 schedule:check for Dewi on
      monday+2 does NOT yet hit SHIFT_OVER_LEAVE from leave_requests (the seeded approved_leave_days has NO row for
      Dewi on monday+2 yet — only the Phase-6 fixture on monday+3 for a different scenario; confirm baseline).
      Step 2 (the approval): openLeaveDetail(LR.inv3Overlap=8007) → click t('detail.actionApprove') → success toast,
      status APPROVED. The detail screen's schedule_impact[] section now lists SWP-SCH-6002 with new_status 'LEAVE'
      — assert the Section title t('detail.sectionScheduleImpact') + the 'LEAVE' label rendered. (This is the E6
      contract value: openapi ScheduleImpactEntry.new_status enum [LEAVE, UNASSIGNED]; the underlying DB status is
      'CANCELLED_BY_LEAVE', asserted in Step 4.)
      Step 3 (the loop closed — over-leave from the REAL source): via apiAs(hrAdmin) call scheduleCheckOverLeave(token,
      'SWP-EMP-3001', mondayPlus(2)) → assert the response carries code SHIFT_OVER_LEAVE with
      errorDetails.leave_request_id === 'SWP-LR-8007' (the conflict now fires from the production approved_leave_days
      row that the approval inserted — NOT the Phase-6 seeded fixture). This proves E6 wired the production source.
      Step 4 (schedule cancelled — assert the E4-surfaced DB status, NOT a non-existent 'LEAVE' row): via
      apiAs(hrAdmin) GET /schedule for CMP-0021 that week → find SWP-SCH-6002 and assert its status === 'CANCELLED_BY_LEAVE'
      (the E4 ScheduleEntry.status enum value the DB stores; there is NO raw status='LEAVE'). Prefer this API assertion
      for determinism (mirrors 07-04's apiAs choice); optionally also assert the E4 grid cell shows the cancelled label.
  </action>
  <verify>
    <automated>cd frontend && pnpm e2e -- e6/inv3-loop-closer.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `inv3-loop-closer.spec.ts` exists and contains "SHIFT_OVER_LEAVE" and asserts `errorDetails.leave_request_id === 'SWP-LR-8007'`
    - The spec: approves SWP-LR-8007 via the real detail screen → E6 detail schedule_impact shows SWP-SCH-6002 new_status 'LEAVE' → a fresh E4 schedule:check for Dewi on monday+2 now returns SHIFT_OVER_LEAVE from the real approved_leave_days row → GET /schedule shows SWP-SCH-6002 status === 'CANCELLED_BY_LEAVE' (NOT 'LEAVE')
    - `pnpm e2e -- e6/inv3-loop-closer.spec.ts` green
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd frontend && pnpm e2e` green headless: all e6 specs pass AND no e1/e2/e3/e4/e5 regressions.
- Selectors are anchored on the REAL e6-leave components; BE response shapes match what the screens consume.
- The INV-3 loop-closer is proven end-to-end from the real production source (approved_leave_days populated by the approval); assertions use the correct boundary value per surface (E6 schedule_impact='LEAVE', E4 schedule.status='CANCELLED_BY_LEAVE').
- `pnpm e2e:ui` lists each e6 scenario as an individually-runnable case.
</verification>

<success_criteria>
- LVE-01: leave requests list/detail + L1/final/override approval + reject work against the real BE (approvals.spec + scope-negatives green).
- LVE-02: quotas list/adjust/bulk-grant + QUOTA_EXCEEDED/balance-recheck → override (quotas.spec green).
- LVE-03: leave calendar renders the range + show_pending toggle (calendar.spec green).
- INV-3 loop-closer proven: approval cancels the overlapping schedule entry (E6 schedule_impact 'LEAVE' / E4 grid 'CANCELLED_BY_LEAVE') AND a fresh schedule attempt hits SHIFT_OVER_LEAVE from the real approved_leave_days row (inv3-loop-closer.spec green).
</success_criteria>

<output>
After completion, create `.planning/phases/08-e6-leave/08-04-SUMMARY.md`
</output>
</content>
</invoke>
