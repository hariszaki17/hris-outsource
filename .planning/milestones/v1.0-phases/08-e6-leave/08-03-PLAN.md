---
phase: 08-e6-leave
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - backend/internal/handler/leave/leave_handler_test.go
  - backend/internal/handler/leave/quota_handler_test.go
  - backend/internal/handler/leave/calendar_handler_test.go
  - backend/internal/handler/leave/leave_testkit_test.go
autonomous: true
requirements: [LVE-01, LVE-02, LVE-03]

must_haves:
  truths:
    - "Every E6 endpoint has a contract test asserting the openapi response shape + status code (the drift gate replacing server codegen)"
    - "State transitions (l1→final→approved, reject, override) + terminal/wrong-state 409 are asserted"
    - "QUOTA_EXCEEDED/BALANCE_RECHECK_FAILED 422 (+field errors), OVERLAPPING_LEAVE 409, OUT_OF_SCOPE 403, bulk-grant partial success, calendar shape, routing.no_leader are asserted"
  artifacts:
    - path: "backend/internal/handler/leave/leave_handler_test.go"
      provides: "approval state-machine + 409 + 403 + balance-recheck contract tests over the real service+handler"
      contains: "BALANCE_RECHECK_FAILED"
    - path: "backend/internal/handler/leave/quota_handler_test.go"
      provides: "quota list/adjust(refuse total<used)/bulk-grant partial-success contract tests"
      contains: "QUOTA_EXCEEDED"
    - path: "backend/internal/handler/leave/leave_testkit_test.go"
      provides: "fakeTx + fake repos + newHarness(role,company,employee) mounting the real services+handler on chi"
      contains: "newHarness"
  key_links:
    - from: "leave_handler_test.go"
      to: "real LeaveService + handler"
      via: "httptest harness with mutable-principal middleware"
      pattern: "newHarness"
---

<objective>
Go contract tests for all 10 E6 endpoints — the drift gate that replaces server codegen. Mirror the
Phase-7 attendance contract-test harness (07-03) EXACTLY: `fakeTx` (Exec no-op for audit-in-tx) +
`fakeTxRunner` + in-memory fake repos over the REAL leave/quota/calendar service ports +
`newHarness(role, company, employee)` mounting the real services+handler on chi with a
mutable-principal closure middleware. Assert response shapes against the openapi examples, every
contract error code+status, the state transitions, and the INV-3 side-effect envelope (schedule_impact + cancelled entries) at the service-contract level.

Purpose: pin the E6 wire contract so it can't silently drift from `docs/api/E6-leave/openapi.yaml`.
Output: leave/quota/calendar handler tests + the shared testkit.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-e6-leave/08-CONTEXT.md
@.planning/phases/08-e6-leave/08-02-SUMMARY.md
@docs/api/E6-leave/openapi.yaml
@docs/epics/E6-leave/prds/leave-approval.md
@docs/epics/E6-leave/prds/leave-quota-balances.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testkit harness + leave approval contract tests</name>
  <files>backend/internal/handler/leave/leave_testkit_test.go, backend/internal/handler/leave/leave_handler_test.go</files>
  <read_first>
    - backend/internal/handler/attendance/attendance_testkit_test.go (07-03: fakeTx, fakeTxRunner, fake repos, newHarness(role,company,employee) with mutable-principal closure middleware, stubIdempotency at the same router position as server.go)
    - backend/internal/handler/attendance/attendance_handler_test.go (07-03: table-driven assertions of status+code+fields+details; terminal-409; OUT_OF_SCOPE-403)
    - backend/internal/handler/scheduling/scheduling_testkit_test.go (06-03: fake approved-leave/schedule repos — REUSE the fake schedule/approved_leave_days port to assert the INV-3 side-effect calls)
    - docs/api/E6-leave/openapi.yaml examples: HRQueueExample, ApprovedL1Example, ApprovedFinalExample, OverrideApprovedExample, ErrorBalanceRecheckExample, ErrorOutOfScopeExample, ErrorOverlappingExample, CreatedPendingHRNoLeaderExample
  </read_first>
  <action>
    **leave_testkit_test.go** — copy the 07-03 harness: `fakeTx` (Exec no-op), `fakeTxRunner` (InTx runs the closure
    with fakeTx), in-memory `fakeLeaveRepo` (map[id]LeaveRequest with shared state for status transitions + ForUpdate),
    `fakeQuotaRepo` (map keyed by id + by (emp,type,period)), `fakeApprovalRepo`, `fakeCalendarRepo`, and a
    `fakeScheduleRepo` implementing the INV-3 port (records CancelScheduleEntriesForLeave + InsertApprovedLeaveDay
    calls so tests assert they fired). `newHarness(role, companyID, employeeID)` builds the REAL LeaveService /
    QuotaService / CalendarService + the real leavehttp.Handler, mounts them on a chi router with a closure middleware
    injecting a mutable principal (role/company/employee) — mirroring server.go positions.

    **leave_handler_test.go** — table-driven, hitting the handler with seeded fakes:
      - approve-l1 happy: PENDING_L1, leader of the company → 200, status PENDING_HR, timeline has L1/APPROVED.
      - approve-l1 wrong-state: PENDING_HR → 409.
      - approve-l1 cross-company leader → 403 OUT_OF_SCOPE.
      - approve-l1 self-approve (caller employee == request employee) → 403 FORBIDDEN.
      - approve-final happy (remaining>=duration): 200 APPROVED, quota used += duration, fakeScheduleRepo recorded
        CancelScheduleEntriesForLeave + InsertApprovedLeaveDay (INV-3 fired), schedule_impact[] populated.
      - approve-final over-balance: duration>remaining → 422 BALANCE_RECHECK_FAILED, fields.requires_override=true; no state change.
      - approve-override: over-balance + override_reason(min10) → 200 APPROVED, timeline OVERRIDE_APPROVED, INV-3 fired, quota.last_override set.
      - approve-override missing/short reason → 400/422.
      - reject happy (PENDING_L1 or PENDING_HR + reason min5) → 200 REJECTED; reject terminal → 409; reject no reason → 400/422.
      - no-leader routing: a PENDING_HR request with routing.no_leader=true serializes routing.no_leader:true (CreatedPendingHRNoLeaderExample shape).
      - list shape: LeaveRequestListResponse {data, next_cursor, has_more}; get shape: full LeaveRequest incl timeline.
    Assert exact codes, HTTP status, error.fields keys, and the response JSON shape vs the openapi examples.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handler/leave/... -run 'Approve|Reject|List|Get' 2>&1 | tail -8; grep -q "newHarness" internal/handler/leave/leave_testkit_test.go && grep -q "BALANCE_RECHECK_FAILED" internal/handler/leave/leave_handler_test.go && grep -q "OUT_OF_SCOPE" internal/handler/leave/leave_handler_test.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `leave_testkit_test.go` defines `newHarness`, `fakeTx`, and a `fakeScheduleRepo` recording the INV-3 calls
    - `leave_handler_test.go` asserts: l1→final→APPROVED, reject, override; 409 terminal/wrong-state; 403 OUT_OF_SCOPE + self-approve; 422 BALANCE_RECHECK_FAILED(requires_override); routing.no_leader:true
    - `cd backend && go test ./internal/handler/leave/...` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Quota + calendar contract tests</name>
  <files>backend/internal/handler/leave/quota_handler_test.go, backend/internal/handler/leave/calendar_handler_test.go</files>
  <read_first>
    - backend/internal/handler/leave/leave_testkit_test.go (Task 1 harness — reuse fakeQuotaRepo/fakeCalendarRepo)
    - docs/api/E6-leave/openapi.yaml examples: LeaveQuotaListResponseExample, QuotaAfterAdjustExample, BulkGrantApplyResponseExample (succeeded[]/failed[]), BulkGrantPreviewResponseExample, CalendarHRJuneExample
    - docs/epics/E6-leave/prds/leave-quota-balances.md (LQ-1/2/5/6/8 AC) + leave-calendar-views.md
  </read_first>
  <action>
    **quota_handler_test.go:**
      - list quotas: LeaveQuotaListResponse shape; remaining = total - used - pending in each row.
      - adjust happy: delta + reason → 200, total adjusted, last_adjustment{delta,reason,...} present.
      - adjust refuse: delta makes total < used → 422 RULE_VIOLATION, fields.delta present, no change.
      - adjust missing reason → 400/422.
      - bulk-grant apply: pro_rate=true → some rows prorated (is_prorated, prorate_months), one row would set total<used →
        appears in failed[] with code RULE_VIOLATION, the rest in succeeded[] (200 partial success); response shape
        {preview:false, total_affected, succeeded[], failed[]}.
      - bulk-grant preview: preview=true → succeeded[] computed (quota_id null), failed[] for over-used, NO writes.
    **calendar_handler_test.go:**
      - GET /leave-calendar default: LeaveCalendarResponse {period, entries[], clashes[]}; show_pending=false → only APPROVED.
      - show_pending=true → entries include PENDING_L1/PENDING_HR.
      - shift_leader calling without own-company match (or cross-company company_id) → 403.
      - clash: two same-service-line agents off same day/company → a clashes[] entry with agent_count>=2.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handler/leave/... 2>&1 | tail -8; grep -q "QUOTA_EXCEEDED\|RULE_VIOLATION" internal/handler/leave/quota_handler_test.go && grep -q "show_pending\|ShowPending" internal/handler/leave/calendar_handler_test.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `quota_handler_test.go` asserts list remaining math, adjust refuse total<used (422 RULE_VIOLATION fields.delta), bulk-grant partial success ({succeeded[],failed[]}) + preview no-write
    - `calendar_handler_test.go` asserts LeaveCalendarResponse shape, show_pending toggle, leader scope 403, clash detection
    - `cd backend && go test ./internal/handler/leave/...` exits 0 (all E6 contract tests green)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd backend && go test ./internal/handler/leave/...` exits 0.
- Every contract error code (QUOTA_EXCEEDED, BALANCE_RECHECK_FAILED, OVERLAPPING_LEAVE, OUT_OF_SCOPE, RULE_VIOLATION) asserted with its exact status + field keys.
- INV-3 side-effects asserted at the service-contract level (fakeScheduleRepo recorded the cancel + insert).
- `make verify` green (no regressions in e1..e5 contract tests).
</verification>

<success_criteria>
- All 10 E6 endpoints have a contract test pinning their openapi response shape + status.
- State transitions + 409s + 403s + 422s + bulk partial-success + calendar shape + routing.no_leader all asserted.
</success_criteria>

<output>
After completion, create `.planning/phases/08-e6-leave/08-03-SUMMARY.md`
</output>
