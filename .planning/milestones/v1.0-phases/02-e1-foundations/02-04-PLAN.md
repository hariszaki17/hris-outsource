---
phase: 02-e1-foundations
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - frontend/e2e/lib/db.ts
  - frontend/e2e/tests/e1/user-management.spec.ts
  - frontend/e2e/tests/e1/audit-log.spec.ts
  - frontend/e2e/tests/e1/platform-settings.spec.ts
  - frontend/apps/web/src/features/e1-foundations/users-screen.tsx
autonomous: true
requirements: [FND-01, FND-02, FND-03]
must_haves:
  truths:
    - "The E1 foundations screens (users, audit log, settings) render data from the real BE with MSW off"
    - "An exhaustive Playwright suite covers user list/create/edit/change-role/deactivate/reactivate/send-reset, audit list+filter+pagination+detail, settings load, and RBAC negatives"
    - "The whole E1 E2E suite passes headless against the real stack (pnpm e2e)"
  artifacts:
    - path: "frontend/e2e/tests/e1/user-management.spec.ts"
      provides: "one test() per user-management Gherkin/RB-# scenario, named traceably"
      min_lines: 200
    - path: "frontend/e2e/tests/e1/audit-log.spec.ts"
      provides: "audit list + filter + cursor pagination + detail + AL-7 RBAC tests"
      min_lines: 150
    - path: "frontend/e2e/tests/e1/platform-settings.spec.ts"
      provides: "settings load (PC-1/PC-2/PC-5) test"
      min_lines: 40
  key_links:
    - from: "E1 foundations screens"
      to: "real Go API :8081"
      via: "generated @swp/api-client hooks with VITE_ENABLE_MSW=false (already set in playwright.config webServer)"
      pattern: "useListUsers|useListAuditLog|useGetPlatformSettings"
    - from: "user-management.spec.ts"
      to: "lib/fixtures loginAs + lib/db helpers"
      via: "loginAs(page, PERSONAS.hrAdmin); resetDb() in beforeEach"
      pattern: "loginAs|resetDb"
---

<objective>
Take the E1 foundations FE screens off MSW onto the real BE and prove the whole feature with an
exhaustive Playwright E2E suite per the E1 PRDs (rbac-roles.md, audit-log.md, platform-conventions.md).
Cover user list/create/edit/change-role/deactivate→reactivate/send-password-reset; audit-log
list+filter+pagination+detail; settings load; and RBAC negatives. The suite runs green headless
against the real Go API + ephemeral Postgres.

Purpose: This is the milestone's definition of done for E1 — the FE features work end-to-end on
the real backend, with one runnable test() per Gherkin scenario (traceable + individually runnable
in --ui).
Output: Three new spec files under frontend/e2e/tests/e1/, a few new lib/db.ts helpers, and any
FE screen tweak needed to make a screen testable against real data.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-e1-foundations/02-CONTEXT.md
@.planning/reference/e2e-harness-spec.md
@docs/epics/E1-foundations/prds/rbac-roles.md
@docs/epics/E1-foundations/prds/audit-log.md
@docs/epics/E1-foundations/prds/platform-conventions.md
@.planning/phases/02-e1-foundations/02-02-SUMMARY.md
@frontend/e2e/tests/e1/authentication.spec.ts
@frontend/e2e/lib/fixtures.ts
@frontend/e2e/lib/personas.ts
@frontend/e2e/lib/db.ts
@frontend/e2e/lib/reset-db.ts

<interfaces>
<!-- The E2E harness from Phase 1 is reused as-is. Patterns the executor copies. -->

Harness (already built, do not re-create):
  - frontend/e2e/playwright.config.ts: webServer runs FE preview with VITE_ENABLE_MSW=false,
    VITE_API_BASE_URL=http://localhost:8081/api/v1; globalSetup boots Go API + ephemeral Postgres
    + seed. So screens automatically hit the real BE — no per-spec MSW disabling needed.
  - lib/fixtures.ts: `import { test, expect, loginAs } from '../../lib/fixtures.js'`. loginAs drives
    the real login screen. The extended `test` also exposes loginAs as a fixture.
  - lib/personas.ts: PERSONAS.hrAdmin / superAdmin / shiftLeader / agent (email+password+role).
  - lib/reset-db.ts: `resetDb()` — TRUNCATEs app tables then re-runs `go run ./cmd/seed`. The 02-02
    extended seed (extra users + audit_log rows) is re-applied on every reset, so audit-log + a
    multi-user list are always present after resetDb(). Call resetDb() in test.beforeEach.
  - lib/db.ts: existing helpers (seedResetToken, disableUser, getLastLoginAt, countResetTokensFor,
    withClient). ADD any new helpers here (e.g. getUserStatus(email), countAuditRowsByEntityType).

FE screens (already use real generated hooks; route paths from the router):
  - users-screen.tsx — route '/authed/settings/users' (frame kHNWT). useListUsers; filters in URL
    search params; row kebab opens user-overlays actions.
  - user-overlays.tsx — useCreateUser/useUpdateUser/useChangeUserRole/useDeactivateUser/
    useReactivateUser/useSendUserPasswordReset (overlays: create, edit, change-role, deactivate confirm, etc.).
  - audit-log-screen.tsx — useListAuditLog; audit-detail-drawer.tsx — useGetAuditLogEntry.
  - settings-general-screen.tsx — useGetPlatformSettings (frame tch6k).
  Find the exact route URLs by reading frontend/apps/web/src/app/router.tsx and nav.ts before
  writing page.goto() targets. Find stable selectors (ids, role+name, data-testid) by reading each
  screen's tsx; prefer getByRole/getByText with i18n copy (app default language = Bahasa Indonesia,
  so assert ID strings — read packages/shared/src/i18n/id.ts for the exact E1 keys/values).

Spec sources (one test() per scenario, named with RB-#/AL-#/PC-#/C-#):
  rbac-roles.md §7: permission enforced server-side (agent->HR endpoint 403), HR cross-company,
    role change is audited (RB-6).
  audit-log.md §7: mutation writes an audit entry; search by entity; access restricted (AL-7 agent->403);
    system actions attributed; §8 C-2 bulk granularity (N/A here, skip with note).
  platform-conventions.md §7: Indonesian UI; settings load (locale/timezone/currency).
  Plus the user-management CRUD flows implied by FND-01 (the screens + openapi user ops).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: verify screens render on real BE + add E2E db helpers + user-management spec</name>
  <read_first>
    - frontend/apps/web/src/app/router.tsx + nav.ts (exact authed route URLs for users/audit-log/settings)
    - frontend/apps/web/src/features/e1-foundations/users-screen.tsx + user-overlays.tsx (selectors, i18n keys, overlay flow)
    - frontend/packages/shared/src/i18n/id.ts (exact Bahasa strings for E1 user-management copy)
    - frontend/e2e/lib/db.ts (withClient + helper pattern to extend)
    - frontend/e2e/tests/e1/authentication.spec.ts (spec structure, beforeEach resetDb, naming)
  </read_first>
  <files>frontend/e2e/lib/db.ts, frontend/e2e/tests/e1/user-management.spec.ts, frontend/apps/web/src/features/e1-foundations/users-screen.tsx</files>
  <action>
    First, smoke-verify the wiring: run the FE preview with MSW off against the real BE (the
    playwright webServer config already does this) and confirm the users screen loads real data.
    If any E1 screen still hard-codes MSW-only assumptions or breaks against the real shape, make
    the MINIMAL fix in users-screen.tsx (the only FE file this plan may touch — e.g. a guard for an
    empty page or a field that the real BE returns null for like company_name). Do not refactor.

    Add to frontend/e2e/lib/db.ts (mirror the withClient pattern):
      getUserStatus(email): Promise<string|null>  — SELECT status FROM users WHERE lower(email)=lower($1).
      getUserRole(email): Promise<string|null>     — SELECT role.
      countAuditRowsByEntityType(entityType): Promise<number> — COUNT FROM audit_log WHERE entity_type=$1.
      getLatestAuditAction(): Promise<string|null>  — SELECT action FROM audit_log ORDER BY created_at DESC LIMIT 1.

    Create frontend/e2e/tests/e1/user-management.spec.ts (package style mirrors authentication.spec.ts):
      test.beforeEach(resetDb). Each test logs in via loginAs(page, PERSONAS.hrAdmin) unless it tests RBAC.
      Tests (name each with FND-01 + the action):
        'FND-01 · users list renders seeded users from the real BE' — goto users route, assert the
          seeded persona names (e.g. "Sari Hadi") are visible in the table.
        'FND-01 · create user' — open the create overlay, fill email/role/employee_id, submit; assert
          a success toast/row appears AND the DB has the new user (a new lib/db helper or getUserRole).
        'FND-01 · edit user email' — open edit overlay, change email, submit; assert updated value.
        'FND-01/RB-6 · change role is audited' — change a user's role via the overlay; assert success,
          then assert getUserRole(email) == new role AND countAuditRowsByEntityType('user') increased
          (or getLatestAuditAction() == 'user.change_role').
        'FND-01 · deactivate then reactivate' — deactivate a user (confirm overlay), assert
          getUserStatus(email)=='disabled' and the row shows the disabled badge; reactivate, assert 'active'.
        'FND-01 · send password reset' — trigger the row action; assert the 202 success toast AND
          countResetTokensFor(email) >= 1.
        'RB-2/AL-7 · agent is denied the users screen' — loginAs(agent); goto users route; assert the
          no-permission empty state renders (frame TqMQ6 copy) OR the API returns 403 surfaced as the
          permission-denied UI (read users-screen.tsx for how it renders the 403 state).
      Use getByRole/getByText with the exact id.ts Bahasa strings. Prefer asserting both the UI state
      AND the DB side-effect (the latter via lib/db.ts) for every mutation.
  </action>
  <acceptance_criteria>
    - frontend/e2e/lib/db.ts contains "getUserStatus", "getUserRole", "countAuditRowsByEntityType".
    - frontend/e2e/tests/e1/user-management.spec.ts exists and contains at least 7 test( occurrences (grep -c "test(" >= 7).
    - The spec contains the strings "loginAs", "resetDb", and "PERSONAS.agent".
    - `cd frontend/e2e && pnpm exec playwright test --list tests/e1/user-management.spec.ts` lists the user-management tests (exits 0).
    - `cd frontend && pnpm e2e tests/e1/user-management.spec.ts` runs the suite green (or document the exact command if the root script differs).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend/e2e && pnpm exec playwright test --list tests/e1/user-management.spec.ts exits 0; cd frontend && pnpm e2e tests/e1/user-management.spec.ts runs green</automated>
  </verify>
  <done>The users screen drives the real BE and the exhaustive user-management E2E suite passes, asserting both UI and DB side-effects, including the agent RBAC negative.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: audit-log + platform-settings E2E specs</name>
  <read_first>
    - frontend/apps/web/src/features/e1-foundations/audit-log-screen.tsx + audit-detail-drawer.tsx (filters, pagination controls, detail drawer selectors)
    - frontend/apps/web/src/features/e1-foundations/settings-general-screen.tsx (settings row selectors)
    - frontend/packages/shared/src/i18n/id.ts (audit-log + settings Bahasa copy)
    - docs/epics/E1-foundations/prds/audit-log.md §7 (AL-5 search, AL-7 access restricted)
    - docs/epics/E1-foundations/prds/platform-conventions.md §7 (PC-1 ID UI, PC-2 timezone, PC-5 IDR)
  </read_first>
  <files>frontend/e2e/tests/e1/audit-log.spec.ts, frontend/e2e/tests/e1/platform-settings.spec.ts, frontend/e2e/lib/db.ts</files>
  <action>
    Create frontend/e2e/tests/e1/audit-log.spec.ts (beforeEach resetDb; loginAs hrAdmin unless RBAC):
      'FND-02 · audit log lists seeded entries' — goto audit-log route; assert >=1 seeded row visible.
      'FND-02/AL-5 · filter by entity type' — apply the entity_type=user filter (via the screen's
        filter control; filters live in URL search params); assert only user-entity rows show, OR
        assert the row count matches countAuditRowsByEntityType('user').
      'FND-02 · cursor pagination advances' — if the seed has more than one page at the default/limited
        page size, click the next-page control; assert the rows change (different ids/first row). If the
        seed volume is below one page, extend the 02-02 seed note is referenced — instead seed extra
        audit rows in beforeEach via a new lib/db helper insertAuditRows(n) so pagination is testable.
      'FND-02 · open entry detail drawer' — click a row; assert the detail drawer opens and shows
        before/after content (useGetAuditLogEntry); assert request_id or a before→after value is visible.
      'AL-7 · agent is denied the audit log' — loginAs(agent); goto audit-log route; assert the
        no-permission state (or 403-driven UI).

    Create frontend/e2e/tests/e1/platform-settings.spec.ts (beforeEach resetDb; loginAs hrAdmin):
      'FND-03/PC-1/PC-2/PC-5 · settings load from the real BE' — goto settings-general route; assert
        the locale label "Bahasa Indonesia", timezone "Asia/Jakarta" (WIB), and currency "Rupiah (IDR)"
        are visible (exact labels from the openapi v1Defaults / id.ts).

    If the audit pagination test needs more rows than the seed provides, add insertAuditRows(count)
    to lib/db.ts (INSERT INTO audit_log with 'SWP-AL-' || swp_next_id('AL')).
  </action>
  <acceptance_criteria>
    - frontend/e2e/tests/e1/audit-log.spec.ts exists; grep -c "test(" >= 4; contains "FND-02" and "AL-7".
    - frontend/e2e/tests/e1/platform-settings.spec.ts exists; contains "FND-03" and "Asia/Jakarta".
    - `cd frontend/e2e && pnpm exec playwright test --list tests/e1/audit-log.spec.ts tests/e1/platform-settings.spec.ts` lists the tests (exits 0).
    - `cd frontend && pnpm e2e tests/e1/` runs ALL e1 specs (auth + the 3 new) green headless.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend/e2e && pnpm exec playwright test --list tests/e1/audit-log.spec.ts tests/e1/platform-settings.spec.ts exits 0; cd frontend && pnpm e2e tests/e1/ runs all e1 specs green</automated>
  </verify>
  <done>Audit-log (list/filter/paginate/detail/RBAC) and platform-settings E2E specs pass against the real stack; the full e1 suite is green.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm e2e tests/e1/` (headless) is fully green — authentication + user-management + audit-log + platform-settings.
- `cd frontend/e2e && pnpm exec playwright test --ui --list` shows each new scenario as an individually runnable case.
- Every mutation test asserts both the UI result AND the DB side-effect via lib/db.ts.
- No regression: the existing authentication.spec.ts still passes.
</verification>

<success_criteria>
- The E1 foundations screens work end-to-end against the real BE (MSW off).
- Exhaustive E2E per the E1 PRDs is green headless: user CRUD + role + status + reset, audit
  list/filter/paginate/detail, settings load, and RBAC negatives.
- Each test is named with its requirement/scenario id and is individually runnable in --ui.
</success_criteria>

<output>
After completion, create `.planning/phases/02-e1-foundations/02-04-SUMMARY.md` documenting:
the exact route URLs used, the selectors/i18n keys relied on, any FE screen fix made to
users-screen.tsx, new lib/db.ts helpers added, the audit-pagination seeding approach, and the
final green test count per spec file.
</output>
