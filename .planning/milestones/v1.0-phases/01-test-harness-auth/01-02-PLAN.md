---
phase: 01-test-harness-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cmd/seed/main.go
  - backend/cmd/seed/seed.go
  - backend/cmd/seed/README.md
autonomous: true
requirements: [HARN-02]
must_haves:
  truths:
    - "go run ./cmd/seed against a migrated empty DB inserts the four personas with known argon2id passwords and minimal fixtures, idempotently (re-running does not error or duplicate)"
    - "go run ./cmd/seed -genkeys prints a base64 Ed25519 private key on line 1 and public key on line 2 (consumable by the harness)"
    - "sari.hadi@swp.test (hr_admin), rudi.wijaya@swp.test (shift_leader scoped to company Plaza Senayan), a super_admin, and an agent all exist with status=active"
  artifacts:
    - path: "backend/cmd/seed/main.go"
      provides: "Seed entrypoint with -genkeys flag and default seed run"
      contains: "genkeys"
    - path: "backend/cmd/seed/seed.go"
      provides: "Persona + fixture seeding using auth.HashPassword and the users table"
      contains: "sari.hadi@swp.test"
  key_links:
    - from: "backend/cmd/seed/seed.go"
      to: "backend/internal/platform/auth.HashPassword"
      via: "password hashing for seeded users"
      pattern: "auth\\.HashPassword"
    - from: "backend/cmd/seed/main.go"
      to: "backend/internal/platform/auth.GenerateKeypair"
      via: "-genkeys flag"
      pattern: "GenerateKeypair"
---

<objective>
Add `backend/cmd/seed`: a deterministic seed command that (a) inserts the four demo personas
with known passwords + minimal supporting fixtures so screens render, and (b) provides a
`-genkeys` flag that prints a dev Ed25519 keypair (base64) for the test/dev stack. The seed
module is the one place every later phase extends with the rows its screens need.

Purpose: The E2E harness (01-02 is a dependency of 01-01's globalSetup) needs deterministic
login personas + dev JWT keys, with no manual setup.
Output: `backend/cmd/seed/` (main.go, seed.go, README.md).
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/reference/backend-build-conventions.md
@.planning/phases/01-test-harness-auth/01-CONTEXT.md

<interfaces>
Verified from the codebase — use these exact signatures; do NOT hand-edit generated sqlc.

From backend/internal/platform/auth/password.go:
```go
func HashPassword(plain string) (string, error) // returns PHC argon2id encoded string
```

From backend/internal/platform/auth/jwt.go:
```go
func GenerateKeypair() (privB64, pubB64 string, err error) // base64 std Ed25519
```

From backend/internal/platform/config/config.go (Load reads .env then env):
```go
func Load() (Config, error) // Config.DB.URL from DATABASE_URL (required)
```

From backend/internal/platform/db:
```go
func Open(ctx, url string, maxConns int32) (*db.Pool, error) // *db.Pool wraps pgx pool; .Pool is the *pgxpool.Pool
```

Existing users table (migration 00002_users.sql) columns:
  id (text PK, 'SWP-USR-<N>'), email, password_hash, role
  CHECK role IN ('super_admin','hr_admin','shift_leader','agent'),
  employee_id (text null), company_id (text null, set for shift_leader),
  status (text 'active'|'disabled' default 'active'), created_at, updated_at, deleted_at.
Unique index on lower(email) where deleted_at IS NULL.

Existing query (backend/db/queries/identity/users.sql):
```sql
-- name: CreateUser :one  (allocates SWP-USR id inline; status defaults 'active')
INSERT INTO users (id, email, password_hash, role, employee_id, company_id, status)
VALUES ('SWP-USR-' || swp_next_id('USR'), $email, $password_hash, $role, $employee_id, $company_id, 'active') RETURNING ...
```
swp_next_id('CMP') / swp_next_id('USR') are available (migration 00001). The shift_leader's
company_id should reference a seeded client-company id; in Phase 1 no companies table exists
yet (it lands in Phase 3), so seed the shift_leader's company_id as a deterministic literal
string `SWP-CMP-0021` matching the spec example (the FK is not enforced until Phase 3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cmd/seed entrypoint with -genkeys flag and DB wiring</name>
  <files>backend/cmd/seed/main.go</files>
  <read_first>
    - backend/cmd/migrate/main.go (pattern for a cmd: config.Load, open DB, run, exit on error)
    - backend/cmd/api/main.go (pattern for db.Open + config.Load)
    - backend/internal/platform/auth/jwt.go (GenerateKeypair signature + base64 std)
    - backend/internal/platform/config/config.go (Load reads DATABASE_URL)
  </read_first>
  <action>
Create `backend/cmd/seed/main.go`, package `main`:
  - Define a `flag.Bool("genkeys", false, ...)`. Parse flags.
  - If `-genkeys`: call `auth.GenerateKeypair()`, then print EXACTLY two lines to stdout — line 1 = the base64 private key, line 2 = the base64 public key (no labels, no extra output), and `os.Exit(0)`. This is the contract the harness (01-01 `lib/backend.ts`) parses: first line → AUTH_JWT_PRIVATE_KEY, second line → AUTH_JWT_PUBLIC_KEY.
  - Otherwise: `cfg, _ := config.Load()` (reads DATABASE_URL), `pool, _ := db.Open(ctx, cfg.DB.URL, cfg.DB.MaxConns)`, then call `Seed(ctx, pool)` (Task 2). Log "seed complete" and exit 0; on error log + os.Exit(1).
  - Mirror the error-handling shape of `cmd/migrate/main.go` (a `run() error` wrapper).
  </action>
  <verify>
    <automated>cd /Users/diaz/Documents/MIG/hris-outsource/backend && grep -q 'genkeys' cmd/seed/main.go && grep -q 'GenerateKeypair' cmd/seed/main.go && go build ./cmd/seed && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `backend/cmd/seed/main.go` contains `flag.Bool("genkeys"` and `auth.GenerateKeypair`.
    - `cd backend && go build ./cmd/seed` exits 0.
    - `cd backend && go run ./cmd/seed -genkeys` prints exactly two non-empty lines; both base64-decode to byte lengths 64 (private) and 32 (public) respectively. Verify lengths: `go run ./cmd/seed -genkeys | head -1 | base64 -d | wc -c` prints `64`; `go run ./cmd/seed -genkeys` line 2 decodes to `32`.
  </acceptance_criteria>
  <done>cmd/seed builds; `-genkeys` emits a parseable two-line base64 Ed25519 keypair (priv 64 bytes, pub 32 bytes); default mode loads config + opens the DB and calls Seed.</done>
</task>

<task type="auto">
  <name>Task 2: Seed the four personas + minimal fixtures, idempotently</name>
  <files>backend/cmd/seed/seed.go, backend/cmd/seed/README.md</files>
  <read_first>
    - backend/db/queries/identity/users.sql (CreateUser query + columns)
    - backend/internal/platform/auth/password.go (HashPassword)
    - backend/internal/repository/sqlc (generated Queries: New(pool), CreateUser, GetUserByEmail)
    - .planning/phases/01-test-harness-auth/01-CONTEXT.md (persona spec)
    - docs/api/CONVENTIONS.md §20 (example personas Sari Hadi / Rudi Wijaya)
  </read_first>
  <action>
Create `backend/cmd/seed/seed.go`, package `main`, exporting `func Seed(ctx context.Context, pool *db.Pool) error`.

Define exported password constants so the harness/personas registry references the same values:
  - `const PasswordHRAdmin = "Pass1ng-Garuda!"`
  - `const PasswordShiftLeader = "Lead3r-Senayan!"`
  - `const PasswordSuperAdmin = "Sup3r-Admin-2026!"`
  - `const PasswordAgent = "Ag3nt-Budi-2026!"`

Implement seeding using the generated sqlc Queries (`q := sqlcgen.New(pool.Pool)`):
  - For each persona: check existence via `q.GetUserByEmail(ctx, email)`; if found, SKIP (idempotent). If not found, `hash, _ := auth.HashPassword(password)` then `q.CreateUser(ctx, sqlcgen.CreateUserParams{ Email, PasswordHash: hash, Role, EmployeeID, CompanyID, })`.
  - Personas (all status active via the query default):
    - `sari.hadi@swp.test` — role `hr_admin`, employee_id `SWP-EMP-1042`, company_id null.
    - `rudi.wijaya@swp.test` — role `shift_leader`, employee_id `SWP-EMP-1108`, company_id `SWP-CMP-0021` (literal; companies table lands in Phase 3, FK not yet enforced).
    - `super.admin@swp.test` — role `super_admin`, employee_id null, company_id null.
    - `agent.budi@swp.test` — role `agent`, employee_id `SWP-EMP-2891`, company_id null.
  - Wrap the inserts in a single transaction via the pool (begin/commit) OR insert sequentially with each CreateUser auto-committed — either is acceptable; sequential is simplest and matches an idempotent skip-if-exists loop.

Add a `// Phase markers:` comment block noting that later phases append their fixtures (client company "Plaza Senayan", site, service line, employee, placement, etc.) to this same Seed function, so the seed grows per phase per the harness spec.

Create `backend/cmd/seed/README.md` documenting: the four personas + their passwords (referencing the exported constants), how `-genkeys` is consumed by the harness, idempotency behavior, and the "extend this per phase" rule.
  </action>
  <verify>
    <automated>cd /Users/diaz/Documents/MIG/hris-outsource/backend && grep -q 'sari.hadi@swp.test' cmd/seed/seed.go && grep -q 'rudi.wijaya@swp.test' cmd/seed/seed.go && grep -q 'SWP-CMP-0021' cmd/seed/seed.go && grep -q 'auth.HashPassword' cmd/seed/seed.go && grep -q 'GetUserByEmail' cmd/seed/seed.go && go build ./cmd/seed && go vet ./cmd/seed && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `backend/cmd/seed/seed.go` contains `sari.hadi@swp.test`, `rudi.wijaya@swp.test`, `super.admin@swp.test`, `agent.budi@swp.test`, `SWP-CMP-0021`, `auth.HashPassword`, and `GetUserByEmail` (idempotency check).
    - Exported password constants `PasswordHRAdmin`, `PasswordShiftLeader`, `PasswordSuperAdmin`, `PasswordAgent` are defined in `cmd/seed/seed.go`.
    - `cd backend && go build ./cmd/seed && go vet ./cmd/seed` exits 0.
    - `backend/cmd/seed/README.md` lists all four personas with their passwords.
    - Idempotency: the code path for an existing email calls `GetUserByEmail` and returns/continues without calling `CreateUser`.
  </acceptance_criteria>
  <done>Seed inserts the four personas (correct roles, shift_leader scoped to SWP-CMP-0021) with argon2id-hashed known passwords, idempotently (skip-if-exists); password constants are exported for the harness; README documents personas + the per-phase extension rule.</done>
</task>

</tasks>

<verification>
- `cd backend && go build ./... && go vet ./cmd/seed` exits 0.
- `go run ./cmd/seed -genkeys` prints two base64 lines (priv decodes to 64 bytes, pub to 32).
- Against a migrated DB: `DATABASE_URL=... go run ./cmd/migrate up && DATABASE_URL=... go run ./cmd/seed` exits 0; re-running seed exits 0 (idempotent). (This live check is exercised by 01-01's globalSetup; build+vet is the gate here.)
</verification>

<success_criteria>
HARN-02: `backend/cmd/seed` seeds the four deterministic personas (hr_admin Sari Hadi, shift_leader Rudi Wijaya @ Plaza Senayan/SWP-CMP-0021, super_admin, agent) with known passwords + minimal data, idempotently; `-genkeys` provides the dev Ed25519 keypair for the harness.
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-harness-auth/01-02-SUMMARY.md` documenting: the exact persona emails + passwords (and the exported constant names), the `-genkeys` output format (two lines: priv then pub, base64 std), the idempotency mechanism, and the company_id literal used for the shift_leader.
</output>
