---
phase: 04-e2-people
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - backend/internal/domain/people.go
  - backend/internal/repository/people/employees_repo.go
  - backend/internal/service/people/employees_service.go
  - backend/internal/handler/people/employees_dto.go
  - backend/internal/handler/people/employees_handler.go
  - backend/internal/platform/i18n/i18n.go
  - backend/internal/server/server.go
  - backend/cmd/api/main.go
  - backend/cmd/seed/seed.go
autonomous: true
requirements: [PPL-01]
must_haves:
  truths:
    - "HR can list, get, create, update, deactivate, reactivate employees against the real BE"
    - "Duplicate NIK on create returns 409 DUPLICATE_NIK with a fields.nik message"
    - "Employee list/detail JSON matches the OpenAPI Employee schema exactly (snake_case, uppercase status)"
    - "Seeded persona employee rows (SWP-EMP-1042/1108/2891) exist so /auth/me resolves"
  artifacts:
    - path: "backend/internal/service/people/employees_service.go"
      provides: "Employee business logic + DUPLICATE_NIK + audit on writes"
      contains: "DUPLICATE_NIK"
    - path: "backend/internal/handler/people/employees_handler.go"
      provides: "6 employee HTTP handlers (list/get/create/update/deactivate/reactivate)"
      contains: "func (h *Handler) ListEmployees"
  key_links:
    - from: "backend/internal/server/server.go"
      to: "d.People.* employee handlers"
      via: "new authenticated route group after the ORG slice (04-02 coordination marker)"
      pattern: "/employees"
    - from: "backend/cmd/seed/seed.go"
      to: "employees table"
      via: "seedEmployees inserted BEFORE the persona user loop"
      pattern: "SWP-EMP-1042"
---

<objective>
Implement the employees slice end-to-end: domain types, repository over sqlc,
service (DUPLICATE_NIK guard + audit), hand-written chi handlers + DTOs matching
the OpenAPI Employee schema, RBAC route groups in server.go, wiring in cmd/api,
i18n messages for the new codes, and seed employee rows for the personas.

Purpose: PPL-01. Also unblocks /auth/me employee resolution (personas reference
SWP-EMP-* ids that previously had no row) and gives agreements/change-requests
real employees to FK against.
Output: /employees GET/POST/PATCH + :deactivate/:reactivate live against real BE;
seeded persona employees.

COORDINATION: server.go, cmd/api/main.go, and cmd/seed/seed.go are shared by the
three backend slices (04-02 employees, 04-03 agreements, 04-04 change-requests).
This plan runs in its OWN wave (wave 2). 04-03 (wave 3) and 04-04 (wave 4)
depend on it and append their own blocks — they MUST NOT run concurrently with
this plan. Add a clearly-commented "04-02 employees slice end" marker so the
next slice knows where to append.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-e2-people/04-CONTEXT.md
@.planning/reference/backend-build-conventions.md
@docs/api/E2-identity/openapi.yaml
@docs/epics/E2-identity/prds/employee-profile.md

<interfaces>
<!-- Mirror the ORG companies slice EXACTLY (handler/service/repo/domain shape). -->

Service shape (from internal/service/org/companies_service.go):
- consumer-defined repo interface (`type EmployeeRepository interface { ... }`)
- `TxRunner interface { InTx(ctx, fn func(tx pgx.Tx) error) error }`
- `Clock func() time.Time`, `Service struct { repo; txm; now }`, `NewService(repo, txm)`
- writes wrapped in `s.txm.InTx`, each calls `audit.Record(ctx, tx, ...)`
- reads on the pool; cursor encode/decode via httpx.EncodeCursor/DecodeCursor + local pageCursor{CreatedAt,ID}

Handler shape (from internal/handler/org/companies_handler.go):
- `Handler struct { svc *svc.Service }`, `NewHandler(s)`
- decode → call service → `httpx.WriteJSON(w, status, resp)`; errors → `httpx.WriteError(w, r, err)`
- list returns `httpx.PageResponse[employeeResponse]{Data, NextCursor, HasMore}`
- DTO helpers `toEmployeeResponse(domain.Employee) employeeResponse`, `queryStringPtr`, `parseLimit`

Error codes (apperr):
- `apperr.Conflict("DUPLICATE_NIK")` → 409 (Conflict() sets HTTPStatus 409 explicitly; the default
  for unknown codes is 422, so you MUST use Conflict(), not Rule()).
- `apperr.Conflict("CONFLICT")` already exists for already-active/already-inactive 409s.
- `apperr.NotFound()` → 404 when employee missing.

i18n: add new code messages to internal/platform/i18n/i18n.go `messages` map
(both `id` and `en` lang blocks) for: DUPLICATE_NIK.

OpenAPI Employee response fields (openapi.yaml 2782-2851), all snake_case:
id, user_id(nullable), full_name, nik, nip, join_at, gender, birth_date, birth_place,
phone, email_personal, address, npwp, bpjs_kesehatan, bpjs_ketenagakerjaan,
bank_account{bank_name,account_number,account_holder_name}, status(ACTIVE|INACTIVE),
has_login, current_position(null), current_service_line(null), current_client_company(null),
created_at, updated_at, created_by. EmployeeWriteRequest (2853): same minus derived;
plus provision_login + login_email (Phase 4 NOTE below).

Seed reference (cmd/seed/seed.go): seedClientCompanies uses direct INSERT with
`ON CONFLICT (id) DO NOTHING`, explicit ids (SWP-CMP-0021). Seed() body calls
seedClientCompanies/seedServiceLines/seedMasterData in order. Personas list (line 47+)
already declares employeeID literals: SWP-EMP-1042 (Sari Hadi), 1108 (Rudi Wijaya),
2891 (Budi/agent), plus 3001/3002/3003 for extraPersonas.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: domain + repository + service for employees</name>
  <files>backend/internal/domain/people.go, backend/internal/repository/people/employees_repo.go, backend/internal/service/people/employees_service.go, backend/internal/platform/i18n/i18n.go</files>
  <read_first>
    - backend/internal/domain/org.go (domain type + filter struct pattern)
    - backend/internal/repository/org/companies_repo.go (sqlc → domain mapping; pgx.ErrNoRows → domain.ErrNotFound; tx writes)
    - backend/internal/service/org/companies_service.go (full service skeleton to mirror)
    - backend/internal/platform/apperr/apperr.go (Conflict/Rule/NotFound constructors)
    - backend/db/queries/people/employees.sql (the query method names you call)
  </read_first>
  <action>
    Create `backend/internal/domain/people.go` with:
      - `Employee` struct: all OpenAPI Employee fields; bank as nested `BankAccount{BankName,AccountNumber,AccountHolderName}` with pointer/empty handling; Status string (lowercase from DB), Gender/BirthDate/etc as *string or *time.Time for nullables; HasLogin bool derived from UserID != nil.
      - `EmployeeFilter` struct: Q, Status *string; Limit int; CursorCreatedAt *time.Time; CursorID *string.
    (Reuse domain.ErrNotFound from internal/domain/errors.go.)

    Create `backend/internal/repository/people/employees_repo.go`:
      - `New(pool)` returning a repo; package `people`.
      - Implements the service's EmployeeRepository interface over sqlcgen
        (`internal/repository/sqlc`): ListEmployees, GetEmployeeByID, GetEmployeeByNIK,
        CreateEmployee (tx), UpdateEmployee (tx), SetEmployeeStatus (tx).
      - Map sqlc rows → domain.Employee; map pgx.ErrNoRows → domain.ErrNotFound.
      - Reads use the pool; writes take pgx.Tx (mirror companies_repo.go).

    Create `backend/internal/service/people/employees_service.go` (package `people`):
      - `EmployeeRepository` consumer interface (the 6 methods above).
      - `TxRunner`, `Clock`, `Service`, `NewService`, `SetClock` — copy from companies_service.go.
      - ListEmployees: fetch limit+1, build next cursor, return ([]Employee, *string, error).
      - GetEmployee: repo.GetEmployeeByID; map ErrNotFound → apperr.NotFound().
      - CreateEmployee: validate required (full_name, nik, join_at) → apperr.Invalid(fields)
        on missing; pre-check repo.GetEmployeeByNIK — if found, return
        `apperr.Conflict("DUPLICATE_NIK")` with `.Fields = {"nik": "..."}` (set Fields via
        struct literal since Conflict() doesn't take fields). Inside InTx: repo.CreateEmployee
        + audit.Record(ctx, tx, action="employee.create", entity_type="employee", entity_id, actor).
        NOTE (Phase 4 scope): `provision_login`/`login_email` — DO NOT create a linked E1 user
        this milestone; accept the fields, ignore login provisioning, set user_id NULL,
        has_login=false. Document this stub in SUMMARY (FE create form does not require it for E2E).
      - UpdateEmployee: load existing (404 if missing); on NIK change, re-run duplicate check;
        InTx update + audit (action="employee.update").
      - DeactivateEmployee: load; if already inactive → apperr.Conflict("CONFLICT");
        InTx SetEmployeeStatus('inactive') + audit (action="employee.deactivate", include reason).
        NOTE: disabling the linked E1 login is deferred (matches Phase-2 decision
        "Session revocation on deactivate ... deferred"); document the stub.
      - ReactivateEmployee: load; if already active → apperr.Conflict("CONFLICT");
        InTx SetEmployeeStatus('active') + audit (action="employee.reactivate").

    Add the DUPLICATE_NIK message to i18n.go `messages` map under both `id`
    ("NIK sudah terdaftar untuk karyawan lain.") and `en` ("NIK is already registered
    to another employee.").
  </action>
  <verify>
    <automated>cd backend && go build ./internal/domain/... ./internal/repository/people/... ./internal/service/people/... ; echo "build-exit=$?"</automated>
  </verify>
  <done>
    Packages compile. grep confirms `DUPLICATE_NIK` in employees_service.go and i18n.go;
    `func (s *Service) DeactivateEmployee` and `ReactivateEmployee` exist;
    repository maps `pgx.ErrNoRows` to `domain.ErrNotFound`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: employee handlers + DTOs + routes + cmd/api wiring</name>
  <files>backend/internal/handler/people/employees_dto.go, backend/internal/handler/people/employees_handler.go, backend/internal/server/server.go, backend/cmd/api/main.go</files>
  <read_first>
    - backend/internal/handler/org/companies_handler.go + companies_dto.go (handler + DTO mapping reference)
    - backend/internal/server/server.go (route groups; the ORG slice end marker at line ~199)
    - backend/cmd/api/main.go (org slice wiring at lines 91-118)
    - docs/api/E2-identity/openapi.yaml lines 76-467 (employee operations + x-rbac)
  </read_first>
  <action>
    Create `backend/internal/handler/people/employees_dto.go`:
      - `employeeResponse` struct with json tags EXACTLY matching the OpenAPI Employee
        schema (snake_case): id, user_id, full_name, nik, nip, join_at, gender, birth_date,
        birth_place, phone, email_personal, address, npwp, bpjs_kesehatan, bpjs_ketenagakerjaan,
        bank_account{bank_name,account_number,account_holder_name}, status, has_login,
        current_position, current_service_line, current_client_company, created_at, updated_at, created_by.
        Nullable fields use *string / omitempty per the schema (status uppercased ACTIVE/INACTIVE;
        join_at/birth_date as "YYYY-MM-DD"; current_* set to null in Phase 4).
      - `employeeWriteRequest` struct matching EmployeeWriteRequest (incl. provision_login, login_email).
      - `toEmployeeResponse(domain.Employee) employeeResponse` (uppercase status at this boundary).
      - reuse local `queryStringPtr`, `parseLimit`, `pageCursor` helpers (copy the small ones
        from org handler into this package — do not import across handler packages).

    Create `backend/internal/handler/people/employees_handler.go` (package `people`):
      - `Handler struct { svc *svc.Service }`, `NewHandler(s)`.
      - ListEmployees (GET /employees): parse q/status/limit/cursor → filter → svc.ListEmployees
        → PageResponse. GetEmployee (GET /employees/{employee_id}).
      - CreateEmployee (POST): decode employeeWriteRequest, call svc.CreateEmployee, WriteJSON 201
        with Location header `/api/v1/employees/{id}`.
      - UpdateEmployee (PATCH /employees/{employee_id}): decode, svc.UpdateEmployee, 200.
      - DeactivateEmployee (POST /employees/{employee_id}:deactivate): optional {reason} body, 200.
      - ReactivateEmployee (POST /employees/{employee_id}:reactivate): 200.
      - All errors via httpx.WriteError.

    Edit `backend/internal/server/server.go`:
      - Add import `peoplehttp "github.com/hariszaki17/hris-outsource/backend/internal/handler/people"`.
      - Add `People *peoplehttp.Handler` to Deps.
      - AFTER the "ORG slice end (03-04)" marker (the last line inside the authenticated group),
        add a new commented block "PEOPLE slice (04-02): employees (E2 F2.1 / PPL-01)" with TWO groups:
        * reads group `rbac.RequireRole(RoleSuperAdmin, RoleHRAdmin, RoleShiftLeader)`:
            GET /employees, GET /employees/{employee_id}.
            (Spec x-rbac for GET detail also lists agent, but the FE web app never calls it as
            agent; keep web-roles. Note this in SUMMARY.)
        * writes group `rbac.RequireRole(RoleSuperAdmin, RoleHRAdmin)`:
            POST /employees (with d.Idempotency.Handler),
            PATCH /employees/{employee_id},
            POST /employees/{employee_id}:deactivate (with Idempotency),
            POST /employees/{employee_id}:reactivate (with Idempotency).
      - Add a comment "PEOPLE slice end (04-02). 04-03 agreements: append r.Group{} here."

    Edit `backend/cmd/api/main.go`:
      - Add imports peoplehttp/peoplerepo/peoplesvc (handler/repository/service people).
      - After the org slice wiring (line ~104), build:
        `peopleRepo := peoplerepo.New(pool)`,
        `peopleSvc := peoplesvc.NewService(peopleRepo, txm)`,
        `peopleHandler := peoplehttp.NewHandler(peopleSvc)`.
      - Add `People: peopleHandler,` to the server.Deps literal.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./internal/handler/people/... ; echo "build-exit=$?"</automated>
  </verify>
  <done>
    `go build ./...` exits 0. grep server.go shows `r.Get("/employees"` and
    `:deactivate` + `:reactivate` employee routes inside the authenticated group;
    cmd/api/main.go has `People: peopleHandler`; the "PEOPLE slice end (04-02)" marker exists.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: seed persona employee rows</name>
  <files>backend/cmd/seed/seed.go</files>
  <read_first>
    - backend/cmd/seed/seed.go (Seed() body; seedClientCompanies pattern with ON CONFLICT DO NOTHING; personas + extraPersonas lists)
    - backend/db/migrations/00016_employees.sql (column names to INSERT)
  </read_first>
  <action>
    Edit `backend/cmd/seed/seed.go`:
      - Add `seedEmployees(ctx, pool)` (mirror seedClientCompanies: direct INSERT with explicit
        ids + `ON CONFLICT (id) DO NOTHING`, idempotent).
      - Insert employee rows for every persona employeeID literal:
        SWP-EMP-1042 (Sari Hadi, hr_admin), SWP-EMP-1108 (Rudi Wijaya, shift_leader),
        SWP-EMP-2891 (Budi Santoso, agent), SWP-EMP-3001/3002/3003 (extraPersonas).
        Give each: full_name, a deterministic unique NIK (e.g. "317500000000NNNN"),
        nip = the numeric suffix, join_at (a fixed past date), status 'active'.
        Budi (SWP-EMP-2891) MUST have phone "+62-812-3344-5566" and a BCA bank_account
        (bank_name BCA, account_number "1234567890", holder "Budi Santoso") so the
        change-request diff E2E (04-04 seed) has a real "old" value.
      - CRITICAL ORDERING: call seedEmployees BEFORE the persona user-creation loop in
        Seed() (employees referenced by employment_agreements/change_requests FKs and by
        the personas' employee_id). The user loop's CreateUser sets EmployeeID — those ids
        must already exist. Place `if err := seedEmployees(ctx, pool); err != nil { return ... }`
        at the very top of Seed(), before `for _, p := range allPersonas`.
      - Add a Phase-4 comment block explaining the ordering.
      - Also add `'employees'` to nothing here (the E2E TRUNCATE list is updated in 04-06);
        seed must stay idempotent (ON CONFLICT DO NOTHING) so reset-db reseed works.
  </action>
  <verify>
    <automated>cd backend && go build ./cmd/seed/... && go run ./cmd/migrate up >/dev/null 2>&1 ; go run ./cmd/seed 2>&1 | tail -3 ; echo "seed-exit=$?"</automated>
  </verify>
  <done>
    `go run ./cmd/seed` succeeds twice in a row (idempotent). grep seed.go shows
    `func seedEmployees`, `SWP-EMP-1042`, `SWP-EMP-2891`, and the seedEmployees call
    placed before the persona loop. A DB query `SELECT count(*) FROM employees` returns ≥ 6.
  </done>
</task>

</tasks>

<verification>
- `cd backend && go build ./... && go vet ./...` clean.
- After migrate+seed, `curl` (or the contract test in 04-05) on GET /employees returns
  the seeded personas with snake_case fields and uppercase status.
- server.go authenticated group contains all 6 employee routes under the correct RBAC.
</verification>

<success_criteria>
PPL-01 endpoints implemented and wired; DUPLICATE_NIK → 409; persona employees seeded
before users; build + vet clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-e2-people/04-02-SUMMARY.md` documenting:
the login-provisioning stub (accepted-but-ignored), the deactivate-login stub, the
GET-detail RBAC choice (web roles, agent excluded), and the seedEmployees ordering
contract for 04-03/04-04.
</output>
