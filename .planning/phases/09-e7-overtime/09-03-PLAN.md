---
phase: 09-e7-overtime
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - backend/internal/handler/overtime/overtime_testkit_test.go
  - backend/internal/handler/overtime/overtime_handler_test.go
  - backend/internal/handler/overtime/holiday_handler_test.go
autonomous: true
requirements: [OVT-01, OVT-02]
user_setup: []

must_haves:
  truths:
    - "Each OT state transition asserts the exact status change and a wrong-state transition asserts 409"
    - "OT_BELOW_MIN asserts 422 + field errors; HOLIDAY_DATE_CLASH and HOLIDAY_IN_USE assert 409"
    - "OUT_OF_SCOPE and SELF_APPROVAL_FORBIDDEN assert 403 with the exact code"
    - "Bulk approve/reject asserts the {succeeded, failed} envelope with partial success"
  artifacts:
    - path: "backend/internal/handler/overtime/overtime_handler_test.go"
      provides: "state-transition + 409 + 403 + OT_BELOW_MIN + bulk contract tests"
      contains: "SELF_APPROVAL_FORBIDDEN"
    - path: "backend/internal/handler/overtime/holiday_handler_test.go"
      provides: "holiday CRUD clash/in-use contract tests"
      contains: "HOLIDAY_IN_USE"
    - path: "backend/internal/handler/overtime/overtime_testkit_test.go"
      provides: "fake repos + fakeTx + newHarness mounting real services+handler on chi"
      contains: "newHarness"
  key_links:
    - from: "overtime_handler_test.go"
      to: "real overtime service + handler"
      via: "newHarness(role, company, employee) on chi with mutable-principal middleware"
      pattern: "newHarness"
---

<objective>
Write the Go contract tests for E7 — the drift gate that replaces server codegen. Mirrors the
Phase-8 leave harness (08-03) EXACTLY: fakeTx + in-memory fake overtime/holiday/rule/schedule
repos over the REAL services + handler via `newHarness(role, company, employee)` on chi with a
mutable-principal closure middleware + a `stubIdempotency` at the server.go router position.
Asserts every state transition + 409, OT_BELOW_MIN 422 + field errors, HOLIDAY_DATE_CLASH /
HOLIDAY_IN_USE 409, OUT_OF_SCOPE / SELF_APPROVAL_FORBIDDEN 403, bulk partial-success, cursor shapes.

Purpose: Pin the E7 BE responses to the openapi so the FE generated client never drifts.
Output: testkit + two table-driven test files; `go test ./internal/handler/overtime/...` green.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-e7-overtime/09-CONTEXT.md
@.planning/reference/backend-build-conventions.md
@docs/api/E7-overtime/openapi.yaml

<interfaces>
<!-- COPY the Phase-8 leave harness shape exactly. -->
  backend/internal/handler/leave/leave_testkit_test.go  (fakeTx Exec no-op for audit-in-tx + fakeTxRunner + in-memory fake repos + newHarness(role,company,employee) mounting real services+handler on chi + mutable-principal closure middleware + stubIdempotency)
  backend/internal/handler/leave/leave_handler_test.go  (table-driven transition + 409 + 403 + 422 + bulk + cursor assertions; decodeBody snapshots rr.Body.Bytes() so one response re-decodes for errCode + errFields — decision [08-03])
  backend/internal/handler/attendance/  (bulk {succeeded, failed} envelope + idempotency-replay stub assertions)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test kit (fakes + harness) + transition/scope/409 tests</name>
  <files>backend/internal/handler/overtime/overtime_testkit_test.go, backend/internal/handler/overtime/overtime_handler_test.go</files>
  <read_first>
    - backend/internal/handler/leave/leave_testkit_test.go (the harness to copy: fakeTx, fakeTxRunner, fake repos sharing state, newHarness, mutable-principal middleware, stubIdempotency at the router position)
    - backend/internal/handler/leave/leave_handler_test.go (transition + 409 terminal + OUT_OF_SCOPE + self-approve 403 table tests; decodeBody snapshot)
    - backend/internal/service/overtime/overtime_service.go (this phase's state machine — the system under test)
    - docs/api/E7-overtime/openapi.yaml the 200/403/409/422 responses + schemas.Overtime + OvertimeStatus enum
  </read_first>
  <action>
    Create `backend/internal/handler/overtime/overtime_testkit_test.go`: `fakeTx` (Exec no-op so audit-in-tx + InsertOvertimeApproval don't panic — copy leave's), `fakeTxRunner` (InTx runs the closure with fakeTx), in-memory `fakeOvertimeRepo` (map[id]domain.Overtime + ListOvertimeApprovals slice + recorded transitions), `fakeHolidayRepo` (map + CountOvertimeUsingHoliday configurable), `fakeRuleRepo` (configurable min_minutes + rate), `fakeScheduleRepo` (configurable scheduled-shift presence for day_type). `newHarness(role, company, employee)` builds the REAL OvertimeService + HolidayService + Handler, mounts them on a chi router with the same group structure + `stubIdempotency` middleware at the server.go position, and a mutable-principal closure middleware so a test can swap role/company/employee. `decodeBody` snapshots `rr.Body.Bytes()` for re-decode (errCode + errFields) per decision [08-03].

    Create `backend/internal/handler/overtime/overtime_handler_test.go` table-driven tests:
    - Happy path chain: PENDING_AGENT_CONFIRM `:confirm` (agent) → status PENDING_L1 ; PENDING_L1 `:approve-l1` (leader in-scope) → PENDING_HR + an OvertimeApproval level-1 row ; PENDING_HR `:approve-final` (HR) → APPROVED + level-2 row.
    - `:reject` with reason → REJECTED ; reject missing/short reason → 400.
    - `:withdraw` from PENDING_L1 → 204 ; `:withdraw` from APPROVED → 409 CONFLICT.
    - Wrong-state 409: `:approve-l1` on a PENDING_HR record ; `:approve-final` on a PENDING_L1 (non-override) ; `:confirm` on a PENDING_L1.
    - 403 OUT_OF_SCOPE: leader @ CMP-0021 `:approve-l1` on a CMP-0022 OT (assert status 403 + code OUT_OF_SCOPE).
    - 403 SELF_APPROVAL_FORBIDDEN: leader `:approve-l1` on an OT whose employee == the leader's employee_id (assert 403 + code SELF_APPROVAL_FORBIDDEN).
    - 422 OVERRIDE_REASON_REQUIRED: HR `:approve-final` with is_override:true + empty note.
    - GET /overtime/{id}: assert the `{data:{...}}` envelope, required fields present (calculation block with tier_breakdown, tier_indicator, flagged_no_preapproval, approvals timeline), required-nullable fields serialize as JSON null (attendance_id null for a REQUESTED row).
    - GET /overtime list: assert `{data, next_cursor, has_more}` cursor envelope + leader-scope filtering (cross-company rows absent).
  </action>
  <verify>
    <automated>cd backend && grep -q "newHarness" internal/handler/overtime/overtime_testkit_test.go && grep -q "SELF_APPROVAL_FORBIDDEN" internal/handler/overtime/overtime_handler_test.go && grep -q "OUT_OF_SCOPE" internal/handler/overtime/overtime_handler_test.go && grep -q "PENDING_HR" internal/handler/overtime/overtime_handler_test.go && go test ./internal/handler/overtime/... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `overtime_testkit_test.go` defines `newHarness`, `fakeTx`, fake overtime/holiday/rule/schedule repos, and `stubIdempotency`.
    - `overtime_handler_test.go` asserts the confirm→L1→final chain, wrong-state 409s, OUT_OF_SCOPE 403, SELF_APPROVAL_FORBIDDEN 403, withdraw 204/409, and the `{data}` + cursor envelopes.
    - `go test ./internal/handler/overtime/...` exits 0.
  </acceptance_criteria>
  <done>State-machine + scope + self + 409 contract tests pass against the real services through the chi harness.</done>
</task>

<task type="auto">
  <name>Task 2: OT_BELOW_MIN + holiday clash/in-use + bulk partial-success tests</name>
  <files>backend/internal/handler/overtime/overtime_handler_test.go, backend/internal/handler/overtime/holiday_handler_test.go</files>
  <read_first>
    - backend/internal/handler/leave/quota_handler_test.go (QUOTA_EXCEEDED 422 + field-errors + bulk-grant partial-success assertions — the OT_BELOW_MIN + bulk analog)
    - backend/internal/service/overtime/holiday_service.go (HOLIDAY_DATE_CLASH + HOLIDAY_IN_USE)
    - docs/api/E7-overtime/openapi.yaml schemas.BulkResult + /holidays POST/DELETE 409 + the OT_BELOW_MIN error envelope (fields object)
  </read_first>
  <action>
    Extend `overtime_handler_test.go` with the bulk + min-minutes seam tests and create `backend/internal/handler/overtime/holiday_handler_test.go`:
    - OT_BELOW_MIN: drive the exported `enforceMinMinutes` seam (or the confirm/seed path) where counted_minutes < rule.min_minutes → assert 422, code OT_BELOW_MIN, and `error.fields` carries `counted_minutes` + `min_minutes` (assert via the decodeBody errFields snapshot).
    - Bulk approve: ids = [in-scope PENDING_L1, the leader's OWN OT, a CMP-0022 OT, a terminal APPROVED] → assert 200 BulkResult: `succeeded` contains the in-scope id; `failed[]` carries the self (SELF_APPROVAL_FORBIDDEN), the cross-company (OUT_OF_SCOPE), and the terminal (CONFLICT) ids each with its `error.code`.
    - Bulk reject: ids with reason → mix of succeeded + a terminal failure; assert envelope. All-failed selection → 422.
    - Holiday create happy → 201 + Holiday shape (in_use_by_overtime false). Create on a date+category that already has a live holiday → 409 HOLIDAY_DATE_CLASH.
    - Holiday update happy → 200. Holiday delete on a holiday with CountOvertimeUsingHoliday>0 (configure fakeHolidayRepo) → 409 HOLIDAY_IN_USE ; delete on a free holiday → 204.
    - Holiday list → `{data, next_cursor, has_more}` cursor envelope + each item's in_use_by_overtime computed.
  </action>
  <verify>
    <automated>cd backend && grep -q "OT_BELOW_MIN" internal/handler/overtime/overtime_handler_test.go && grep -q "HOLIDAY_DATE_CLASH" internal/handler/overtime/holiday_handler_test.go && grep -q "HOLIDAY_IN_USE" internal/handler/overtime/holiday_handler_test.go && grep -qE "succeeded|failed" internal/handler/overtime/overtime_handler_test.go && go test ./internal/handler/overtime/... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `overtime_handler_test.go` asserts OT_BELOW_MIN 422 + `error.fields`, and bulk `{succeeded, failed}` partial-success (self/out-of-scope/terminal in failed[]).
    - `holiday_handler_test.go` asserts HOLIDAY_DATE_CLASH 409, HOLIDAY_IN_USE 409, the create-201 / update-200 / delete-204 happy paths, and the holiday cursor envelope.
    - `go test ./internal/handler/overtime/...` exits 0.
  </acceptance_criteria>
  <done>OT_BELOW_MIN, holiday clash/in-use, and bulk partial-success are pinned to the openapi; full overtime test package green.</done>
</task>

</tasks>

<verification>
- `cd backend && go test ./internal/handler/overtime/...` exits 0.
- `go test ./...` exits 0 (no regression in earlier epics' contract tests).
- Every E7 error code in the openapi ErrorEnvelope description is asserted by at least one test: OT_BELOW_MIN, HOLIDAY_DATE_CLASH, HOLIDAY_IN_USE, OUT_OF_SCOPE, SELF_APPROVAL_FORBIDDEN, OVERRIDE_REASON_REQUIRED, plus the state-transition 409s.
</verification>

<success_criteria>
The E7 contract tests are the drift gate: state transitions + 409s, OT_BELOW_MIN 422 with field errors, HOLIDAY_DATE_CLASH / HOLIDAY_IN_USE 409, OUT_OF_SCOPE / SELF_APPROVAL_FORBIDDEN 403, bulk partial-success {succeeded, failed}, and cursor envelopes are all asserted against the REAL services through the chi harness. `go test ./...` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/09-e7-overtime/09-03-SUMMARY.md`.
</output>
