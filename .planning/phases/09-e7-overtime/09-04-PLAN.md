---
phase: 09-e7-overtime
plan: 04
type: execute
wave: 4
depends_on: ["09-02", "09-03"]
files_modified:
  - frontend/apps/web/src/features/e7-overtime/overtime-approvals-screen.tsx
  - frontend/apps/web/src/features/e7-overtime/overtime-detail-screen.tsx
  - frontend/apps/web/src/features/e7-overtime/overtime-records-screen.tsx
  - frontend/apps/web/src/features/e7-overtime/overtime-rules-screen.tsx
  - frontend/e2e/lib/e7-helpers.ts
  - frontend/e2e/lib/reset-db.ts
  - frontend/e2e/tests/e7/approvals.spec.ts
  - frontend/e2e/tests/e7/workflow.spec.ts
  - frontend/e2e/tests/e7/bulk.spec.ts
  - frontend/e2e/tests/e7/holidays.spec.ts
  - frontend/e2e/tests/e7/scope-negatives.spec.ts
autonomous: true
requirements: [OVT-01, OVT-02]
user_setup: []

must_haves:
  truths:
    - "The e7-overtime screens render real BE data with MSW off"
    - "confirm→L1→final, reject, and withdraw drive the real state machine green"
    - "Bulk approve reports partial success; OT_BELOW_MIN surfaces; holiday CRUD + clash/in-use work"
    - "A leader cannot approve another company's OT (403 OUT_OF_SCOPE) or their own (403 SELF_APPROVAL_FORBIDDEN)"
  artifacts:
    - path: "frontend/e2e/tests/e7/workflow.spec.ts"
      provides: "confirm→L1→final + reject + withdraw E2E"
      contains: "approve-final"
    - path: "frontend/e2e/tests/e7/holidays.spec.ts"
      provides: "holiday CRUD + clash + in-use E2E"
      contains: "HOLIDAY_IN_USE"
    - path: "frontend/e2e/tests/e7/scope-negatives.spec.ts"
      provides: "OUT_OF_SCOPE + SELF_APPROVAL_FORBIDDEN E2E"
      contains: "SELF_APPROVAL_FORBIDDEN"
    - path: "frontend/e2e/lib/reset-db.ts"
      provides: "overtime + holidays added to TRUNCATE list"
      contains: "overtime"
  key_links:
    - from: "frontend/e2e/tests/e7/*.spec.ts"
      to: "real Go BE + ephemeral Postgres"
      via: "loginAs PERSONAS + apiAs token + REAL e7-overtime selectors"
      pattern: "apiAs"
    - from: "overtime-approvals-screen.tsx"
      to: "BE list response"
      via: "{data} envelope unwrap (already present) + classifyError"
      pattern: "query.data?.data"
---

<objective>
Wire the e7-overtime screens off MSW to the real Go BE and prove every E7 Gherkin AC with
exhaustive full-stack Playwright E2E under a NEW `frontend/e2e/tests/e7/`. Selectors are
derived from the REAL e7-overtime components (approvals/records/detail/rules/holiday overlays),
not invented. Mirrors the Phase-8 E6 E2E plan (08-04): real FE ↔ real Go ↔ ephemeral Postgres,
one `test()` per scenario. Apply the recurring `{data}`-envelope-unwrap + classifyError FE
fixes only where the e7 components need them. Add overtime + holidays to reset-db TRUNCATE.

Purpose: Close Phase 9 — E7 overtime works end-to-end against the real backend.
Output: FE wiring fixes (if any), e7-helpers, reset-db edit, 5 E2E specs green headless.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-e7-overtime/09-CONTEXT.md
@.planning/reference/e2e-harness-spec.md
@docs/api/E7-overtime/openapi.yaml
@frontend/apps/web/src/features/e7-overtime/overtime-approvals-screen.tsx
@frontend/apps/web/src/features/e7-overtime/holiday-overlays.tsx
@frontend/apps/web/src/features/e7-overtime/overtime-rules-screen.tsx

<interfaces>
<!-- REAL component selectors (anchor E2E here — NOT assumptions). -->

overtime-approvals-screen.tsx (route /overtime, role-branched HR/SL):
  - Reads `query.data?.data as { data?: Overtime[]; has_more?; next_cursor? }` → BE list {data,...} envelope ALREADY unwrapped. Default status: HR=PENDING_HR, SL=PENDING_L1.
  - Row per-id action buttons: "Setujui" (t('approvals.approve') → useApproveOvertimeFinal HR / useApproveOvertimeL1 SL, body {}); "Tolak" (t('approvals.reject') → opens RejectOvertimeModal); detail link `<a href="/overtime/{id}">` "Lihat" (t('approvals.detail')).
  - Row checkbox aria `t('approvals.selectRow', {id})`; header "Setujui Massal" (t('approvals.bulkApprove'/'bulkApproveCount')); bulk-reject button appears when selection>0 (t('approvals.bulkReject')).
  - RejectOvertimeModal (overtime-queue-overlays.tsx): textarea for reason → onConfirm(reason). BulkApproveModal / BulkRejectModal: optional note / required reason.
  - flagged_no_preapproval pill (StatusBadge tone=warn, t('approvals.flagNoPreapproval')).
  - classifyError used for toasts (error.code → message). On forbidden → EmptyState no-permission.

overtime-rules-screen.tsx (route /overtime/aturan, HR only):
  - Right pane "Kalender Hari Libur" lists holidays from useListHolidays({limit:200}) read as `holidaysQuery.data?.data as HolidayPage` → `.data ?? []`.
  - Add holiday: header "+" button aria t('holidays.addTitle') → HolidayFormModal. Per-row edit "Pencil" aria t('holidays.editTitle'); delete "Trash2" aria t('holidays.deleteTitle') → DeleteHolidayConfirm.
  - HolidayFormModal fields: #holiday-name, #holiday-date (type=date), #holiday-category (FilterSelect), recurring Toggle (role=switch). Save button "Simpan" (t('common.save')). noValidate needed on RHF+Zod (decision [03-04]).
  - DeleteHolidayConfirm: confirm disabled when holiday.in_use_by_overtime → Banner warn (t('holidays.inUseWarning')); conflict toast t('holidays.inUseError').

overtime-detail-screen.tsx (route /overtime/{id}): action buttons for confirm/approve/reject/withdraw + calculation/tier-breakdown card + approvals timeline. Read its selectors during the task.

E2E patterns (COPY from e6):
  frontend/e2e/lib/e6-helpers.ts (apiAs token call + errorCode + errorDetails + waitForToken re-exported from e5-helpers; DataTable rows div.border-b filter; toggles role=switch; .js imports; PERSONAS.*)
  frontend/e2e/tests/e6/scope-negatives.spec.ts (the apiAs real-403 OUT_OF_SCOPE pattern)
  frontend/e2e/lib/reset-db.ts (TRUNCATE_TABLES — add overtime + overtime_approvals + holidays BEFORE placements/employees; seed is ON CONFLICT DO NOTHING)
  frontend/e2e/lib/personas.ts (PERSONAS.hrAdmin / shiftLeader[Rudi @ CMP-0021] / superAdmin / agent)

Seed fixtures (from 09-02): SWP-OT-30001 PENDING_AGENT_CONFIRM, 30002 PENDING_L1 @ CMP-0021,
  30003 PENDING_HR, 30004 self (Rudi's employee), 30005 @ CMP-0022, 30006 below-min, 30007 APPROVED,
  30008 REJECTED, 30009 HOLIDAY+APPROVED (refs SWP-HOL-9001), 30010 RESTDAY.
  SWP-HOL-9001 (in-use, delete-blocked), SWP-HOL-9002 (free).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FE wiring fixes + e7-helpers + reset-db TRUNCATE</name>
  <files>frontend/apps/web/src/features/e7-overtime/overtime-detail-screen.tsx, frontend/e2e/lib/e7-helpers.ts, frontend/e2e/lib/reset-db.ts</files>
  <read_first>
    - frontend/apps/web/src/features/e7-overtime/overtime-approvals-screen.tsx + overtime-detail-screen.tsx + overtime-records-screen.tsx + overtime-rules-screen.tsx (find any place reading `query.data?.data` that needs the bare-fallback unwrap, and any classifyError mismatch — apply the Phase-8 leave-detail fix pattern ONLY where needed)
    - frontend/e2e/lib/e6-helpers.ts (the helper shape to mirror in e7-helpers.ts)
    - frontend/e2e/lib/reset-db.ts (TRUNCATE_TABLES ordering — most-restrictive first; FK to placements/employees/attendance/overtime_rules/holidays)
    - frontend/e2e/lib/personas.ts (persona handles)
  </read_first>
  <action>
    Run the FE against the real BE (MSW off) and fix ONLY the components that misread the BE
    response. The recurring fix (Phase-8): a detail GET typed by Orval as the bare object but
    returned `{data:<obj>}` by the handler — unwrap with a bare fallback
    `const ot = (resp?.data as { data?: Overtime })?.data ?? (resp?.data as Overtime)`. Apply to
    overtime-detail-screen.tsx if its GET /overtime/{id} read assumes the bare object. The
    approvals + rules screens already unwrap `query.data?.data` — leave them unless they break.
    If a toast classifyError mismatches (Bahasa message lacks the code), branch on
    `ApiError.code` like the leave-detail BALANCE_RECHECK fix.

    Create `frontend/e2e/lib/e7-helpers.ts` mirroring e6-helpers.ts: re-export apiAs/errorCode/
    errorDetails/waitForToken from e5-helpers; export the seeded OT fixture ids
    (`OT = { confirmTarget:'SWP-OT-30001', l1Target:'SWP-OT-30002', finalTarget:'SWP-OT-30003',
    self:'SWP-OT-30004', outOfScope:'SWP-OT-30005', belowMin:'SWP-OT-30006', approved:'SWP-OT-30007',
    rejected:'SWP-OT-30008', holidayOt:'SWP-OT-30009', restday:'SWP-OT-30010' }`) and holiday ids
    (`HOL = { inUse:'SWP-HOL-9001', free:'SWP-HOL-9002' }`); add row + overlay locator helpers
    anchored on the REAL component DOM (approval row via div.border-b filtered by the OT work_date /
    employee text; "Setujui"/"Tolak" buttons; holiday list li filtered by name; HolidayFormModal
    #holiday-name/#holiday-date/#holiday-category + role=switch toggle + "Simpan").

    Edit `frontend/e2e/lib/reset-db.ts`: add `'overtime_approvals'`, `'overtime'`, `'holidays'` to
    TRUNCATE_TABLES in FK-safe order (overtime_approvals → overtime BEFORE placements/employees/
    attendance; holidays AFTER overtime since overtime.holiday_id FKs holidays — TRUNCATE ... CASCADE
    handles it, but order overtime before holidays to be explicit). Document with a comment.
  </action>
  <verify>
    <automated>cd frontend && grep -q "overtime" e2e/lib/reset-db.ts && grep -q "holidays" e2e/lib/reset-db.ts && grep -q "SWP-OT-30004" e2e/lib/e7-helpers.ts && grep -q "SWP-HOL-9001" e2e/lib/e7-helpers.ts && pnpm --filter @swp/web build && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `reset-db.ts` TRUNCATE list contains `overtime` and `holidays`.
    - `e7-helpers.ts` exports the OT + HOL fixture-id maps and real-DOM locator helpers + re-exports apiAs/errorCode/waitForToken.
    - Any applied FE fix is the minimal `{data}`-bare-fallback or classifyError-by-code change; `pnpm --filter @swp/web build` succeeds.
  </acceptance_criteria>
  <done>FE reads the real BE shapes; e7-helpers + reset-db ready for the specs.</done>
</task>

<task type="auto">
  <name>Task 2: Workflow + approvals + bulk E2E specs</name>
  <files>frontend/e2e/tests/e7/workflow.spec.ts, frontend/e2e/tests/e7/approvals.spec.ts, frontend/e2e/tests/e7/bulk.spec.ts</files>
  <read_first>
    - frontend/e2e/tests/e6/approvals.spec.ts + quotas.spec.ts (the loginAs → goto → waitForToken → drive UI / apiAs pattern; bulk partial-success assertion)
    - frontend/apps/web/src/features/e7-overtime/overtime-approvals-screen.tsx + overtime-detail-screen.tsx + overtime-queue-overlays.tsx (the REAL selectors)
    - docs/api/E7-overtime/openapi.yaml the transition + bulk semantics
  </read_first>
  <action>
    Create `frontend/e2e/tests/e7/workflow.spec.ts` — one test() per Gherkin AC, beforeEach resetDb():
    - WF-confirm: agent (or per CONTEXT the web-triggered confirm) `:confirm` SWP-OT-30001 → status PENDING_L1 (assert via GET or the detail screen).
    - WF-l1: leader Rudi approves SWP-OT-30002 (PENDING_L1) via the approvals "Setujui" button → success toast; row leaves the SL queue; GET shows PENDING_HR + an L1 approval entry.
    - WF-final: HR approves SWP-OT-30003 (PENDING_HR) → APPROVED + level-2 approval; row leaves HR queue.
    - WF-l1-then-final: chain 30002 leader→HR then HR final → APPROVED end-to-end.
    - WF-reject: HR opens RejectOvertimeModal on SWP-OT-30003, enters reason → REJECTED + toast; reject without reason blocked.
    - WF-withdraw: `:withdraw` SWP-OT-30001 (PENDING_AGENT_CONFIRM) → 204/WITHDRAWN ; `:withdraw` on APPROVED 30007 → 409.
    - WF-terminal-409: approve an already-APPROVED 30007 → 409 (assert via apiAs).

    Create `frontend/e2e/tests/e7/approvals.spec.ts`:
    - LIST-HR: HR /overtime lists PENDING_HR rows; flagged_no_preapproval pill visible where seeded.
    - LIST-SL: leader /overtime lists own-company PENDING_L1 rows only.
    - DETAIL: open /overtime/{id} → calculation/tier-breakdown card + approvals timeline render from the real `{data}` envelope.
    - FILTER: source filter narrows the queue.

    Create `frontend/e2e/tests/e7/bulk.spec.ts`:
    - BULK-partial: HR selects [SWP-OT-30003 (in PENDING_HR), SWP-OT-30007 (already APPROVED → terminal)] → bulk-approve → BulkResult succeeded contains 30003, failed contains 30007 with a CONFLICT code; assert the partial-success toast + at least one failed row surfaced. Drive via the "Setujui Massal" header button (and/or apiAs for the envelope shape).
    - BULK-reject-partial: leader selects [30002 in-scope PENDING_L1, 30005 cross-company] with reason → succeeded 30002, failed 30005 OUT_OF_SCOPE.
    - BULK-all-fail: select only terminal rows → 422.
  </action>
  <verify>
    <automated>cd frontend && grep -q "approve-final\|approve-l1\|Setujui" e2e/tests/e7/workflow.spec.ts && grep -q "withdraw\|WITHDRAWN" e2e/tests/e7/workflow.spec.ts && grep -qE "succeeded|failed|bulk" e2e/tests/e7/bulk.spec.ts && pnpm --filter @swp/e2e exec playwright test tests/e7/workflow.spec.ts tests/e7/approvals.spec.ts tests/e7/bulk.spec.ts --reporter=line && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `workflow.spec.ts` covers confirm, L1, final, L1→final chain, reject (+missing-reason block), withdraw (204 + terminal-409).
    - `bulk.spec.ts` asserts {succeeded, failed} partial success incl. a terminal-409 and an OUT_OF_SCOPE failure, plus the all-fail 422.
    - The three specs run green headless against the real stack.
  </acceptance_criteria>
  <done>Workflow + approvals + bulk specs green vs real FE↔Go↔ephemeral Postgres.</done>
</task>

<task type="auto">
  <name>Task 3: Holiday + OT_BELOW_MIN + scope-negatives E2E + full-suite regression</name>
  <files>frontend/e2e/tests/e7/holidays.spec.ts, frontend/e2e/tests/e7/scope-negatives.spec.ts</files>
  <read_first>
    - frontend/apps/web/src/features/e7-overtime/holiday-overlays.tsx + overtime-rules-screen.tsx (HolidayFormModal + DeleteHolidayConfirm REAL selectors)
    - frontend/e2e/tests/e6/scope-negatives.spec.ts (apiAs real-403 OUT_OF_SCOPE pattern to copy)
    - docs/api/E7-overtime/openapi.yaml /holidays POST/DELETE 409 + OT_BELOW_MIN
  </read_first>
  <action>
    Create `frontend/e2e/tests/e7/holidays.spec.ts` (HR persona, /overtime/aturan):
    - HOL-create: open "+" → HolidayFormModal → fill #holiday-name + #holiday-date (a free clearly-in-range date) + category → "Simpan" → success toast; the new holiday appears in the "Kalender Hari Libur" list.
    - HOL-clash: create a holiday on SWP-HOL-9001's date+category → 409 HOLIDAY_DATE_CLASH surfaced as the error toast / field error (assert via apiAs errorCode HOLIDAY_DATE_CLASH).
    - HOL-update: edit SWP-HOL-9002 name → 200 + list reflects.
    - HOL-delete-free: delete SWP-HOL-9002 → 204 + removed from list.
    - HOL-delete-inuse: open DeleteHolidayConfirm on SWP-HOL-9001 (in_use_by_overtime true) → confirm button disabled + inUse Banner; and apiAs DELETE SWP-HOL-9001 → 409 HOLIDAY_IN_USE.

    Create `frontend/e2e/tests/e7/scope-negatives.spec.ts` (mirror e6/scope-negatives):
    - OT-SCOPE-403: leader Rudi apiAs POST /overtime/SWP-OT-30005:approve-l1 (CMP-0022) → 403 OUT_OF_SCOPE.
    - OT-SELF-403: leader Rudi apiAs POST /overtime/SWP-OT-30004:approve-l1 (Rudi's own employee) → 403 SELF_APPROVAL_FORBIDDEN.
    - OT-QUEUE-hidden: the SL queue (/overtime default PENDING_L1) does NOT list the cross-company 30005.
    - OT-LIST-403: leader apiAs GET /overtime?company_id=SWP-CMP-0022 → 403 OUT_OF_SCOPE.
    - OT-BELOW-MIN: apiAs the OT_BELOW_MIN seam (the seeded below-min 30006 path or the confirm-with-short-duration) → 422 errorCode OT_BELOW_MIN with error.fields present.

    Run the FULL e1–e7 suite headless to confirm zero regressions; commit specs under
    `frontend/e2e/tests/e7/`.
  </action>
  <verify>
    <automated>cd frontend && grep -q "HOLIDAY_IN_USE" e2e/tests/e7/holidays.spec.ts && grep -q "HOLIDAY_DATE_CLASH" e2e/tests/e7/holidays.spec.ts && grep -q "SELF_APPROVAL_FORBIDDEN" e2e/tests/e7/scope-negatives.spec.ts && grep -q "OUT_OF_SCOPE" e2e/tests/e7/scope-negatives.spec.ts && grep -q "OT_BELOW_MIN" e2e/tests/e7/scope-negatives.spec.ts && pnpm --filter @swp/e2e exec playwright test tests/e7 --reporter=line && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `holidays.spec.ts` covers create, clash (409), update, delete-free (204), delete-in-use (409 + disabled confirm + Banner).
    - `scope-negatives.spec.ts` asserts OUT_OF_SCOPE 403 (approve + list), SELF_APPROVAL_FORBIDDEN 403, queue-hidden, and OT_BELOW_MIN 422 + fields.
    - All e7 specs green; the full e1–e7 headless suite passes with zero failures.
  </acceptance_criteria>
  <done>Holiday + scope + below-min specs green; no regression across e1–e7; Phase 9 closes OVT-01/OVT-02.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm --filter @swp/e2e exec playwright test tests/e7 --reporter=line` → all e7 specs pass headless.
- Full suite `pnpm --filter @swp/e2e exec playwright test --reporter=line` → 0 failures (no regression in e1–e6).
- Every one of the 13 FE-used E7 endpoints is exercised by a spec or apiAs call; no out-of-scope endpoint (no :auto-detect, no POST /overtime create); useListOvertimeRules reused from E2 (rules pane renders, not re-implemented).
</verification>

<success_criteria>
The e7-overtime screens drive the real Go BE green: confirm→L1→final, reject, withdraw, bulk
partial success, OT_BELOW_MIN, holiday CRUD + clash/in-use, and OUT_OF_SCOPE +
SELF_APPROVAL_FORBIDDEN are all asserted via the REAL component selectors / apiAs against an
ephemeral Postgres, headless. The full e1–e7 suite is green. Closes OVT-01 / OVT-02.
</success_criteria>

<output>
After completion, create `.planning/phases/09-e7-overtime/09-04-SUMMARY.md`.
</output>
