---
phase: 04-e2-people
plan: 06
type: execute
wave: 5
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - frontend/e2e/lib/reset-db.ts
  - frontend/e2e/lib/db.ts
  - frontend/e2e/fixtures/sample.pdf
  - frontend/e2e/tests/e2/employees.spec.ts
  - frontend/e2e/tests/e2/employment-agreements.spec.ts
  - frontend/e2e/tests/e2/change-requests.spec.ts
  - frontend/apps/web/src/features/e2-identity/agreement-detail-screen.tsx
autonomous: true
requirements: [PPL-01, PPL-02, PPL-03]
must_haves:
  truths:
    - "The employees, agreements, and change-request web screens drive the real BE (MSW off)"
    - "Every Gherkin scenario in employee-profile.md + employment-agreement.md (+ CR scenarios) is its own test()"
    - "Attachment upload E2E uploads a REAL file and asserts the returned metadata; download requires auth"
    - "PKWT period-rule (422) and RBAC-negative cases are covered"
    - "pnpm e2e (headless) is green for the new specs against the real stack"
  artifacts:
    - path: "frontend/e2e/tests/e2/employees.spec.ts"
      provides: "employee CRUD + deactivate/reactivate + duplicate-NIK + RBAC E2E"
      contains: "loginAs"
    - path: "frontend/e2e/tests/e2/employment-agreements.spec.ts"
      provides: "agreement create/renew/close + real attachment upload + PKWT 422 E2E"
      contains: "setInputFiles"
    - path: "frontend/e2e/tests/e2/change-requests.spec.ts"
      provides: "queue list/detail-diff/approve/reject E2E"
      contains: "resetDb"
  key_links:
    - from: "frontend/e2e/lib/reset-db.ts"
      to: "new people tables"
      via: "TRUNCATE_TABLES includes change_requests, agreement_attachments, employment_agreements, employees"
      pattern: "employment_agreements"
    - from: "agreement-detail-screen.tsx file input"
      to: "POST /agreements/{id}/attachments"
      via: "useUploadAgreementAttachment with a real File"
      pattern: "type=\"file\""
---

<objective>
Wire the E2 people web screens to the real BE (MSW off — the screens already call
the Orval hooks; finish the agreement-attachment upload-input stub) and write
exhaustive Playwright E2E: one test() per Gherkin scenario/case from
employee-profile.md and employment-agreement.md plus the change-request scenarios.
Add the new people tables to the E2E truncate list, add DB helpers + a real PDF
fixture, and prove the whole stack green headless.

Purpose: PPL-01/02/03 end-to-end verification against the real stack.
Output: 3 spec files, reset-db + db helpers updated, a sample.pdf fixture, green `pnpm e2e`.

Parallel-safe with 04-05 (disjoint files: frontend vs backend tests).
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/reference/e2e-harness-spec.md
@docs/epics/E2-identity/prds/employee-profile.md
@docs/epics/E2-identity/prds/employment-agreement.md

<interfaces>
<!-- Mirror the Phase-3 E2E specs EXACTLY. -->

From frontend/e2e/tests/e2/client-companies.spec.ts:
- imports: `{ test, expect, loginAs } from '../../lib/fixtures.js'`,
  `{ PERSONAS } from '../../lib/personas.js'`, `{ resetDb } from '../../lib/reset-db.js'`,
  DB helpers from '../../lib/db.js'.
- `test.beforeEach(async () => { await resetDb(); })`.
- Pattern: `await loginAs(page, PERSONAS.hrAdmin); await page.goto('/<route>');`
  then assert visible text/heading with `{ timeout: 30_000 }` on first load.
- DataTable rows are `div.border-b` (NOT <tr>) — row locators use div.border-b.filter() (STATE.md decision).
- Toggle is role=switch; conflict toast text matches /konflik/i (STATE.md decisions).
- RBAC negative: login as agent persona, goto the screen, assert access denied.

PERSONAS (frontend/e2e/lib/personas.ts): hrAdmin (sari.hadi), shiftLeader (rudi.wijaya),
superAdmin, agent (budi). Passwords come from the seed constants.

FE routes — GREP these before writing goto()s (router.tsx / nav.ts):
employees screen, employee detail, agreements screen, agreement detail, change-requests queue.

Seeded fixtures available after resetDb (from 04-02/03/04):
- employees: SWP-EMP-1042 Sari Hadi, 1108 Rudi Wijaya, 2891 Budi Santoso (+ 3001/3002/3003).
  Budi phone "+62-812-3344-5566", BCA bank "1234567890".
- agreement: SWP-AG-7001 active PKWT for Budi + attachment SWP-FILE-9001.
- change-requests: SWP-CHG-2117 (MULTIPLE: phone+bank → new "+62-812-9988-7766" / "9999000011"),
  SWP-CHG-2118 (ADDRESS), both pending for Budi.

DB helper pattern (frontend/e2e/lib/db.ts): each helper opens a pg.Client on DATABASE_URL,
runs one query, returns a scalar. e.g. getCompanyStatus(id).

reset-db.ts TRUNCATE_TABLES (line 62): currently ends at overtime_rules. The new people
tables must be added in FK-safe order (most-dependent first): change_requests,
agreement_attachments, employment_agreements, employees.

Gherkin scenarios → tests (employee-profile.md §7/§8): create-with-login, create-data-only,
reject-duplicate-NIK, agent-edits-limited (web side = the change-request queue, covered in
change-requests.spec), deactivate-disables-login; C-1 provision-later, C-2 email-in-use,
C-3 reactivate, C-4 statutory-blocked (covered via change-request), C-5 (E9 — skip).
employment-agreement.md §7/§8: create-PKWT, create-PKWTT, reject-PKWT-no-end-date,
renewal-linked-successor, only-one-active, agent-cannot-see-comp (mobile — skip web);
C-1 resignation(close), C-2 PKWT→PKWTT renew, C-3 comp-update, C-5 historical (skip).
PLUS milestone-specific: attachment upload (real file) + PKWT > 5y → 422.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: harness updates — truncate list, DB helpers, PDF fixture, FE upload wiring</name>
  <files>frontend/e2e/lib/reset-db.ts, frontend/e2e/lib/db.ts, frontend/e2e/fixtures/sample.pdf, frontend/apps/web/src/features/e2-identity/agreement-detail-screen.tsx</files>
  <read_first>
    - frontend/e2e/lib/reset-db.ts (TRUNCATE_TABLES array)
    - frontend/e2e/lib/db.ts (helper pattern: getCompanyStatus etc.)
    - frontend/apps/web/src/features/e2-identity/agreement-detail-screen.tsx (upload "file input stub" at line ~14; useUploadAgreementAttachment at line ~426)
    - frontend/apps/web/src/features/e2-identity/employees-screen.tsx (confirm useListEmployees is already wired; no MSW-specific code)
    - frontend/apps/web/src/app/router.tsx + frontend/apps/web/src/app/nav.ts (route paths for the 5 screens)
  </read_first>
  <action>
    1. reset-db.ts: add to TRUNCATE_TABLES (FK-safe, most-dependent first), placed after the
       Phase-3 entries: 'change_requests', 'agreement_attachments', 'employment_agreements',
       'employees'. (CASCADE is already used; keep order sane so a non-cascade run would also work.)

    2. db.ts: add helpers (each mirrors getCompanyStatus — open pg.Client, one query, return scalar):
       - getEmployeeStatus(id): SELECT status FROM employees WHERE id=$1.
       - getEmployeePhone(id): SELECT phone FROM employees WHERE id=$1.
       - getEmployeeIdByNIK(nik): SELECT id FROM employees WHERE nik=$1.
       - getAgreementStatus(id): SELECT status FROM employment_agreements WHERE id=$1.
       - countAttachmentsForAgreement(agreementId): SELECT count(*) FROM agreement_attachments WHERE agreement_id=$1.
       - getChangeRequestStatus(id): SELECT status FROM change_requests WHERE id=$1.

    3. Create frontend/e2e/fixtures/sample.pdf — a real minimal valid PDF (a few hundred bytes;
       a single-page "%PDF-1.4 ... %%EOF" document). This is the upload fixture used by setInputFiles.

    4. FE upload wiring (agreement-detail-screen.tsx): the screen already imports
       useUploadAgreementAttachment. Finish the file-input stub so selecting a file calls the
       mutation with a real File via FormData (field name `file`, optional `caption`/`category`).
       Give the <input type="file"> a stable affordance (data-testid="agreement-attachment-input"
       or an aria-label) so Playwright setInputFiles can target it, and render the returned file
       name in the attachments list on success. Change nothing else.

    5. Confirm MSW is off in E2E (VITE_ENABLE_MSW=false set by playwright.config webServer per the
       harness spec). employees-screen / change-requests-screen already call their hooks
       (useListEmployees, useListPendingChangeRequests, useApproveChangeRequest) — leave them; only
       add testid/aria-label affordances where a Playwright locator would otherwise be ambiguous.
  </action>
  <verify>
    <automated>cd frontend && grep -q "employment_agreements" e2e/lib/reset-db.ts && grep -q "getEmployeeStatus" e2e/lib/db.ts && test -f e2e/fixtures/sample.pdf && grep -q "type=\"file\"\|type='file'" apps/web/src/features/e2-identity/agreement-detail-screen.tsx && echo OK</automated>
  </verify>
  <done>
    reset-db.ts TRUNCATE_TABLES includes all 4 people tables; db.ts exports the 6 new helpers;
    e2e/fixtures/sample.pdf exists and is a valid PDF (starts with %PDF); agreement-detail-screen
    has a file input + calls useUploadAgreementAttachment with a real File.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: employees.spec.ts (CRUD + deactivate/reactivate + duplicate-NIK + RBAC)</name>
  <files>frontend/e2e/tests/e2/employees.spec.ts</files>
  <read_first>
    - frontend/e2e/tests/e2/client-companies.spec.ts (whole file — structure/locators/timeouts to mirror)
    - frontend/apps/web/src/features/e2-identity/employees-screen.tsx + employee-form.tsx + employee-detail-screen.tsx + employee-overlays.tsx (selectors, headings, form fields, Bahasa copy)
    - docs/epics/E2-identity/prds/employee-profile.md §7 + §8
  </read_first>
  <action>
    Create frontend/e2e/tests/e2/employees.spec.ts. Header comment lists coverage mapped to
    Gherkin scenarios + C-#. `test.beforeEach(resetDb)`. One test() per scenario:
      - EP-list: hrAdmin opens employees screen → seeded Budi Santoso + Sari Hadi visible.
      - EP-create-data-only: create an employee (full_name + NIK + join_at, no login) → toast +
        row appears; assert getEmployeeIdByNIK(newNik) is non-null and getEmployeeStatus === 'active'.
      - EP-create-with-login: create with provision_login + login_email → succeeds (login provisioning
        is stubbed BE-side; assert the employee row is created — do not assert a User row this milestone;
        note the stub in a comment).
      - EP-reject-duplicate-NIK: create with Budi's seeded NIK → 409 surfaced in UI (inline/toast error);
        assert no new row.
      - EP-detail: open Budi's detail → Profil tab shows phone/bank values.
      - EP-update: edit an employee non-statutory field (e.g. phone via the HR edit form) → toast +
        getEmployeePhone reflects the new value.
      - EP-deactivate: deactivate an employee → status badge Nonaktif + getEmployeeStatus === 'inactive'.
      - EP-reactivate: reactivate → status Aktif + getEmployeeStatus === 'active'.
      - RB: agent persona denied the employees screen (RBAC negative).
    Use div.border-b.filter() for rows; 30s timeout on first visible assertion.
  </action>
  <verify>
    <automated>cd frontend && pnpm --filter @swp/e2e exec playwright test --list tests/e2/employees.spec.ts 2>&1 | tail -20; echo "list-exit=$?"</automated>
  </verify>
  <done>
    `playwright test --list tests/e2/employees.spec.ts` lists ≥ 8 tests covering create
    (data-only + with-login), duplicate-NIK, update, deactivate, reactivate, and RBAC.
    (Full green run is gated by Task 4.)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: employment-agreements.spec.ts (+ real upload + PKWT 422) and change-requests.spec.ts</name>
  <files>frontend/e2e/tests/e2/employment-agreements.spec.ts, frontend/e2e/tests/e2/change-requests.spec.ts</files>
  <read_first>
    - frontend/e2e/tests/e2/client-companies.spec.ts (structure)
    - frontend/apps/web/src/features/e2-identity/agreements-screen.tsx + agreement-form.tsx + agreement-detail-screen.tsx (selectors, copy, renew/close overlays, upload input)
    - frontend/apps/web/src/features/e2-identity/change-requests-screen.tsx + change-request-overlays.tsx (queue list, approve inline, reject modal)
    - docs/epics/E2-identity/prds/employment-agreement.md §7 + §8
  </read_first>
  <action>
    Create frontend/e2e/tests/e2/employment-agreements.spec.ts (beforeEach resetDb). Tests:
      - AG-list: hrAdmin opens agreements screen → seeded SWP-AG-7001 (Budi PKWT) visible.
      - AG-create-PKWT: create a PKWT for an employee with no active agreement (use SWP-EMP-1108
        Rudi or a freshly-created employee) with start+end+comp → toast; getAgreementStatus(new)==='active'.
      - AG-create-PKWTT: create a PKWTT (start, no end) → succeeds, no end_date shown.
      - AG-reject-PKWT-no-end: attempt PKWT without end_date → 400 surfaced (form/inline error); no row.
      - AG-PKWT-exceeds-max: attempt PKWT with end_date 6 years after start → 422
        PKWT_PERIOD_EXCEEDS_MAX surfaced as a field error on end_date (assert the Bahasa message
        / field highlight).
      - AG-only-one-active: attempt a 2nd active agreement for Budi (already has SWP-AG-7001) → 409
        ACTIVE_AGREEMENT_EXISTS surfaced (toast/inline).
      - AG-renew: renew SWP-AG-7001 → new successor created; old becomes SUPERSEDED
        (getAgreementStatus('SWP-AG-7001')==='superseded').
      - AG-close: close an active agreement (reason + effective_date) → status CLOSED.
      - AG-upload-attachment (REAL FILE): on Budi's agreement detail, setInputFiles the
        e2e/fixtures/sample.pdf into the attachment input → success; the new attachment name renders;
        countAttachmentsForAgreement('SWP-AG-7001') increases by 1.
      - AG-download-requires-auth: request GET /api/v1/files/SWP-FILE-9001 WITHOUT auth (a bare
        request context / no storageState) → 401; WITH the hrAdmin session → 200 + application/pdf
        (use page.request or an api helper that controls credentials).

    Create frontend/e2e/tests/e2/change-requests.spec.ts (beforeEach resetDb). Tests:
      - CR-queue: hrAdmin opens change-requests queue → seeded SWP-CHG-2117 + 2118 (Budi) visible.
      - CR-detail-diff: open SWP-CHG-2117 detail → diff shows old phone "+62-812-3344-5566" →
        new "+62-812-9988-7766" and old/new bank account.
      - CR-approve: approve SWP-CHG-2117 → toast; getChangeRequestStatus('SWP-CHG-2117')==='approved';
        getEmployeePhone('SWP-EMP-2891') === '+62-812-9988-7766' (change applied to employee).
      - CR-reject-requires-reason: open reject modal, submit empty reason → blocked/validation error.
      - CR-reject: reject SWP-CHG-2118 with a reason → getChangeRequestStatus('SWP-CHG-2118')==='rejected';
        employee address UNCHANGED.
      - CR-already-resolved: approve SWP-CHG-2117 twice (second via UI after refetch, or assert the
        row leaves the pending queue) → conflict handled gracefully (no crash; queue no longer lists it).
      - RB: agent persona denied the change-requests queue (RBAC negative).
  </action>
  <verify>
    <automated>cd frontend && pnpm --filter @swp/e2e exec playwright test --list tests/e2/employment-agreements.spec.ts tests/e2/change-requests.spec.ts 2>&1 | tail -25; echo "list-exit=$?"</automated>
  </verify>
  <done>
    `playwright test --list` shows the agreement specs (incl. AG-upload-attachment,
    AG-PKWT-exceeds-max, AG-renew) and the change-request specs (incl. CR-approve applying
    the change, CR-reject, RBAC). grep employment-agreements.spec.ts shows `setInputFiles`
    and `sample.pdf`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: run the full people E2E suite green against the real stack</name>
  <files>frontend/e2e/tests/e2/employees.spec.ts, frontend/e2e/tests/e2/employment-agreements.spec.ts, frontend/e2e/tests/e2/change-requests.spec.ts</files>
  <read_first>
    - frontend/e2e/playwright.config.ts (webServer + globalSetup; how the stack boots)
    - frontend/e2e/README.md (how to run; .env.e2e requirements)
    - .planning/reference/e2e-harness-spec.md
  </read_first>
  <action>
    Run the three new specs headless against the real stack (Docker Postgres + Go API + Vite,
    booted by globalSetup, MSW off). Fix any failures in the specs OR the FE wiring (NOT the
    backend — if a real backend bug surfaces, fix it minimally and note it, since 04-05 should
    have caught contract issues). Iterate until green:
      `cd frontend && pnpm --filter @swp/e2e exec playwright test tests/e2/employees.spec.ts tests/e2/employment-agreements.spec.ts tests/e2/change-requests.spec.ts`
    Then run the FULL e2e suite once to confirm no regression in the Phase 1-3 specs:
      `cd frontend && pnpm e2e`.
    Common gotchas to watch (from STATE.md): rows are div.border-b; toggles are role=switch;
    conflict toast matches /konflik/i; noValidate on number-input forms; 90s first-compile timeout.
  </action>
  <verify>
    <automated>cd frontend && pnpm --filter @swp/e2e exec playwright test tests/e2/employees.spec.ts tests/e2/employment-agreements.spec.ts tests/e2/change-requests.spec.ts 2>&1 | tail -25; echo "e2e-exit=$?"</automated>
  </verify>
  <done>
    All three people specs pass headless (exit 0). The full `pnpm e2e` run is green
    (no regression in e1/e2 org/smoke specs). Report the pass count per spec in the SUMMARY.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @swp/e2e exec playwright test tests/e2/employees.spec.ts tests/e2/employment-agreements.spec.ts tests/e2/change-requests.spec.ts` exits 0.
- `pnpm e2e` (full suite) exits 0.
- The attachment upload test uses a real sample.pdf via setInputFiles and asserts the returned metadata + a download auth gate.
</verification>

<success_criteria>
The E2 people web screens drive the real BE; every Gherkin scenario (+ PKWT 422,
real attachment upload, change-request approve-applies, RBAC negatives) is a runnable
test(); `pnpm e2e` green headless.
</success_criteria>

<output>
After completion, create `.planning/phases/04-e2-people/04-06-SUMMARY.md` documenting:
the pass counts per spec, the FE upload-wiring change, any backend bug found+fixed during
E2E, and confirmation that Phase 4 (PPL-01/02/03) is fully green against the real stack.
</output>
