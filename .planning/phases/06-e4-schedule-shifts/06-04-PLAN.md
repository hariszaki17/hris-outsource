---
phase: 06-e4-schedule-shifts
plan: 04
type: execute
wave: 4
depends_on: ["06-02", "06-03"]
files_modified:
  - frontend/e2e/lib/e4-helpers.ts
  - frontend/e2e/tests/e4/shift-masters.spec.ts
  - frontend/e2e/tests/e4/schedule-grid.spec.ts
  - frontend/e2e/tests/e4/conflict-negatives.spec.ts
  - frontend/e2e/tests/e4/bulk-apply.spec.ts
  - frontend/e2e/tests/e4/leader-scope.spec.ts
  - frontend/apps/web/src/features/e4-scheduling/schedule-grid-screen.tsx
  - frontend/apps/web/src/features/e4-scheduling/schedule-overlays.tsx
  - frontend/apps/web/src/features/e4-scheduling/shift-masters-screen.tsx
autonomous: true
requirements: [SCH-01, SCH-02, SCH-03]
user_setup: []

must_haves:
  truths:
    - "the real e4-scheduling screens drive the real Go API (MSW off): shift-master CRUD, schedule grid cell CRUD, bulk-apply"
    - "conflict negatives are exercised against REAL 409/422/403 responses: DOUBLE_SHIFT, OUTSIDE_PLACEMENT_PERIOD, SHIFT_OVER_LEAVE (honestly seeded), leader-scope OUT_OF_SCOPE"
    - "bulk-apply E2E asserts partial success (some dates applied, >=1 skipped with a conflict code)"
    - "one test() per Gherkin scenario / C-# is individually runnable in the Playwright UI"
    - "pnpm e2e is green headless with NO e1/e2/e3 regressions"
  artifacts:
    - path: "frontend/e2e/tests/e4/schedule-grid.spec.ts"
      provides: "happy-path grid CRUD E2E driving the real cell popover"
      contains: "test("
    - path: "frontend/e2e/tests/e4/conflict-negatives.spec.ts"
      provides: "DOUBLE_SHIFT / OUTSIDE_PLACEMENT_PERIOD / SHIFT_OVER_LEAVE negatives via real API"
      contains: "SHIFT_OVER_LEAVE"
    - path: "frontend/e2e/tests/e4/bulk-apply.spec.ts"
      provides: "bulk-apply partial-success E2E"
      contains: "bulk"
    - path: "frontend/e2e/tests/e4/leader-scope.spec.ts"
      provides: "shift_leader cross-company → 403 OUT_OF_SCOPE"
      contains: "OUT_OF_SCOPE"
    - path: "frontend/e2e/lib/e4-helpers.ts"
      provides: "E4 DOM/selectors + API helpers anchored on the real components"
      contains: "export"
  key_links:
    - from: "e4 specs"
      to: "real Go API (MSW off)"
      via: "loginAs persona + page interactions + apiAs(real-409 negatives)"
      pattern: "apiAs|loginAs"
    - from: "schedule-overlays popover"
      to: "POST /schedule + /schedule:check real responses"
      via: "cell click → shift pick → publish/conflict toast"
      pattern: "ShiftPickerPopover"
---

<objective>
Wire the E4 screens off MSW to the real BE and prove them with exhaustive full-stack
Playwright E2E under NEW `frontend/e2e/tests/e4/`. One test() per Gherkin scenario / C-# from
the E4 PRDs: shift-master CRUD, schedule grid cell CRUD, conflict negatives (DOUBLE_SHIFT,
OUTSIDE_PLACEMENT_PERIOD, SHIFT_OVER_LEAVE), bulk-apply partial success, leader-scope 403.
Selectors derived from the REAL components (schedule-grid-screen.tsx, schedule-overlays.tsx,
shift-masters-screen.tsx). Fix any FE response-shape mismatch found against the real BE
(e.g. the Phase-5 `error.details` precedent, or the failed-item `error.code` shape).

Purpose: close the phase with green real-stack proof of the 4 ROADMAP success criteria.
Output: an e4-helpers lib + 5 spec files; `pnpm e2e` green, no e1/e2/e3 regressions.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-e4-schedule-shifts/06-CONTEXT.md
@.planning/reference/e2e-harness-spec.md
@docs/epics/E4-shift-scheduling/prds/shift-master-catalog.md
@docs/epics/E4-shift-scheduling/prds/daily-schedule-assignment.md
@docs/epics/E4-shift-scheduling/prds/schedule-changes-swaps.md
@frontend/apps/web/src/features/e4-scheduling/schedule-grid-screen.tsx
@frontend/apps/web/src/features/e4-scheduling/schedule-overlays.tsx
@frontend/apps/web/src/features/e4-scheduling/shift-masters-screen.tsx

<interfaces>
<!-- Real component DOM (anchor ALL selectors here, NOT assumptions): -->
<!-- shift-masters-screen.tsx (route /shifts): -->
<!--   "Tambah" add Button; SearchField; FilterSelect status (option ACTIVE/INACTIVE); -->
<!--   DataTable rows; RowActionsMenu button aria-label t('rowActions') aria-haspopup="menu"; -->
<!--   menuitems: edit / deactivate / reactivate; ShiftMasterModal inputs ids: -->
<!--   #sm-name #sm-start-time(type=time) #sm-end-time #sm-break-start #sm-break-end; -->
<!--   service line via ServiceLinePicker (Combobox); is_active Toggle (role=switch); -->
<!--   save Button t('modal.save'); ConfirmDialog deactivate/reactivate. -->
<!-- schedule-grid-screen.tsx (route /schedule, validateSearch {company_id?,week?}): -->
<!--   ClientCompanyPicker (Combobox) sets company_id; week nav buttons aria-label t('weekNav.prev/next'); -->
<!--   "Terapkan ke rentang" secondary Button (t('action.bulkApply')); -->
<!--   each cell is a <button> with aria-label t('cell.ariaLabel',{agent,date}); click → ShiftPickerPopover. -->
<!-- schedule-overlays.tsx: -->
<!--   ShiftPickerPopover: search input; ShiftRow buttons (shift.name); -->
<!--     quick actions: Mark day off / Clear cell; success toast t('toast.published'); -->
<!--     conflict → error toast title t('conflict.blockedTitle') + conflictMessage(code). -->
<!--     NOTE: the popover pre-flights POST /schedule:check then writes POST /schedule. -->
<!--   BulkApplyModal: shift search+select; #start/#end date inputs (type=date); weekday toggles; -->
<!--     override checkbox; "Preview" then "Apply"; preview shows succeeded/failed counts. -->

<!-- e3-helpers.ts (REUSE — same lib patterns): -->
From frontend/e2e/lib/e3-helpers.ts:
```ts
export const API_BASE = 'http://localhost:8081/api/v1';
export async function apiAs(page, method, path, body?): Promise<{status,body}>  // adds Idempotency-Key + Bearer
export function errorCode(body): string|undefined        // body.error.code
export function errorDetails(body): Record<string,unknown>|undefined
export async function pickCombobox(page, triggerScope, optionText, search?)
export function comboFieldById(page, htmlFor): Locator
```
From frontend/e2e/lib/fixtures.ts: `test`, `loginAs(page, persona)`.
From frontend/e2e/lib/personas.ts: PERSONAS.hrAdmin (sari.hadi), PERSONAS.shiftLeader (rudi.wijaya @ Plaza Senayan / CMP-0021).
From frontend/e2e/lib/reset-db.ts: `resetDb()` in beforeEach.
</interfaces>

<!-- SEEDED FIXTURES (from 06-02 SUMMARY — re-read it for exact dates): -->
<!--   Shift masters: SWP-SHF-001 "Pagi" (all lines), SWP-SHF-002 "Malam" (SVC-003, cross-midnight). -->
<!--   Placements @ CMP-0021 (Plaza Senayan): SWP-PL-5001 EMP-1108 (Rudi), 5003 EMP-1042 (Sari), 5004 EMP-3001 (Dewi). -->
<!--   Schedule entries seeded for EMP-1108 + EMP-3001 in the current week. -->
<!--   approved_leave_days: EMP-3001 on a known in-week date (SWP-LR-44210, ANNUAL) → SHIFT_OVER_LEAVE target. -->
<!--   Leader-scope-403 target: EMP-2891 (Budi) placed at CMP-0022 (NOT Rudi's company). -->
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: FE wiring (MSW off) + e4-helpers + shift-masters + schedule-grid happy-path specs</name>
  <files>frontend/apps/web/src/features/e4-scheduling/schedule-overlays.tsx, frontend/apps/web/src/features/e4-scheduling/schedule-grid-screen.tsx, frontend/apps/web/src/features/e4-scheduling/shift-masters-screen.tsx, frontend/e2e/lib/e4-helpers.ts, frontend/e2e/tests/e4/shift-masters.spec.ts, frontend/e2e/tests/e4/schedule-grid.spec.ts</files>
  <read_first>
    - frontend/apps/web/src/features/e4-scheduling/shift-masters-screen.tsx + schedule-grid-screen.tsx + schedule-overlays.tsx (confirm hooks already wired to @swp/api-client/e4; identify any response-shape assumption that diverges from the real BE — esp. schedule-overlays reads `checkResult.data.failed[].error.code` and `first?.conflict_details` (line 200): the real BE nests details under `error.details`, NOT `conflict_details`. FIX the popover to read `first?.error?.details` so the DOUBLE_SHIFT/over-leave block message renders — mirror the Phase-5 `error.details` fix.)
    - frontend/e2e/lib/e3-helpers.ts (apiAs/errorCode/errorDetails/pickCombobox/comboFieldById to reuse or re-export)
    - frontend/e2e/tests/e3/shift-leader-assignment.spec.ts + company-roster.spec.ts (test() structure, loginAs, resetDb, isoDaysFromNow helper, real-409 assertion via apiAs)
    - docs/epics/E4-shift-scheduling/prds/shift-master-catalog.md (SM-1..5 Gherkin) + daily-schedule-assignment.md (SA-1..9 Gherkin, C-1..5)
  </read_first>
  <action>
    **FE wiring fixes** (minimal — screens are already built against the e4 hooks):
    1. Confirm `VITE_ENABLE_MSW=false` path works for /shifts + /schedule (the harness already sets it). No mock toggle needed beyond the harness env.
    2. In `schedule-overlays.tsx` ShiftPickerPopover.handleAssignShift: the failed-item details are read as `first?.conflict_details` (undefined against the real BE). Change to read `first?.error?.details` (the real envelope nests details under `error`). Apply the same correction anywhere the popover/bulk modal reads conflict details. Keep behavior identical otherwise (DOUBLE_SHIFT still routes to the replace flow).
    3. Verify the schedule grid's `entries` extraction handles the real `{data:[...], warnings:[...]}` envelope (it already has a flat-array fallback; confirm the `{data}` path is taken).
    Make NO unrelated changes; do not touch styling/i18n keys.

    **e4-helpers.ts** — create `frontend/e2e/lib/e4-helpers.ts`. Re-export `apiAs, errorCode, errorDetails, API_BASE` from e3-helpers (or import). Add E4-specific helpers anchored on the real DOM:
    - `selectCompany(page, name)` — drive ClientCompanyPicker (Combobox) via pickCombobox to "Plaza Senayan".
    - `cellButton(page, agentName, dateIso)` — locate the grid cell button by its aria-label (t('cell.ariaLabel',{agent,date})); since copy is Bahasa, match on a substring/regex containing the agent name AND the date, or compute the rendered aria-label from the i18n value (prefer matching `[aria-label*="<agentName>"][aria-label*="<dateIso>"]`).
    - `assignShiftViaPopover(page, shiftName)` — inside the open popover, click the ShiftRow button with text shiftName.
    - `expectPublishedToast(page)` / `expectConflictToast(page, codeMessageRegex)`.
    - `mondayOfCurrentWeekIso()` / `isoDaysFromNow(n)` (reuse e3 pattern; account for Asia/Jakarta — match the seed's in-week dates).

    **shift-masters.spec.ts** (route /shifts, login hrAdmin) — one test() each:
    - `SM-list`: list renders seeded Pagi/Malam; Malam shows cross-midnight indicator.
    - `SM-create`: Tambah → fill #sm-name/#sm-start-time/#sm-end-time → save → row appears (success toast).
    - `SM-create-cross-midnight`: end<=start → cross-midnight note shows; after save row shows cross-midnight chip.
    - `SM-duplicate-name`: create with name "Pagi" → real 409 DUPLICATE_NAME → field/toast error (assert via field error or toast; cross-check real code via apiAs POST /shift-masters duplicate → errorCode==='DUPLICATE_NAME').
    - `SM-break-outside-window`: break outside [start,end] → 422 BREAK_OUTSIDE_WINDOW (assert via apiAs + the form field error).
    - `SM-deactivate-reactivate`: row menu → deactivate → status INACTIVE; reactivate → ACTIVE.
    - `SM-filter-status`: FilterSelect ACTIVE/INACTIVE narrows the table.
    - `SM-rbac-leader-readonly`: login shiftLeader → can SEE /shifts list but apiAs POST /shift-masters → 403 (writes are super/hr).

    **schedule-grid.spec.ts** (route /schedule, login shiftLeader rudi @ CMP-0021) — one test() each (SA-1/2/4/6/7, CH-1/2):
    - `SA-1-assign-autopublish`: select Plaza Senayan → click an EMPTY cell for a seeded agent on an in-window date → popover → pick "Pagi" → published toast → cell now shows the chip (grid refetched).
    - `SA-2-replace`: click a cell that already has a shift → pick a different shift → replace path → cell updates (status MODIFIED; assert chip changed).
    - `SA-4-picker-filtered-by-service-line`: open popover for an SVC-003 agent → "Malam" (SVC-003) appears; untagged "Pagi" also appears (SM-3).
    - `SA-7-mark-day-off`: popover → "Mark day off" → cell shows OFF/Libur.
    - `CH-1-clear-cell`: popover on an existing cell → "Clear cell" → cell empties (real DELETE 204).
    - `CH-2-edit-swap`: change an existing entry's shift → status MODIFIED.
    - `grid-empty`: select a company with no placements/agents → empty state.
  </action>
  <verify>
    <automated>cd frontend && grep -q "first?.error?.details" apps/web/src/features/e4-scheduling/schedule-overlays.tsx && test -f e2e/lib/e4-helpers.ts && test -f e2e/tests/e4/shift-masters.spec.ts && test -f e2e/tests/e4/schedule-grid.spec.ts && pnpm --filter @swp/e2e exec playwright test tests/e4/shift-masters.spec.ts tests/e4/schedule-grid.spec.ts --reporter=line && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - schedule-overlays.tsx reads conflict details via `first?.error?.details` (not `conflict_details`) so block messages render against the real BE
    - e4-helpers.ts exists with selectCompany / cellButton / assignShiftViaPopover anchored on the REAL component aria-labels + ids (#sm-name etc.)
    - shift-masters.spec.ts covers SM list/create/cross-midnight/duplicate-name/break-window/deactivate-reactivate/filter/rbac (one test() each)
    - schedule-grid.spec.ts covers SA-1/SA-2/SA-4/SA-7/CH-1/CH-2 + empty (one test() each), driving the REAL cell popover (not invented selectors)
    - both spec files pass headless against the real stack
  </acceptance_criteria>
  <done>schedule-overlays reads conflict details via error.details (not conflict_details); e4-helpers provides selectors anchored on the real component DOM; shift-masters.spec + schedule-grid.spec cover SM-* and SA-1/2/4/7/CH-1/2 with one test() each and pass headless against the real stack.</done>
</task>

<task type="auto">
  <name>Task 2: Conflict-negative + bulk-apply + leader-scope specs; full green run</name>
  <files>frontend/e2e/tests/e4/conflict-negatives.spec.ts, frontend/e2e/tests/e4/bulk-apply.spec.ts, frontend/e2e/tests/e4/leader-scope.spec.ts</files>
  <read_first>
    - .planning/phases/06-e4-schedule-shifts/06-02-SUMMARY.md (EXACT seeded dates: the EMP-3001 approved-leave date, the in-window schedule dates, the placement windows) — anchor the negative dates on these
    - frontend/apps/web/src/features/e4-scheduling/schedule-overlays.tsx BulkApplyModal (preview → apply flow; preview shows succeeded/failed counts; apply persists)
    - docs/epics/E4-shift-scheduling/prds/daily-schedule-assignment.md C-1 (bulk apply-to-range) + C-2/C-3 (placement period) + SA-3 (leader scope)
    - frontend/e2e/tests/e3/replacement-transfer.spec.ts (apiAs real-409 negative-assertion pattern to mirror for the conflict envelopes)
  </read_first>
  <action>
    **conflict-negatives.spec.ts** — REAL 409/422 via apiAs (login shiftLeader rudi for in-scope cases; the BE returns the real envelope). One test() per code:
    - `CONF-double-shift`: POST /schedule for EMP-1108 on a date that already has a seeded entry, force_replace=false → apiAs → status 409, errorCode==='DOUBLE_SHIFT', errorDetails.existing_entry_id present. Also assert via the UI: clicking the occupied cell and picking a shift triggers the replace flow (DOUBLE_SHIFT routed to force_replace per the popover).
    - `CONF-outside-placement-period`: POST /schedule for a seeded agent on a date BEFORE the placement start (or after end) → 422, errorCode==='OUTSIDE_PLACEMENT_PERIOD', errorDetails.placement_id.
    - `CONF-shift-over-leave`: POST /schedule for EMP-3001 on the seeded approved-leave date (SWP-LR-44210) → 409, errorCode==='SHIFT_OVER_LEAVE', errorDetails.leave_request_id==='SWP-LR-44210'. (This is the honest over-leave proof — drives the E4-owned approved_leave_days seed.) Also assert the UI popover renders the over-leave block toast/message for that cell.
    - `CONF-shift-deactivated`: deactivate SWP-SHF master via apiAs, then POST /schedule with it → 422 SHIFT_DEACTIVATED.
    - `CONF-shift-not-for-service-line`: POST /schedule assigning the SVC-003-tagged "Malam" to an agent whose placement is SVC-002 (EMP-1042 / SWP-PL-5003) → 422 SHIFT_NOT_FOR_SERVICE_LINE.

    **bulk-apply.spec.ts** (login hrAdmin or shiftLeader) — partial success:
    - `BULK-partial`: open "Terapkan ke rentang" → pick "Pagi" → start/end spanning the seeded approved-leave date for EMP-3001 (so >=1 cell fails SHIFT_OVER_LEAVE) across EMP-3001 (+ another in-scope agent) → Preview shows succeeded>0 AND failed>0 → Apply → success toast; assert via apiAs GET /schedule that the applied dates exist and the leave date was skipped. Also assert directly: apiAs POST /schedule:bulk-apply with the same range → status 200, body.succeeded non-empty, body.failed[0].error.code==='SHIFT_OVER_LEAVE'.
    - `BULK-all-failed-422`: bulk range where every cell conflicts (e.g. all dates outside placement) → apiAs POST /schedule:bulk-apply → status 422, body.failed non-empty, body.succeeded empty.
    - `BULK-weekdays-mask`: weekdays_mask Mon-Fri over a 7-day range → only weekday cells attempted (assert succeeded+failed count).
    - `CHECK-dry-run`: apiAs POST /schedule:check (single, conflicting) → 200, failed[0].error.code set, and a follow-up GET /schedule shows NO new entry (no side effect).

    **leader-scope.spec.ts** (login shiftLeader rudi @ CMP-0021):
    - `SCOPE-403-create`: apiAs POST /schedule for EMP-2891 (Budi, placed at CMP-0022, outside Rudi's scope) → 403, errorCode==='OUT_OF_SCOPE'.
    - `SCOPE-403-list`: apiAs GET /schedule?company_id=SWP-CMP-0022 as rudi → 403 OUT_OF_SCOPE (leader cannot read another company).
    - `SCOPE-hr-global`: login hrAdmin → GET /schedule?company_id=SWP-CMP-0022 → 200 (global scope).

    Each test is a single `test()` named with its scenario/C-# so it is individually runnable in `--ui`. Use `resetDb()` in `beforeEach`. Use `isoDaysFromNow`/seed-anchored dates; respect the Asia/Jakarta-vs-UTC boundary (Phase-5 05-03 note) — never pick today's exact boundary date.

    Finally run the FULL suite: `pnpm e2e` — assert green with NO e1/e2/e3 regressions. Auto-fix any FE shape mismatch surfaced (document each fix in the SUMMARY, Phase-5 style).
  </action>
  <verify>
    <automated>cd frontend && test -f e2e/tests/e4/conflict-negatives.spec.ts && test -f e2e/tests/e4/bulk-apply.spec.ts && test -f e2e/tests/e4/leader-scope.spec.ts && grep -q "SHIFT_OVER_LEAVE" e2e/tests/e4/conflict-negatives.spec.ts && grep -q "OUT_OF_SCOPE" e2e/tests/e4/leader-scope.spec.ts && grep -q "SHIFT_OVER_LEAVE\|failed" e2e/tests/e4/bulk-apply.spec.ts && pnpm e2e --reporter=line && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - conflict-negatives.spec.ts asserts REAL responses for DOUBLE_SHIFT (409+details), OUTSIDE_PLACEMENT_PERIOD (422+details), SHIFT_OVER_LEAVE (409, details.leave_request_id==='SWP-LR-44210'), SHIFT_DEACTIVATED (422), SHIFT_NOT_FOR_SERVICE_LINE (422)
    - bulk-apply.spec.ts asserts partial success (Preview succeeded>0 AND failed>0; bulk-apply 200 with failed[].error.code), all-failed→422, weekdays_mask, and :check no-side-effect
    - leader-scope.spec.ts asserts OUT_OF_SCOPE 403 on cross-company create AND list, plus hr-admin global 200
    - every scenario is its own test(); SHIFT_OVER_LEAVE is honestly driven by the seeded approved_leave_days row (not mocked)
    - `pnpm e2e` is green headless; no e1/e2/e3 regressions
  </acceptance_criteria>
  <done>conflict-negatives asserts real DOUBLE_SHIFT/OUTSIDE_PLACEMENT_PERIOD/SHIFT_OVER_LEAVE(+SWP-LR-44210)/SHIFT_DEACTIVATED/SHIFT_NOT_FOR_SERVICE_LINE; bulk-apply asserts partial success + all-failed 422 + weekdays_mask + :check no-side-effect; leader-scope asserts OUT_OF_SCOPE 403 (create+list) + hr global 200; pnpm e2e is green with no e1/e2/e3 regressions.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm e2e` green headless (all e1/e2/e3/e4 specs); no regressions.
- E4 specs cover: shift-master CRUD, grid CRUD, all reachable conflict codes, bulk-apply partial success, leader scope 403.
- SHIFT_OVER_LEAVE honestly exercised via the E4-owned approved_leave_days seed (SWP-LR-44210).
- Selectors anchored on the real e4-scheduling component DOM (aria-labels, #sm-* ids, ShiftRow text).
</verification>

<success_criteria>
- The 4 ROADMAP Phase-6 success criteria are TRUE: (1) HR/leader manage shift masters + schedule entries; (2) conflict check returns correct codes + bulk-apply partial success; (3) schedule lists cursor-paginated + leader-scoped; (4) exhaustive E4 E2E green.
- Over-leave delivered honestly (or, if 06-02 deferred it, surfaced as a deferral in the SUMMARY — NOT silently green).
</success_criteria>

<output>
After completion, create `.planning/phases/06-e4-schedule-shifts/06-04-SUMMARY.md` documenting:
- the spec→scenario coverage map, any FE response-shape fixes made (the conflict_details→error.details correction + any others),
- the SHIFT_OVER_LEAVE delivery status (honest via seed, with the E6 production-source note),
- final `pnpm e2e` result + regression status,
- any divergence + reason.
</output>
