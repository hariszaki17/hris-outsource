---
phase: 06-e4-schedule-shifts
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/internal/repository/scheduling/shift_master_repo.go
  - backend/internal/repository/scheduling/schedule_repo.go
  - backend/internal/repository/scheduling/mapping.go
  - backend/internal/service/scheduling/shift_master_service.go
  - backend/internal/service/scheduling/schedule_service.go
  - backend/internal/service/scheduling/conflict_engine.go
  - backend/internal/handler/scheduling/shift_master_dto.go
  - backend/internal/handler/scheduling/shift_master_handler.go
  - backend/internal/handler/scheduling/schedule_dto.go
  - backend/internal/handler/scheduling/schedule_handler.go
  - backend/internal/server/server.go
  - backend/cmd/api/main.go
  - backend/cmd/seed/seed.go
autonomous: true
requirements: [SCH-01, SCH-02, SCH-03]
user_setup: []

must_haves:
  truths:
    - "HR/leader can create/update/deactivate/reactivate shift masters and CRUD schedule entries against the real BE"
    - "the conflict engine returns DOUBLE_SHIFT / OUTSIDE_PLACEMENT_PERIOD / SHIFT_OVER_LEAVE / OUT_OF_SCOPE / SHIFT_NOT_FOR_SERVICE_LINE / SHIFT_DEACTIVATED with the exact codes + HTTP statuses from the openapi"
    - "POST /schedule:check is a side-effect-free dry-run returning the same succeeded/failed envelope as bulk-apply"
    - "POST /schedule:bulk-apply reports per-date partial success (200 if >=1 succeeded, 422 if all failed); one failing date does not roll back successes"
    - "shift_leader is auto-scoped to their own company (read + write); cross-company → 403 OUT_OF_SCOPE"
    - "SHIFT_OVER_LEAVE is honestly delivered: engine reads approved_leave_days + seed plants one approved-leave day so it is exercisable now"
    - "every write produces an audit row; auto-publish notify is a TODO(Phase-11) stub"
  artifacts:
    - path: "backend/internal/service/scheduling/conflict_engine.go"
      provides: "ordered 6-check conflict evaluator returning a typed conflict result (code + details) reused by check/bulk-apply/create/update"
      contains: "DOUBLE_SHIFT"
    - path: "backend/internal/handler/scheduling/schedule_handler.go"
      provides: "GET/POST/PATCH/DELETE /schedule + :check + :bulk-apply handlers"
      contains: "BulkApplySchedule"
    - path: "backend/internal/handler/scheduling/shift_master_handler.go"
      provides: "shift-master CRUD + deactivate/reactivate handlers"
      contains: "DeactivateShiftMaster"
    - path: "backend/internal/server/server.go"
      provides: "scheduling routes mounted under RequireRole groups"
      contains: "/schedule:bulk-apply"
    - path: "backend/cmd/seed/seed.go"
      provides: "seed shift masters + schedule entries + one approved-leave day"
      contains: "approved_leave_days"
  key_links:
    - from: "schedule_handler write path"
      to: "conflict_engine.Evaluate"
      via: "every POST/PATCH runs all checks before persist"
      pattern: "conflict|Evaluate"
    - from: "schedule_service over-leave check"
      to: "approved_leave_days"
      via: "FindApprovedLeaveForAgentDate"
      pattern: "FindApprovedLeaveForAgentDate"
    - from: "leader writes/reads"
      to: "rbac.GuardCompany"
      via: "scope guard on resolved placement company"
      pattern: "GuardCompany"
---

<objective>
Implement the E4 service + handler layer: shift-master CRUD + deactivate/reactivate, schedule
CRUD, the conflict engine (all six contract codes), POST /schedule:check (dry-run), and
POST /schedule:bulk-apply (per-date partial success). Enforce INV-1/2/3, leader scope via
GuardCompany, audit every write, stub auto-publish notify. Mount routes, wire main.go, extend
the seed. Match docs/api/E4-shift-scheduling/openapi.yaml byte-for-shape.

Purpose: this is the heart of the phase. The conflict engine is the gnarly part and is shared
by four endpoints (create, update, check, bulk-apply) — build it ONCE as a pure evaluator.

Output: scheduling repository/service/handler packages, scheduling routes in server.go, Deps
wiring in main.go, and a seed extension. `go build ./... && go vet ./...` clean.
This plan OWNS the shared server.go / main.go / seed.go edits for the phase (sequential).
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-e4-schedule-shifts/06-CONTEXT.md
@.planning/reference/backend-build-conventions.md
@docs/api/E4-shift-scheduling/openapi.yaml
@docs/api/CONVENTIONS.md

<interfaces>
<!-- apperr helpers (use these EXACT constructors for the right HTTP status): -->
From backend/internal/platform/apperr/apperr.go:
```go
func Forbidden() *Error                                              // 403 FORBIDDEN
func OutOfScope() *Error // (line 72) → 403 OUT_OF_SCOPE
func NotFound() *Error                                              // 404
func Conflict(code string) *Error                                   // 409
func ConflictWithDetails(code string, fields map[string]string, details any) *Error // 409 + details
func Rule(code string, fields map[string]string) *Error            // 422
// For a 403 with code OUT_OF_SCOPE + details, build &apperr.Error{Code:"OUT_OF_SCOPE", Details:..., HTTPStatus:403}
```

<!-- rbac scope guard (call AFTER loading the resource's company): -->
From backend/internal/platform/rbac/rbac.go:
```go
func GuardCompany(ctx context.Context, resourceCompanyID string) error // returns OUT_OF_SCOPE err for leader outside scope; nil for hr/super
```

<!-- Phase-5 placement service is the structural analog. Mirror its: consumer-defined repo -->
<!-- interface, TxRunner.InTx for multi-write+audit, FOR UPDATE re-check + 23505 backstop, -->
<!-- Clock for tests, supersede-before-insert (here: force_replace soft-deletes old then inserts new). -->
From backend/internal/service/placement/placement_service.go:
```go
type TxRunner interface { InTx(ctx context.Context, fn func(tx pgx.Tx) error) error }
type Clock func() time.Time
```

<!-- server.go mount point — append the SCHEDULING slice AFTER "// PLACEMENT slice end (05-02)". -->
<!-- main.go (cmd/api): build Repo→Service→Handler and add d.Scheduling to server.Deps. -->
<!-- seed.go: seedPlacements already creates SWP-PL-5001..5004; add seedScheduling AFTER it. -->
</interfaces>

<!-- THE CONFLICT CONTRACT (openapi /schedule POST description + BulkApplyResult enum) — exact codes/statuses: -->
<!--   OUT_OF_SCOPE              403  (leader company != agent placement company)            INV-3 -->
<!--   OUTSIDE_PLACEMENT_PERIOD  422  (date outside active placement window)                 INV-2 -->
<!--   SHIFT_DEACTIVATED         422  (picked master is_active=false)                        SM-5 -->
<!--   SHIFT_NOT_FOR_SERVICE_LINE 422 (master.service_line_id set & != placement.svc_line)   SA-4 -->
<!--   SHIFT_OVER_LEAVE          409  (approved leave covers date)                           EPICS §8 -->
<!--   DOUBLE_SHIFT              409  (live entry exists & force_replace=false)              INV-1 -->
<!-- ORDER MATTERS: scope → placement-period → deactivated → service-line → over-leave → double-shift. -->
<!-- DOUBLE_SHIFT details: { existing_entry_id, existing_shift_name }. -->
<!-- SHIFT_OVER_LEAVE details: { leave_request_id, leave_type }. -->
<!-- OUTSIDE_PLACEMENT_PERIOD details: { placement_id, placement_start, placement_end }. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repository + conflict engine + services (shift masters, schedule, conflict evaluator)</name>
  <files>backend/internal/repository/scheduling/shift_master_repo.go, backend/internal/repository/scheduling/schedule_repo.go, backend/internal/repository/scheduling/mapping.go, backend/internal/service/scheduling/shift_master_service.go, backend/internal/service/scheduling/schedule_service.go, backend/internal/service/scheduling/conflict_engine.go</files>
  <read_first>
    - backend/internal/repository/placement/placements_repo.go + placements_mapping.go (repo-over-sqlc + row→domain mapping pattern)
    - backend/internal/service/placement/placement_service.go (INV pre-check + FOR UPDATE re-check + 23505 backstop; TxRunner.InTx wrapping write+audit; Clock; apperr codes)
    - backend/internal/platform/apperr/apperr.go (Conflict/ConflictWithDetails/Rule/OutOfScope/Forbidden constructors + HTTPStatus override)
    - backend/internal/platform/rbac/rbac.go (GuardCompany signature + how leader scope is read from ctx)
    - backend/internal/platform/audit/*.go (audit.Record signature for in-tx audit)
    - docs/api/E4-shift-scheduling/openapi.yaml lines 468-616 (POST /schedule conflict order + examples) + 1320-1415 (BulkApplyResult + ConflictDetails)
  </read_first>
  <action>
    **Repository** `backend/internal/repository/scheduling/` (mirror placement repo):
    - `shift_master_repo.go` — implements the shift-master service port over sqlc (List/Get/Create/Update/SetActive/GetForUpdate). Map name-uniqueness 23505 → return a sentinel the service turns into `DUPLICATE_NAME`.
    - `schedule_repo.go` — implements the schedule service port (ListSchedule, GetScheduleEntry, GetForUpdate, FindLiveEntryForAgentDate, FindActivePlacementForAgentDate, FindApprovedLeaveForAgentDate, GetShiftMaster (for service-line + is_active + times snapshot), Create/Update/SoftDelete). Writes take `pgx.Tx`.
    - `mapping.go` — sqlc rows → `domain.ShiftMaster` / `domain.ScheduleEntry`. `pgx.ErrNoRows → domain.ErrNotFound`.

    **Conflict engine** `backend/internal/service/scheduling/conflict_engine.go` — the SHARED pure evaluator. Define:
    ```go
    type ConflictInput struct {
        EmployeeID    string
        ShiftMasterID *string      // nil when is_day_off
        Date          time.Time
        IsDayOff      bool
        ForceReplace  bool
    }
    type ConflictResult struct {
        Code    string                 // "" = OK
        Status  int                    // HTTP status for the code
        Fields  map[string]string
        Details map[string]any
        // resolved context for a successful write:
        PlacementID, CompanyID string
        ServiceLineID *string
        StartTime, EndTime *string
        CrossMidnight bool
        ExistingEntryID *string        // set when DOUBLE_SHIFT or force_replace replace
    }
    ```
    `Evaluate(ctx, q queries, leaderScope, in ConflictInput) ConflictResult` runs the SIX checks IN ORDER (short-circuit on first failure). Each branch returns the exact code + status + details per the openapi (see the context block). Resolution data needed:
    1. Resolve active placement covering Date via `FindActivePlacementForAgentDate`. If none → `OUTSIDE_PLACEMENT_PERIOD` (422) + details {placement_id?, placement_start?, placement_end?} (when an inactive/out-of-range placement is the closest, include its window; otherwise just the code). Capture CompanyID + ServiceLineID + PlacementID.
    2. SCOPE: `rbac.GuardCompany(ctx, CompanyID)` — if it errors → `OUT_OF_SCOPE` (403) + details {leader_company_id, agent_company_id}. (Run scope FIRST per openapi "rule 1" — resolve placement company first since scope needs it, but EMIT OUT_OF_SCOPE before any 422; if no active placement AND leader is out of scope, openapi order lists OUT_OF_SCOPE as rule 1 → emit scope 403 if the agent is resolvable to a company the leader can't see; if agent has NO placement at all, OUTSIDE_PLACEMENT_PERIOD. Implement: resolve placement → if found, scope-check its company → then period/etc. If not found, OUTSIDE_PLACEMENT_PERIOD.)
    3. If not is_day_off: load shift master. If `is_active=false` → `SHIFT_DEACTIVATED` (422).
    4. If master.service_line_id != nil AND != placement.service_line_id → `SHIFT_NOT_FOR_SERVICE_LINE` (422).
    5. `FindApprovedLeaveForAgentDate` — if a row → `SHIFT_OVER_LEAVE` (409) + details {leave_request_id, leave_type}.
    6. `FindLiveEntryForAgentDate` — if a live entry exists AND !ForceReplace → `DOUBLE_SHIFT` (409) + details {existing_entry_id, existing_shift_name}. If exists AND ForceReplace → set ExistingEntryID (replace path), no conflict.
    On full pass: snapshot StartTime/EndTime/CrossMidnight from the master (day-off → all nil/false), set Code="".

    **Services** `backend/internal/service/scheduling/`:
    - `shift_master_service.go` — List/Get/Create/Update/Deactivate/Reactivate. Derive `cross_midnight = endTime <= startTime` (string HH:MM compare works lexicographically for zero-padded 24h). Validate break window inside [start,end] → `BREAK_OUTSIDE_WINDOW` (422). DUPLICATE_NAME (409) from repo sentinel. Deactivate when already inactive → `ALREADY_INACTIVE` (409); reactivate when already active → 409. Audit every write in-tx.
    - `schedule_service.go` — methods:
      - `Check(ctx, req)` → runs the engine per (employee×date) WITHOUT persisting, returns succeeded/failed arrays. Single-kind = one cell; bulk-kind = expand date range × employees × weekdays_mask.
      - `CreateEntry(ctx, req)` → InTx: lock (GetForUpdate not needed for new; rely on FindLive + the partial-unique 23505 backstop), run engine; on conflict return apperr with the engine's code/status/details; on pass, if ExistingEntryID set (force_replace) soft-delete it + set replaced_entry_id + status MODIFIED, else insert SCHEDULED; audit; notify stub. Map 23505 on schedule_entries_active_agent_date_uq → `DOUBLE_SHIFT` (race backstop).
      - `UpdateEntry(ctx, id, req)` → GetForUpdate, re-run engine (status→MODIFIED), audit.
      - `DeleteEntry(ctx, id)` → soft-delete (hard-remove semantics per spec DELETE → 204), audit prior state. Leader past-date clear restriction (C-5): leader cannot delete an entry whose work_date < today(Asia/Jakarta) → `OUT_OF_SCOPE`/403 (or FORBIDDEN per openapi 403 "leader attempting to clear a past date").
      - `BulkApply(ctx, req)` → for each (employee×date in weekdays_mask): run engine; on pass persist in its OWN tx (atomic per-cell — a failing cell does NOT roll back others) + audit + notify stub, append to succeeded; on conflict append to failed[] with {employee_id, date, error:{code,message,details}}. Return BulkApplyResult. (Handler decides 200 vs 422.)

    Notify stub: a `// TODO(Phase-11): dispatch "Schedule published" notification` comment at each successful write, mirroring Phase-4/5.
  </action>
  <verify>
    <automated>cd backend && grep -q "DOUBLE_SHIFT" internal/service/scheduling/conflict_engine.go && grep -q "OUTSIDE_PLACEMENT_PERIOD" internal/service/scheduling/conflict_engine.go && grep -q "SHIFT_OVER_LEAVE" internal/service/scheduling/conflict_engine.go && grep -q "SHIFT_NOT_FOR_SERVICE_LINE" internal/service/scheduling/conflict_engine.go && grep -q "SHIFT_DEACTIVATED" internal/service/scheduling/conflict_engine.go && grep -q "OUT_OF_SCOPE\|GuardCompany" internal/service/scheduling/conflict_engine.go && grep -q "FindApprovedLeaveForAgentDate" internal/service/scheduling/schedule_service.go && grep -q "BREAK_OUTSIDE_WINDOW" internal/service/scheduling/shift_master_service.go && grep -q "TODO(Phase-11)" internal/service/scheduling/schedule_service.go && go build ./internal/service/scheduling/... ./internal/repository/scheduling/... && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - conflict_engine.go references ALL six codes: DOUBLE_SHIFT, OUTSIDE_PLACEMENT_PERIOD, SHIFT_OVER_LEAVE, SHIFT_NOT_FOR_SERVICE_LINE, SHIFT_DEACTIVATED, OUT_OF_SCOPE — in a single ordered evaluator
    - schedule_service.go BulkApply persists each successful cell in its own tx (failing cell does not roll back successes) and builds succeeded/failed arrays
    - schedule_service.go over-leave branch calls `FindApprovedLeaveForAgentDate` (honest delivery, not faked)
    - shift_master_service.go derives cross_midnight from end<=start and raises BREAK_OUTSIDE_WINDOW (422) + DUPLICATE_NAME (409) + ALREADY_INACTIVE (409)
    - notify is a `TODO(Phase-11)` stub comment; every write calls audit.Record in-tx
    - `go build ./internal/service/scheduling/... ./internal/repository/scheduling/...` exits 0
  </acceptance_criteria>
  <done>conflict_engine evaluates all six codes in order; schedule_service Check/Create/Update/Delete/BulkApply implemented with per-cell tx atomicity and the honest approved_leave_days over-leave branch; shift_master_service derives cross_midnight + raises BREAK_OUTSIDE_WINDOW/DUPLICATE_NAME/ALREADY_INACTIVE; audit in-tx; notify TODO(Phase-11) stub; go build of service+repository packages exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Handlers + DTOs (shift-master + schedule, byte-for-shape with openapi)</name>
  <files>backend/internal/handler/scheduling/shift_master_dto.go, backend/internal/handler/scheduling/shift_master_handler.go, backend/internal/handler/scheduling/schedule_dto.go, backend/internal/handler/scheduling/schedule_handler.go</files>
  <read_first>
    - backend/internal/handler/placement/placements_handler.go + placements_dto.go (decode→service→httpx.WriteJSON; cursor PageResponse; error→httpx.WriteError; toResponse DTO mapping)
    - backend/internal/handler/placement/roster_handler.go (company-scoped list handler shape)
    - backend/internal/platform/httpx/*.go (WriteJSON, WriteError, PageResponse, EncodeCursor/DecodeCursor)
    - docs/api/E4-shift-scheduling/openapi.yaml: list envelopes (108-118 shift-masters `{data,next_cursor,has_more}`; 422-435 schedule `{data,warnings}`), ScheduleEntry response, BulkApplyResult (1320-1369)
    - frontend/apps/web/src/features/e4-scheduling/schedule-overlays.tsx (lines 188-215: FE reads checkResult.data.failed[].error.code — failed item carries `error:{code,message,details}`; succeeded carries {id,employee_id,date,status})
  </read_first>
  <action>
    **DTOs** `backend/internal/handler/scheduling/{shift_master_dto.go, schedule_dto.go}`:
    - `shiftMasterResponse` — JSON snake_case EXACTLY matching openapi ShiftMaster (id,name,start_time,end_time,break_start,break_end,break_minutes,service_line_id,service_line_name,cross_midnight,is_active,status,in_use_count,created_by,created_at,updated_at). `status` = ACTIVE/INACTIVE derived from is_active. `break_minutes` derived (break_end−break_start in minutes; null when no break). Nullable fields use `*string`/pointer + NO omitempty where openapi marks the field nullable-but-present (match Phase-5 convention: emit JSON null, not absent).
    - `scheduleEntryResponse` — match openapi ScheduleEntry: id,employee_id,employee_name,company_id,company_name,placement_id,service_line_id,shift_master_id,shift_master_name,start_time,end_time,cross_midnight,work_date("YYYY-MM-DD" via Asia/Jakarta TZ layer),status,is_day_off,replaced_entry_id,created_by,created_at,updated_at.
    - Request DTOs: `shiftMasterWriteRequest` (name,start_time,end_time,break_start?,break_end?,service_line_id?,is_active?); `scheduleEntryWriteRequest` (kind,employee_id,shift_master_id?,date,is_day_off?,force_replace?); `bulkApplyRequest` (kind,shift_master_id,start_date,end_date,employee_ids,weekdays_mask?,override_existing?).
    - `bulkApplyResult` DTO: `succeeded:[]{id,employee_id,date,status}`, `failed:[]{employee_id,date,error:{code,message,details}}`, `warnings:[]`. The failed item's `error` is the nested object the FE reads (NOT a top-level code). details = ConflictDetails subset.

    **Handlers** `backend/internal/handler/scheduling/{shift_master_handler.go, schedule_handler.go}`:
    - Shift masters: `ListShiftMasters` (cursor list → `{data,next_cursor,has_more}`), `CreateShiftMaster` (201 + Location), `UpdateShiftMaster`, `DeactivateShiftMaster`, `ReactivateShiftMaster`. Decode → validate → service → WriteJSON. Errors → httpx.WriteError (carries code/fields/details/status from apperr).
    - Schedule: `ListSchedule` (required company_id+start_date+end_date; → `{data, warnings:[]}`), `CreateScheduleEntry` (201 + Location + warnings:[]; on conflict the apperr 403/409/422 envelope flows through WriteError), `UpdateScheduleEntry` (200), `DeleteScheduleEntry` (204 no body), `CheckScheduleConflicts` (200 BulkApplyResult; route single vs bulk on `kind` discriminator), `BulkApplySchedule` (200 if len(succeeded)>=1 else 422; same BulkApplyResult body in both).
    - For `:check` discriminator: peek `kind` ("single"|"bulk") to decode the right request shape, call service.Check.

    Build the `Handler` struct + constructor (NewHandler(svc...)) mirroring placement's handler aggregation, so server.go gets ONE `d.Scheduling *schedulinghttp.Handler`.
  </action>
  <verify>
    <automated>cd backend && grep -q "next_cursor" internal/handler/scheduling/shift_master_dto.go && grep -q "func.*BulkApplySchedule" internal/handler/scheduling/schedule_handler.go && grep -q "func.*CheckScheduleConflicts" internal/handler/scheduling/schedule_handler.go && grep -q "func.*DeactivateShiftMaster" internal/handler/scheduling/shift_master_handler.go && grep -q "break_minutes" internal/handler/scheduling/shift_master_dto.go && grep -q "work_date" internal/handler/scheduling/schedule_dto.go && go build ./internal/handler/scheduling/... && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - shift_master_dto.go emits exact openapi fields incl. `break_minutes`, `status` (ACTIVE/INACTIVE from is_active), `in_use_count`, `next_cursor`/`has_more` on the list envelope
    - schedule_dto.go scheduleEntryResponse emits `work_date` as "YYYY-MM-DD", and bulkApplyResult failed item nests `error:{code,message,details}` (matches FE reading failed[].error.code)
    - schedule_handler.go has CheckScheduleConflicts (kind-discriminated) + BulkApplySchedule (200 if any succeeded else 422) + DeleteScheduleEntry (204)
    - shift_master_handler.go has Deactivate/Reactivate + Create returns 201+Location
    - `go build ./internal/handler/scheduling/...` exits 0
  </acceptance_criteria>
  <done>DTOs match the openapi byte-for-shape (break_minutes, status ACTIVE/INACTIVE, next_cursor/has_more, work_date YYYY-MM-DD, bulkApplyResult failed[].error.{code,message,details}); handlers cover all 11 endpoints incl. :check (no idempotency) and :bulk-apply (200 if any succeeded else 422) and DELETE 204; a single schedulinghttp.Handler aggregates them; go build of the handler package exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Routes (server.go) + wiring (main.go) + seed extension</name>
  <files>backend/internal/server/server.go, backend/cmd/api/main.go, backend/cmd/seed/seed.go</files>
  <read_first>
    - backend/internal/server/server.go lines 285-320 (PLACEMENT slice: read group RequireRole(super,hr,leader); write group; `r.With(d.Idempotency.Handler).Post(...)`; action-suffix `:` routes) — append AFTER "// PLACEMENT slice end (05-02)"
    - backend/cmd/api/main.go (where placement Repo→Service→Handler is built + added to server.Deps — add d.Scheduling the same way)
    - backend/cmd/seed/seed.go: Seed() func (line 119+), seedPlacements (SWP-PL-5001..5004 + SWP-SLA-3001), seedEmployees IDs — add seedScheduling() called AFTER seedPlacements
    - docs/api/E4-shift-scheduling/openapi.yaml x-rbac per op (shift-master writes: super_admin+hr_admin; shift-master reads + ALL /schedule ops: super_admin+hr_admin+shift_leader)
  </read_first>
  <action>
    **Routes** in server.go — append a SCHEDULING slice block after the placement block. Two RBAC groups:
    - reads + schedule writes group `RequireRole(RoleSuperAdmin, RoleHRAdmin, RoleShiftLeader)`:
      ```
      r.Get("/shift-masters", d.Scheduling.ListShiftMasters)
      r.Get("/shift-masters/{id}", d.Scheduling.GetShiftMaster)
      r.Get("/schedule", d.Scheduling.ListSchedule)
      r.With(d.Idempotency.Handler).Post("/schedule", d.Scheduling.CreateScheduleEntry)
      r.With(d.Idempotency.Handler).Patch("/schedule/{id}", d.Scheduling.UpdateScheduleEntry)
      r.With(d.Idempotency.Handler).Delete("/schedule/{id}", d.Scheduling.DeleteScheduleEntry)
      r.Post("/schedule:check", d.Scheduling.CheckScheduleConflicts)               // no idempotency (no side effects)
      r.With(d.Idempotency.Handler).Post("/schedule:bulk-apply", d.Scheduling.BulkApplySchedule)
      ```
    - shift-master WRITES group `RequireRole(RoleSuperAdmin, RoleHRAdmin)`:
      ```
      r.With(d.Idempotency.Handler).Post("/shift-masters", d.Scheduling.CreateShiftMaster)
      r.With(d.Idempotency.Handler).Patch("/shift-masters/{id}", d.Scheduling.UpdateShiftMaster)
      r.With(d.Idempotency.Handler).Post("/shift-masters/{id}:deactivate", d.Scheduling.DeactivateShiftMaster)
      r.With(d.Idempotency.Handler).Post("/shift-masters/{id}:reactivate", d.Scheduling.ReactivateShiftMaster)
      ```
    Add `Scheduling *schedulinghttp.Handler` to `server.Deps` + the import. Leave a "// SCHEDULING slice end (06-02). Phase 6+ appends after this line." marker.

    **main.go** — construct scheduling Repo (shift_master_repo + schedule_repo over the pool + tx), Service (with Clock=time.Now, TxRunner=db.TxManager, audit), Handler; set `deps.Scheduling = ...`.

    **seed.go** — add `seedScheduling(ctx, pool)` called from Seed() right after `seedPlacements`. Insert (explicit, ON CONFLICT (id) DO NOTHING / idempotent, deterministic IDs):
    - 2 shift masters: `SWP-SHF-001` "Pagi" 07:00–15:00 break 12:00–13:00 service_line NULL (all lines); `SWP-SHF-002` "Malam" 23:00–07:00 (cross_midnight=true) service_line_id `SWP-SVC-003`.
    - Schedule entries for the CMP-0021 placements so the grid renders agents: for SWP-PL-5001 (EMP-1108) and SWP-PL-5004 (EMP-3001) — insert one SCHEDULED entry each on a clearly-in-range date. **TZ note (Phase-5 05-03):** the E2E fixed clock derives statuses at Asia/Jakarta vs UTC midnight — use dates a few days INTO the current week, never today's boundary. Use a computed monday-of-current-week + 1 day so it lands inside the visible week and inside the placement window.
    - 1 approved-leave day to exercise SHIFT_OVER_LEAVE: insert into `approved_leave_days` (employee_id=`SWP-EMP-3001` (Dewi, EMP at CMP-0021), leave_date = a known in-week date NOT already taken by a schedule entry, leave_request_id `SWP-LR-44210`, leave_type `ANNUAL`). 06-04 E2E will attempt to schedule EMP-3001 on that exact date and assert SHIFT_OVER_LEAVE.
    - Leave EMP-2891 (Budi) WITHOUT a CMP-0021 placement (his placement SWP-PL-5002 is at CMP-0022) — this is the leader-scope-403 target for 06-04.
    Document the seeded fixture dates in a comment so 06-03/06-04 reference the same values.

    Then `make verify` (or `go build ./... && go vet ./... && gofmt -l`) clean.
  </action>
  <verify>
    <automated>cd backend && grep -q 'Post("/schedule:bulk-apply"' internal/server/server.go && grep -q 'Post("/schedule:check"' internal/server/server.go && grep -q 'Post("/shift-masters/{id}:deactivate"' internal/server/server.go && grep -q "Scheduling" internal/server/server.go && grep -q "deps.Scheduling\|Scheduling:" cmd/api/main.go && grep -q "func seedScheduling" cmd/seed/seed.go && grep -q "approved_leave_days" cmd/seed/seed.go && grep -q "SWP-LR-44210" cmd/seed/seed.go && go build ./... && go vet ./... && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - server.go registers all 11 scoped routes: GET/POST/PATCH/DELETE /schedule, POST /schedule:check, POST /schedule:bulk-apply, GET/POST/PATCH /shift-masters, POST /shift-masters/{id}:deactivate + :reactivate (reads+schedule under super/hr/leader; shift-master writes under super/hr)
    - `:check` route has NO idempotency wrapper; all other writes DO
    - main.go builds scheduling Handler and sets `deps.Scheduling`
    - seed.go seedScheduling inserts SWP-SHF-001/002, schedule entries at CMP-0021, and one approved_leave_days row (SWP-LR-44210 / EMP-3001) — fixture dates documented in a comment for 06-03/06-04
    - `go build ./... && go vet ./...` exits 0
  </acceptance_criteria>
  <done>All 11 scoped routes registered under the right RequireRole groups (:check without idempotency, others with); main.go builds the scheduling Handler into server.Deps; seedScheduling seeds SWP-SHF-001/002, CMP-0021 schedule entries, and one approved_leave_days row (SWP-LR-44210 / EMP-3001) with documented dates; make verify (go build + go vet) clean.</done>
</task>

</tasks>

<verification>
- `cd backend && make verify` (or go build + go vet + gofmt -l) clean.
- All 11 FE-used E4 endpoints routed; no out-of-scope endpoints (no /schedule/by-agent, no swap endpoints).
- Conflict engine emits every contract code with the right HTTP status; over-leave reads approved_leave_days (honest).
- Bulk-apply: 200 when >=1 succeeded, 422 when all failed; per-cell atomicity.
- Seed plants the DOUBLE_SHIFT / OUTSIDE_PLACEMENT_PERIOD / SHIFT_OVER_LEAVE / leader-scope fixtures for 06-03/06-04.
</verification>

<success_criteria>
- Shift-master CRUD + deactivate/reactivate and schedule CRUD work against the real BE.
- :check is side-effect-free; :bulk-apply reports partial success.
- Leader scope enforced via GuardCompany; audit on every write; notify stubbed.
- SHIFT_OVER_LEAVE honestly delivered and seeded.
</success_criteria>

<output>
After completion, create `.planning/phases/06-e4-schedule-shifts/06-02-SUMMARY.md` documenting:
- the conflict-engine ordering + each code's HTTP status + details shape,
- the bulk-apply 200/422 rule + per-cell tx atomicity decision,
- the exact seeded fixtures (shift master ids, schedule-entry dates, the approved-leave date + EMP + LR id) for 06-03/06-04 to consume,
- the over-leave honesty note (E4-owned approved_leave_days; E6 wires production source),
- any divergence + reason.
</output>
