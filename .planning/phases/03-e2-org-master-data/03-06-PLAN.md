---
phase: 03-e2-org-master-data
plan: 06
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - frontend/e2e/lib/db.ts
  - frontend/e2e/tests/e2/client-companies.spec.ts
  - frontend/e2e/tests/e2/client-sites-geofence.spec.ts
  - frontend/e2e/tests/e2/service-lines-positions.spec.ts
  - frontend/e2e/tests/e2/operational-master-data.spec.ts
  - frontend/apps/web/src/features/e2-identity/master-data-hub-screen.tsx
autonomous: true
requirements: [ORG-01, ORG-02, ORG-03, ORG-04]

must_haves:
  truths:
    - "The E2 org/master screens (client companies, sites, service lines, positions, leave types, attendance codes, overtime rules) drive the REAL backend (MSW off) — list/create/update/deactivate/reactivate/discontinue/soft-delete all work end-to-end."
    - "Exhaustive Playwright specs exist — one test() per Gherkin scenario/case in the 4 E2 PRDs — and pass headless against the real stack."
    - "Geofence persists: a site created/updated with lat/lng + radius shows the saved geofence on reload (verified via UI and/or DB helper)."
    - "RBAC negatives covered: agent denied master-data writes; hr_admin denied service-line create (super_admin-only) → 403 UI."
  artifacts:
    - path: "frontend/e2e/tests/e2/client-companies.spec.ts"
      provides: "company directory E2E (CC-1..CC-5 + RBAC + pagination)"
      min_lines: 80
    - path: "frontend/e2e/tests/e2/operational-master-data.spec.ts"
      provides: "leave-type/attendance-code/overtime-rule E2E (CRUD + min_minutes 422 + soft-delete)"
      min_lines: 80
  key_links:
    - from: "frontend/e2e/tests/e2/*.spec.ts"
      to: "real Go API :8081"
      via: "MSW off, loginAs drives real login, resetDb in beforeEach"
      pattern: "loginAs"
    - from: "frontend/e2e/lib/db.ts"
      to: "ephemeral Postgres :5433"
      via: "new org/master verification helpers"
      pattern: "client_companies|client_sites|overtime_rules"
---

<objective>
Wire the E2 org/master-data screens to the real BE (confirm MSW-off behavior, fix any FE drift) and write exhaustive Playwright E2E — one test() per Gherkin scenario/case across the 4 E2 PRDs — passing headless against the real stack. Add the DB verification helpers the specs need.

Purpose: This is the proof that ORG-01..04 actually work end-to-end (the milestone's core value). The contract tests (03-05) gate shape; these specs gate real behavior.
Output: 4 new spec files under frontend/e2e/tests/e2/, new helpers in e2e/lib/db.ts, and any FE wiring fix needed for the master-data hub.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/reference/e2e-harness-spec.md
@.planning/phases/03-e2-org-master-data/03-02-SUMMARY.md   # seeded company/site ids (SWP-CMP-0021 etc.)
@.planning/phases/03-e2-org-master-data/03-03-SUMMARY.md   # seeded service-line/position ids
@.planning/phases/03-e2-org-master-data/03-04-SUMMARY.md   # seeded master-data ids

<interfaces>
COPY the existing E1 E2E pattern:
@frontend/e2e/tests/e1/user-management.spec.ts   # test() naming by req-id, loginAs, resetDb beforeEach, DataTable row locator note
@frontend/e2e/lib/fixtures.ts                     # test, expect, loginAs
@frontend/e2e/lib/personas.ts                     # PERSONAS.hrAdmin/superAdmin/shiftLeader/agent
@frontend/e2e/lib/reset-db.ts                     # resetDb()
@frontend/e2e/lib/db.ts                           # existing helpers (countAuditRowsByEntityType, getUserStatus pattern) — ADD org/master helpers here

Critical FE facts (from Phase-2 decisions / repo scan):
- MSW is OFF when VITE_ENABLE_MSW !== 'true' (frontend/apps/web/src/main.tsx) — the harness sets it false. The E2 screens already call real @swp/api-client hooks (useListClientCompanies, useCreateSite, useDiscontinueServiceLine, useUpdateOvertimeRule, etc. — confirmed in features/e2-identity/*). So wiring is mostly verification, not new hook plumbing.
- DataTable rows are `div.border-b` NOT `tr` — row locators use `div.border-b` + filter (Phase-2 decision).
- Routes (frontend/apps/web/src/app/router.tsx): /client-companies, /client-companies/new, /client-companies/$clientCompanyId, /service-lines, /service-lines/$serviceLineId, /master-data, /master-data/leave-types, /master-data/attendance-codes, /master-data/overtime-rules.
- playwright timeout 90s; use 30s expect timeouts for first-load.
</interfaces>

# Behavior source (one test per scenario/case):
@docs/epics/E2-identity/prds/client-company-directory.md     # §7 Gherkin (5 scenarios) + §8 cases
@docs/epics/E2-identity/prds/client-sites-geofence.md        # geofence scenarios + cases
@docs/epics/E2-identity/prds/service-lines-positions.md      # SP scenarios + cases
@docs/epics/E2-identity/prds/operational-master-data.md      # LT/AC/OR/MD scenarios + cases
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify FE wiring (MSW off) + DB verification helpers</name>
  <read_first>
    - frontend/apps/web/src/features/e2-identity/{client-companies-screen,client-company-detail-screen,client-company-form,site-form,service-lines-screen,service-line-detail-screen,leave-types-screen,attendance-codes-screen,overtime-rules-screen,master-data-hub-screen}.tsx (confirm each calls real hooks; note any TODO/MSW-only stub)
    - frontend/e2e/lib/db.ts (helper pattern — pg client query → return scalar)
    - .planning/phases/03-e2-org-master-data/03-02-SUMMARY.md..03-04-SUMMARY.md (seeded ids)
  </read_first>
  <action>
1) Run the stack once and smoke each E2 screen list to confirm it renders real data (no MSW). If any screen still references an MSW-only path or a missing hook, fix the minimal wiring (e.g. master-data-hub-screen.tsx is listed as "static; no endpoint yet" — if its sub-tabs just link to the leave-types/attendance-codes/overtime-rules screens, that's fine; only change it if it blocks navigation to those screens). Do NOT redesign screens — match existing layout.

2) Add verification helpers to `frontend/e2e/lib/db.ts` (same pg-client pattern as existing helpers):
- `getCompanyStatus(id: string): Promise<string|null>` — SELECT status FROM client_companies WHERE id=$1.
- `countSitesForCompany(companyId: string): Promise<number>` — count active client_sites.
- `getSiteGeofence(siteId: string): Promise<{lat:number|null,lng:number|null,radius:number}|null>` — SELECT geo_lat, geo_lng, geofence_radius_m.
- `getServiceLineStatus(id): Promise<string|null>`; `getPositionStatus(id): Promise<string|null>`.
- `getLeaveTypeStatus(id)`, `getAttendanceCodeStatus(id)`, `getOvertimeRuleStatus(id)` (status by id).
- `countActivePositionsForLine(lineId): Promise<number>`.
Reuse the existing pg connection/query helper in db.ts (do not open a new pool pattern).
  </action>
  <verify>
    <automated>cd frontend && grep -q "getSiteGeofence" e2e/lib/db.ts && grep -q "getCompanyStatus" e2e/lib/db.ts && grep -q "getOvertimeRuleStatus" e2e/lib/db.ts && pnpm --filter @swp/web typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>E2 screens confirmed driving the real BE with MSW off (any blocking stub fixed minimally); db.ts has the 8 new org/master verification helpers; web typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: E2E specs — companies + sites/geofence</name>
  <read_first>
    - frontend/e2e/tests/e1/user-management.spec.ts (structure/naming/row-locator)
    - docs/epics/E2-identity/prds/client-company-directory.md §7 + §8 (every Scenario + C-#)
    - docs/epics/E2-identity/prds/client-sites-geofence.md (every Scenario + C-#)
    - frontend/apps/web/src/features/e2-identity/{client-companies-screen,client-company-form,client-company-detail-screen,site-form}.tsx (selectors: headings, buttons, field labels — use getByRole/getByLabel matching the screens' i18n copy)
  </read_first>
  <action>
Create `frontend/e2e/tests/e2/client-companies.spec.ts` — `test.beforeEach(resetDb)`; one test() per scenario/case, named with CC-#/C-#:
- list renders seeded companies (Plaza Senayan + others) for hr_admin.
- create company → toast + row appears + getCompanyStatus('<new id>') (or by name) and countSitesForCompany == 1 (auto Main Site, CC-1c).
- duplicate company name → 409 surfaced as field/inline error (CC-2).
- edit company (e.g. PIC/leader_scope) → updated value visible.
- deactivate company → status badge INACTIVE + getCompanyStatus=='inactive'; reactivate → ACTIVE.
- (CC-5) deactivate-with-active-placements guard: Phase 3 has no placements so document this as a happy deactivate; add a `test.skip` placeholder named for CC-5 with a comment that the guard activates in Phase 5.
- cursor pagination: if seed has > default page size, assert load-more; otherwise seed enough rows in a test via API and assert next page (use the api login fixture or just assert has_more behavior with a small limit query through the UI if supported). Keep deterministic.
- RBAC: agent denied the client-companies screen (403 → no-permission UI), mirroring the E1 agent-denied test.

Create `frontend/e2e/tests/e2/client-sites-geofence.spec.ts` — named ST-#/C-#:
- list sites of Plaza Senayan (SWP-CMP-0021) shows the seeded primary site first.
- add a site with geo lat/lng + radius → toast + getSiteGeofence shows persisted lat/lng/radius; reload shows geofence active.
- radius out of range (e.g. 5000) → GEOFENCE_RADIUS_INVALID inline error (ST-8/CC bad_radius), no row created.
- duplicate site name within company → 409 (ST-2).
- set another site primary → previous primary demoted (assert via UI primary badge and/or a DB check that only one is_primary).
- site with no geo allowed → geofence inactive (ST-8).
Use selectors from the actual screens (site-form labels). Use 30s timeouts on first load.
  </action>
  <verify>
    <automated>cd frontend && pnpm --filter @swp/e2e exec playwright test tests/e2/client-companies.spec.ts tests/e2/client-sites-geofence.spec.ts --list 2>&1 | tail -20</automated>
  </verify>
  <done>Both spec files exist; `playwright test --list` enumerates one case per CC-/ST-/C- scenario; each test is named with its scenario id; geofence persistence + radius validation + primary reassignment + RBAC covered.</done>
</task>

<task type="auto">
  <name>Task 3: E2E specs — service-lines/positions + master-data; full suite green</name>
  <read_first>
    - docs/epics/E2-identity/prds/service-lines-positions.md (every Scenario + C-#)
    - docs/epics/E2-identity/prds/operational-master-data.md (every Scenario + C-#)
    - frontend/apps/web/src/features/e2-identity/{service-lines-screen,service-line-detail-screen,leave-types-screen,attendance-codes-screen,overtime-rules-screen}.tsx (selectors + i18n copy)
  </read_first>
  <action>
Create `frontend/e2e/tests/e2/service-lines-positions.spec.ts` — named SP-#/C-#:
- list shows the 3 seeded service lines with position_count.
- super_admin creates a service line → row appears; hr_admin attempt → 403 / action hidden or denied (SP-1 super_admin-only).
- rename service line → updated.
- discontinue service line with active positions → SERVICE_LINE_IN_USE error (use Parking which has seeded positions); discontinue an empty line → success (status INACTIVE, getServiceLineStatus).
- positions: list positions under Parking; create position (unique line+name) → row; duplicate → POSITION_IN_USE (SP-3); update position; soft-delete position → status INACTIVE (getPositionStatus) / removed from active list.

Create `frontend/e2e/tests/e2/operational-master-data.spec.ts` — named LT-#/AC-#/OR-#/MD-#/C-#, covering all 3 sets:
- leave-types: list seeded (Cuti Tahunan/Sakit); create (e.g. Cuti Melahirkan); duplicate name/code → 409; update; soft-delete → status INACTIVE (getLeaveTypeStatus).
- attendance-codes: list seeded (PRESENT/LATE); create; duplicate code/label → 409; update; soft-delete (getAttendanceCodeStatus).
- overtime-rules: list seeded (Default OT); create with min_minutes=20 → RULE_VIOLATION 422 inline error (D4 min 30); create valid; update; soft-delete (getOvertimeRuleStatus).
- RBAC: agent denied master-data write screens / actions.

Then run the FULL e2 suite headless against the real stack and ensure green.
  </action>
  <verify>
    <automated>cd frontend && pnpm --filter @swp/e2e exec playwright test tests/e2/ --reporter=line 2>&1 | tail -25</automated>
  </verify>
  <done>Both spec files exist and enumerate one case per scenario; the full `tests/e2/` Playwright suite runs green headless against the real Go API + ephemeral Postgres; min_minutes 422, in-use 409s, dup 409s, soft-deletes, and RBAC negatives all pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm --filter @swp/e2e exec playwright test tests/e2/ --reporter=line` exits 0 (all E2 specs green).
- `pnpm --filter @swp/web typecheck` and `pnpm --filter @swp/web build` clean (no FE drift introduced).
- `playwright test tests/e2/ --list` shows each Gherkin scenario as its own runnable case (traceable to CC-/ST-/SP-/LT-/AC-/OR-/MD- ids).
</verification>

<success_criteria>
ORG-01..04 proven end-to-end: every E2 org/master screen drives the real BE with MSW off; exhaustive per-Gherkin Playwright specs are green headless; geofence persists; cursor pagination + filters work; RBAC negatives and validation/conflict states render correctly.
</success_criteria>

<output>
Create `.planning/phases/03-e2-org-master-data/03-06-SUMMARY.md` listing the spec files + test count per PRD, any FE wiring fixes made, the new db.ts helpers, and any scenario deferred (e.g. CC-5 active-placement guard → Phase 5) with the reason.
</output>
