---
phase: 03-e2-org-master-data
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - backend/internal/domain/org_serviceline.go
  - backend/internal/repository/org/serviceline_repo.go
  - backend/internal/service/org/serviceline_service.go
  - backend/internal/handler/org/serviceline_dto.go
  - backend/internal/handler/org/serviceline_handler.go
  - backend/internal/server/server.go
  - backend/cmd/api/main.go
  - backend/cmd/seed/seed.go
autonomous: true
requirements: [ORG-03]

must_haves:
  truths:
    - "Anyone authenticated can list/get service lines (picker shape); super_admin can create/rename/discontinue; discontinue is soft (status→INACTIVE) and returns SERVICE_LINE_IN_USE (409) when active positions reference it."
    - "HR can list positions in a line (picker), create (unique (line,name)→409 POSITION_IN_USE), update, and soft-delete (status→INACTIVE; 409 POSITION_IN_USE TODO Phase-5 when referenced by placements)."
    - "ServiceLine.position_count reflects active positions in the line; responses match the ServiceLine/Position schemas with UPPERCASE status."
  artifacts:
    - path: "backend/internal/handler/org/serviceline_handler.go"
      provides: "service-line + position handlers"
      min_lines: 90
    - path: "backend/internal/service/org/serviceline_service.go"
      provides: "service-line/position rules + audit + in-use guards"
      min_lines: 90
  key_links:
    - from: "backend/internal/server/server.go"
      to: "service-line/position handlers"
      via: "routes /service-lines, /service-lines/{id}/positions, /positions/{id}"
      pattern: "service-lines"
    - from: "serviceline_service.go"
      to: "audit.Record"
      via: "audit on every write"
      pattern: "audit\\.Record"
---

<objective>
Implement the service-lines + positions slice (ORG-03): domain, repository, service (rename, discontinue with in-use guard, position CRUD + soft-delete), handlers, routes, deps wiring, and seed (3 service lines + sample positions). Runs in parallel with 03-02 and 03-04 (different files except the shared server.go/main.go/seed.go coordination points).

Purpose: Service lines + positions are the placement taxonomy (E3 dependency) and supply the ServiceLine/Position pickers.
Output: serviceline_* files in the existing `org` package, 9 endpoints mounted, seed extended.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/reference/backend-build-conventions.md
@.planning/phases/03-e2-org-master-data/03-CONTEXT.md
@.planning/phases/03-e2-org-master-data/03-01-SUMMARY.md   # sqlc method names
@.planning/phases/03-e2-org-master-data/03-02-SUMMARY.md   # server.go/main.go/seed coordination contract + org package conventions

<interfaces>
Reference slice (copy shape): foundations service/repository/handler (see 03-02 for the same references).
Sibling slice 03-02 already created the `org` packages (domain/org.go, repository/org, service/org, handler/org) and the ORG route group in server.go. ADD your files alongside (serviceline_*.go) — do NOT duplicate the Repository/Service structs; either extend the existing org Service with service-line methods OR create a separate ServiceLineService struct in the same package. Read 03-02-SUMMARY for which approach was taken and follow it. Default: separate ServiceLineService + ServiceLineRepository to keep parallel-merge clean.

apperr: apperr.Conflict("SERVICE_LINE_IN_USE"), apperr.Conflict("POSITION_IN_USE"), apperr.NotFound(), apperr.Invalid(fields).
RBAC: service-lines reads allow [hr_admin,super_admin,shift_leader,agent] scope global; create/rename/discontinue are [super_admin] only; positions writes [hr_admin,super_admin]; positions reads same broad set.
Status: DB lowercase; DTO UPPERCASE.
</interfaces>

# Contract — match EXACTLY:
@docs/api/E2-identity/openapi.yaml   # /service-lines 1704–1876; /positions 1878–2034; ServiceLine 3245, Position 3270, write reqs 3264/3288
@docs/api/CONVENTIONS.md             # §8 cursor, §9 filtering, §11 errors, §16.1 audit, §17 RBAC, §18 pickers
@docs/epics/E2-identity/prds/service-lines-positions.md   # SP-1..SP-4 rules + Gherkin (discontinue/soft-delete/in-use)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain + repository for service lines & positions</name>
  <read_first>
    - backend/internal/repository/sqlc/service_lines.sql.go + positions.sql.go (generated names from 03-01)
    - backend/internal/repository/org/companies_repo.go (mapping/helpers/WithTx pattern from sibling 03-02)
    - docs/api/E2-identity/openapi.yaml 3245–3296 (field names)
  </read_first>
  <action>
Create `backend/internal/domain/org_serviceline.go` with `ServiceLine` (ID, Name, Status, PositionCount int) and `Position` (ID, ServiceLineID, Name, Alias, Status), plus `ServiceLineFilter` (Status, Limit) and `PositionFilter` (Status, Limit, CursorCreatedAt, CursorID).

Create `backend/internal/repository/org/serviceline_repo.go`: ServiceLineRepository struct (pool,q) with ListServiceLines, GetServiceLineByID, CreateServiceLine(tx,name), UpdateServiceLine(tx,id,name), SetServiceLineStatus(tx,id,status), CountActivePositionsForLine(id); ListPositionsForLine(lineID, filter), GetPositionByID, CreatePosition(tx, params), UpdatePosition(tx, params), SetPositionStatus(tx,id,status). Populate ServiceLine.PositionCount from CountActivePositionsForLine in list/get mappers. Reuse the package's mapErr/null helpers from companies_repo.go (do not redeclare).
  </action>
  <verify>
    <automated>cd backend && go build ./internal/domain/... ./internal/repository/org/... 2>&1 | head; test -f internal/domain/org_serviceline.go && grep -q "CountActivePositionsForLine" internal/repository/org/serviceline_repo.go</automated>
  </verify>
  <done>domain + repository for service lines/positions compile; PositionCount wired; no redeclared helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Service + handlers/DTOs for service lines & positions</name>
  <read_first>
    - backend/internal/service/org/companies_service.go (Service shape, InTx+audit, cursor, ClampLimit, status lowercasing — from 03-02)
    - backend/internal/handler/org/companies_handler.go + companies_dto.go (handler+DTO+cursor decode conventions from 03-02)
    - docs/api/E2-identity/openapi.yaml 1704–2034 (statuses + error codes)
    - docs/epics/E2-identity/prds/service-lines-positions.md (SP-1 discontinue-while-referenced→409 SERVICE_LINE_IN_USE; SP-3 dup (line,name)→409 POSITION_IN_USE; SP-4 soft-delete)
  </read_first>
  <action>
Create `backend/internal/service/org/serviceline_service.go` (ServiceLineService{repo, txm, now}):
- ListServiceLines(filter) → []ServiceLine, *nextCursor (status filter; ClampLimit+1). Note: listServiceLines spec has only Limit+status (no cursor param) but return PageEnvelope with next_cursor null/has_more — still ClampLimit and support cursor encode for consistency.
- GetServiceLine(id) → 404.
- CreateServiceLine(name): super_admin only (route-enforced); InTx CreateServiceLine + audit; dup name→apperr.Conflict("CONFLICT").
- UpdateServiceLine(id, name): 404; InTx + audit; dup→409.
- DiscontinueServiceLine(id): 404; if CountActivePositionsForLine(id) > 0 → apperr.Conflict("SERVICE_LINE_IN_USE") (also TODO(Phase-5): block when active placements reference the line); else InTx SetServiceLineStatus 'inactive' + audit.
- ListPositions(lineID, filter): verify line exists (404); list + cursor.
- CreatePosition(lineID, params): unique (line, name) violation from index → apperr.Conflict("POSITION_IN_USE"); InTx CreatePosition + audit.
- UpdatePosition(id, params): 404; partial name/alias/status; InTx + audit; dup→409 POSITION_IN_USE.
- SoftDeletePosition(id): 404; TODO(Phase-5) 409 POSITION_IN_USE when referenced by active placements; InTx SetPositionStatus 'inactive' + audit. Returns nothing (204).

Create `backend/internal/handler/org/serviceline_dto.go`: serviceLineResponse{id,name,status,position_count}, positionResponse{id,service_line_id,name,alias,status}; write request structs; mappers (status UPPERCASE).

Create `backend/internal/handler/org/serviceline_handler.go`: handlers ListServiceLines, GetServiceLine, CreateServiceLine, UpdateServiceLine, DiscontinueServiceLine, ListPositionsInServiceLine, CreatePosition, UpdatePosition, SoftDeletePosition. URL params: chi.URLParam("service_line_id"), ("position_id"). SoftDeletePosition returns 204 No Content (w.WriteHeader(http.StatusNoContent)). Create returns 201. Reuse decodeJSON/parseLimit/cursor helpers already in the org handler package.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/service/org/... ./internal/handler/org/... 2>&1 | head; grep -q "SERVICE_LINE_IN_USE" internal/service/org/serviceline_service.go && grep -q "POSITION_IN_USE" internal/service/org/serviceline_service.go && grep -q "StatusNoContent" internal/handler/org/serviceline_handler.go && grep -q "position_count" internal/handler/org/serviceline_dto.go</automated>
  </verify>
  <done>service + handlers compile; discontinue/soft-delete return the correct in-use codes; soft-delete returns 204; position_count in DTO; audit on every write.</done>
</task>

<task type="auto">
  <name>Task 3: Routes (server.go — COORDINATION POINT), deps wiring, seed service lines + positions</name>
  <read_first>
    - .planning/phases/03-e2-org-master-data/03-02-SUMMARY.md (where to append the ORG route group / Deps field / seed insert order)
    - backend/internal/server/server.go (current ORG group from 03-02)
    - backend/cmd/api/main.go (current org deps construction)
    - backend/cmd/seed/seed.go (idempotent insert pattern + 03-02's company inserts)
    - docs/api/E2-identity/openapi.yaml x-rbac per op
  </read_first>
  <action>
COORDINATION POINT (server.go, main.go, seed.go shared with 03-02/03-04). Append a NEW dedicated route group block AFTER the existing ORG companies/sites group — do not edit other groups. Two RBAC sub-groups:
- reads group RequireRole(SuperAdmin,HRAdmin,ShiftLeader,Agent): GET /service-lines, GET /service-lines/{service_line_id}, GET /service-lines/{service_line_id}/positions.
- writes: positions writes group RequireRole(SuperAdmin,HRAdmin): `r.With(d.Idempotency.Handler).Post("/service-lines/{service_line_id}/positions", ...)`, `Patch("/positions/{position_id}", ...)`, `Delete("/positions/{position_id}", ...)`. service-line writes group RequireRole(SuperAdmin): `r.With(d.Idempotency.Handler).Post("/service-lines", ...)`, `Patch("/service-lines/{service_line_id}", ...)`, `r.With(d.Idempotency.Handler).Post("/service-lines/{service_line_id}:discontinue", ...)`.

Add the handler to Deps (e.g. `OrgServiceLines *orghttp.ServiceLineHandler` or reuse a single Org handler per 03-02's decision). Construct in cmd/api/main.go (serviceLineRepo→serviceLineSvc→handler) and add to Deps literal.

Seed (cmd/seed/seed.go): idempotently insert the 3 seeded service lines with literal ids matching the spec examples: SWP-SVC-001 'Facility Services', SWP-SVC-002 'Building Management', SWP-SVC-003 'Parking' (ON CONFLICT (id) DO NOTHING, status='active'). Insert a few positions under Parking, e.g. SWP-POS-014 'Petugas Parkir' alias 'Parking Attendant', SWP-POS-015 'Koordinator Lokasi' alias 'Parking Supervisor' (service_line_id='SWP-SVC-003', status='active', ON CONFLICT DO NOTHING). Use literal ids so the discontinue/in-use E2E is deterministic.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && grep -q "service-lines/{service_line_id}:discontinue" internal/server/server.go && grep -q "positions/{position_id}" internal/server/server.go && grep -q "SWP-SVC-003" cmd/seed/seed.go && grep -q "SWP-POS-014" cmd/seed/seed.go</automated>
  </verify>
  <done>Backend builds/vets clean; 9 service-line/position routes mounted with correct RBAC (service-line writes super_admin-only); seed has 3 service lines + Parking positions with literal ids.</done>
</task>

</tasks>

<verification>
- `cd backend && go build ./... && go vet ./... && gofmt -l internal cmd` clean.
- Routes resolve: GET /service-lines (any role), POST /service-lines (super_admin only → hr_admin gets 403), DELETE /positions/{id} → 204.
</verification>

<success_criteria>
ORG-03 endpoints implemented end-to-end; discontinue + soft-delete are soft with the correct in-use 409 codes; position_count accurate; super_admin-only service-line writes; seed has the 3 canonical service lines + Parking positions.
</success_criteria>

<output>
Create `.planning/phases/03-e2-org-master-data/03-03-SUMMARY.md` noting the Deps field name added, generated route paths, and the seeded service-line/position ids (for E2E reference).
</output>
