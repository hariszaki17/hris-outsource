---
phase: 07-e5-attendance
plan: 04
type: execute
wave: 4
depends_on: ["07-02", "07-03"]
files_modified:
  - frontend/e2e/lib/reset-db.ts
  - frontend/e2e/lib/e5-helpers.ts
  - frontend/e2e/tests/e5/attendance-list-detail.spec.ts
  - frontend/e2e/tests/e5/verify-reject.spec.ts
  - frontend/e2e/tests/e5/bulk-idempotency.spec.ts
  - frontend/e2e/tests/e5/corrections.spec.ts
  - frontend/e2e/tests/e5/scope-negatives.spec.ts
  - frontend/apps/web/src/features/e5-attendance/correction-overlays.tsx
autonomous: true
requirements: [ATT-01, ATT-02]
must_haves:
  truths:
    - "The real e5-attendance screens (verification queue, detail, corrections) work against the real BE with MSW off: list renders seeded Pending exceptions, detail opens, single verify/reject succeed."
    - "Bulk verify reports REAL partial success (>=1 verified + >=1 skipped with a code) and idempotency replay returns the same result."
    - "Corrections approve updates the target attendance; reject sets REJECTED; OUTSIDE_CORRECTION_WINDOW and OUT_OF_SCOPE/VERIFY_OWN_RECORD negatives return the real codes."
    - "pnpm e2e is green headless with the new e5/ specs and no e1/e2/e3/e4 regressions; reset-db truncates the new tables."
  artifacts:
    - path: "frontend/e2e/tests/e5/bulk-idempotency.spec.ts"
      provides: "bulk partial-success + idempotency replay E2E"
      contains: "succeeded"
    - path: "frontend/e2e/tests/e5/scope-negatives.spec.ts"
      provides: "OUT_OF_SCOPE + VERIFY_OWN_RECORD E2E"
      contains: "VERIFY_OWN_RECORD"
    - path: "frontend/e2e/lib/reset-db.ts"
      provides: "TRUNCATE list incl. attendance + attendance_corrections"
      contains: "attendance_corrections"
  key_links:
    - from: "frontend/e2e/tests/e5/*.spec.ts"
      to: "real BE attendance endpoints"
      via: "loginAs + real screen interactions + apiAs"
      pattern: "loginAs"
    - from: "frontend/e2e/lib/reset-db.ts"
      to: "attendance + attendance_corrections tables"
      via: "TRUNCATE_TABLES additions before placements/employees"
      pattern: "attendance_corrections"
---

<objective>
Wire the built e5-attendance screens off MSW against the real BE and prove the full E5 surface with
exhaustive full-stack Playwright E2E under a NEW frontend/e2e/tests/e5/. Selectors come from the REAL
components (attendance-verification-screen, attendance-detail-screen, corrections-screen,
correction-overlays). Extend reset-db to truncate the new tables. Fix any FE conflict_details→error.details
read in correction-overlays if present (the recurring Phase-5/6 FE bug — read details from error.details).

Purpose: Close ATT-01 + ATT-02 — every E5 screen works end-to-end. Final wave.
Output: e5-helpers, 5 e5/ specs, reset-db update, FE error-details fix if needed.

The screens + routes already exist (/attendance, /attendance/verification, /attendance/$attendanceId,
/corrections) and already call the real @swp/api-client/e5 hooks; this plan verifies them against the
real BE and adds the E2E coverage.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-e5-attendance/07-CONTEXT.md
@.planning/reference/e2e-harness-spec.md
@.planning/phases/07-e5-attendance/07-02-SUMMARY.md
@frontend/apps/web/src/features/e5-attendance/attendance-verification-screen.tsx
@frontend/apps/web/src/features/e5-attendance/attendance-detail-screen.tsx
@frontend/apps/web/src/features/e5-attendance/corrections-screen.tsx
@frontend/apps/web/src/features/e5-attendance/correction-overlays.tsx

<interfaces>
E2E harness (mirror frontend/e2e/tests/e4/leader-scope.spec.ts + e4-helpers.ts):
  - `import { expect, loginAs, test } from '../../lib/fixtures.js'`
  - `import { PERSONAS } from '../../lib/personas.js'` — PERSONAS.shiftLeader = Rudi (rudi.wijaya@swp.test,
    leads Plaza Senayan / CMP-0021, employee EMP-1108); PERSONAS.hrAdmin = Sari.
  - `import { resetDb } from '../../lib/reset-db.js'` in beforeEach.
  - `import { apiAs, errorCode, waitForToken } from '../../lib/e5-helpers.js'` (re-export from e3/e4-helpers,
    like e4-helpers re-exports apiAs/errorCode/pickCombobox). `.js` import suffix (ESM).
  - apiAs(page, method, path, body?) auto-sends a FRESH Idempotency-Key per call. For the idempotency
    REPLAY test, add `apiAsWithKey(page, method, path, body, key)` to e5-helpers that sends a FIXED
    Idempotency-Key so two calls replay identically.

Real component selectors (DO NOT invent):
  - Verification queue (/attendance/verification): row = DataTable `div.border-b` (decision [02]);
    select-all Checkbox aria-label = t('selectAll'); row Checkbox aria-label = t('selectRow',{name});
    per-row "Setujui"/"Tolak" buttons = t('verifyBtn')/t('rejectBtn'); BulkBar appears when someSelected:
    t('bulkVerifyBtn')/t('bulkRejectBtn'); ConfirmDialog confirm = t('bulkVerifyConfirm') etc.; reject reason
    Input id="bulk-reject-reason" / "single-reject-reason" (min 5 chars). SL sees scopeBanner + company
    filter hidden.
  - Detail (/attendance/$attendanceId): verify button t('verifyBtn'), reject t('rejectBtn'); reject reason
    Input id="detail-reject-reason"; own-escalated record hides actions (shows t('escalatedToHR')).
  - Corrections (/corrections): DataTable rows clickable → CorrectionDetailDrawer; footer Approve button
    t('corrections.approve'), Reject opens RejectCorrectionModal (textarea register('reason')); approve is
    inline. Status badges via correctionStatusTone.
  Resolve the actual rendered text by reading the attendance + translation i18n namespace, OR prefer
  role/aria + id selectors (getByRole('switch'/'button'/'checkbox'), '#bulk-reject-reason') over literal
  copy where possible (Phase-3 finding: copy via i18n).

Seed targets (from 07-02 seed):
  - SWP-ATT-9002/9003/9004 = CMP-0021 PENDING exceptions (Rudi's scope) → verifiable in his queue.
  - SWP-ATT-9006 = Rudi's OWN ESCALATED record → VERIFY_OWN_RECORD.
  - SWP-ATT-9005 = CMP-0022 PENDING → OUT_OF_SCOPE for Rudi.
  - SWP-COR-8001 PENDING on 9004 → approve target; SWP-COR-8002 PENDING → reject target.
</interfaces>

<conflict_details_fix>
correction-overlays.tsx + api-error.ts read error details from `error.details` (Phase-5/6 contract).
If any E5 component reads `conflict_details` (the recurring FE bug) instead of `error.details`, fix it
toward the contract (decision [05-04]/[06-04]). Verify the verification + corrections screens surface BE
error.message/code via classifyError → toast. No FE change is needed beyond this if the screens already
use classifyError(err).message (they do — see the read components).
</conflict_details_fix>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reset-db truncate update + e5-helpers + FE error-details audit</name>
  <files>frontend/e2e/lib/reset-db.ts, frontend/e2e/lib/e5-helpers.ts, frontend/apps/web/src/features/e5-attendance/correction-overlays.tsx</files>
  <read_first>
    - frontend/e2e/lib/reset-db.ts (TRUNCATE_TABLES ordering — most-dependent first, before placements/employees)
    - frontend/e2e/lib/e4-helpers.ts (re-export pattern + waitForToken + apiAs + errorCode)
    - frontend/apps/web/src/features/e5-attendance/correction-overlays.tsx + frontend/apps/web/src/lib/api-error.ts (error.details vs conflict_details)
  </read_first>
  <action>
    reset-db.ts: add `'attendance_corrections'` then `'attendance'` to TRUNCATE_TABLES ABOVE the
    Phase-5 placement tables (FK order: corrections → attendance → placements/employees/schedule_entries).
    Add a comment "// Phase 7: E5 attendance tables (FK order: corrections before attendance before placements)".

    e5-helpers.ts: re-export `{ API_BASE, apiAs, errorCode, errorDetails }` from e3/e4-helpers and
    `waitForToken`. Add `apiAsWithKey(page, method, path, body, idempotencyKey)` — identical to apiAs but
    uses the PASSED Idempotency-Key (no fresh random) so a replay sends the same key. Add seed-id constants:
    ATT = { autoApproved:'SWP-ATT-9001', lateCmp21:'SWP-ATT-9002', geoCmp21:'SWP-ATT-9003',
    autoClosedCmp21:'SWP-ATT-9004', cmp22OutOfScope:'SWP-ATT-9005', rudiOwnEscalated:'SWP-ATT-9006' }
    and COR = { approveTarget:'SWP-COR-8001', rejectTarget:'SWP-COR-8002' }.

    correction-overlays.tsx: confirm error rendering reads from classifyError(err).message (error.details
    path). If a `conflict_details` literal is found anywhere in e5-attendance/*, replace with error.details.
    If none exists, make NO change (document "no conflict_details bug in E5" in SUMMARY).
  </action>
  <verify>
    <automated>cd frontend && grep -q "attendance_corrections" e2e/lib/reset-db.ts && grep -q "'attendance'" e2e/lib/reset-db.ts && grep -q "apiAsWithKey" e2e/lib/e5-helpers.ts && grep -q "SWP-ATT-9006" e2e/lib/e5-helpers.ts && ! grep -rq "conflict_details" apps/web/src/features/e5-attendance/ && echo HELPERS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `reset-db.ts` TRUNCATE_TABLES contains `attendance_corrections` and `attendance`, ordered before placements/employees.
    - `e5-helpers.ts` exports `apiAs`, `errorCode`, `waitForToken`, `apiAsWithKey`, and the ATT/COR seed-id constants incl. `SWP-ATT-9006`.
    - No `conflict_details` literal remains under `apps/web/src/features/e5-attendance/`.
  </acceptance_criteria>
  <done>reset-db truncates the new tables; e5-helpers provides apiAs/apiAsWithKey/seed-ids; FE reads error details from error.details.</done>
</task>

<task type="auto">
  <name>Task 2: E2E specs — list/detail, verify/reject, corrections (real screen interactions)</name>
  <files>frontend/e2e/tests/e5/attendance-list-detail.spec.ts, frontend/e2e/tests/e5/verify-reject.spec.ts, frontend/e2e/tests/e5/corrections.spec.ts</files>
  <read_first>
    - frontend/e2e/tests/e4/schedule-grid.spec.ts + shift-masters.spec.ts (real-UI interaction spec style)
    - frontend/apps/web/src/features/e5-attendance/attendance-verification-screen.tsx + attendance-detail-screen.tsx + corrections-screen.tsx + correction-overlays.tsx (selectors)
    - frontend/e2e/lib/fixtures.ts + personas.ts (loginAs, PERSONAS)
  </read_first>
  <action>
    attendance-list-detail.spec.ts (beforeEach resetDb):
      - LIST-hr: loginAs(hrAdmin); goto /attendance/verification; assert the queue lists seeded PENDING
        exceptions (rows present for 9002/9003/9004) and the AUTO_APPROVED 9001 is NOT listed (exceptions_only).
      - LIST-sl-scope: loginAs(shiftLeader); goto /attendance/verification; assert scopeBanner visible,
        company filter hidden, and only CMP-0021 rows shown (9005 from CMP-0022 absent).
      - DETAIL-open: click a row → /attendance/$attendanceId; assert HeaderCard + clock + flags render
        for the seeded record (e.g. LATE flag on 9002).

    verify-reject.spec.ts (beforeEach resetDb):
      - VERIFY-single: loginAs(hrAdmin); open detail for 9002; click verify button → ConfirmDialog confirm;
        assert success toast and the record's verification badge flips to VERIFIED (refetch).
      - REJECT-single: open detail for 9003; click reject; fill #detail-reject-reason (>=5 chars); confirm;
        assert success toast + REJECTED state + reject reason rendered.
      - REJECT-validation: reject with <5 char reason → confirm disabled (assert button disabled) OR 400 on submit.
      - VERIFY-queue-inline: in /attendance/verification, use a row's inline Setujui button → confirm → row
        leaves the PENDING queue on refetch.

    corrections.spec.ts (beforeEach resetDb):
      - COR-list: loginAs(hrAdmin); goto /corrections; assert PENDING corrections 8001/8002 listed.
      - COR-approve: click 8001 row → drawer; assert diff table renders; click Approve → success toast;
        assert correction status → APPLIED (re-open or list badge) AND target attendance 9004 now carries
        CORRECTED (verify via apiAs GET /attendance/SWP-ATT-9004 → flags includes CORRECTED).
      - COR-reject: click 8002 → drawer → Reject → RejectCorrectionModal textarea reason (>=5) → submit;
        assert success toast + status REJECTED.
    Use role/id/aria selectors where possible; DataTable rows = div.border-b filtered by id/text.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec playwright test e2e/tests/e5/attendance-list-detail.spec.ts e2e/tests/e5/verify-reject.spec.ts e2e/tests/e5/corrections.spec.ts --reporter=line && echo E5_CORE_E2E_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - Three specs exist under `frontend/e2e/tests/e5/` covering list+scope, detail, single verify/reject (+validation), corrections approve (with CORRECTED assertion on target attendance) + reject.
    - All three specs pass headless against the real BE.
    - Selectors reference real component ids/aria (`#detail-reject-reason`, t('verifyBtn'), div.border-b), not invented ones.
  </acceptance_criteria>
  <done>List/detail, single verify/reject, and corrections approve/reject E2E pass against the real BE using the real screens.</done>
</task>

<task type="auto">
  <name>Task 3: E2E specs — bulk partial-success + idempotency, scope negatives; full green run</name>
  <files>frontend/e2e/tests/e5/bulk-idempotency.spec.ts, frontend/e2e/tests/e5/scope-negatives.spec.ts</files>
  <read_first>
    - frontend/e2e/tests/e4/bulk-apply.spec.ts (bulk partial-success assertion style)
    - frontend/e2e/tests/e4/leader-scope.spec.ts (403 OUT_OF_SCOPE via apiAs)
    - frontend/e2e/lib/e5-helpers.ts (apiAs, apiAsWithKey, errorCode, ATT seed-ids)
    - docs/api/E5-attendance/openapi.yaml BulkActionResponse + VERIFY_OWN_RECORD + OUT_OF_SCOPE
  </read_first>
  <action>
    bulk-idempotency.spec.ts (beforeEach resetDb):
      - BULK-partial-ui: loginAs(hrAdmin); goto /attendance/verification; select rows including a valid
        PENDING (9002) AND the escalated-own/terminal mix is HR-global so to force a failure use apiAs for
        determinism: select 9002 + 9006 (ESCALATED-own is HR-verifiable; instead use an already-terminal id).
        SIMPLEST deterministic path: drive bulk via apiAs POST /attendance:bulk-verify with
        ids=[9002, <already-rejected id>] — but seeds are all actionable; so first verify 9002 via apiAs,
        THEN bulk-verify ids=[9002(now terminal), 9003] → assert 200 with succeeded=[9003] and
        failed=[{id:9002, error.code: CONFLICT}]. (Mirrors openapi partial example.)
      - BULK-idempotency-replay: build a fixed key K; call apiAsWithKey(POST /attendance:bulk-verify,
        {ids:[9004]}, K) → 200 succeeded=[9004]; call AGAIN with same K + same body → SAME 200 body
        (replayed, 9004 not double-processed); call with same K + DIFFERENT body → 409 IDEMPOTENCY_KEY_REUSED.
      - BULK-reject-partial: apiAs POST /attendance:bulk-reject {ids:[9003, <terminal>], reason:'bukti tidak sesuai'}
        → 200 partial (succeeded + failed).
      - BULK-all-fail: bulk-verify ids=[<two terminal ids>] → 422 with failed[] and empty succeeded.

    scope-negatives.spec.ts (beforeEach resetDb):
      - SCOPE-403-verify: loginAs(shiftLeader Rudi); waitForToken; apiAs POST /attendance/SWP-ATT-9005:verify
        ({}) → 403 errorCode OUT_OF_SCOPE (CMP-0022 record).
      - SCOPE-403-list: Rudi apiAs GET /attendance?company_id=SWP-CMP-0022&exceptions_only=true → 403 OUT_OF_SCOPE.
      - VERIFY-OWN-403: Rudi apiAs POST /attendance/SWP-ATT-9006:verify ({}) → 403 errorCode VERIFY_OWN_RECORD
        (his own escalated record).
      - HR-global-ok: loginAs(hrAdmin) apiAs GET /attendance?company_id=SWP-CMP-0022 → 200.
      - WINDOW-422 (optional/if reachable): approve a stale-dated PENDING correction as leader → 422
        OUTSIDE_CORRECTION_WINDOW (only if 07-02 seeds an out-of-window correction approvable by leader;
        else assert via the contract test note in SUMMARY — the create path is out of web scope).

    Then run the FULL suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec playwright test e2e/tests/e5/bulk-idempotency.spec.ts e2e/tests/e5/scope-negatives.spec.ts --reporter=line && grep -q "VERIFY_OWN_RECORD" e2e/tests/e5/scope-negatives.spec.ts && grep -q "IDEMPOTENCY_KEY_REUSED" e2e/tests/e5/bulk-idempotency.spec.ts && grep -q "succeeded" e2e/tests/e5/bulk-idempotency.spec.ts && pnpm e2e --reporter=line && echo E5_FULL_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `bulk-idempotency.spec.ts` asserts a partial-success envelope ({succeeded}+{failed}), idempotency replay (same key+body → same body; same key+different body → 409 IDEMPOTENCY_KEY_REUSED), and a 422 all-fail case.
    - `scope-negatives.spec.ts` asserts 403 OUT_OF_SCOPE (verify + list cross-company) and 403 VERIFY_OWN_RECORD, plus HR-global 200.
    - `pnpm e2e` is green headless with the new e5/ specs and no e1/e2/e3/e4 regressions.
  </acceptance_criteria>
  <done>Bulk partial-success + idempotency replay + scope/own-record negatives pass; full pnpm e2e green with zero prior-phase regressions.</done>
</task>

</tasks>

<verification>
- `pnpm e2e` green headless; new frontend/e2e/tests/e5/ specs all pass; no e1/e2/e3/e4 regressions.
- reset-db truncates attendance + attendance_corrections (correct FK order).
- Bulk partial-success + idempotency replay (incl. IDEMPOTENCY_KEY_REUSED), VERIFY_OWN_RECORD, OUT_OF_SCOPE all asserted via the real BE.
- Corrections approve mutates target attendance (CORRECTED); reject sets REJECTED.
- E2E selectors anchored on the real e5-attendance components.
</verification>

<success_criteria>
ATT-01 + ATT-02 complete: the built E5 screens (verification queue, detail, corrections) work end-to-end
against the real Go BE with MSW off, and exhaustive Playwright E2E (list/detail, single + bulk verify/reject
with partial success + idempotency, corrections approve/reject, scope 403, VERIFY_OWN_RECORD) is green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-e5-attendance/07-04-SUMMARY.md`.
</output>
