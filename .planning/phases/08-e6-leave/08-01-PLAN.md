---
phase: 08-e6-leave
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/db/migrations/00028_leave_requests.sql
  - backend/db/migrations/00029_leave_quotas.sql
  - backend/db/migrations/00030_leave_approvals.sql
  - backend/db/queries/leave/leave_requests.sql
  - backend/db/queries/leave/leave_quotas.sql
  - backend/db/queries/leave/leave_approvals.sql
  - backend/db/queries/leave/leave_calendar.sql
  - backend/internal/domain/leave/leave.go
  - backend/internal/repository/sqlc/* (generated by make gen)
autonomous: true
requirements: [LVE-01, LVE-02, LVE-03]

must_haves:
  truths:
    - "leave_requests/leave_quotas/leave_approvals tables exist with the openapi-authoritative status enum + quota balance columns"
    - "sqlc generates typed querier methods for all E6 reads/writes; make gen + go build are clean"
    - "domain types model LeaveRequest (timeline/routing/balance_check), LeaveQuota (remaining math), LeaveApproval decision rows"
  artifacts:
    - path: "backend/db/migrations/00028_leave_requests.sql"
      provides: "leave_requests table with status enum PENDING_L1/PENDING_HR/APPROVED/REJECTED/CANCELLED/DRAFT, FKs, SWP-LR id default"
      contains: "PENDING_HR"
    - path: "backend/db/migrations/00029_leave_quotas.sql"
      provides: "leave_quotas table (total/used/pending/period_start/period_end), SWP-LQ id"
      contains: "period_end"
    - path: "backend/db/migrations/00030_leave_approvals.sql"
      provides: "leave_approvals decision-trail table (stage/decision/actor/reason/override)"
      contains: "leave_approvals"
    - path: "backend/internal/domain/leave/leave.go"
      provides: "LeaveRequest/LeaveQuota/LeaveApproval/LeaveTimelineEntry/LeaveCalendarEntry domain structs"
      contains: "type LeaveQuota struct"
  key_links:
    - from: "backend/db/queries/leave/*.sql"
      to: "backend/internal/repository/sqlc"
      via: "make gen (sqlc glob db/queries/*)"
      pattern: "LeaveRequest|LeaveQuota"
---

<objective>
Stand up the E6 leave DATA LAYER: three new migrations (`leave_requests`, `leave_quotas`,
`leave_approvals`), the `backend/db/queries/leave/` sqlc query set the Phase-8 services need,
and the `internal/domain/leave` types. This is the wave-1 foundation — no services, no routes,
no HTTP. Mirror the Phase-6 scheduling 06-01 and Phase-7 attendance 07-01 data-layer plans
EXACTLY (migration → sqlc `make gen` → domain types).

Purpose: every downstream E6 service/handler (08-02) and the INV-3 loop-closer write into these
tables; the openapi status enum + quota balance model are pinned here so they cannot drift.
Output: migrations 00028–00030, `db/queries/leave/*.sql`, generated sqlc, `domain/leave/leave.go`.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-e6-leave/08-CONTEXT.md
@.planning/reference/backend-build-conventions.md
@docs/api/E6-leave/openapi.yaml
@docs/epics/E6-leave/FEATURE.md

<interfaces>
<!-- Authoritative contract shapes the migrations + domain types MUST match (openapi). -->

LeaveStatus enum (openapi schemas.LeaveStatus — AUTHORITATIVE):
  DRAFT | PENDING_L1 | PENDING_HR | APPROVED | REJECTED | CANCELLED

LeaveQuota (openapi schemas.LeaveQuota — required: id, employee_id, leave_type_id, period,
total, used, pending, remaining):
  id (SWP-LQ-*), employee_id, leave_type_id, period (int calendar year),
  period_start/period_end (date), total, used, pending (int),
  remaining = total - used - pending (DERIVED at DTO boundary, NOT stored),
  is_prorated bool, prorate_months int, closed bool,
  last_adjustment {delta,reason,adjusted_by,adjusted_at} nullable,
  last_override {leave_request_id,override_reason,overridden_by,overridden_at} nullable.

LeaveRequest (openapi schemas.LeaveRequest — required: id, employee_id, leave_type_id,
start_date, end_date, duration_days, status, timeline, created_at):
  id (SWP-LR-*), employee_id, employee_name/company_id/company_name/service_line (denorm),
  leave_type_id, leave_type_name, start_date/end_date (date), duration_days (int),
  reason, notes, status (LeaveStatus), delegate_id/name, document_file_id/url,
  backdated bool, clock_in_conflict bool, routing {no_leader, assigned_leader_id/name},
  balance_check {leave_quota_id, period, requested_days, remaining_days_at_check, requires_override},
  timeline[] (LeaveTimelineEntry), schedule_impact[] (ScheduleImpactEntry), created_at/updated_at.

LeaveTimelineEntry (openapi): stage (L1|HR), status (PENDING|APPROVED|REJECTED|OVERRIDE_APPROVED),
  actor_id/name/role, decision (APPROVED|REJECTED|OVERRIDE_APPROVED), decision_note,
  reject_reason, override bool, override_reason, occurred_at.

ScheduleImpactEntry (openapi): schedule_id (SWP-SCH-*), date, prior_status, new_status (LEAVE|UNASSIGNED), clock_in_conflict.

LeaveCalendarEntry (openapi): leave_request_id, employee_id, employee_name, company_id,
  company_name, service_line, leave_type_id, leave_type_code, start_date, end_date, status, delegate_id/name.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations 00028 leave_requests + 00029 leave_quotas + 00030 leave_approvals</name>
  <files>backend/db/migrations/00028_leave_requests.sql, backend/db/migrations/00029_leave_quotas.sql, backend/db/migrations/00030_leave_approvals.sql</files>
  <read_first>
    - backend/db/migrations/00025_approved_leave_days.sql (the E4-owned table this phase writes into; DO NOT rename/collide — leave_request_id there is a denormalized SWP-LR-* display id with NO FK)
    - backend/db/migrations/00026_attendance.sql + 00027_attendance_corrections.sql (Phase-7 migration shape: soft-delete, SWP id DEFAULT, denormalized company_id, status text)
    - backend/db/migrations/00024_schedule_entries.sql (FK style to placements/employees; id DEFAULT 'SWP-SCH-'||swp_next_id('SCH'))
    - backend/internal/platform/ids/ids.go (CONFIRM LeaveRequest="LR" + LeaveQuota="LQ" prefixes already exist — they do; DO NOT re-add)
    - docs/api/E6-leave/openapi.yaml schemas.LeaveStatus (status enum) + schemas.LeaveQuota (columns) + schemas.LeaveRequest (columns)
  </read_first>
  <action>
    Goose Up/Down migrations. Times/dates: leave dates are DATE columns. Mirror 00024/00026 style.

    **00028_leave_requests.sql** — CREATE TABLE leave_requests:
      - id text PRIMARY KEY DEFAULT ('SWP-LR-' || swp_next_id('LR'))   -- reconcile note below
      - employee_id text NOT NULL REFERENCES employees(id)
      - placement_id text REFERENCES placements(id)         -- nullable; resolved at submit (scope source)
      - company_id text REFERENCES client_companies(id)     -- DENORMALIZED for leader-scope queue + calendar (no JOIN)
      - service_line_id text                                -- denormalized service-line slug source (nullable)
      - leave_type_id text NOT NULL REFERENCES leave_types(id)
      - start_date date NOT NULL, end_date date NOT NULL
      - duration_days integer NOT NULL DEFAULT 0
      - reason text, notes text
      - status text NOT NULL DEFAULT 'PENDING_L1'           -- enum lives in app; CHECK constraint:
            CHECK (status IN ('DRAFT','PENDING_L1','PENDING_HR','APPROVED','REJECTED','CANCELLED'))
      - delegate_id text, document_file_id text
      - backdated boolean NOT NULL DEFAULT false
      - clock_in_conflict boolean NOT NULL DEFAULT false
      - no_leader boolean NOT NULL DEFAULT false            -- routing.no_leader (LA-2)
      - assigned_leader_id text                             -- routing.assigned_leader_id (nullable)
      - balance_quota_id text, balance_requested_days integer, balance_remaining_at_check integer, balance_requires_override boolean
            -- balance_check snapshot columns (nullable)
      - created_by text
      - created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now()
      - deleted_at timestamptz
      Indexes: CREATE INDEX leave_requests_company_status_idx ON leave_requests (company_id, status) WHERE deleted_at IS NULL;
               CREATE INDEX leave_requests_employee_idx ON leave_requests (employee_id) WHERE deleted_at IS NULL;
      Down: DROP TABLE IF EXISTS leave_requests;

    **00029_leave_quotas.sql** — CREATE TABLE leave_quotas:
      - id text PRIMARY KEY DEFAULT ('SWP-LQ-' || swp_next_id('LQ'))
      - employee_id text NOT NULL REFERENCES employees(id)
      - leave_type_id text NOT NULL REFERENCES leave_types(id)
      - period integer NOT NULL                             -- calendar year
      - period_start date NOT NULL, period_end date NOT NULL  -- YYYY-01-01 / YYYY-12-31
      - total integer NOT NULL DEFAULT 0
      - used integer NOT NULL DEFAULT 0
      - pending integer NOT NULL DEFAULT 0                  -- soft-reservation; remaining = total-used-pending DERIVED, not stored
      - is_prorated boolean NOT NULL DEFAULT false, prorate_months integer NOT NULL DEFAULT 0
      - closed boolean NOT NULL DEFAULT false
      - last_adjustment jsonb, last_override jsonb           -- nullable embedded objects
      - created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now()
      CONSTRAINT: CREATE UNIQUE INDEX leave_quotas_emp_type_period_uq ON leave_quotas (employee_id, leave_type_id, period);
      Down: DROP TABLE IF EXISTS leave_quotas;

    **00030_leave_approvals.sql** — CREATE TABLE leave_approvals (the decision-trail per FEATURE ER; bigserial PK, no SWP id — mirrors placement_history 00021):
      - id bigserial PRIMARY KEY
      - leave_request_id text NOT NULL REFERENCES leave_requests(id)
      - stage text NOT NULL CHECK (stage IN ('L1','HR'))
      - decision text NOT NULL CHECK (decision IN ('APPROVED','REJECTED','OVERRIDE_APPROVED'))
      - actor_id text, actor_role text
      - decision_note text, reject_reason text
      - is_override boolean NOT NULL DEFAULT false, override_reason text
      - occurred_at timestamptz NOT NULL DEFAULT now()
      Index: CREATE INDEX leave_approvals_request_idx ON leave_approvals (leave_request_id, occurred_at);
      Down: DROP TABLE IF EXISTS leave_approvals;

    **Reconcile the SWP-LR display id (Claude's-Discretion decision, document it):** Phase-6 seeded
    `approved_leave_days.leave_request_id = 'SWP-LR-44210'` as a DISPLAY id with NO FK. The new
    leave_requests.id uses the SAME `swp_next_id('LR')` sequence; that is fine — 44210 was a fixed
    literal, the sequence allocates fresh ids well below it for seeded/created requests. The INV-3
    write-through (08-02) inserts the REAL new leave_requests.id into approved_leave_days, replacing
    the Phase-6 fixture mechanism. Note this in the migration header comment.
  </action>
  <verify>
    <automated>cd backend && grep -q "PENDING_HR" db/migrations/00028_leave_requests.sql && grep -q "period_end" db/migrations/00029_leave_quotas.sql && grep -q "leave_quotas_emp_type_period_uq" db/migrations/00029_leave_quotas.sql && grep -q "OVERRIDE_APPROVED" db/migrations/00030_leave_approvals.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `00028_leave_requests.sql` contains "PENDING_HR" and the full status CHECK list and "SWP-LR-' || swp_next_id('LR')"
    - `00029_leave_quotas.sql` contains "period_end", "pending", and "leave_quotas_emp_type_period_uq"
    - `00030_leave_approvals.sql` contains "leave_approvals" and "OVERRIDE_APPROVED" and "bigserial"
    - Down sections drop all three tables; `cd backend && go build ./...` unaffected (no Go yet)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: sqlc queries db/queries/leave/*.sql + make gen + domain/leave types</name>
  <files>backend/db/queries/leave/leave_requests.sql, backend/db/queries/leave/leave_quotas.sql, backend/db/queries/leave/leave_approvals.sql, backend/db/queries/leave/leave_calendar.sql, backend/internal/domain/leave/leave.go</files>
  <read_first>
    - backend/db/queries/scheduling/schedule_entries.sql (sqlc annotation style: :one/:many/:execrows, sqlc.arg/sqlc.narg, FOR UPDATE re-check, denormalized JOINs to employees/placements/client_companies)
    - backend/db/queries/scheduling/approved_leave_days.sql (the INV-3 read target — 08-02 will ADD an Insert/Delete query here too, but THIS plan adds the leave/ read+write queries)
    - backend/internal/domain/attendance/attendance.go (Phase-7 domain SUBPACKAGE shape; pointers-without-omitempty mapping notes) — create internal/domain/leave/ as a SUBPACKAGE the same way
    - docs/api/CONVENTIONS.md §11 (cursor pagination — keyset on created_at/id)
  </read_first>
  <action>
    Write sqlc-annotated queries. Times back as time.Time, dates as pgtype.Date (08-02 repo converts,
    like Phase-5/6). All denormalized names via LEFT JOIN employees/client_companies/leave_types.

    **leave_requests.sql:**
      - `ListLeaveRequests :many` — keyset cursor (created_at,id) DESC; filters: company_id (narg),
        status (narg), status__in (narg text[] → status = ANY), employee_id (narg), leave_type_id (narg),
        start_date >= / <= (narg), q free-text (ILIKE on employee name + id + reason). LEFT JOIN employees e,
        client_companies c, leave_types lt for employee_name/company_name/leave_type_name/service_line.
        `WHERE deleted_at IS NULL`. LIMIT sqlc.arg(lim).
      - `GetLeaveRequest :one` — by id with the same denormalized JOINs.
      - `GetLeaveRequestForUpdate :one` — `... WHERE id=$1 AND deleted_at IS NULL FOR UPDATE` (state-machine guard).
      - `CreateLeaveRequest :one` — INSERT (omit id → DEFAULT fires) RETURNING all cols (seed/HR-on-behalf path; FE doesn't create but seed + tests may).
      - `UpdateLeaveRequestStatus :one` — UPDATE leave_requests SET status, no_leader, assigned_leader_id,
        clock_in_conflict, balance_* , updated_at=now() WHERE id=$1 RETURNING * (the approval transitions).
      - `CountPendingLeaveDaysForQuota :one` — sum duration_days of this employee+leave_type's
        PENDING_L1/PENDING_HR requests in the period (drives quota.pending recompute on read).
    **leave_quotas.sql:**
      - `ListLeaveQuotas :many` — keyset cursor; filters employee_id/leave_type_id/period/company_id (via
        placement)/include_closed; LEFT JOIN employees for employee_name, leave_types for leave_type_name.
      - `GetLeaveQuota :one` + `GetLeaveQuotaForUpdate :one (FOR UPDATE)`.
      - `FindQuotaForEmployeeTypePeriod :one` — by (employee_id, leave_type_id, period) for the guard.
      - `UpsertLeaveQuota :one` — INSERT ... ON CONFLICT (employee_id,leave_type_id,period) DO UPDATE
        SET total=EXCLUDED.total, is_prorated, prorate_months, updated_at=now() RETURNING * (bulk-grant; does NOT overwrite used).
      - `AdjustLeaveQuotaTotal :one` — UPDATE leave_quotas SET total=total+sqlc.arg(delta), last_adjustment=$json, updated_at=now() WHERE id=$1 RETURNING *.
      - `DeductLeaveQuota :one` / `RestoreLeaveQuota :one` — UPDATE used = used +/- delta RETURNING * (final-approval deduct; cancel restore — restore wired later, deduct used now).
      - `ListActivePlacedEmployeesForGrant :many` — employees with an ACTIVE/EXPIRING placement covering any day of the period (drives employee_ids:["all"] sentinel + pro_rate join-date).
    **leave_approvals.sql:**
      - `InsertLeaveApproval :one` — INSERT (leave_request_id, stage, decision, actor_id, actor_role, decision_note, reject_reason, is_override, override_reason) RETURNING *.
      - `ListLeaveApprovalsForRequest :many` — by leave_request_id ORDER BY occurred_at (timeline source).
    **leave_calendar.sql:**
      - `ListCalendarEntries :many` — leave_requests overlapping a [period/month] date range, scoped by
        company_id (narg) + service_line (narg) + leave_type_id (narg); status filter = APPROVED only when
        show_pending=false else APPROVED+PENDING_L1+PENDING_HR (pass a status text[] narg the service builds).
        LEFT JOIN employees/client_companies/leave_types for the LeaveCalendarEntry denorm fields incl. leave_type_code.

    Run `cd backend && make gen` (sqlc glob picks up db/queries/leave/ automatically). Then `go build ./...`.

    **domain/leave/leave.go** — NEW subpackage `internal/domain/leave`. Structs mirroring the openapi:
      LeaveRequest, LeaveQuota, LeaveApproval, LeaveTimelineEntry, LeaveRouting, BalanceCheck,
      ScheduleImpactEntry, LeaveCalendarEntry, LeaveCalendarClash, LeaveStatus consts
      (LeaveStatusPendingL1="PENDING_L1", etc.), LeaveQuotaAdjustParams, LeaveQuotaBulkGrantParams.
      Add a method `func (q LeaveQuota) Remaining() int { return q.Total - q.Used - q.Pending }`.
      ErrNotFound reuse from internal/domain (pgx.ErrNoRows mapping happens in 08-02 repo).
  </action>
  <verify>
    <automated>cd backend && make gen && go build ./... && grep -q "func (q LeaveQuota) Remaining()" internal/domain/leave/leave.go && grep -q "ListLeaveRequests" db/queries/leave/leave_requests.sql && grep -q "UpsertLeaveQuota" db/queries/leave/leave_quotas.sql && grep -q "InsertLeaveApproval" db/queries/leave/leave_approvals.sql && grep -q "ListCalendarEntries" db/queries/leave/leave_calendar.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `make gen` regenerates `internal/repository/sqlc` with `ListLeaveRequests`, `GetLeaveRequestForUpdate`, `UpsertLeaveQuota`, `AdjustLeaveQuotaTotal`, `DeductLeaveQuota`, `InsertLeaveApproval`, `ListCalendarEntries` methods on the querier
    - `cd backend && go build ./...` and `go vet ./...` exit 0
    - `internal/domain/leave/leave.go` defines LeaveRequest, LeaveQuota (with `Remaining()`), LeaveApproval, LeaveTimelineEntry, LeaveCalendarEntry and the LeaveStatus constants matching the openapi enum
    - NO hand-edits to `internal/repository/sqlc` (generated only)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd backend && make gen && go build ./... && go vet ./...` clean.
- Three migrations apply Up and roll back Down (goose) without error against an ephemeral PG.
- Status enum strings in the migration CHECK + domain consts match openapi schemas.LeaveStatus byte-for-byte.
- LR/LQ id prefixes resolved (already in ids.go; not re-added).
</verification>

<success_criteria>
- leave_requests, leave_quotas, leave_approvals tables exist with the authoritative status enum + quota balance columns + the routing/balance_check snapshot columns.
- The `db/queries/leave/` set covers every read/write 08-02 needs (list/get/for-update/create/transition/quota upsert/adjust/deduct/approval insert/calendar).
- `domain/leave` types model the full openapi LeaveRequest/LeaveQuota/timeline/calendar surface; remaining = total-used-pending is a derived method.
</success_criteria>

<output>
After completion, create `.planning/phases/08-e6-leave/08-01-SUMMARY.md`
</output>
