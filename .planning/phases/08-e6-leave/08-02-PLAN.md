---
phase: 08-e6-leave
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - backend/internal/repository/leave/leave_repo.go
  - backend/internal/repository/leave/quota_repo.go
  - backend/internal/repository/leave/mapping.go
  - backend/internal/service/leave/leave_service.go
  - backend/internal/service/leave/quota_service.go
  - backend/internal/service/leave/calendar_service.go
  - backend/internal/handler/leave/handler.go
  - backend/internal/handler/leave/leave_handler.go
  - backend/internal/handler/leave/quota_handler.go
  - backend/internal/handler/leave/calendar_handler.go
  - backend/internal/handler/leave/dto.go
  - backend/db/queries/scheduling/approved_leave_days.sql
  - backend/internal/repository/scheduling/schedule_repo.go
  - backend/internal/service/scheduling/schedule_service.go
  - backend/internal/server/server.go
  - backend/cmd/api/main.go
  - backend/cmd/seed/seed.go
  - backend/internal/repository/sqlc/* (generated by make gen)
autonomous: true
requirements: [LVE-01, LVE-02, LVE-03]

must_haves:
  truths:
    - "A PENDING_L1 request → leader :approve-l1 → PENDING_HR → HR :approve-final → APPROVED (the two-level state machine), with leave_approvals decision rows + audit + a notify dispatch STUB (TODO Phase-11, no live delivery) each step"
    - "Acting on a terminal/wrong-state request returns the contract 409; a leader acting cross-company returns 403 OUT_OF_SCOPE"
    - "An over-balance annual :approve-final is blocked (422 BALANCE_RECHECK_FAILED with requires_override); :approve-override force-approves with override_reason recorded"
    - "On :approve-final / :approve-override, overlapping schedule_entries are cancelled (DB status='CANCELLED_BY_LEAVE', surfaced to the contract as schedule_impact[].new_status='LEAVE') AND a row is INSERTed into approved_leave_days in the SAME tx (the INV-3 loop-closer)"
    - "Quotas list with remaining=total-used-pending; :adjust refuses total<used; :bulk-grant pro-rates + reports partial success (skips total<used)"
    - "GET /leave-calendar returns entries for the range; show_pending includes PENDING_L1/PENDING_HR"
  artifacts:
    - path: "backend/internal/service/leave/leave_service.go"
      provides: "two-level approval state machine + quota guard + INV-3 side effects + no-leader routing + scope guard"
      contains: "BALANCE_RECHECK_FAILED"
    - path: "backend/internal/service/leave/quota_service.go"
      provides: "quota list/adjust/bulk-grant (partial success, pro_rate, refuse total<used) + QUOTA_EXCEEDED guard"
      contains: "QUOTA_EXCEEDED"
    - path: "backend/db/queries/scheduling/approved_leave_days.sql"
      provides: "InsertApprovedLeaveDay + DeleteApprovedLeaveDaysForRequest (the INV-3 write target)"
      contains: "InsertApprovedLeaveDay"
    - path: "backend/internal/server/server.go"
      provides: "10 E6 routes under RequireRole + Idempotency"
      contains: "/leave-requests/{id}:approve-final"
  key_links:
    - from: "backend/internal/service/leave/leave_service.go"
      to: "approved_leave_days + schedule_entries"
      via: "in-tx InsertApprovedLeaveDay + cancel overlapping entries (status='CANCELLED_BY_LEAVE') on final/override approval"
      pattern: "InsertApprovedLeaveDay"
    - from: "backend/internal/server/server.go"
      to: "leave handler"
      via: "chi routes :approve-l1/:approve-final/:approve-override/:reject + quotas + calendar"
      pattern: "leave-requests/\\{id\\}:approve"
---

<objective>
Implement the E6 service + handler layer for the 10 FE-used endpoints, the INV-3 loop-closer, and
the seed — wave 2, sequential after 08-01. This is the gnarly plan: the two-level approval state
machine, the quota soft-reservation balance model + QUOTA_EXCEEDED guard, the bulk-grant partial
success engine, and the cross-epic INV-3 write-through (cancel overlapping schedule_entries +
INSERT approved_leave_days in the same tx) that fulfils the hand-off Phase-6 documented.

Mirror the Phase-7 attendance slice (07-02) EXACTLY for the state machine / scope / audit-in-tx /
bulk partial-success shape, and the Phase-6 scheduling slice (06-02) for the conflict-detail
envelope + the schedule_entries cancel path. This plan owns the shared server.go / main.go /
seed.go edits.

Purpose: every E6 web screen works against the real BE, and the over-leave conflict engine + the
attendance Absent-suppression now read a REAL production source.
Output: leave repo/service/handler, the approved_leave_days write queries, routes, wiring, seed.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-e6-leave/08-CONTEXT.md
@.planning/reference/backend-build-conventions.md
@.planning/phases/08-e6-leave/08-01-SUMMARY.md
@docs/api/E6-leave/openapi.yaml
@docs/api/CONVENTIONS.md
@docs/epics/E6-leave/FEATURE.md
@docs/epics/E6-leave/prds/leave-approval.md
@docs/epics/E6-leave/prds/leave-quota-balances.md
@docs/epics/E6-leave/prds/leave-schedule-integration.md

<interfaces>
<!-- E6 error codes (openapi ErrorDetail) the service MUST emit with EXACT codes + statuses: -->
  QUOTA_EXCEEDED (422)        — fields.requested_days, fields.remaining_days   (submit/guard path)
  BALANCE_RECHECK_FAILED (422)— fields.requires_override:true, requested_days, remaining_days (final re-check)
  OVERLAPPING_LEAVE (409)     — fields.conflicting_leave_request_id
  INVALID_DATE_RANGE (422)    — fields.end_date
  OUT_OF_SCOPE (403)          — leader acting on non-own-company resource
  RULE_VIOLATION (422)        — quota :adjust would set total<used (fields.delta); bulk-grant row failure
  NO_LEADER_ASSIGNED          — NOT a 4xx; carried as routing.no_leader:true on 200/201

<!-- Phase-6 schedule cancel + approved_leave_days targets the INV-3 write-through uses: -->
  schedule_entries.status is CHECK-constrained to ('SCHEDULED','CANCELLED_BY_LEAVE','MODIFIED')
    (migration 00024) — NEVER write 'LEAVE' to the DB. The INV-3 cancel sets
    status='CANCELLED_BY_LEAVE' for overlapping live entries on the leave dates.
  DB↔DTO boundary mapping: the DB value 'CANCELLED_BY_LEAVE' maps to the openapi
    ScheduleImpactEntry.new_status='LEAVE' (enum [LEAVE, UNASSIGNED]) — the 'LEAVE' string lives
    ONLY at the API/contract layer, not in the column.
  approved_leave_days(employee_id, leave_date, leave_request_id, leave_type) — UNIQUE(employee_id,leave_date);
    Phase-6 read query FindApprovedLeaveForAgentDate drives SHIFT_OVER_LEAVE — THIS is what we populate.

<!-- Phase-7 platform seams to reuse: rbac.GuardCompany, audit.Record(ctx,tx,...), db.TxManager.InTx,
     apperr.Conflict()/ConflictWithDetails()/Rule()/struct-literal, httpx cursor, jobs notify stub. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repository + INV-3 write queries (leave repo/quota repo + approved_leave_days insert/delete)</name>
  <files>backend/internal/repository/leave/leave_repo.go, backend/internal/repository/leave/quota_repo.go, backend/internal/repository/leave/mapping.go, backend/db/queries/scheduling/approved_leave_days.sql, backend/internal/repository/scheduling/schedule_repo.go</files>
  <read_first>
    - backend/internal/repository/attendance/attendance_repo.go + mapping.go (Phase-7 repo port impl over sqlc; pgx.ErrNoRows→domain.ErrNotFound; pgtype.Date↔time.Time; jsonb↔[]byte; writes take pgx.Tx)
    - backend/internal/repository/scheduling/schedule_repo.go (the FindApprovedLeaveForAgentDate impl + how schedule queries map; ADD the cancel + insert methods here)
    - backend/db/queries/scheduling/approved_leave_days.sql (ADD InsertApprovedLeaveDay + DeleteApprovedLeaveDaysForRequest)
    - backend/db/queries/scheduling/schedule_entries.sql (the ListSchedule/Update shape — for the cancel query)
    - backend/db/migrations/00024_schedule_entries.sql (the status CHECK constraint — allowed status values are SCHEDULED|CANCELLED_BY_LEAVE|MODIFIED — never write 'LEAVE' to the DB)
    - backend/internal/domain/leave/leave.go (08-01 domain types this repo maps to)
  </read_first>
  <action>
    **db/queries/scheduling/approved_leave_days.sql — ADD two write queries (this table is E4-owned; E6 writes into it):**
      - `InsertApprovedLeaveDay :exec` — INSERT INTO approved_leave_days (employee_id, leave_date, leave_request_id, leave_type)
        VALUES (...) ON CONFLICT (employee_id, leave_date) DO UPDATE SET leave_request_id=EXCLUDED.leave_request_id, leave_type=EXCLUDED.leave_type;
      - `DeleteApprovedLeaveDaysForRequest :execrows` — DELETE FROM approved_leave_days WHERE leave_request_id = sqlc.arg(leave_request_id); (cancel/shorten restore — used later; add now).
    **db/queries/scheduling/schedule_entries.sql — ADD a cancel-overlap query:**
      - `CancelScheduleEntriesForLeave :many` — UPDATE schedule_entries SET status='CANCELLED_BY_LEAVE', updated_at=now()
        WHERE employee_id=sqlc.arg(employee_id) AND work_date BETWEEN sqlc.arg(start_date)::date AND sqlc.arg(end_date)::date
        AND deleted_at IS NULL AND status <> 'CANCELLED_BY_LEAVE'
        RETURNING id, work_date, status;   -- status='CANCELLED_BY_LEAVE' is the ONLY value the schedule_entries
        -- CHECK (migration 00024) permits for this transition; 'LEAVE' is the DTO value, NOT a DB value.
        -- RETURNING drives schedule_impact[] (id+date+new DB status; service maps DB→DTO new_status='LEAVE').
        -- The `status <> 'CANCELLED_BY_LEAVE'` guard makes the cancel idempotent across re-runs.
    Run `cd backend && make gen`.

    **repository/scheduling/schedule_repo.go** — implement two new port methods (called by the leave service inside its tx):
      - `InsertApprovedLeaveDay(ctx, tx pgx.Tx, employeeID string, date time.Time, leaveRequestID, leaveType string) error`
      - `CancelScheduleEntriesForLeave(ctx, tx pgx.Tx, employeeID string, start, end time.Time) ([]ScheduleImpact, error)` → returns {ScheduleID, Date} for schedule_impact (carrying the DB status 'CANCELLED_BY_LEAVE'; the service maps it to the DTO new_status='LEAVE').
      Expose them on a small interface the leave service imports (define the interface in service/leave, satisfied here — avoid import cycle by putting the port in service/leave).

    **repository/leave/leave_repo.go + quota_repo.go + mapping.go** — implement the leave service ports over the
    08-01 sqlc methods: List/Get/GetForUpdate/Create/UpdateStatus leave requests; List/Get/GetForUpdate/Find/
    Upsert/AdjustTotal/Deduct quotas; InsertLeaveApproval + ListLeaveApprovalsForRequest; ListCalendarEntries;
    CountPendingLeaveDaysForQuota; ListActivePlacedEmployeesForGrant. Map sqlc rows → domain/leave types in
    mapping.go (pgtype.Date→time.Time; jsonb last_adjustment/last_override→struct; pgx.ErrNoRows→domain.ErrNotFound;
    pointers-without-omitempty for required-nullable openapi fields per Phase-7 note).
  </action>
  <verify>
    <automated>cd backend && make gen && go build ./... && grep -q "InsertApprovedLeaveDay" db/queries/scheduling/approved_leave_days.sql && grep -q "CancelScheduleEntriesForLeave" db/queries/scheduling/schedule_entries.sql && grep -q "CANCELLED_BY_LEAVE" db/queries/scheduling/schedule_entries.sql && grep -q "InsertApprovedLeaveDay" internal/repository/scheduling/schedule_repo.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `approved_leave_days.sql` contains "InsertApprovedLeaveDay" and "DeleteApprovedLeaveDaysForRequest"; `schedule_entries.sql` contains "CancelScheduleEntriesForLeave" setting `status='CANCELLED_BY_LEAVE'` (NOT 'LEAVE') with the `status <> 'CANCELLED_BY_LEAVE'` idempotency guard
    - `make gen && go build ./...` clean; sqlc gains the three new querier methods
    - `repository/scheduling/schedule_repo.go` implements InsertApprovedLeaveDay + CancelScheduleEntriesForLeave taking `pgx.Tx`
    - `repository/leave/*` implements all leave/quota/approval/calendar ports; mapping maps pgx.ErrNoRows→domain.ErrNotFound
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Leave service — two-level approval state machine + quota guard + INV-3 side effects + quota/calendar services</name>
  <files>backend/internal/service/leave/leave_service.go, backend/internal/service/leave/quota_service.go, backend/internal/service/leave/calendar_service.go</files>
  <read_first>
    - backend/internal/service/attendance/attendance_service.go + correction_service.go (Phase-7 state machine: *ForUpdate guard → terminal/wrong-state 409 CONFLICT(fields.status); VERIFY-style self-guard 403; bulk loop-single → {succeeded,failed}; audit.Record in InTx; notify stub)
    - backend/internal/service/scheduling/schedule_service.go + conflict_engine.go (06-02: InTx side-effect pattern; ConflictWithDetails; the approved_leave_days/schedule cancel targets; the CANCELLED_BY_LEAVE/SHIFT_OVER_LEAVE pairing)
    - backend/db/migrations/00024_schedule_entries.sql (the status CHECK constraint — allowed status values are SCHEDULED|CANCELLED_BY_LEAVE|MODIFIED — never write 'LEAVE' to the DB; 'LEAVE' is the DTO-only boundary value)
    - backend/internal/service/placement/* (05-02: rbac.GuardCompany scope, ConflictWithDetails)
    - docs/epics/E6-leave/prds/leave-approval.md (BR-#, Gherkin AC: LA-1/2/4/5/6/8) + leave-quota-balances.md (LQ-1/2/5/6/8) + leave-schedule-integration.md (LI-1/2 — INV-3)
    - docs/api/E6-leave/openapi.yaml :approve-l1/:approve-final/:approve-override/:reject + :adjust/:bulk-grant + /leave-calendar (status transitions, request/response bodies, error codes/statuses; ScheduleImpactEntry.new_status enum [LEAVE, UNASSIGNED])
  </read_first>
  <behavior>
    State machine (LeaveService):
    - Test approve-l1: PENDING_L1 + caller=assigned leader (GuardCompany own-company) → status becomes PENDING_HR; leave_approvals row {stage:L1, decision:APPROVED}; no quota deducted.
    - Test approve-l1 wrong state: status=PENDING_HR/APPROVED → 409 CONFLICT.
    - Test approve-l1 cross-company leader → 403 OUT_OF_SCOPE.
    - Test approve-l1 self-approve (caller.employee_id == request.employee_id) → 403 FORBIDDEN, stays PENDING_L1.
    - Test approve-final: PENDING_HR + HR → re-check balance; if duration_days <= remaining → APPROVED, used += duration_days, leave_approvals {stage:HR, decision:APPROVED}, INV-3 side effects fire.
    - Test approve-final over-balance: duration_days > remaining → 422 BALANCE_RECHECK_FAILED, fields.requires_override=true, NO state change.
    - Test approve-override: PENDING_HR + HR + override_reason(min10) → APPROVED even over-balance (used += d, remaining may go negative), leave_approvals {decision:OVERRIDE_APPROVED, is_override:true}, last_override set on quota, INV-3 side effects fire.
    - Test reject: PENDING_L1 or PENDING_HR + reason(min5) → REJECTED, leave_approvals {decision:REJECTED, reject_reason}; terminal → 409.
    - Test INV-3 (loop-closer): on final/override, overlapping schedule_entries → DB status='CANCELLED_BY_LEAVE' (RETURNING drives schedule_impact[]; service maps DB 'CANCELLED_BY_LEAVE' → DTO new_status='LEAVE') AND approved_leave_days rows INSERTed for each leave date, ALL in the same tx as the status update + quota deduct + audit + leave_approvals.
    QuotaService:
    - Test list: remaining = total - used - pending (pending recomputed from open PENDING_* requests).
    - Test adjust total<used → 422 RULE_VIOLATION fields.delta; valid delta → total adjusted, last_adjustment recorded, audited.
    - Test bulk-grant: pro_rate=true → total = round_half_up(entitlement * remaining_months/12) for probation/mid-year; does NOT overwrite used; rows where new total<used reported in failed[] (partial success 200 if >=1 succeeded); preview=true writes nothing.
  </behavior>
  <action>
    Build the three services. Inject the schedule/approved_leave_days port (Task 1) into LeaveService for INV-3.

    **LeaveService** methods ApproveL1 / ApproveFinal / ApproveOverride / Reject (id, principal, body), Get, List.
    Each action: `repo.GetLeaveRequestForUpdate` under InTx → guard state (return apperr.Conflict("...") with the
    contract code on wrong/terminal state) → scope guard (rbac.GuardCompany on request.company_id for shift_leader;
    OUT_OF_SCOPE via apperr struct-literal HTTPStatus:403 code "OUT_OF_SCOPE") → self-approve guard (403 FORBIDDEN) →
    apply transition via UpdateLeaveRequestStatus → InsertLeaveApproval row → audit.Record(ctx,tx,...) → notify
    dispatch STUB (TODO Phase-11 — record intent only, NO live delivery). ApproveL1 = PENDING_L1→PENDING_HR.
    ApproveFinal/Override = PENDING_HR→APPROVED + the side-effects:
      1. balance re-check: find quota (employee_id, leave_type_id, period from start_date year). The quota-tracked
         flag resolves to the real `leave_types.is_annual` column (do NOT invent is_quota_tracked). For final, if
         is_annual && duration_days > remaining → apperr.Rule("BALANCE_RECHECK_FAILED") with
         fields{requires_override:true, requested_days, remaining_days}, abort tx. Override skips this.
      2. DeductLeaveQuota(used += duration_days) (quota-tracked only); override sets last_override on the quota.
      3. **INV-3:** `scheduleRepo.CancelScheduleEntriesForLeave(tx, employee_id, start, end)` → sets DB
         status='CANCELLED_BY_LEAVE' (NOT 'LEAVE' — the schedule_entries CHECK rejects 'LEAVE'), collect the
         returned rows; MAP each to the contract schedule_impact[] entry with new_status='LEAVE' (the openapi
         ScheduleImpactEntry boundary value, enum [LEAVE, UNASSIGNED]); then for EACH leave date in [start,end]
         `scheduleRepo.InsertApprovedLeaveDay(tx, employee_id, date, request.id, leave_type_code)`. Same tx — if
         anything fails, the whole approval rolls back.
    Set clock_in_conflict + schedule_impact (DTO new_status='LEAVE') on the returned LeaveRequest DTO.
    **Quota guard (QUOTA_EXCEEDED)** — exported `CheckQuota(quota, requestedDays)` seam used by the submit/guard path:
      if is_annual && requestedDays > remaining → apperr.Rule("QUOTA_EXCEEDED") fields{requested_days, remaining_days}.

    **QuotaService** ListQuotas (recompute pending via CountPendingLeaveDaysForQuota; remaining = total-used-pending),
    AdjustQuota (refuse total+delta < used → 422 RULE_VIOLATION fields.delta; else AdjustLeaveQuotaTotal + last_adjustment
    + audit), BulkGrant (resolve employee set incl. ["all"] sentinel via ListActivePlacedEmployeesForGrant; per-employee:
    compute pro-rated total when pro_rate; UpsertLeaveQuota; if new total < existing used → push to failed[] with
    RULE_VIOLATION; preview=true → compute only, no writes; return {preview, total_affected, succeeded[], failed[]}).

    **CalendarService** GetCalendar (params: company_id/service_line/leave_type_id/period/month/show_pending; build the
    status text[] = APPROVED [+ PENDING_L1, PENDING_HR when show_pending]; GuardCompany own-company for shift_leader;
    ListCalendarEntries; compute service-line-aware clashes = (date,company,service_line) with >=2 same-line agents off).
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/service/leave/... 2>&1 | tail -5; grep -q "BALANCE_RECHECK_FAILED" internal/service/leave/leave_service.go && grep -q "QUOTA_EXCEEDED" internal/service/leave/quota_service.go && grep -q "InsertApprovedLeaveDay" internal/service/leave/leave_service.go && grep -q "OUT_OF_SCOPE" internal/service/leave/leave_service.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `leave_service.go` contains "BALANCE_RECHECK_FAILED", "OUT_OF_SCOPE", and "InsertApprovedLeaveDay" (the INV-3 in-tx write); the INV-3 cancel writes DB status='CANCELLED_BY_LEAVE' and maps it to DTO new_status='LEAVE' (never writes 'LEAVE' to the column)
    - `quota_service.go` contains "QUOTA_EXCEEDED" and "RULE_VIOLATION"; remaining computed as total-used-pending; quota-tracked gate reads `leave_types.is_annual`
    - The state machine uses `GetLeaveRequestForUpdate` under InTx and returns the contract 409 on terminal/wrong-state
    - `go build ./...` + `go vet ./...` clean; service unit tests (if added) pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Handlers + routes + main.go wiring + seed extension</name>
  <files>backend/internal/handler/leave/handler.go, backend/internal/handler/leave/leave_handler.go, backend/internal/handler/leave/quota_handler.go, backend/internal/handler/leave/calendar_handler.go, backend/internal/handler/leave/dto.go, backend/internal/server/server.go, backend/cmd/api/main.go, backend/cmd/seed/seed.go</files>
  <read_first>
    - backend/internal/handler/attendance/handler.go + attendance_handler.go + attendance_dto.go (Phase-7 hand-written chi handler shape; decodeOptionalJSON; httpx.WriteJSON/WriteError; bulk 200/422 split; DTO snake_case + required-nullable pointers without omitempty)
    - backend/internal/server/server.go (the ATTENDANCE slice block + its COORDINATION POINT comment — append the LEAVE slice AFTER it; reuse rbac.RequireRole + d.Idempotency.Handler)
    - backend/cmd/api/main.go (how attendance/scheduling Deps are constructed + injected into server.Deps; the scheduling repo is already built — REUSE it for the leave service's INV-3 port)
    - backend/cmd/seed/seed.go (seedScheduling @ ~line 1000 + seedAttendance; mondayOfCurrentWeek helper; SWP-PL-5001..5004 placements + SWP-CMP-0021/0022 + SWP-SLA-3001 leader; add seedLeave AFTER seedScheduling so schedule_entries exist to overlap)
    - docs/api/E6-leave/openapi.yaml (the 10 op request/response bodies + x-rbac.roles per op)
  </read_first>
  <action>
    **Handlers** (hand-written chi, mirror attendance): NewHandler(leaveSvc, quotaSvc, calendarSvc). Methods:
      ListLeaveRequests, GetLeaveRequest, ApproveLeaveRequestL1, ApproveLeaveRequestFinal,
      ApproveLeaveRequestOverride (override_reason required, min10), RejectLeaveRequest (reason required, min5),
      ListLeaveQuotas, AdjustLeaveQuota (delta+reason required), BulkGrantLeaveQuotas, GetLeaveCalendar.
      Decode → validate → service → httpx.WriteJSON; apperr via httpx.WriteError. Cursor lists via httpx.PageResponse.
      DTOs in dto.go: snake_case, required-nullable openapi fields as pointers WITHOUT omitempty (serialize null);
      LeaveRequest DTO emits routing{no_leader,...}, balance_check{...}, timeline[] (built from leave_approvals +
      implicit pending marker), schedule_impact[] (new_status='LEAVE' per the openapi ScheduleImpactEntry boundary
      value — this is the DTO surface for the DB 'CANCELLED_BY_LEAVE' status). Build the timeline EXACTLY as the FE
      detail screen consumes it (no-leader → single HR-stage entry, L1 absent).

    **server.go** — append a LEAVE slice block after the ATTENDANCE slice COORDINATION POINT:
      - Reads + L1 approve + reject + calendar (shift_leader scope enforced in service via GuardCompany):
        Group RequireRole(SuperAdmin, HRAdmin, ShiftLeader):
          r.Get("/leave-requests", ...); r.Get("/leave-requests/{id}", ...);
          r.With(Idempotency).Post("/leave-requests/{id}:approve-l1", ...);
          r.With(Idempotency).Post("/leave-requests/{id}:reject", ...);
          r.Get("/leave-quotas", ...);            // list visible to leader (own-company)
          r.Get("/leave-calendar", ...);
      - HR/super only (final/override + quota writes):
        Group RequireRole(SuperAdmin, HRAdmin):
          r.With(Idempotency).Post("/leave-requests/{id}:approve-final", ...);
          r.With(Idempotency).Post("/leave-requests/{id}:approve-override", ...);
          r.With(Idempotency).Post("/leave-quotas/{id}:adjust", ...);
          r.With(Idempotency).Post("/leave-quotas:bulk-grant", ...);
      Add `Leave *leavehttp.Handler` to server.Deps. NOTE chi static-vs-param: register {id}:approve-* and
      :bulk-grant carefully (action-suffix routes match natively per Phase-2 decision).

    **main.go** — construct leave repo (leave + quota), the three services (inject the EXISTING scheduling repo as
      the INV-3 port), build leavehttp.Handler, set Deps.Leave.

    **seed.go — seedLeave** (call AFTER seedScheduling so schedule_entries SWP-SCH-6001/6002 exist; idempotent ON CONFLICT):
      Anchor dates on mondayOfCurrentWeek (clearly-in-range Asia/Jakarta dates), period = current year.
      leave_quotas (annual SWP-LT-001, calendar year):
        - SWP-LQ-... for Dewi (EMP-3001): total 12, used 4, pending 0 → remaining 8 (final-approve target)
        - SWP-LQ-... for Budi (EMP-2891): total 12, used 11, pending 0 → remaining 1 (near-exhausted → QUOTA_EXCEEDED/override target)
      leave_requests (web approval targets — all seeded in Pending, FE/mobile-create out of scope):
        - SWP-LR-8001  Dewi (EMP-3001) @ CMP-0021, PENDING_L1, 1 day on (monday+4 Friday) → Rudi L1 target
        - SWP-LR-8002  Dewi (EMP-3001) @ CMP-0021, PENDING_HR (leader-approved), 1 day → HR final target; seed a leave_approvals {L1,APPROVED} row
        - SWP-LR-8003  Budi (EMP-2891) @ CMP-0022, PENDING_HR, 3 days, duration_days 3 vs remaining 1 → BALANCE_RECHECK/override target (no leader at CMP-0022 → no_leader=true, PENDING_HR direct)
        - SWP-LR-8004  Budi (EMP-2891) @ CMP-0022, PENDING_L1-style but routed PENDING_HR — actually use as the leader OUT_OF_SCOPE target: a PENDING_L1 request whose company is CMP-0022 so Rudi (leads CMP-0021) gets 403 on :approve-l1
        - SWP-LR-8005  Dewi (EMP-3001), APPROVED terminal (list-filter + calendar render)
        - SWP-LR-8006  Dewi (EMP-3001), REJECTED terminal (list filter)
        - **SWP-LR-8007 (the INV-3 target):** Dewi (EMP-3001) @ CMP-0021, PENDING_HR, dates OVERLAPPING the seeded
          schedule entry SWP-SCH-6002 (work_date = monday+2 Wednesday) — i.e. start_date=end_date=monday+2 — so
          HR :approve-final sets SWP-SCH-6002 → status='CANCELLED_BY_LEAVE' (DB) / new_status='LEAVE' (DTO) AND
          inserts an approved_leave_days row, and a fresh E4 schedule attempt that day then hits SHIFT_OVER_LEAVE
          from the real source. duration_days vs Dewi's remaining 8 → passes (clean final approval).
      For SWP-LR-8002/8007 ensure status=PENDING_HR and quota.pending reflects them if needed.
  </action>
  <verify>
    <automated>cd backend && make gen && go build ./... && go vet ./... && grep -q "/leave-requests/{id}:approve-final" internal/server/server.go && grep -q "/leave-quotas:bulk-grant" internal/server/server.go && grep -q "/leave-calendar" internal/server/server.go && grep -q "SWP-LR-8007" cmd/seed/seed.go && grep -q "seedLeave" cmd/seed/seed.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `server.go` registers all 10 E6 routes: `/leave-requests` (GET + {id} GET + 4 action posts), `/leave-quotas` (GET + {id}:adjust + :bulk-grant), `/leave-calendar` (GET); final/override/quota-writes under RequireRole(SuperAdmin,HRAdmin)
    - `main.go` builds the leave handler injecting the existing scheduling repo as the INV-3 port; `go build ./...` clean
    - `seed.go` adds `seedLeave` with SWP-LR-8001..8007 + leave_quotas; SWP-LR-8007 overlaps SWP-SCH-6002; SWP-LR-8003 near-exhausted-quota target; SWP-LR-8004 leader-OUT_OF_SCOPE target
    - `make gen && go build ./... && go vet ./... && gofmt -l` clean
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd backend && make gen && go build ./... && go vet ./...` clean; `make verify` green.
- The 10 E6 routes resolve under the authenticated group with the correct RequireRole + Idempotency.
- Approval state machine + INV-3 side-effects + quota math match the openapi codes/statuses byte-for-shape;
  the INV-3 cancel writes DB status='CANCELLED_BY_LEAVE' and surfaces new_status='LEAVE' only at the DTO boundary.
- seedLeave creates the full target set including the INV-3 overlap row (SWP-LR-8007 vs SWP-SCH-6002).
</verification>

<success_criteria>
- L1→final approval transitions work with leave_approvals rows + audit + notify dispatch STUB (TODO Phase-11, no live delivery); terminal/wrong-state → 409; cross-company leader → 403 OUT_OF_SCOPE.
- Over-balance final → 422 BALANCE_RECHECK_FAILED(requires_override); override force-approves with reason recorded.
- final/override cancel overlapping schedule_entries (DB status='CANCELLED_BY_LEAVE', DTO new_status='LEAVE') AND INSERT approved_leave_days in the same tx.
- Quotas list (remaining=total-used-pending), adjust (refuse total<used), bulk-grant (pro_rate + partial success).
- /leave-calendar returns range entries; show_pending includes pending.
</success_criteria>

<output>
After completion, create `.planning/phases/08-e6-leave/08-02-SUMMARY.md`
</output>
</content>
</invoke>
