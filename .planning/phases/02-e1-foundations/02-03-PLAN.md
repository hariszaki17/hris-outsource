---
phase: 02-e1-foundations
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - backend/internal/handler/foundations/handler_test.go
autonomous: true
requirements: [FND-01, FND-02, FND-03]
must_haves:
  truths:
    - "A Go contract test asserts every E1 endpoint's response JSON matches the openapi shape (exact snake_case field names, status codes)"
    - "RBAC negative cases assert agent/shift_leader receive 403 FORBIDDEN on E1 endpoints"
    - "List endpoints assert the cursor envelope {data, next_cursor, has_more} and that paging advances"
    - "go test ./internal/handler/foundations/... passes"
  artifacts:
    - path: "backend/internal/handler/foundations/handler_test.go"
      provides: "table-driven contract tests for users mgmt, audit-log, settings, incl. RBAC 403 and cursor envelope"
      min_lines: 250
  key_links:
    - from: "handler_test.go"
      to: "foundations handler + service + fake repo"
      via: "httptest server with a real Service wired to a fakeRepo (no DB), seeded principal in context"
      pattern: "httptest"
---

<objective>
Write the Go contract tests for the E1 Foundations handlers — the drift gate that replaces
server-side OpenAPI codegen. Assert exact response shapes (field names, types, status codes),
RBAC negative cases (403 for non-admin roles), and the cursor pagination envelope, all against
the openapi.yaml examples.

Purpose: This guarantees the BE responses match the spec the FE client is generated from, so the
wave-3 E2E (and the generated @swp/api-client) never silently drift. Mirrors the existing auth
contract-test pattern (httptest + Service + fakeRepo, no DB).
Output: backend/internal/handler/foundations/handler_test.go.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-e1-foundations/02-CONTEXT.md
@.planning/reference/backend-build-conventions.md
@docs/api/E1-foundations/openapi.yaml
@docs/epics/E1-foundations/prds/rbac-roles.md
@docs/epics/E1-foundations/prds/audit-log.md
@.planning/phases/02-e1-foundations/02-02-SUMMARY.md

<interfaces>
<!-- Reference test pattern the executor copies verbatim. -->

Reference: backend/internal/handler/identity/handler_test.go (read it fully). It shows:
  - package identity_test (external test package).
  - fakeTxRunner{} implementing InTx(ctx, fn) error -> fn(nil).
  - fakeRepo with in-memory maps implementing the service Repository port.
  - Building a chi router, mounting handlers, hitting them with httptest.NewRequest +
    httptest.NewRecorder, decoding the JSON body, asserting exact field presence/values.

To inject an authenticated principal in tests, set it on the request context the same way the
auth middleware does — read backend/internal/platform/auth/context.go for the context-key setter
(e.g. auth.WithPrincipal(ctx, auth.Principal{...}) or the equivalent helper) and how
auth.PrincipalFrom reads it. For RBAC 403 tests, wrap the route with
rbac.RequireRole(auth.RoleSuperAdmin, auth.RoleHRAdmin) and inject an agent/shift_leader principal.

The foundations service Repository PORT (defined in service/foundations/service.go from 02-02)
is the interface your fakeRepo must satisfy — read that interface and implement each method with
in-memory data.

Spec examples to assert against (docs/api/E1-foundations/openapi.yaml):
  listUsers firstPage (lines ~459-497): data[] of User {id,email,role,status,employee_id,full_name,
    company_id,company_name,last_login_at,created_at,updated_at}, next_cursor, has_more.
  createUser 201 -> User; 409 emailTaken {error.code: CONFLICT} / employeeAlreadyHasUser.
  changeUserRole 422 roleNotAllowed {error.code: ROLE_NOT_ALLOWED}.
  deactivate/reactivate 409 (already disabled/active).
  sendUserPasswordReset 202 {message}.
  listAuditLog firstPage (lines ~1016-1048): data[] of AuditLogEntrySummary, next_cursor, has_more.
  getAuditLogEntry 200 -> AuditLogEntry (before, after, request_id present).
  getPlatformSettings 200 -> PlatformSettings (7 keys, each {value,label,locked}).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: contract tests for users management + RBAC negatives</name>
  <read_first>
    - backend/internal/handler/identity/handler_test.go (full pattern: fakeRepo, fakeTxRunner, httptest)
    - backend/internal/service/foundations/service.go (the Repository port to fake; service constructor)
    - backend/internal/handler/foundations/handler.go + dto.go (response field names to assert)
    - backend/internal/platform/auth/context.go (principal context setter for injecting roles)
  </read_first>
  <files>backend/internal/handler/foundations/handler_test.go</files>
  <action>
    Create backend/internal/handler/foundations/handler_test.go in package foundations_test.
    Build a fakeRepo (in-memory) satisfying the foundations service Repository port, a fakeTxRunner
    (InTx -> fn(nil)), and a helper that mounts the foundations handlers on a chi router wrapped with
    rbac.RequireRole(super_admin, hr_admin), with a middleware that injects a configurable principal.

    Write table-driven tests:
      TestListUsers_ShapeAndEnvelope: seed 3 users; GET /users as hr_admin; assert 200, body has keys
        "data","next_cursor","has_more"; data[0] has exactly the User keys (assert presence of
        id,email,role,status,employee_id,full_name,company_id,company_name,last_login_at,created_at,
        updated_at via a map[string]json.RawMessage decode); status value is "ACTIVE" (UPPER).
      TestListUsers_CursorAdvances: seed enough users that limit=2 yields has_more=true and a
        non-null next_cursor; second request with that cursor returns the next page (different ids).
      TestCreateUser_201: POST /users valid body as hr_admin -> 201, Location header set, body is a User.
      TestCreateUser_409_EmailTaken: pre-seed a user with the same email -> 409, error.code "CONFLICT".
      TestChangeUserRole_422_RoleNotAllowed: POST :change-role with an invalid new_role -> 422,
        error.code "ROLE_NOT_ALLOWED".
      TestDeactivateReactivate: :deactivate -> 200 status DISABLED; :deactivate again -> 409;
        :reactivate -> 200 status ACTIVE; :reactivate again -> 409.
      TestSendPasswordReset_202: POST :send-password-reset -> 202 with a "message" field.
      TestRBAC_NonAdmin_403: for each of GET /users, POST /users, GET /audit-log, GET /platform/settings,
        inject an agent principal and assert 403 with error.code "FORBIDDEN"; repeat with shift_leader.
  </action>
  <acceptance_criteria>
    - backend/internal/handler/foundations/handler_test.go exists, package foundations_test, contains funcs "TestListUsers_ShapeAndEnvelope", "TestListUsers_CursorAdvances", "TestCreateUser_201", "TestCreateUser_409_EmailTaken", "TestChangeUserRole_422_RoleNotAllowed", "TestDeactivateReactivate", "TestSendPasswordReset_202", "TestRBAC_NonAdmin_403".
    - grep -c "FORBIDDEN" backend/internal/handler/foundations/handler_test.go returns >= 1.
    - grep "next_cursor" backend/internal/handler/foundations/handler_test.go returns a match.
    - `cd backend && go test ./internal/handler/foundations/... -run 'TestListUsers|TestCreateUser|TestChangeUserRole|TestDeactivateReactivate|TestSendPasswordReset|TestRBAC' -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handler/foundations/... -run 'TestListUsers|TestCreateUser|TestChangeUserRole|TestDeactivateReactivate|TestSendPasswordReset|TestRBAC' -count=1 exits 0</automated>
  </verify>
  <done>User-management + RBAC contract tests pass, asserting exact shapes, status codes, and the cursor envelope.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: contract tests for audit-log + platform settings</name>
  <read_first>
    - backend/internal/handler/foundations/handler.go + dto.go (audit + settings response field names)
    - docs/api/E1-foundations/openapi.yaml lines 942-1195 (AuditLogEntrySummary, AuditLogEntry, PlatformSettings)
  </read_first>
  <files>backend/internal/handler/foundations/handler_test.go</files>
  <action>
    Extend backend/internal/handler/foundations/handler_test.go with:
      TestListAuditLog_ShapeAndFilters: seed several audit entries (varied entity_type/actor); GET
        /audit-log as hr_admin -> 200; assert envelope {data,next_cursor,has_more}; data[0] has the
        AuditLogEntrySummary keys (id, actor_user_id, actor_label, action, entity_type, entity_id,
        change_summary, ip, created_at). Then GET /audit-log?entity_type=user and assert only
        entity_type=user rows are returned.
      TestListAuditLog_CursorAdvances: limit=2 -> has_more true + next_cursor; next page differs.
      TestGetAuditLogEntry_200: GET /audit-log/{id} -> 200; body has before, after, request_id keys.
      TestGetAuditLogEntry_404: unknown id -> 404 error.code "NOT_FOUND".
      TestGetPlatformSettings_200: GET /platform/settings -> 200; body has exactly the 7 keys
        (locale,timezone,date_format,currency,version,stack,legacy_data_source); each value object
        has value/label/locked; assert locale.value == "id-ID" and locale.locked == true.
      TestAuditLog_RBAC_403: agent -> GET /audit-log returns 403 (covered if not already in Task 1).
  </action>
  <acceptance_criteria>
    - handler_test.go additionally contains "TestListAuditLog_ShapeAndFilters", "TestListAuditLog_CursorAdvances", "TestGetAuditLogEntry_200", "TestGetAuditLogEntry_404", "TestGetPlatformSettings_200".
    - grep "legacy_data_source" backend/internal/handler/foundations/handler_test.go returns a match.
    - `cd backend && go test ./internal/handler/foundations/... -count=1` exits 0 (whole file).
    - `cd backend && go vet ./internal/handler/foundations/...` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handler/foundations/... -count=1 && go vet ./internal/handler/foundations/... exit 0</automated>
  </verify>
  <done>Audit-log and platform-settings contract tests pass, asserting the exact spec shapes incl. the 7 settings keys and the audit detail before/after.</done>
</task>

</tasks>

<verification>
- `cd backend && go test ./internal/handler/foundations/... -count=1` is green.
- `cd backend && go test ./... -count=1` is green (no regression in auth/service tests).
- Tests assert UPPER status, snake_case field names, the cursor envelope, RBAC 403, and the 7 settings keys.
</verification>

<success_criteria>
- Every FE-used E1 endpoint has a contract test asserting its spec shape + status code.
- RBAC negative cases (agent + shift_leader -> 403) are covered.
- List endpoints prove the cursor envelope and that paging advances.
</success_criteria>

<output>
After completion, create `.planning/phases/02-e1-foundations/02-03-SUMMARY.md` documenting the
fakeRepo approach, how the principal was injected for RBAC tests, and any shape mismatch found vs
the spec (and how it was resolved — code fix preferred over loosening the test).
</output>
