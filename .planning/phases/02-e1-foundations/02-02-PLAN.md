---
phase: 02-e1-foundations
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/internal/domain/foundations.go
  - backend/internal/repository/foundations/repository.go
  - backend/internal/service/foundations/service.go
  - backend/internal/handler/foundations/handler.go
  - backend/internal/handler/foundations/dto.go
  - backend/internal/server/server.go
  - backend/internal/platform/i18n/i18n.go
  - backend/cmd/seed/seed.go
autonomous: true
requirements: [FND-01, FND-02, FND-03]
must_haves:
  truths:
    - "An hr_admin/super_admin can list, create, update, change-role, deactivate, reactivate, and send-password-reset against the real BE, matching the E1 openapi shapes exactly"
    - "An hr_admin/super_admin can list audit-log entries (filtered + cursor paginated) and fetch a single entry with before/after"
    - "GET /platform/settings returns the 7-key snapshot in the exact PlatformSettings shape"
    - "Every write endpoint (create/update/change-role/deactivate/reactivate) writes one audit_log row in the same tx"
    - "Non-admin roles (agent, shift_leader) receive 403 FORBIDDEN on every E1 endpoint"
  artifacts:
    - path: "backend/internal/handler/foundations/handler.go"
      provides: "chi handlers for users mgmt, audit-log read, platform settings"
      min_lines: 200
    - path: "backend/internal/service/foundations/service.go"
      provides: "business logic: status case-mapping, audit on writes, send-password-reset reusing reset-token mechanism, cursor encode/decode"
      min_lines: 150
    - path: "backend/internal/repository/foundations/repository.go"
      provides: "sqlc-backed repo implementing the service port; jsonb decode of before/after"
      min_lines: 120
    - path: "backend/cmd/seed/seed.go"
      provides: "extra users + audit_log rows so E1 screens render in E2E"
      contains: "audit_log"
  key_links:
    - from: "backend/internal/server/server.go"
      to: "foundations handler"
      via: "chi routes under authenticated group, wrapped with rbac.RequireRole(super_admin, hr_admin) + d.Idempotency.Handler on writes"
      pattern: "/users:change-role"
    - from: "foundations service writes"
      to: "audit.Record"
      via: "called inside txm.InTx for every mutation"
      pattern: "audit\\.Record"
    - from: "foundations service send-password-reset"
      to: "password_reset_tokens"
      via: "reuses InsertResetToken pattern (sha256 token, 1h TTL)"
      pattern: "InsertResetToken|ResetToken"
---

<objective>
Build the E1 Foundations vertical slice — domain types, repository, service, handlers, routes —
for the FE-used endpoints: user management (list/create/update/change-role/deactivate/reactivate/
send-password-reset), audit-log read (list+detail), and platform settings read. Wire RBAC
(super_admin + hr_admin), idempotency on write actions, and audit on every mutation. Extend
cmd/seed so the E1 screens have data to render in E2E.

Purpose: This is the BE that wave-3 contract tests verify and wave-4 E2E drives. It mirrors the
existing identity auth slice exactly (handler → service → repository over sqlc, apperr codes,
txm.InTx for writes + audit).
Output: A foundations epic package set, routes mounted in server.go, i18n codes added, seed extended.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-e1-foundations/02-CONTEXT.md
@.planning/reference/backend-build-conventions.md
@docs/api/CONVENTIONS.md
@docs/api/E1-foundations/openapi.yaml
@docs/epics/E1-foundations/prds/rbac-roles.md
@docs/epics/E1-foundations/prds/audit-log.md
@.planning/phases/02-e1-foundations/02-01-SUMMARY.md

<interfaces>
<!-- Reference patterns the executor copies. The auth slice IS the template. -->

Reference slice to mirror (read all four):
  backend/internal/handler/identity/handler.go  — chi handlers, decodeJSON with DisallowUnknownFields,
    httpx.WriteJSON / httpx.WriteError, auth.PrincipalFrom(r.Context()).
  backend/internal/service/identity/service.go   — service struct {repo, txm, ...}; Repository PORT
    interface defined in the SERVICE package (consumer-defined, mockable); writes wrapped in
    s.txm.InTx(ctx, func(tx pgx.Tx) error {...}); apperr.* for errors.
  backend/internal/repository/identity/repository.go — struct{pool, q *sqlcgen.Queries}; reads on pool,
    writes via r.q.WithTx(tx); pgx.ErrNoRows -> domain.ErrNotFound via mapErr; pointer<->"" helpers.
  backend/internal/handler/identity/dto.go (read it) — request/response struct shapes + json tags.

Platform kernel (use these, do not reinvent):
  httpx.WriteJSON(w, status, v) / httpx.WriteError(w, r, err)
  httpx.PageResponse[T]{Data, NextCursor *string, HasMore bool}  (CONVENTIONS §8 envelope)
  httpx.ClampLimit(n) (default 50, max 200); httpx.EncodeCursor[T](p)/DecodeCursor[T](s,*p)
  apperr.Invalid(fields) 400 · apperr.NotFound() 404 · apperr.Conflict("CONFLICT") 409 ·
    apperr.Rule("ROLE_NOT_ALLOWED", fields) 422 · apperr.Internal(err) 500
  rbac.RequireRole(auth.RoleSuperAdmin, auth.RoleHRAdmin) — middleware, wraps a chi route group.
  audit.Record(ctx, tx, audit.Entry{Action, EntityType, EntityID, Before, After}) error
    Actions: audit.ActionCreate/ActionUpdate or a domain verb string cast to audit.Action.
  ids: prefixes USR and AL already exist. New IDs allocated in SQL via swp_next_id (handled in 02-01 CreateUser/audit.Record).

Auth principal: auth.PrincipalFrom(ctx) (Principal, bool) -> {UserID, EmployeeID, Role, CompanyID}.
auth.Role constants: RoleSuperAdmin, RoleHRAdmin, RoleShiftLeader, RoleAgent.

server.go (backend/internal/server/server.go): add the new handler to Deps, then mount routes
inside the EXISTING authenticated group (the r.Group with d.Authn.Require + rl.Middleware), e.g.:
  r.Group(func(r chi.Router) {
    r.Use(rbac.RequireRole(auth.RoleSuperAdmin, auth.RoleHRAdmin))
    r.Get("/users", h.ListUsers)
    r.With(d.Idempotency.Handler).Post("/users", h.CreateUser)
    r.Patch("/users/{user_id}", h.UpdateUser)
    r.With(d.Idempotency.Handler).Post("/users/{user_id}:change-role", h.ChangeUserRole)
    r.With(d.Idempotency.Handler).Post("/users/{user_id}:deactivate", h.DeactivateUser)
    r.With(d.Idempotency.Handler).Post("/users/{user_id}:reactivate", h.ReactivateUser)
    r.Post("/users/{user_id}:send-password-reset", h.SendUserPasswordReset)
    r.Get("/audit-log", h.ListAuditLog)
    r.Get("/audit-log/{audit_log_id}", h.GetAuditLogEntry)
    r.Get("/platform/settings", h.GetPlatformSettings)
  })
  NOTE: chi path params with a literal ':' — register the literal in the pattern as shown
  ("/users/{user_id}:change-role"); read how the auth routes use literal segments. If chi
  cannot match the ':' action suffix in a path param route, fall back to registering the action
  routes with a trailing wildcard or a dedicated subrouter and document the approach in SUMMARY.
  Do NOT mount /users:bulk-deactivate (out of scope per CONTEXT).

cmd/seed/seed.go: the existing Seed(ctx, *db.Pool) inserts 4 personas idempotently via
q.CreateUser. EXTEND the same function (append after the persona loop) — keep it idempotent
(skip-if-exists). Add a few extra users (so the user list has >1 page testable content) and a
handful of audit_log rows (so the audit-log screen renders). Use direct pool.Exec for audit rows
(audit_log has no CreateUser-style query; insert with 'SWP-AL-' || swp_next_id('AL')).
</interfaces>

<status_mapping_note>
DB users.status is lowercase 'active'/'disabled'. The API User.status enum is 'ACTIVE'/'DISABLED'.
Map in the DTO/service boundary: domain status -> strings.ToUpper for the response; request
filter status -> strings.ToLower before the query; :deactivate sets 'disabled', :reactivate sets 'active'.
</status_mapping_note>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: domain types + foundations repository</name>
  <read_first>
    - backend/internal/domain/identity.go (domain.User shape; add foundations types in a sibling file)
    - backend/internal/repository/identity/repository.go (struct, WithTx, mapErr, pointer helpers — copy the shape)
    - .planning/phases/02-e1-foundations/02-01-SUMMARY.md (generated sqlc method signatures + param structs)
    - docs/api/E1-foundations/openapi.yaml lines 1676-1753 (AuditLogEntrySummary + AuditLogEntry fields)
  </read_first>
  <files>backend/internal/domain/foundations.go, backend/internal/repository/foundations/repository.go</files>
  <action>
    Create backend/internal/domain/foundations.go with dependency-free types:
      AuditEntry{ID, ActorUserID *string, ActorRole *string, Action, EntityType, EntityID string,
                 Before, After map[string]any (or json.RawMessage), RequestID *string, CreatedAt time.Time}
      PlatformSetting{Key, Value, Label string, Locked bool}
      UserListItem — reuse domain.User (already has all fields the list needs).
      Define filter structs: UserFilter{Q, Role, Status, CompanyID *string; CursorCreatedAt *time.Time; CursorID *string; Limit int}
      and AuditFilter{Q, ActorUserID, Action, EntityType, EntityID *string; CreatedGTE, CreatedLTE, CursorCreatedAt *time.Time; CursorID *string; Limit int}.

    Create backend/internal/repository/foundations/repository.go mirroring the identity repo:
      struct Repository{pool *db.Pool; q *sqlcgen.Queries}; New(pool) constructor; var _ svc.Repository check.
      Implement (using the 02-01 generated methods):
        ListUsers(ctx, UserFilter) ([]domain.User, error)         — calls q.ListUsers, maps rows; lowercase status passthrough.
        UpdateUserEmail(ctx, tx, id, email) (domain.User, error)
        ChangeUserRole(ctx, tx, id, role) (domain.User, error)
        SetUserStatus(ctx, tx, id, status) (domain.User, error)   — status is the lowercase DB value.
        GetUserByID(ctx, id) (domain.User, error)                 — reuse existing GetUserByID generated query.
        CreateUser(ctx, tx, params) (domain.User, error)          — reuse existing CreateUser generated query (status defaults 'active').
        GetUserByEmail(ctx, email) (domain.User, error)           — reuse for uniqueness pre-check.
        ListAuditLog(ctx, AuditFilter) ([]domain.AuditEntry, error)
        GetAuditLogByID(ctx, id) (domain.AuditEntry, error)       — pgx.ErrNoRows -> domain.ErrNotFound.
        ListPlatformSettings(ctx) ([]domain.PlatformSetting, error)
        InsertResetToken(ctx, tx, userID, tokenHash, expiresAt) error — reuse the existing identity InsertResetToken generated query (for send-password-reset).
      Decode before_state/after_state jsonb ([]byte) into map[string]any with json.Unmarshal (nil -> nil map).
      Map pgx.ErrNoRows -> domain.ErrNotFound (copy mapErr); copy deref/nullStr helpers.
  </action>
  <acceptance_criteria>
    - backend/internal/domain/foundations.go exists and contains "type AuditEntry", "type PlatformSetting", "type UserFilter", "type AuditFilter".
    - backend/internal/repository/foundations/repository.go exists and contains "func (r *Repository) ListUsers", "func (r *Repository) ListAuditLog", "func (r *Repository) GetAuditLogByID", "func (r *Repository) ListPlatformSettings", "var _ ".
    - `cd backend && go build ./internal/domain/... ./internal/repository/...` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./internal/domain/... ./internal/repository/... exits 0</automated>
  </verify>
  <done>Domain types and a compiling sqlc-backed foundations repository exist, mirroring the identity slice.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: foundations service (business logic + audit + send-password-reset)</name>
  <read_first>
    - backend/internal/service/identity/service.go (Repository port pattern, TxRunner, txm.InTx, apperr errors, reset-token reuse: NewRefreshToken()+HashRefreshToken pattern)
    - backend/internal/platform/audit/audit.go (audit.Record signature + Entry fields)
    - backend/internal/platform/httpx/cursor.go (EncodeCursor/DecodeCursor + PageResponse + ClampLimit)
    - docs/api/E1-foundations/openapi.yaml lines 505-865 (createUser 409 INV_1_VIOLATION/CONFLICT, changeUserRole 422 ROLE_NOT_ALLOWED, deactivate/reactivate 409 already-disabled/already-active, send-password-reset 202)
  </read_first>
  <files>backend/internal/service/foundations/service.go</files>
  <action>
    Create backend/internal/service/foundations/service.go.
    Define the Repository PORT interface in THIS package (consumer-defined) listing exactly the
    methods Task 1's repo implements. Define TxRunner interface (InTx) like identity. Service
    struct{repo, txm, now Clock}; NewService(repo, txm) constructor.

    Cursor payload type: type pageCursor struct{ CreatedAt time.Time `json:"c"`; ID string `json:"i"` }.

    Implement:
      ListUsers(ctx, UserFilter) (items []domain.User, nextCursor *string, error):
        clamp limit, request limit+1 from repo; if len > limit, set hasMore, trim, EncodeCursor the
        last returned row's {created_at,id} as nextCursor. Lowercase the status filter if present.
      CreateUser(ctx, email, role, employeeID string, sendInvite bool) (domain.User, error):
        validate role in the 4 enum values (else apperr.Rule("ROLE_NOT_ALLOWED", ...) 422);
        pre-check email uniqueness: GetUserByEmail -> if found, apperr.Conflict("CONFLICT") with
        fields{email}; in txm.InTx: CreateUser (full_name "" — employee link not resolvable until
        E2; pass employeeID through), then audit.Record(Create, "user", newID, nil, afterSnapshot).
        Return the created user. (send_invitation_email: if true, also InsertResetToken so the
        invite/reset flow has a token — reuse the identity reset mechanism; do NOT email.)
      UpdateUser(ctx, id, email string) (domain.User, error):
        load current (GetUserByID -> NotFound), in tx UpdateUserEmail, audit.Record(Update,"user",id,before,after).
      ChangeUserRole(ctx, id string, newRole string, reason string) (domain.User, error):
        validate newRole enum (422 ROLE_NOT_ALLOWED otherwise); load current; in tx ChangeUserRole,
        audit.Record with action "user.change_role" (domain verb), before{role}, after{role,reason}.
      DeactivateUser(ctx, id, reason string) (domain.User, error):
        load current; if already 'disabled' -> apperr.Conflict("CONFLICT"); in tx SetUserStatus('disabled')
        + audit.Record(action "user.deactivate"). (Session revocation is auth-side; out of scope here — note in SUMMARY.)
      ReactivateUser(ctx, id, reason string) (domain.User, error):
        load current; if already 'active' -> apperr.Conflict("CONFLICT"); in tx SetUserStatus('active')
        + audit.Record(action "user.reactivate").
      SendUserPasswordReset(ctx, id string) (string, error):
        load user (NotFound if missing); in tx InsertResetToken(sha256 token, now+1h) reusing the
        identity helper pattern; audit.Record(action "user.send_password_reset"); return the user's
        email for the 202 message. (No mailer — E2E reads the token from the DB like Phase 1.)
      ListAuditLog(ctx, AuditFilter) ([]domain.AuditEntry, *string, error): same cursor pattern as ListUsers.
      GetAuditLogEntry(ctx, id string) (domain.AuditEntry, error): NotFound mapping.
      GetPlatformSettings(ctx) ([]domain.PlatformSetting, error): pass-through.

    All errors use apperr.* with the EXACT codes from the spec. Validate cursor decode errors as
    httpx CURSOR_MISMATCH (DecodeCursor already returns it).
  </action>
  <acceptance_criteria>
    - backend/internal/service/foundations/service.go contains "type Repository interface", "type Service struct", and functions "ListUsers", "CreateUser", "UpdateUser", "ChangeUserRole", "DeactivateUser", "ReactivateUser", "SendUserPasswordReset", "ListAuditLog", "GetAuditLogEntry", "GetPlatformSettings".
    - grep -c "audit.Record" backend/internal/service/foundations/service.go returns >= 5 (one per write path).
    - grep "ROLE_NOT_ALLOWED" backend/internal/service/foundations/service.go returns a match.
    - `cd backend && go build ./internal/service/...` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./internal/service/... exits 0; grep -c "audit.Record" backend/internal/service/foundations/service.go returns >= 5</automated>
  </verify>
  <done>The foundations service implements all FE-used E1 behaviors with audit on every write and spec-exact apperr codes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: handlers + DTOs + routes + i18n + seed extension</name>
  <read_first>
    - backend/internal/handler/identity/handler.go + dto.go (handler shape, decodeJSON, chi.URLParam usage, JSON tags)
    - backend/internal/server/server.go (Deps struct + authenticated group; see <interfaces> for the exact mount block)
    - backend/internal/platform/i18n/i18n.go (add new codes to BOTH id and en maps)
    - backend/cmd/seed/seed.go (idempotent Seed function to extend)
    - docs/api/E1-foundations/openapi.yaml lines 408-1195 (every request/response/example shape — match field names byte-for-shape, snake_case)
  </read_first>
  <files>backend/internal/handler/foundations/handler.go, backend/internal/handler/foundations/dto.go, backend/internal/server/server.go, backend/internal/platform/i18n/i18n.go, backend/cmd/seed/seed.go</files>
  <action>
    Create backend/internal/handler/foundations/dto.go — request + response structs with EXACT
    snake_case json tags from the spec:
      userResponse{id, email, role, status, employee_id, full_name, company_id *string, company_name *string,
                   last_login_at *string, created_at, updated_at string}  (timestamps RFC3339; status UPPER).
      createUserRequest{email, role, employee_id string; send_invitation_email *bool}
      updateUserRequest{email *string}
      changeRoleRequest{new_role string; reason *string}
      reasonRequest{reason *string}  (deactivate/reactivate optional body)
      auditSummaryResponse{id, actor_user_id *string, actor_label string, action, entity_type, entity_id,
                           change_summary string, ip *string, created_at string}
      auditEntryResponse{...summary fields..., before, after map[string]any, request_id string}
      platformSettingsResponse{locale, timezone, date_format, currency, version, stack, legacy_data_source platformSetting}
      platformSetting{value, label string; locked bool}
      Helpers: toUserResponse(domain.User) (status->UPPER; company_name -> nil in Phase 2; build last_login_at).
        actor_label: if actor_user_id nil -> "system" else build from actor_role (full name resolution
        needs E2; use actor_user_id + role as the label, document in SUMMARY).
        change_summary: derive a one-line "field: before → after" from before/after (best-effort; for
        audit rows seeded with explicit summaries use them).
        ip: audit_log has no ip column (migration 00004 omitted it) -> always nil. Document this gap in SUMMARY.

    Create backend/internal/handler/foundations/handler.go — chi handlers, one per endpoint, each:
      decode (decodeJSON with DisallowUnknownFields for bodies), pull chi.URLParam(r,"user_id")/
      "audit_log_id", call the service, map to DTO, httpx.WriteJSON with the spec status:
        ListUsers 200 (PageResponse[userResponse]); CreateUser 201 + Location header /api/v1/users/{id};
        UpdateUser 200; ChangeUserRole 200; DeactivateUser 200; ReactivateUser 200;
        SendUserPasswordReset 202 {message: "Tautan reset dikirim ke <email>."};
        ListAuditLog 200 (PageResponse[auditSummaryResponse]); GetAuditLogEntry 200 (auditEntryResponse);
        GetPlatformSettings 200 (platformSettingsResponse).
      Parse query params for filters (role, status, q, company_id, actor_user_id, action, entity_type,
      entity_id, created_at__gte, created_at__lte, cursor, limit). Decode cursor via the service.
      NewHandler(svc *foundations.Service) constructor.

    Update backend/internal/server/server.go: add `Foundations *foundationshttp.Handler` to Deps,
    mount the routes per the <interfaces> block inside the authenticated group wrapped with
    rbac.RequireRole(auth.RoleSuperAdmin, auth.RoleHRAdmin) and d.Idempotency.Handler on the
    write/action POSTs. Also wire construction in backend/cmd/api (build repo->service->handler;
    follow how the identity handler is constructed there).

    Update backend/internal/platform/i18n/i18n.go: add to BOTH id and en maps:
      "ROLE_NOT_ALLOWED" (id: "Perubahan peran tidak diizinkan." / en: "Role change not allowed."),
      "CURSOR_MISMATCH" (id: "Kursor tidak valid." / en: "Invalid cursor.").

    Extend backend/cmd/seed/seed.go Seed(): append (idempotently) ~3 extra users (e.g.
    dewi.lestari@swp.test agent, agus.pratama@swp.test shift_leader, bambang@swp.test hr_admin)
    and ~5 audit_log rows via pool.Exec("INSERT INTO audit_log (id, actor_user_id, actor_role,
    action, entity_type, entity_id, before_state, after_state, request_id) VALUES ('SWP-AL-' ||
    swp_next_id('AL'), ...)") covering a couple of entity_types (user, placement) and one system row
    (actor_user_id NULL). Keep skip-if-exists semantics (guard audit insert with a count check so
    re-running the seed doesn't duplicate).
  </action>
  <acceptance_criteria>
    - backend/internal/handler/foundations/handler.go contains handler funcs: "ListUsers", "CreateUser", "UpdateUser", "ChangeUserRole", "DeactivateUser", "ReactivateUser", "SendUserPasswordReset", "ListAuditLog", "GetAuditLogEntry", "GetPlatformSettings".
    - backend/internal/server/server.go contains the strings "/users:change-role", "/audit-log", "/platform/settings", and "rbac.RequireRole".
    - backend/internal/platform/i18n/i18n.go contains "ROLE_NOT_ALLOWED" and "CURSOR_MISMATCH".
    - backend/cmd/seed/seed.go contains "audit_log" and at least one new persona email.
    - `cd backend && go build ./... && go vet ./...` exit 0.
    - `cd backend && gofmt -l internal/handler/foundations internal/service/foundations internal/repository/foundations internal/server cmd/seed` prints nothing (no unformatted files).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... exit 0; gofmt -l internal/handler/foundations internal/service/foundations internal/repository/foundations internal/server cmd/seed prints nothing</automated>
  </verify>
  <done>All E1 endpoints are mounted with RBAC + idempotency + audit, i18n codes added, seed extended; the module builds, vets, and is gofmt-clean.</done>
</task>

</tasks>

<verification>
- `cd backend && make gen && go build ./... && go vet ./... && gofmt -l ./internal ./cmd` all clean.
- Routes registered: grep "/users\b\|/audit-log\|/platform/settings" backend/internal/server/server.go.
- Every write service path calls audit.Record (grep count >= 5).
- send-password-reset reuses the reset-token mechanism (no mailer).
</verification>

<success_criteria>
- All FE-used E1 endpoints exist, are RBAC-gated to super_admin/hr_admin, idempotent where the spec flags it, and audited on write.
- Responses match the E1 openapi shapes (snake_case, UPPER status, RFC3339 timestamps, PlatformSettings 7-key object).
- The seed renders the E1 screens (users list + audit-log list have content).
</success_criteria>

<output>
After completion, create `.planning/phases/02-e1-foundations/02-02-SUMMARY.md` documenting:
the chi route registration approach for the ':action' path suffixes, the status case-mapping
decision, the actor_label / change_summary / ip derivation (and the missing ip column gap),
the send-password-reset token approach, and the construction wiring added in cmd/api.
</output>
