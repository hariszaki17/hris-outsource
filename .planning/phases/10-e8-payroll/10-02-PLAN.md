---
phase: 10-e8-payroll
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - backend/internal/repository/payroll/payslip_repo.go
  - backend/internal/repository/payroll/export_repo.go
  - backend/internal/repository/payroll/mapping.go
  - backend/internal/service/payroll/ports.go
  - backend/internal/service/payroll/helpers.go
  - backend/internal/service/payroll/payslip_service.go
  - backend/internal/service/payroll/export_service.go
  - backend/internal/handler/payroll/handler.go
  - backend/internal/handler/payroll/payslip_handler.go
  - backend/internal/handler/payroll/export_handler.go
  - backend/internal/handler/payroll/dto.go
  - backend/internal/platform/jobs/jobs.go
  - backend/internal/platform/jobs/payslip_export.go
  - backend/internal/server/server.go
  - backend/cmd/api/main.go
  - backend/cmd/seed/seed.go
autonomous: true
requirements: [PAY-01, PAY-02]
user_setup: []

must_haves:
  truths:
    - "GET /payslips lists FINAL + DECRYPT_FAIL rows (cursor + filters); decrypt-fail rows return status DECRYPT_FAIL with money nulled at 200 — never a 4xx"
    - "GET /payslips/{id} returns the full breakdown (earnings/deductions/benefits) decrypted at the boundary; a row with bad ciphertext returns status DECRYPT_FAIL + nulled money + empty breakdown arrays at 200"
    - "GET/POST /payslips/{id}/audit-notes list + append (append-only); create writes an audit_log row + notify stub in-tx"
    - "POST /payslips:export inserts an export_jobs QUEUED row + enqueues a River PayslipExportWorker in the SAME tx, returns 202 + {id(job),status,...}; the worker builds the artifact + marks export_jobs DONE"
    - "EXPORT_TOO_LARGE (422) when the scope exceeds the threshold; RBAC restricts every payroll endpoint to hr_admin/super_admin (agent/shift_leader → 403)"
  artifacts:
    - path: "backend/internal/service/payroll/payslip_service.go"
      provides: "list/detail with decrypt-at-boundary + DECRYPT_FAIL row status + RBAC + audit-note append"
      contains: "DECRYPT_FAIL"
    - path: "backend/internal/service/payroll/export_service.go"
      provides: "async export: EXPORT_TOO_LARGE guard + insert QUEUED + River EnqueueTx in one tx + 202 stub"
      contains: "EXPORT_TOO_LARGE"
    - path: "backend/internal/platform/jobs/payslip_export.go"
      provides: "PayslipExportWorker that builds the artifact + marks export_jobs DONE"
      contains: "PayslipExportWorker"
    - path: "backend/internal/platform/jobs/jobs.go"
      provides: "PayslipExportWorker registered in registerWorkers"
      contains: "PayslipExportWorker"
    - path: "backend/internal/server/server.go"
      provides: "E8 routes mounted under RequireRole(hr_admin, super_admin)"
      contains: "/payslips:export"
    - path: "backend/cmd/seed/seed.go"
      provides: "seeded payslips (incl. a DECRYPT_FAIL row) + components/benefits + audit notes (encrypted with the payroll key)"
      contains: "seedPayroll"
  key_links:
    - from: "backend/internal/service/payroll/export_service.go"
      to: "backend/internal/platform/jobs (River)"
      via: "jobs.Client.EnqueueTx(PayslipExportArgs) in the same tx as InsertExportJob(QUEUED)"
      pattern: "EnqueueTx"
    - from: "backend/internal/platform/jobs/payslip_export.go"
      to: "export_jobs table"
      via: "UpdateExportJobStatus → RUNNING then DONE"
      pattern: "DONE"
    - from: "backend/internal/service/payroll/payslip_service.go"
      to: "internal/platform/crypto"
      via: "Cipher.Decrypt at the read boundary → DECRYPT_FAIL on ErrDecrypt"
      pattern: "Decrypt"
    - from: "backend/internal/handler/payroll/payslip_handler.go"
      to: "GET /payslips/{id} response"
      via: "httpx data-envelope wrap (FE unwraps {data})"
      pattern: "dataResponse|data\":"
---

<objective>
Implement the E8 payroll service + handlers: read payslips (list/detail with decrypt-AT-BOUNDARY +
DECRYPT_FAIL as a 200 row status, NOT a 4xx), audit notes list/create (append-only), and the async
Excel export (POST /payslips:export → insert export_jobs QUEUED + River EnqueueTx of a
PayslipExportWorker in the SAME tx → 202 + {job_id,status}; worker builds the artifact + marks the
job DONE; EXPORT_TOO_LARGE guard), all under RBAC RequireRole(hr_admin, super_admin). Wire the
jobs.Client into the API process (it is NOT wired yet), register the new worker, and extend the seed.
This plan OWNS the shared server.go / main.go / seed.go / jobs.go edits.

The encryption/decrypt-fail handling and the async River export are the gnarly parts — be exhaustive.
Mirrors Phase-2 foundations (read + list + simple write + audit) and the existing River
NotificationWorker (async) — there is NO two-level state machine here (simpler than E6/E7).

Purpose: Make every FE-used E8 operation work against the real BE, matching
`docs/api/E8-payroll/openapi.yaml` byte-for-byte, with money encrypted at rest and the export
actually completed by a real River worker.
Output: repository + service + handler packages, the PayslipExportWorker, routes, wiring, seed.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-e8-payroll/10-CONTEXT.md
@.planning/reference/backend-build-conventions.md
@docs/api/E8-payroll/openapi.yaml
@docs/api/CONVENTIONS.md

<interfaces>
<!-- The exact analogs to copy. Phase-2 foundations = read/list/simple-write; NotificationWorker = async. -->

Phase-2 foundations slice (COPY ITS SHAPE for read/list/write/audit):
  backend/internal/service/foundations/  (List cursor + Get + simple write + audit-in-tx via TxManager.InTx)
  backend/internal/handler/foundations/  (decodeJSON, httpx.WriteJSON/PageResponse/WriteError, dataResponse single-object wrap, cursor encode/decode)
  backend/internal/repository/foundations/  (port-impl over sqlc; pgx.ErrNoRows→domain.ErrNotFound; writes take pgx.Tx)

Phase-9 overtime slice (COPY its repo/service/handler/seed file layout + main.go wiring block + server.go group marker):
  backend/internal/{repository,service,handler}/overtime/*
  backend/cmd/api/main.go (the "Overtime slice (09-02)" construction block — add a "Payroll slice (10-02)" block after it)
  backend/internal/server/server.go ("OVERTIME slice end (09-02). Phase 9+ appends after this line." marker — append the PAYROLL slice after it; add Payroll *payrollhttp.Handler to Deps)
  backend/cmd/seed/seed.go (Seed() calls seedOvertime at line ~283 — add seedPayroll after it; explicit SWP-* ids, ON CONFLICT (id) DO NOTHING)

River (the async pattern — jobs.Client EXISTS but is NOT yet wired into the API process):
  backend/internal/platform/jobs/jobs.go  (Client.EnqueueTx(ctx, tx, args) + NewInsertOnlyClient(pool) + registerWorkers(w) — add river.AddWorker(w, &PayslipExportWorker{...}))
  backend/internal/platform/jobs/notify.go (NotificationArgs/NotificationWorker — the Args{Kind()} + Worker{Work()} shape to mirror for PayslipExportArgs/PayslipExportWorker)
  cmd/api/main.go does NOT currently construct jobs.NewInsertOnlyClient — ADD it and thread the Client into Deps/export service.
  NOTE: the worker's Work() needs DB access (UpdateExportJobStatus) — the WORKER process (cmd/worker) constructs the worker WITH a *db.Pool; the worker is registered in registerWorkers which runs in NewWorkerClient(pool). Pass the pool into the worker via a package-level registration that takes the pool (see Task 3 — change registerWorkers to accept the pool, or construct the worker in NewWorkerClient with the pool and AddWorker there).

Platform kernel:
  rbac.RequireRole(rbac.RoleHRAdmin, rbac.RoleSuperAdmin) (route-level) ; the FE archive is hr_admin/super_admin ONLY (CONTEXT INV-3/4 — agent self-summary is mobile, out of web scope).
  apperr.Rule("EXPORT_TOO_LARGE", fields) (422) ; apperr.Forbidden() (403) ; apperr.NotFound() (404) ; apperr.ConflictWithDetails / Conflict for IDEMPOTENCY_KEY_REUSED (handled by d.Idempotency.Handler middleware — do NOT re-implement).
  audit.Record(ctx, tx, audit.Entry{...}) in-tx ; jobs.Client.EnqueueTx notify stub (NotificationArgs) ; httpx.WriteJSON / WriteError / PageResponse / EncodeCursor / DecodeCursor ; d.Idempotency.Handler on POST endpoints.
  crypto.Cipher (10-01) — Decrypt at the read boundary; ErrDecrypt → row status DECRYPT_FAIL.

FE response-shape anchors (the BUILT e8-payroll components — AUTHORITATIVE):
  - payslip-archive-screen.tsx: reads `query.data?.data as PayslipListResponse` → handler returns {data:[...], next_cursor, has_more} at the TOP level (httpx.PageResponse, NOT double-wrapped). meta.code MISSING_PAYROLL_HISTORY when data:[].
  - payslip-detail-screen.tsx: reads `query.data?.data as Payslip` → GET /payslips/{id} MUST be wrapped in dataResponse({...}) → {data:<Payslip>} (recurring Phase-8/9 fix; FE already unwraps `.data`). Detail uses `payslip.earnings/deductions/benefits` (arrays), `decrypt_fail`, `status`, `gross_earnings/gross_deductions/take_home_pay` (Money strings or null), `source.system`/`source.source_id`, `working_days`.
  - audit-note-drawer.tsx: list via `useListPayslipAuditNotes` reads `query.data?.data as PayslipAuditNoteListResponse` → {data:[...], next_cursor, has_more}; create via `useCreatePayslipAuditNote({ id, data:{ text }})` → 201 PayslipAuditNote. Author = caller (hr_admin → SWP-EMP-9001 'Sari Hadi' per seed).
  - payroll-export.tsx: `useExportPayslips({ data:{ period?, year?, employee_ids?, format:'XLSX', confidential:true }})` → reads `res.data as PayslipExportJob` ⇒ 202 body = { id, status:'QUEUED', format:'XLSX', confidential:true, requested_at, requested_by:{id,name}, scope:{period,year,employee_ids}, poll_url }. Status enum on the FE: QUEUED|RUNNING|DONE|FAILED (PayslipExportJobStatus).

Money DTO: a payslip's gross_earnings etc. are emitted as the 2-decimal STRING (e.g. "8500000.00") for FINAL, and JSON `null` for DECRYPT_FAIL (pointer WITHOUT omitempty → serialize null, decision [07-02]). decrypt_fail:true, status:"DECRYPT_FAIL", locked_reason:"decrypt_fail", earnings/deductions/benefits:[] on a decrypt-fail row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repository + ports + payslip service (read + decrypt-at-boundary + DECRYPT_FAIL + audit notes)</name>
  <files>backend/internal/repository/payroll/payslip_repo.go, backend/internal/repository/payroll/export_repo.go, backend/internal/repository/payroll/mapping.go, backend/internal/service/payroll/ports.go, backend/internal/service/payroll/helpers.go, backend/internal/service/payroll/payslip_service.go</files>
  <read_first>
    - backend/internal/repository/foundations/ (port-impl over sqlc + row→domain mapping + pgx.ErrNoRows→domain.ErrNotFound) and backend/internal/repository/overtime/mapping.go (pgtype.Date↔time.Time, []byte for jsonb/bytea, text[]→[]string)
    - backend/internal/service/foundations/ (List cursor scope + Get + simple write + audit-in-tx via InTx)
    - backend/internal/platform/crypto/crypto.go (10-01 — Decrypt/DecryptPtr + ErrDecrypt)
    - docs/api/E8-payroll/openapi.yaml getPayslip (the FINAL vs DECRYPT_FAIL field-nulling table) + listPayslips (empty→meta.code MISSING_PAYROLL_HISTORY) + audit-notes list/create
  </read_first>
  <action>
    Create `backend/internal/repository/payroll/payslip_repo.go`, `export_repo.go`, `mapping.go` implementing the service ports over the 10-01 sqlc queries. Map rows→`domain/payroll` types — but DO NOT decrypt in the repo; the repo returns the RAW `*_enc []byte` ciphertext on an intermediate struct (or on the domain Payslip via a transient `cipher` field). Cleanest: the repo returns a `payslipRow` carrying the ciphertext bytes + plaintext metadata; the SERVICE decrypts. pgx.ErrNoRows→domain.ErrNotFound. Writes take `pgx.Tx`. Methods: ListPayslips(filter)→[]payslipRow, GetPayslip(id)→payslipRow, ListComponents(id)/ListBenefits(id)→raw line rows (with ciphertext), ListAuditNotes(id,cursor)/CountAuditNotes(id)/InsertAuditNote(tx,...), PayslipExists(id). export_repo.go: InsertExportJob(tx,...), GetExportJob(id), CountPayslipsInScope(filter).

    Create `backend/internal/service/payroll/ports.go`: `PayslipRepository`, `ExportRepository`, `TxRunner`, `Clock`, and a `Jobs` interface `EnqueueTx(ctx, tx, args river.JobArgs) error` (so the export service can be unit-tested with a fake in 10-03 without a real River client). The service holds a `*crypto.Cipher`.

    Create `backend/internal/service/payroll/helpers.go`: clampLimit, encode/decode payslip cursor (paid_on DESC, id) + audit-note cursor (created_at ASC, seq), actorRole/actorEmployeeID(ctx), deref, and a `decryptMoney(*string-or-bytes) (*string, decryptFailed bool)` helper that wraps `cipher.Decrypt`: nil/empty ciphertext → (nil, false) [no value, not a failure]; valid ciphertext → (&plaintext, false); `ErrDecrypt` → (nil, **true**). This is the single seam that produces DECRYPT_FAIL.

    Create `backend/internal/service/payroll/payslip_service.go`:
    - `List(ctx, filter)` — RBAC is route-enforced hr/super (global scope); the service still accepts employee_id/period/year/status filters. For EACH row, decrypt the three summary ciphertexts via decryptMoney; if ANY decrypt fails → mark the row `Status=DECRYPT_FAIL`, `DecryptFail=true`, `LockedReason="decrypt_fail"`, null all three money fields + WorkingDays (per the openapi nulling table). FINAL otherwise. Set `ReadOnly=true` always. Build Period="YYYY-MM". Cursor page. Return rows + a flag `missingHistory` (true when zero rows) so the handler emits `meta.code: MISSING_PAYROLL_HISTORY`. DECRYPT_FAIL rows are NEVER filtered out (surfacing them is the point) and NEVER cause a non-200.
    - `Get(ctx, id)` — load payslip + components + benefits. Decrypt summary money + each line value. If the summary OR any line fails to decrypt → the WHOLE payslip is DECRYPT_FAIL: status DECRYPT_FAIL, money nulled, `Earnings/Deductions/Benefits = []` (empty arrays per openapi — do NOT surface partially-decrypted lines), locked_reason set. FINAL otherwise: populate earnings (kind=EARNING), deductions (kind=DEDUCTION), benefits, each with its decrypted Value + for_bpjs. NotFound → 404. (No agent-scope branch — web is hr/super only.)
    - `ListAuditNotes(ctx, payslipID, cursor)` — PayslipExists else 404; cursor page (created_at ASC). Notes are returned even on DECRYPT_FAIL payslips.
    - `CreateAuditNote(ctx, payslipID, text, author)` — PayslipExists else 404; validate `len(strings.TrimSpace(text)) >= 1 && <= 4000` else apperr.Invalid field error (400). InTx: seq = CountAuditNotes+1; id = fmt.Sprintf("%s-NOTE-%d", payslipID, seq); InsertAuditNote(tx,...); audit.Record(ctx, tx, Entry{Action:"payslip_audit_note.create", EntityType:"payslip", EntityID:payslipID, ...}); notify stub via jobs.EnqueueTx(NotificationArgs{Event:"PAYSLIP_NOTE_ADDED",...}) (TODO Phase-11 comment). Return the created note. Idempotency is the router middleware's job (do NOT re-implement).
  </action>
  <verify>
    <automated>cd backend && grep -q "DECRYPT_FAIL\|PayslipStatusDecryptFail" internal/service/payroll/payslip_service.go && grep -q "func.*List" internal/service/payroll/payslip_service.go && grep -q "func.*CreateAuditNote" internal/service/payroll/payslip_service.go && grep -q "MISSING_PAYROLL_HISTORY\|missingHistory\|MissingHistory" internal/service/payroll/payslip_service.go && grep -q "Decrypt" internal/service/payroll/helpers.go && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `payslip_service.go` produces DECRYPT_FAIL as a ROW STATUS (status + decrypt_fail + locked_reason + nulled money + empty breakdown) — never returns a 4xx for a decrypt failure.
    - List sets a missing-history flag (→ meta.code MISSING_PAYROLL_HISTORY) and never filters out DECRYPT_FAIL rows.
    - `CreateAuditNote` validates text length, computes the composite `{payslip_id}-NOTE-{seq}` id, audits in-tx, and stubs notify; PayslipExists gates 404.
    - Money is decrypted to a *string and never parsed/computed (INV-2).
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <done>Read + decrypt-at-boundary + DECRYPT_FAIL-as-200-row-status + append-only audit notes are implemented and compile.</done>
</task>

<task type="auto">
  <name>Task 2: Export service + PayslipExportWorker + handlers + DTO</name>
  <files>backend/internal/service/payroll/export_service.go, backend/internal/platform/jobs/payslip_export.go, backend/internal/handler/payroll/handler.go, backend/internal/handler/payroll/payslip_handler.go, backend/internal/handler/payroll/export_handler.go, backend/internal/handler/payroll/dto.go</files>
  <read_first>
    - backend/internal/platform/jobs/notify.go (the Args{Kind()} + Worker{WorkerDefaults; Work()} shape — mirror for PayslipExportArgs/PayslipExportWorker) + jobs.go (EnqueueTx + registerWorkers)
    - backend/internal/service/foundations/ + overtime export-less services (InTx pattern, audit-in-tx)
    - backend/internal/handler/foundations/ + overtime/dto.go (toResponse mapping; required-nullable pointer-WITHOUT-omitempty → JSON null; dataResponse single-object wrap; httpx.PageResponse top-level)
    - docs/api/E8-payroll/openapi.yaml exportPayslips (202 + PayslipExportJob body + EXPORT_TOO_LARGE 422 + confidential-lock) + PayslipExportRequest (period|year required) + the Payslip/PayslipAuditNote/PayslipExportJob schemas
    - frontend/apps/web/src/features/e8-payroll/payroll-export.tsx (the EXACT 202 body the FE reads: { id, status, format, confidential, requested_at, requested_by, scope, poll_url })
  </read_first>
  <action>
    Create `backend/internal/platform/jobs/payslip_export.go`:
    - `type PayslipExportArgs struct { JobID string \`json:"job_id"\`; Period *string; Year *int; EmployeeIDs []string }` with `func (PayslipExportArgs) Kind() string { return "payslip.export" }`.
    - `type PayslipExportWorker struct { river.WorkerDefaults[PayslipExportArgs]; pool *db.Pool }` (constructed with the pool in the worker process so Work() can write export_jobs). NewPayslipExportWorker(pool) constructor.
    - `func (w *PayslipExportWorker) Work(ctx, job) error`: open a queries handle on w.pool; `UpdateExportJobStatus(job.Args.JobID, RUNNING, ...)`; build the artifact — the SIMPLEST honest artifact per CONTEXT discretion: query CountPayslipsInScope for the row count and write a faithful stand-in CSV/placeholder file to a temp/artifacts dir (or just compute the row count + an artifact_ref string like "payroll-export-{JobID}.csv" WITHOUT a heavy xlsx lib — a real file write of a CSV is acceptable); then `UpdateExportJobStatus(job.Args.JobID, DONE, rowCount, artifactRef, "")`. On any error → `UpdateExportJobStatus(..., FAILED, error_message)` and return the error (River retries). The success criterion is "the job completes via the worker" → export_jobs.status = DONE. Keep it dependency-light; DO NOT pull a heavy Excel library if a CSV/stand-in proves the lifecycle.

    Create `backend/internal/service/payroll/export_service.go`:
    - `Export(ctx, req, actor)` — validate: at least one of period/year present else `apperr.Rule("RULE_VIOLATION", fields)` (422; openapi: "no period AND no year"). FORCE `confidential=true` regardless of input (Wave 2.8 lock). EXPORT_TOO_LARGE guard: `n := CountPayslipsInScope(filter)`; if `n > threshold` (threshold = a const default 50000, configurable later) → `apperr.Rule("EXPORT_TOO_LARGE", map[string]string{"period": fmt.Sprintf("Cakupan menghasilkan %d baris (maks %d).", n, threshold)})` (422). InTx: `job := InsertExportJob(tx, QUEUED, kind=PAYSLIP_EXPORT, format=XLSX, confidential=true, requested_by=actor, scope=...)`; `jobs.EnqueueTx(ctx, tx, PayslipExportArgs{JobID: job.ID, Period, Year, EmployeeIDs})` (transactional outbox — same tx as the QUEUED insert); audit.Record(ctx, tx, Entry{Action:"payslip.export", confidential marking per PA-7}). Return the job stub. The handler responds 202.

    Create `backend/internal/handler/payroll/handler.go` (shared helpers: decodeJSON/decodeOptionalJSON/cursorParam/strPtrParam — copy from foundations/overtime handler.go), `payslip_handler.go`, `export_handler.go`, `dto.go`.
    - `dto.go`: `toPayslipSummary(dom.Payslip)` (list row), `toPayslipDetail(dom.Payslip)` (full breakdown), `toAuditNote(dom.PayslipAuditNote)`, `toExportJob(dom.ExportJob)`. EXACT openapi shapes:
      * Payslip: id, employee_id, employee_name (omitempty), year, month, period, paid_on (pointer no-omitempty → null), working_days (pointer no-omitempty → null), gross_earnings/gross_deductions/take_home_pay (pointer-string no-omitempty → JSON null on DECRYPT_FAIL), status, decrypt_fail (bool), read_only (always true), locked_reason (omitempty — present only on decrypt-fail), earnings/deductions/benefits (only on detail; each line {name, value(no-omitempty→null), for_bpjs(earn/deduct only), locked_reason(omitempty)}), source {system, source_id}, created_at.
      * PayslipAuditNote: id, payslip_id, text, author_id, author_name (no-omitempty→null), created_at.
      * PayslipExportJob: id, status, format, confidential, requested_at, requested_by {id, name(no-omitempty→null)}, scope {period(no-omitempty→null), year(no-omitempty→null), employee_ids:[]}, poll_url ("/api/v1/exports/{id}").
    - `payslip_handler.go`: `ListPayslips` (parse cursor/limit/employee_id/period/year/status → httpx.PageResponse {data, next_cursor, has_more}; attach meta:{code:MISSING_PAYROLL_HISTORY} when the service flags zero rows — write the page envelope with the optional meta field), `GetPayslip` (dataResponse wrap → {data:<Payslip>}), `ListAuditNotes` ({data, next_cursor, has_more}), `CreateAuditNote` (decode {text}, 201 + Location header, body = the created note). Errors via httpx.WriteError. (Do NOT implement POST/PATCH/DELETE on payslips — those are 405 PAYSLIP_IMMUTABLE in the spec but the FE never calls them; out of scope — only the 5 FE-used ops.)
    - `export_handler.go`: `ExportPayslips` (decode PayslipExportRequest {period?, year?, employee_ids?, format, confidential}; call export service; 202 + Location header `/api/v1/exports/{id}` + body = toExportJob).
  </action>
  <verify>
    <automated>cd backend && grep -q "PayslipExportWorker" internal/platform/jobs/payslip_export.go && grep -q "func.*Kind.*return \"payslip.export\"\|payslip.export" internal/platform/jobs/payslip_export.go && grep -q "UpdateExportJobStatus" internal/platform/jobs/payslip_export.go && grep -q "EXPORT_TOO_LARGE" internal/service/payroll/export_service.go && grep -q "EnqueueTx" internal/service/payroll/export_service.go && grep -q "func.*ExportPayslips" internal/handler/payroll/export_handler.go && grep -q "func.*GetPayslip" internal/handler/payroll/payslip_handler.go && grep -qE "dataResponse|data\":" internal/handler/payroll/payslip_handler.go && grep -q "MISSING_PAYROLL_HISTORY" internal/handler/payroll/payslip_handler.go && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `payslip_export.go` defines `PayslipExportArgs.Kind()=="payslip.export"` + `PayslipExportWorker.Work` that flips export_jobs RUNNING→DONE (FAILED on error) and builds a row-count + artifact_ref (CSV/stand-in, no heavy xlsx dep required).
    - `export_service.go` enforces EXPORT_TOO_LARGE (422) + the period-or-year rule, forces confidential=true, and does `InsertExportJob(QUEUED)` + `jobs.EnqueueTx(PayslipExportArgs)` in ONE tx; the handler returns 202 with the exact PayslipExportJob body.
    - `payslip_handler.go`: ListPayslips writes the cursor envelope + meta.code MISSING_PAYROLL_HISTORY on empty; GetPayslip wraps `{data}`; CreateAuditNote returns 201.
    - `dto.go` emits DECRYPT_FAIL money as JSON null (pointer no-omitempty) and FINAL money as the 2-decimal string; only the 5 FE-used ops are handled (no 405 immutable handlers).
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <done>Async export (202 + real River enqueue + worker-completes-DONE), EXPORT_TOO_LARGE, and all read/note handlers exist; DTOs match openapi byte-for-byte; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire jobs.Client into the API + register worker + routes (server.go) + main.go + seed</name>
  <files>backend/internal/platform/jobs/jobs.go, backend/internal/server/server.go, backend/cmd/api/main.go, backend/cmd/seed/seed.go</files>
  <read_first>
    - backend/internal/platform/jobs/jobs.go (registerWorkers — change it to accept a *db.Pool, or AddWorker the PayslipExportWorker inside NewWorkerClient where the pool is in scope; NewInsertOnlyClient is what the API process constructs)
    - backend/cmd/api/main.go (the "Overtime slice (09-02)" construction block + the server.Deps literal — add jobs.NewInsertOnlyClient(pool) + the crypto.Cipher from cfg.Crypto.PayrollKey + the Payroll slice; assign Deps.Payroll)
    - backend/internal/server/server.go (Deps struct + the "OVERTIME slice end (09-02). Phase 9+ appends after this line." marker + the RequireRole group pattern + r.With(d.Idempotency.Handler).Post wrapping)
    - backend/cmd/seed/seed.go (Seed() at ~line 283 calls seedOvertime — add seedPayroll after; the SWP-EMP-1042/1108/2891/3001 employees + SWP-CMP ids; ON CONFLICT (id) DO NOTHING; the seed must ENCRYPT money with the SAME payroll key the API uses — construct crypto.NewFromBase64(os.Getenv("PAYROLL_ENCRYPTION_KEY")) in the seed)
    - docs/api/E8-payroll/openapi.yaml x-rbac (all endpoints: hr_admin, super_admin only on the web archive)
  </read_first>
  <action>
    Edit `backend/internal/platform/jobs/jobs.go`: register the new worker. Since `PayslipExportWorker.Work` needs the pool, construct it in `NewWorkerClient(pool)` (the pool is in scope there) and `river.AddWorker(w, NewPayslipExportWorker(pool))` alongside the NotificationWorker. Keep `registerWorkers(w)` for the notification worker (no-pool) and add a second registration line in NewWorkerClient that has the pool. (Document why: the export worker is the first worker that writes to the DB from Work().)

    Edit `backend/cmd/api/main.go`:
    - Construct `payrollCipher, err := crypto.NewFromBase64(cfg.Crypto.PayrollKey)` (fail fast if the key is set-but-invalid; if empty in dev, log a warning — the E2E harness always sets it).
    - Construct `jobsClient, err := jobs.NewInsertOnlyClient(pool)` (the API process inserts jobs — this is NOT currently wired; add it).
    - Construct the Payroll slice (mirror the overtime block): `payrollRepo := payrollrepo.New(pool); exportRepo := payrollrepo.NewExportRepo(pool); payslipSvc := payrollsvc.NewPayslipService(payrollRepo, txm, payrollCipher, jobsClient); exportSvc := payrollsvc.NewExportService(exportRepo, txm, jobsClient); payrollHandler := payrollhttp.NewHandler(payslipSvc, exportSvc)`.
    - Add `Payroll: payrollHandler,` to the `server.Deps{...}` literal.

    Edit `backend/internal/server/server.go`: AFTER the "OVERTIME slice end (09-02)" marker add a PAYROLL slice in a SINGLE `RequireRole(rbac.RoleHRAdmin, rbac.RoleSuperAdmin)` group (NO agent, NO shift_leader — web archive is hr/super only):
    - `r.Get("/payslips", d.Payroll.ListPayslips)`
    - `r.Get("/payslips/{id}", d.Payroll.GetPayslip)`
    - `r.Get("/payslips/{id}/audit-notes", d.Payroll.ListAuditNotes)`
    - `r.With(d.Idempotency.Handler).Post("/payslips/{id}/audit-notes", d.Payroll.CreateAuditNote)`
    - `r.With(d.Idempotency.Handler).Post("/payslips:export", d.Payroll.ExportPayslips)`
    Add `Payroll *payrollhttp.Handler` to the Deps struct + a "PAYROLL slice end (10-02). Phase 10+ appends after this line." marker. (chi matches the `:export` action suffix natively — decision [02-01].)

    Edit `backend/cmd/seed/seed.go`: add `seedPayroll(ctx, pool)` (call after seedOvertime). Build a `crypto.Cipher` from `os.Getenv("PAYROLL_ENCRYPTION_KEY")` and use `Encrypt` to produce the ciphertext for every money column. Explicit ids, `ON CONFLICT (id) DO NOTHING`. Clearly-in-range Asia/Jakarta dates (period 2025-11 + 2025-12). Rows:
    - `SWP-PS-90121` employee SWP-EMP-1042 (Budi), period 2025-12, FINAL, working_days 22, gross_earnings "8500000.00"/gross_deductions "1175000.00"/take_home_pay "7325000.00" (all encrypted) + components: EARNING "Gaji Pokok" 6500000.00 for_bpjs=true, "Tunjangan Transport" 1200000.00, "Tunjangan Makan" 800000.00; DEDUCTION "BPJS Kesehatan (1%)" 65000.00 for_bpjs=true, "BPJS Ketenagakerjaan (JHT 2%)" 130000.00, "PPh 21" 915000.00; benefits "BPJS Kesehatan (employer 4%)" 260000.00, "BPJS JKK" 16900.00. source lumen_swp #44218.
    - `SWP-PS-90122` SWP-EMP-1108 period 2025-12 FINAL (a second decryptable archive row for list/export volume).
    - `SWP-PS-90123` SWP-EMP-2891 period 2025-11 FINAL.
    - `SWP-PS-90119` SWP-EMP-1108 (Rudi Hartono) period 2025-12 **DECRYPT_FAIL**: write status='DECRYPT_FAIL' AND deliberately-corrupt ciphertext into the `*_enc` columns (e.g. `'\xdeadbeef'::bytea` raw garbage that the AES-GCM Open will reject) so the API's Decrypt returns ErrDecrypt at read time — the row surfaces decrypt_fail honestly, NOT a hardcoded flag the service trusts. working_days NULL. source lumen_swp #44216.
    - A couple more FINAL rows across periods so an export produces a non-trivial-but-under-threshold file.
    - Audit notes on `SWP-PS-90119`: `SWP-PS-90119-NOTE-1` (author SWP-EMP-9001 'Sari Hadi', the decrypt-fail review note) + `SWP-PS-90119-NOTE-2` (seq 2). Insert with explicit composite ids + seq.
    Run `make gen && go build ./... && go vet ./...`.
  </action>
  <verify>
    <automated>cd backend && grep -q "PayslipExportWorker\|NewPayslipExportWorker" internal/platform/jobs/jobs.go && grep -q "/payslips:export" internal/server/server.go && grep -q "/payslips/{id}/audit-notes" internal/server/server.go && grep -q "RoleHRAdmin\|RoleSuperAdmin" internal/server/server.go && grep -q "d.Payroll" cmd/api/main.go && grep -q "NewInsertOnlyClient" cmd/api/main.go && grep -q "NewFromBase64\|crypto.New" cmd/api/main.go && grep -q "seedPayroll" cmd/seed/seed.go && grep -q "SWP-PS-90119" cmd/seed/seed.go && grep -q "SWP-PS-90119-NOTE-1" cmd/seed/seed.go && go build ./... && go vet ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `jobs.go` registers `PayslipExportWorker` (constructed with the pool in NewWorkerClient).
    - `main.go` constructs `crypto.NewFromBase64(cfg.Crypto.PayrollKey)` + `jobs.NewInsertOnlyClient(pool)` (newly wired) + the Payroll slice and assigns `Deps.Payroll`.
    - `server.go` registers the 5 E8 endpoints under a single `RequireRole(hr_admin, super_admin)` group with the two POSTs idempotency-wrapped; no agent/shift_leader access; no 405-immutable handlers.
    - `seed.go` seeds payslips (encrypted with the env payroll key) incl. the garbage-ciphertext `SWP-PS-90119` DECRYPT_FAIL row + its two audit notes; FINAL rows across 2025-11/2025-12.
    - `go build ./... && go vet ./...` exit 0.
  </acceptance_criteria>
  <done>jobs.Client wired into the API, the export worker registered, E8 routes mounted under hr/super RBAC, seed plants FINAL + DECRYPT_FAIL payslips + notes encrypted with the real key; build + vet clean.</done>
</task>

</tasks>

<verification>
- `go build ./... && go vet ./... && gofmt -l backend/internal/{service,handler,repository}/payroll backend/internal/platform/{crypto,jobs}` clean.
- Manual route sanity: exactly the 5 FE-used E8 endpoints map to handlers (GET /payslips, GET /payslips/{id}, GET /payslips/{id}/audit-notes, POST /payslips/{id}/audit-notes, POST /payslips:export). No 405-immutable, no PDF, no forward-export.
- `grep -c "Idempotency.Handler" backend/internal/server/server.go` increased by 2 (the two POSTs).
</verification>

<success_criteria>
Payslips list/detail decrypt at the boundary and surface DECRYPT_FAIL as a 200 row status (money
nulled, breakdown empty), audit notes list + append (append-only, audited in-tx), and
POST /payslips:export inserts an export_jobs QUEUED row + enqueues a real River PayslipExportWorker
in ONE tx (202 + the exact job stub) which the worker completes by flipping export_jobs to DONE.
EXPORT_TOO_LARGE returns 422; every endpoint is hr_admin/super_admin only. Matches the
openapi + the built e8-payroll components byte-for-byte.
</success_criteria>

<output>
After completion, create `.planning/phases/10-e8-payroll/10-02-SUMMARY.md`.
</output>
