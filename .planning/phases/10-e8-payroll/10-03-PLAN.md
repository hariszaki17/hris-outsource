---
phase: 10-e8-payroll
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - backend/internal/handler/payroll/payroll_testkit_test.go
  - backend/internal/handler/payroll/payslip_handler_test.go
  - backend/internal/handler/payroll/export_handler_test.go
autonomous: true
requirements: [PAY-01, PAY-02]
user_setup: []

must_haves:
  truths:
    - "GET /payslips asserts the {data,next_cursor,has_more} cursor envelope with FINAL + a DECRYPT_FAIL row at 200 (status DECRYPT_FAIL, money null) — not a 4xx"
    - "GET /payslips/{id} asserts the {data:<Payslip>} envelope with the full breakdown for FINAL and nulled money + empty arrays for a DECRYPT_FAIL row"
    - "Audit notes list returns {data,...}; create returns 201 with the composite {payslip_id}-NOTE-{seq} id; empty/blank text → 400"
    - "POST /payslips:export returns 202 + {id(job),status:QUEUED,...} and enqueues a job; EXPORT_TOO_LARGE → 422; period-or-year missing → 422"
    - "Every payroll endpoint returns 403 for agent/shift_leader (RBAC)"
  artifacts:
    - path: "backend/internal/handler/payroll/payslip_handler_test.go"
      provides: "list/detail (FINAL + DECRYPT_FAIL) + audit-notes + RBAC 403 contract tests"
      contains: "DECRYPT_FAIL"
    - path: "backend/internal/handler/payroll/export_handler_test.go"
      provides: "export 202 + job enqueue + EXPORT_TOO_LARGE 422 contract tests"
      contains: "EXPORT_TOO_LARGE"
    - path: "backend/internal/handler/payroll/payroll_testkit_test.go"
      provides: "fake repos + fakeTx + fakeJobs + crypto cipher + newHarness mounting real services+handler on chi"
      contains: "newHarness"
  key_links:
    - from: "payslip_handler_test.go"
      to: "real payroll service + handler"
      via: "newHarness(role) on chi with mutable-principal middleware + RequireRole groups"
      pattern: "newHarness"
    - from: "export_handler_test.go"
      to: "fakeJobs.EnqueueTx recorder"
      via: "assert a PayslipExportArgs job was enqueued in the export tx"
      pattern: "EnqueueTx"
---

<objective>
Write the Go contract tests for E8 — the drift gate that replaces server codegen. Mirrors the
Phase-9 overtime harness (09-03) EXACTLY: fakeTx + in-memory fake payslip/export repos + a
fakeJobs EnqueueTx recorder + a real `*crypto.Cipher` over the REAL services + handler via
`newHarness(role)` on chi with a mutable-principal closure middleware + `stubIdempotency` at the
server.go router position. Asserts the cursor + {data} envelopes, the DECRYPT_FAIL-as-200-row-status
(list + detail), audit-notes list/create (composite id, 400 on blank), export 202 + job enqueue,
EXPORT_TOO_LARGE 422, and RBAC 403 for agent/shift_leader.

Purpose: Pin the E8 BE responses to the openapi so the FE generated client never drifts.
Output: testkit + two table-driven test files; `go test ./internal/handler/payroll/...` green.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-e8-payroll/10-CONTEXT.md
@.planning/reference/backend-build-conventions.md
@docs/api/E8-payroll/openapi.yaml

<interfaces>
<!-- COPY the Phase-9 overtime harness shape exactly. -->
  backend/internal/handler/overtime/overtime_testkit_test.go  (fakeTx Exec no-op for audit-in-tx + fakeTxRunner + in-memory fake repos sharing state + newHarness(role,company,employee) mounting real services+handler on chi + mutable-principal closure middleware + stubIdempotency at the router position)
  backend/internal/handler/overtime/overtime_handler_test.go  (table-driven 200/403/422 + cursor + {data} envelope assertions; decodeBody snapshots rr.Body.Bytes() so one response re-decodes for errCode + errFields — decision [08-03])
  backend/internal/handler/foundations/  (the simplest read+list+write contract-test analog — closest to E8's non-workflow shape)

  The E8 payroll DECRYPT_FAIL path is unique: the fake payslip repo must return BOTH valid ciphertext
  (Encrypt with the harness's crypto.Cipher) AND deliberately-garbage ciphertext (random bytes) so the
  REAL service's Decrypt boundary produces FINAL vs DECRYPT_FAIL — assert the row status, not a stub flag.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test kit (fakes + harness + cipher) + payslip list/detail + audit-notes + RBAC tests</name>
  <files>backend/internal/handler/payroll/payroll_testkit_test.go, backend/internal/handler/payroll/payslip_handler_test.go</files>
  <read_first>
    - backend/internal/handler/overtime/overtime_testkit_test.go (the harness to copy: fakeTx, fakeTxRunner, fake repos sharing state, newHarness, mutable-principal middleware, stubIdempotency at the router position, decodeBody snapshot)
    - backend/internal/handler/foundations/*_test.go (read/list/write contract-test assertions — the simplest analog)
    - backend/internal/service/payroll/payslip_service.go (this phase-2 plan's service — the system under test) + internal/platform/crypto/crypto.go (Encrypt to build valid ciphertext fixtures)
    - docs/api/E8-payroll/openapi.yaml getPayslip (FINAL vs DECRYPT_FAIL field-nulling) + listPayslips (cursor + meta.code) + audit-notes list/create + the Forbidden response
  </read_first>
  <action>
    Create `backend/internal/handler/payroll/payroll_testkit_test.go`:
    - `fakeTx` (Exec no-op so audit-in-tx + InsertAuditNote don't panic — copy overtime's), `fakeTxRunner` (InTx runs the closure with fakeTx).
    - `fakeJobs` implementing the service `Jobs` port: `EnqueueTx(ctx, tx, args)` records the args into a slice (`enqueued []river.JobArgs`) so export tests assert a PayslipExportArgs was enqueued.
    - `fakePayslipRepo`: map[id]row where each row carries `*_enc []byte` ciphertext. Helper `seedFinal(id, employee, period, money...)` Encrypts the money with the harness cipher; `seedDecryptFail(id, ...)` stores RANDOM garbage bytes in the money columns so the REAL service Decrypt returns ErrDecrypt. Also holds components/benefits per id + an audit-notes slice + CountAuditNotes/InsertAuditNote/PayslipExists.
    - `fakeExportRepo`: configurable CountPayslipsInScope (to drive EXPORT_TOO_LARGE) + InsertExportJob (returns a QUEUED job) + GetExportJob.
    - A real `*crypto.Cipher` built from a fixed 32-byte test key (the SAME cipher used to seed valid ciphertext AND injected into the service — so FINAL rows decrypt and garbage rows fail).
    - `newHarness(role)` builds the REAL PayslipService + ExportService + Handler with the fakes + cipher + fakeJobs, mounts them on a chi router under the SAME `RequireRole(hr_admin, super_admin)` group + `stubIdempotency` middleware at the server.go position, with a mutable-principal closure middleware so a test can swap the role. `decodeBody` snapshots `rr.Body.Bytes()` for re-decode (errCode + errFields).

    Create `backend/internal/handler/payroll/payslip_handler_test.go` table-driven tests:
    - LIST-mixed: GET /payslips returns `{data:[FINAL, DECRYPT_FAIL], next_cursor, has_more}` — assert the FINAL row has money strings ("8500000.00") + status FINAL + decrypt_fail false, and the DECRYPT_FAIL row has money JSON `null` + status "DECRYPT_FAIL" + decrypt_fail true + locked_reason "decrypt_fail", at HTTP **200** (NOT 4xx).
    - LIST-empty: zero rows → 200 + `data:[]` + `meta.code: "MISSING_PAYROLL_HISTORY"`.
    - LIST-filter: status=DECRYPT_FAIL / period / year narrows the page (assert the filter reaches the repo).
    - DETAIL-final: GET /payslips/{id} → `{data:{...}}` envelope; earnings/deductions/benefits populated with decrypted values + for_bpjs; source {system:"lumen_swp", source_id}; read_only true.
    - DETAIL-decrypt-fail: GET a garbage-ciphertext payslip → 200, `{data}` with status DECRYPT_FAIL, money null, `earnings/deductions/benefits == []` (empty arrays), locked_reason "decrypt_fail".
    - NOTES-list: GET /payslips/{id}/audit-notes → `{data:[...], next_cursor, has_more}` oldest-first; allowed on a DECRYPT_FAIL payslip.
    - NOTES-create: POST {text:"…"} → 201 + body PayslipAuditNote with id `"{payslip_id}-NOTE-{seq}"`, author from the principal; blank/whitespace text → 400; missing payslip → 404.
    - RBAC: for EACH of the 5 endpoints, newHarness("agent") and newHarness("shift_leader") → 403 (the RequireRole group rejects before the handler).
  </action>
  <verify>
    <automated>cd backend && grep -q "newHarness" internal/handler/payroll/payroll_testkit_test.go && grep -q "seedDecryptFail\|garbage\|DecryptFail" internal/handler/payroll/payroll_testkit_test.go && grep -q "fakeJobs\|EnqueueTx" internal/handler/payroll/payroll_testkit_test.go && grep -q "DECRYPT_FAIL" internal/handler/payroll/payslip_handler_test.go && grep -q "MISSING_PAYROLL_HISTORY" internal/handler/payroll/payslip_handler_test.go && grep -qE "NOTE-|audit-notes" internal/handler/payroll/payslip_handler_test.go && go test ./internal/handler/payroll/... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `payroll_testkit_test.go` defines `newHarness`, `fakeTx`, fake payslip/export repos with `seedFinal`/`seedDecryptFail` (valid vs garbage ciphertext), a real `*crypto.Cipher`, a recording `fakeJobs`, and `stubIdempotency`.
    - `payslip_handler_test.go` asserts: list mixed FINAL+DECRYPT_FAIL at 200, empty→meta.code, detail {data} for FINAL + nulled/empty for DECRYPT_FAIL, audit-notes list + create (composite id, 400 blank, 404 missing), and 403 for agent + shift_leader on all 5 endpoints.
    - `go test ./internal/handler/payroll/...` exits 0.
  </acceptance_criteria>
  <done>Read + DECRYPT_FAIL-as-200 + audit-notes + RBAC contract tests pass against the real services through the chi harness.</done>
</task>

<task type="auto">
  <name>Task 2: Export 202 + job-enqueue + EXPORT_TOO_LARGE + confidential-lock tests</name>
  <files>backend/internal/handler/payroll/export_handler_test.go</files>
  <read_first>
    - backend/internal/handler/payroll/payroll_testkit_test.go (the fakeExportRepo + fakeJobs recorder from Task 1)
    - backend/internal/service/payroll/export_service.go (EXPORT_TOO_LARGE + confidential lock + EnqueueTx in-tx)
    - docs/api/E8-payroll/openapi.yaml exportPayslips (202 PayslipExportJob body + EXPORT_TOO_LARGE 422 with fields + the confidential server-enforced-true + period-or-year rule)
  </read_first>
  <action>
    Create `backend/internal/handler/payroll/export_handler_test.go` table-driven tests (HR principal):
    - EXPORT-202: POST /payslips:export {period:"2025-12", format:"XLSX", confidential:false} → **202**; body = PayslipExportJob {id matches `^SWP-EXP-\d+$`, status:"QUEUED", format:"XLSX", confidential:**true** (server-forced despite the false input), requested_by:{id,...}, scope:{period:"2025-12", employee_ids:[]}, poll_url:"/api/v1/exports/{id}"}. ASSERT `fakeJobs.enqueued` contains exactly one `PayslipExportArgs` with `JobID == body.id` (the transactional-outbox enqueue happened).
    - EXPORT-by-year: {year:2025, employee_ids:["SWP-EMP-1042"]} → 202; scope echoes year + employee_ids.
    - EXPORT-too-large: configure fakeExportRepo.CountPayslipsInScope to return > threshold → 422, code EXPORT_TOO_LARGE, `error.fields` carries a period/size message (assert via the decodeBody errFields snapshot). ASSERT NO job was enqueued (fakeJobs.enqueued empty).
    - EXPORT-no-scope: POST {} (no period, no year) → 422 (RULE_VIOLATION or the period-or-year field error).
    - EXPORT-rbac: agent + shift_leader → 403.
  </action>
  <verify>
    <automated>cd backend && grep -q "EXPORT_TOO_LARGE" internal/handler/payroll/export_handler_test.go && grep -qE "202|StatusAccepted" internal/handler/payroll/export_handler_test.go && grep -q "PayslipExportArgs\|enqueued" internal/handler/payroll/export_handler_test.go && grep -q "QUEUED" internal/handler/payroll/export_handler_test.go && go test ./internal/handler/payroll/... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `export_handler_test.go` asserts the 202 + exact PayslipExportJob body (confidential coerced to true, scope echoed, poll_url) AND that exactly one PayslipExportArgs with the matching JobID was enqueued.
    - EXPORT_TOO_LARGE asserts 422 + `error.fields` + NO job enqueued; no-scope → 422; agent/shift_leader → 403.
    - `go test ./internal/handler/payroll/...` exits 0.
  </acceptance_criteria>
  <done>The async-export contract (202 + job-enqueue + EXPORT_TOO_LARGE + confidential lock + RBAC) is pinned to the openapi; full payroll test package green.</done>
</task>

</tasks>

<verification>
- `cd backend && go test ./internal/handler/payroll/...` exits 0.
- `go test ./...` exits 0 (no regression in earlier epics' contract tests).
- Every E8-specific surface in the openapi is asserted by at least one test: DECRYPT_FAIL-as-200 (list + detail), MISSING_PAYROLL_HISTORY, the audit-note composite id + 400/404, EXPORT_TOO_LARGE 422, the 202 + job-enqueue, the confidential-true lock, and RBAC 403. (PAYSLIP_IMMUTABLE 405 is out of scope — the FE never calls a payslip write.)
</verification>

<success_criteria>
The E8 contract tests are the drift gate: the cursor + {data} envelopes, DECRYPT_FAIL surfaced as a
200 row status (money null, breakdown empty) in BOTH list and detail driven by REAL garbage-ciphertext
through the real Decrypt boundary, audit-notes list/create (composite id, 400/404), the export 202 +
a recorded PayslipExportArgs enqueue, EXPORT_TOO_LARGE 422 with field errors + no-enqueue, and RBAC
403 for agent/shift_leader are all asserted against the REAL services through the chi harness.
`go test ./...` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/10-e8-payroll/10-03-SUMMARY.md`.
</output>
