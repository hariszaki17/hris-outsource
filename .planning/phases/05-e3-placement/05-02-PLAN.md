---
phase: 05-e3-placement
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/internal/platform/apperr/apperr.go
  - backend/internal/platform/httpx/response.go
  - backend/internal/platform/i18n/i18n.go
  - backend/internal/repository/placement/placements_repo.go
  - backend/internal/repository/placement/shift_leader_repo.go
  - backend/internal/service/placement/placement_service.go
  - backend/internal/service/placement/shift_leader_service.go
  - backend/internal/handler/placement/placements_handler.go
  - backend/internal/handler/placement/placements_dto.go
  - backend/internal/handler/placement/shift_leader_handler.go
  - backend/internal/handler/placement/roster_handler.go
  - backend/internal/server/server.go
  - backend/cmd/api/main.go
  - backend/cmd/seed/seed.go
autonomous: true
requirements: [PLC-01, PLC-02, PLC-03, PLC-04]
must_haves:
  truths:
    - "HR can create a placement; a second active placement for the same agent returns 409 INV_1_VIOLATION with error.details.current_placement"
    - "Lifecycle actions renew/transfer/end/resign/terminate work, write placement_history + audit, and mutating a terminal placement returns 409 TERMINAL_STATE_IMMUTABLE"
    - "Assigning a second leader to a company returns 409 INV_2_VIOLATION; assigning a non-placed agent returns 409 INV_4_VIOLATION; an already-leading agent returns 409 INV_3_VIOLATION"
    - "GET /client-companies/{id}/roster returns active+scheduled placements, current_shift_leader, and summary counts; shift_leader requesting another company's roster gets 403 OUT_OF_SCOPE"
    - "The response JSON for placement/detail/roster carries every field the built e3-placement components destructure (no missing/renamed field)"
  artifacts:
    - path: "backend/internal/service/placement/placement_service.go"
      provides: "INV-1 pre-check, lifecycle state machine, transfer/renew atomicity, history+audit"
      contains: "INV_1_VIOLATION"
    - path: "backend/internal/service/placement/shift_leader_service.go"
      provides: "INV-2/3/4 enforcement via FOR UPDATE locks + leader_scope (company|site)"
      contains: "INV_2_VIOLATION"
    - path: "backend/internal/platform/apperr/apperr.go"
      provides: "Details field on apperr.Error so INV violations carry INVViolationDetails"
      contains: "Details"
    - path: "backend/cmd/seed/seed.go"
      provides: "active placements for 1042/1108/2891 + one active SLA + one expiring placement"
      contains: "seedPlacements"
  key_links:
    - from: "backend/internal/server/server.go"
      to: "placement handlers"
      via: "chi routes under RequireRole with : action suffixes"
      pattern: "/placements"
    - from: "backend/internal/service/placement/placement_service.go"
      to: "placement_history + audit_log"
      via: "InsertPlacementHistory + audit.Record inside InTx"
      pattern: "InsertPlacementHistory"
    - from: "backend/internal/handler/placement/placements_dto.go"
      to: "docs/api/E3-placement/openapi.yaml Placement schema"
      via: "snake_case JSON response mapping"
      pattern: "lifecycle_status"
---

<objective>
Build the E3 service + handler layers: placement CRUD + lifecycle state machine (renew /
transfer / end / resign / terminate) with INV-1..4 enforcement, transfer/renew atomicity,
date-derived lifecycle_status at the DTO boundary, placement_history + audit on every
action, shift-leader create/replace/end with leader_scope (company AND site), and the
company roster. Wire routes in server.go + Deps in main.go, and extend the seed. Also
extend the platform error envelope to carry structured `error.details` (the spec requires it
for INV violations — the current envelope has no details field).

Purpose: deliver the project's gnarliest epic correctly and race-proof. Mirror the Phase-4
people slice shape EXACTLY (consumer-defined repo interface, TxRunner, Clock, apperr codes,
audit in tx, supersede-before-insert).
Output: the 13 FE-used E3 endpoints, byte-for-shape against openapi.yaml, seeded for E2E.

**Guiding principle (LOCKED):** Visual + interaction reference = the built
`frontend/apps/web/src/features/e3-placement/*` code (authoritative), with `brainstorm.pen` as
secondary. The API contract is `docs/api/E3-placement/openapi.yaml`, but the response shapes this
plan produces MUST also satisfy what the actual FE components consume — verify the built screen
destructures every field the BE returns (no missing/renamed field the component reads). Match real
component data needs, not assumptions.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-e3-placement/05-CONTEXT.md
@.planning/phases/05-e3-placement/05-01-SUMMARY.md
@.planning/reference/backend-build-conventions.md

<interfaces>
<!-- MIRROR: backend/internal/service/people/agreements_service.go (RenewAgreement supersede-before-insert,
     apperr.Conflict, audit.Record in InTx) is the closest analog — copy its structure. -->

<!-- AUTHORITATIVE FE CONSUMERS (LOCKED) — the response JSON this plan emits must satisfy these.
     Fields the built components destructure (BE response MUST carry all of them, snake_case):
       placements-screen.tsx (list rows):   id, employee_id, employee_name, client_company_name,
         service_line_name, position_name, start_date, end_date (null=open-ended), lifecycle_status;
         page wrapper { data, next_cursor, has_more }.
       placement-detail-screen.tsx:          PlacementDetailResponse { placement, history_chain[],
         current_shift_leader|null }. placement reads (additionally): warnings[] ('NO_SHIFT_LEADER_AT_COMPANY'),
         agreement_type, agreement_id, annual_leave_entitlement_days, base_salary_ref_idr, notes, created_by,
         created_at, status_changed_at, successor_id, termination_reason, resign_at, client_company_id.
         history_chain item (PlacementSummary): id, client_company_name, service_line_name, start_date,
         end_date, lifecycle_status. current_shift_leader: id, employee_id, employee_name.
       company-roster-screen.tsx:             CompanyRosterResponse { company_name, placements[],
         current_shift_leader|null (id,employee_id,employee_name), summary { total_active, total_scheduled,
         total_expiring, by_service_line[{service_line_id, service_line_name}], by_status }, has_more, next_cursor }.
       placement-form.tsx (INV-1):            error.details = INVViolationDetails with current_placement
         { client_company_name, client_company_id, lifecycle_status } and suggested_actions[] (enum
         end|transfer|replace|end_existing_first|assign_after_placement).
     Read the component files when wiring the DTOs to confirm exact destructured names. -->

apperr (backend/internal/platform/apperr/apperr.go) TODAY:
  type Error struct { Code string; Message string; Fields map[string]string; HTTPStatus int; cause error }
  func Conflict(code string) *Error   -> {Code:code, HTTPStatus:409}
  func Rule(code string, fields) *Error -> {Code:code, Fields, HTTPStatus:422}
  func Invalid(fields) *Error -> {Code:"INVALID_REQUEST", Fields, HTTPStatus:400}
  func OutOfScope() *Error -> {Code:"OUT_OF_SCOPE", HTTPStatus:403}
  // statusForCode: any INV_*_VIOLATION + CONFLICT + IDEMPOTENCY_KEY_REUSED => 409; else 422.
  // GAP: there is NO Details field. The error envelope (errBody) has Code/Message/Fields/RequestID only.

httpx error envelope (backend/internal/platform/httpx/response.go):
  type errBody struct { Code string `json:"code"`; Message string `json:"message"`;
                        Fields map[string]string `json:"fields,omitempty"`; RequestID string `json:"request_id"` }
  func WriteError reads ae.Code/Message/Fields only.

rbac (backend/internal/platform/rbac):
  func RequireRole(allowed ...auth.Role) middleware
  func GuardCompany(ctx, resourceCompanyID string) error  // SL passes only if p.CompanyID == resourceCompanyID, else OutOfScope
  auth.RoleSuperAdmin / RoleHRAdmin / RoleShiftLeader / RoleAgent

TxRunner / Clock (defined in service/people/employees_service.go) — define equivalents in the placement package.
audit.Record(ctx, tx, audit.Entry{Action, EntityType, EntityID, Before, After}) inside InTx.
httpx: ClampLimit, EncodeCursor/DecodeCursor, PageResponse[T], WriteJSON, WriteError.

server.go COORDINATION: append the new placement r.Group{} blocks AFTER the line
  "// PEOPLE change-requests slice end (04-04). Phase 5+ appends after this line."

seed.go: Seed() currently seeds employees -> users -> audit -> companies -> sites -> service-lines
  -> master-data -> agreements -> change-requests. Only SWP-EMP-2891 has an active agreement
  (SWP-AG-7001). Persona placements for 1042 and 1108 need agreements first.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend platform error envelope with structured details + register E3 i18n codes</name>
  <read_first>
    - backend/internal/platform/apperr/apperr.go              (Error struct, Conflict/Rule helpers, statusForCode/isConflictCode)
    - backend/internal/platform/httpx/response.go             (errBody + WriteError serialization)
    - backend/internal/platform/i18n/i18n.go                  (messages map[Lang]map[string]string; id + en blocks)
    - docs/api/E3-placement/openapi.yaml                      (ErrorResponse + INVViolationDetails schema ~1315-1430)
    - frontend/apps/web/src/features/e3-placement/placement-form.tsx  (AUTHORITATIVE: how the FE reads error.details.current_placement + suggested_actions — the details payload must match these names)
  </read_first>
  <action>
The spec requires `error.details` (INVViolationDetails) on INV-1/2/3/4 409s. The current
envelope cannot carry it. Make the minimal additive change:

1. In `apperr/apperr.go`: add `Details any` field to `Error` (after `Fields`). Add a helper
   `func ConflictWithDetails(code string, fields map[string]string, details any) *Error`
   returning `&Error{Code: code, Fields: fields, Details: details, HTTPStatus: http.StatusConflict}`.
   Leave `Conflict`, `Rule`, `Invalid`, `OutOfScope` untouched.

2. In `httpx/response.go`: add `Details any `json:"details,omitempty"`` to `errBody`; in
   `WriteError`, set `Details: ae.Details` in the constructed `errBody`. `omitempty` keeps all
   existing responses byte-identical (Phase 1-4 errors carry nil details).

3. In `i18n/i18n.go`: add to BOTH the id (Bahasa, default) and en maps these codes with
   messages copied from the spec examples (Bahasa) — use the exact spec strings:
     - `INV_1_VIOLATION`: "Agen sudah memiliki penempatan aktif. Akhiri atau transfer terlebih dahulu."
     - `INV_2_VIOLATION`: "Perusahaan sudah memiliki shift leader aktif. Konfirmasi penggantian."
     - `INV_3_VIOLATION`: "Kandidat sudah menjadi shift leader di perusahaan lain. Akhiri penugasan tersebut dulu."
     - `INV_4_VIOLATION`: "Kandidat tidak ditempatkan secara aktif di perusahaan ini."
     - `COMPANY_INACTIVE`: "Perusahaan klien tidak aktif. Tidak dapat membuat penempatan."
     - `PLACEMENT_OUTSIDE_CONTRACT`: "Periode penempatan di luar masa berlaku perjanjian kerja."
     - `PLACEMENT_PERIOD_OVERLAP`: "Periode penempatan tumpang tindih dengan penempatan lain."
     - `TERMINAL_STATE_IMMUTABLE`: "Penempatan sudah dalam status final dan tidak dapat diubah."
     - `LEADER_NOT_ELIGIBLE`: "Karyawan tidak memenuhi syarat untuk peran shift leader."
     - `ALREADY_ENDED`: "Penugasan sudah berakhir."
     - `NO_ACTIVE_LEADER`: "Perusahaan ini belum memiliki shift leader."
     - `RULE_VIOLATION`: "Permintaan melanggar aturan bisnis."
   Add concise English equivalents in the en map.
   `statusForCode` already maps INV_*_VIOLATION + CONFLICT to 409; `COMPANY_INACTIVE`,
   `TERMINAL_STATE_IMMUTABLE`, `ALREADY_ENDED` are NOT prefixed INV_/CONFLICT so they would
   default to 422 — create them via `apperr.Conflict("COMPANY_INACTIVE")` etc. (explicit 409),
   OR extend `isConflictCode` to include them. Choose explicit `apperr.Conflict(...)` /
   `apperr.ConflictWithDetails(...)` in the services (cleaner; mirrors Phase-4
   `ACTIVE_AGREEMENT_EXISTS` using `apperr.Conflict`). `NO_ACTIVE_LEADER` is 404 — build via
   `&apperr.Error{Code:"NO_ACTIVE_LEADER", HTTPStatus: http.StatusNotFound}`.
  </action>
  <verify>
    <automated>cd backend && grep -q "Details any" internal/platform/apperr/apperr.go && grep -q "details,omitempty" internal/platform/httpx/response.go && grep -q "INV_1_VIOLATION" internal/platform/i18n/i18n.go && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `apperr/apperr.go` contains `Details any` and `func ConflictWithDetails`.
    - `httpx/response.go` `errBody` contains `json:"details,omitempty"` and `WriteError` sets `Details: ae.Details`.
    - `i18n/i18n.go` contains all 12 E3 codes listed above in both the id and en maps.
    - The INVViolationDetails shape carries the field names placement-form.tsx reads (`current_placement.client_company_name/client_company_id/lifecycle_status`, `suggested_actions`).
    - `go build ./...` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Placement repository + service — CRUD, lifecycle state machine, INV-1, transfer/renew atomicity, history+audit</name>
  <read_first>
    - backend/internal/service/people/agreements_service.go    (RenewAgreement supersede-before-insert; CreateAgreement INV pre-check; audit in InTx — the exact pattern to copy)
    - backend/internal/repository/people/agreements_repo.go     (repo mapping sqlc rows -> domain; pgx.ErrNoRows -> domain.ErrNotFound)
    - backend/internal/domain/placement.go                      (05-01 domain types/filters)
    - .planning/phases/05-e3-placement/05-01-SUMMARY.md         (exact sqlc query names + domain field names)
    - docs/api/E3-placement/openapi.yaml                        (POST/PATCH/:transfer/:renew/:end/:terminate/:resign request+response schemas + 409/422 examples)
    - frontend/apps/web/src/features/e3-placement/placements-screen.tsx        (AUTHORITATIVE list consumer: which Placement fields the columns render — list response must carry them)
    - frontend/apps/web/src/features/e3-placement/placement-detail-screen.tsx  (AUTHORITATIVE detail consumer: PlacementDetailResponse + every placement.* field the screen destructures, incl. warnings/agreement_type/successor_id/termination_reason/resign_at)
    - frontend/apps/web/src/features/e3-placement/placement-overlays.tsx       (AUTHORITATIVE: exact transfer/renew/end/terminate/resign request field names the FE sends)
    - docs/epics/E3-placement/prds/agent-placement.md           (BR-1..9, C-#)
    - docs/epics/E3-placement/prds/placement-lifecycle.md       (LC-1..8 state machine)
    - docs/epics/E3-placement/prds/replacement-transfer.md      (TR-1..6 transfer mechanics)
  </read_first>
  <action>
Create `backend/internal/repository/placement/placements_repo.go` implementing a consumer-defined
`PlacementRepository` interface over the 05-01 sqlc queries (mirror agreements_repo.go). Reads on
pool; writes take `pgx.Tx`. Map `pgx.ErrNoRows -> domain.ErrNotFound`.

Create `backend/internal/service/placement/placement_service.go` (mirror agreements_service.go):
define `TxRunner` interface, `Clock`, `PlacementService` struct, `NewPlacementService`, `SetClock`.

Implement methods returning exact apperr codes:

- `ListPlacements(ctx, filter)` — clamp limit, translate `expiring_within_days` to
  end_date <= today(Asia/Jakarta)+N AND status IN (ACTIVE,EXPIRING); keyset cursor on
  (status_changed_at,id); default sort status_changed_at:desc. Derive lifecycle_status at the
  DTO boundary (handler), not here.
- `GetPlacement(ctx, id)` -> placement + history_chain (GetPlacementChain) + current_shift_leader.
  404 -> apperr.NotFound().
- `CreatePlacement(ctx, params)`:
    1. Employee exists + ACTIVE else NotFound/Rule. Company exists + status ACTIVE else
       `apperr.Conflict("COMPANY_INACTIVE")` (BR-3). Site belongs to company else Invalid. Agreement
       belongs to employee + active.
    2. end_date<=start_date -> apperr.Invalid({"end_date":...}) (BR-4). start_date<today without
       backdate_reason -> apperr.Invalid({"backdate_reason":...}) (BR-6).
    3. PKWT period outside agreement: auto-cap end_date to agreement end and append
       warning "END_DATE_AUTO_CAPPED_TO_AGREEMENT"; out-of-range START -> 422
       apperr.Rule("PLACEMENT_OUTSIDE_CONTRACT", {...}) (BR-1b).
    4. INV-1 service pre-check: `GetActivePlacementForEmployee`; if found ->
       `apperr.ConflictWithDetails("INV_1_VIOLATION", {"employee_id":...}, INVViolationDetails{
         Invariant:"INV_1", CurrentPlacement: summary, SuggestedActions:["transfer","end"]})`.
    5. Inside InTx: LockEmployeePlacements (FOR UPDATE) re-check; CreatePlacement; InsertPlacementHistory
       (action="create", status_after=lifecycle); audit.Record(action "placement.create"). The DB
       partial unique index is the final backstop (catch unique-violation -> map to INV_1_VIOLATION).
    6. Soft warning: if target company has no active leader, append "NO_SHIFT_LEADER_AT_COMPANY".
    7. lifecycle on create: start_date<=today -> ACTIVE else PENDING_START (BR-5).
- `UpdatePlacement(ctx, id, patch)` — reject if terminal -> `apperr.Conflict("TERMINAL_STATE_IMMUTABLE")`;
  reject read-only fields present -> Invalid; update limited fields; history(action="update")+audit.
- `EndPlacement` / `ResignPlacement` / `TerminatePlacement`: load; if terminal ->
  TERMINAL_STATE_IMMUTABLE. Validate request (terminate requires `type_company_name_confirm`
  matching client_company.name case-insensitive trimmed else
  apperr.Invalid({"type_company_name_confirm":...}); termination_reason>=10 chars). In InTx:
  SetPlacementLifecycle(ENDED/RESIGNED/TERMINATED + ended_reason + ended_at/resign_at); if the
  agent leads this company, auto-end the SLA (vacated_reason=PLACEMENT_ENDED, assigned_by=system)
  — call the shift-leader service/repo; InsertPlacementHistory + audit. Add a stub comment
  `// TODO(Phase-11 notifications): enqueue NotificationArgs` at each resolution (Phase-4 pattern).
  NOTE the FE request field names (placement-overlays.tsx): End uses `reason` (enum
  END_OF_TERM|MUTUAL_AGREEMENT|CLIENT_REQUEST|OTHER), `effective_date`, `notes`; Resign uses
  `resign_at`, `resignation_reason`, `notes`; Terminate uses `termination_reason`, `effective_date`,
  `type_company_name_confirm`. Accept these exact names.
- `TransferPlacement(ctx, id, req)` (atomic, one InTx): reject terminal -> TERMINAL_STATE_IMMUTABLE.
  Request field names (placement-overlays.tsx TransferModal): `new_client_company_id`,
  `new_service_line_id`, `new_position_id`, `new_start_date`, `new_end_date`, `transfer_reason`.
  TR-1: if new company==current AND new service_line==current -> apperr.Rule("RULE_VIOLATION",...).
  Destination company must be ACTIVE else COMPANY_INACTIVE. In tx: close predecessor
  (lifecycle=TRANSFERRED, ended_reason=TRANSFERRED, ended_at = new_start_date − 1 day); create
  successor (predecessor_id set, status ACTIVE/PENDING_START, agreement defaults to predecessor's);
  backfill predecessor.successor_id; auto-end old leadership if agent led old company
  (vacated_reason=PLACEMENT_ENDED); history rows for both; audit. Warning
  "NO_SHIFT_LEADER_AT_DESTINATION" if new company has no leader.
- `RenewPlacement(ctx, id, req)`: SUPERSEDE-BEFORE-INSERT (copy RenewAgreement): request field names
  (RenewModal): `new_start_date`, `new_end_date`, `notes`. In tx set predecessor lifecycle=SUPERSEDED
  first (releases the partial unique index), then create successor (same employee/company/service_line/
  position unless overridden), backfill successor_id, history+audit. PKWT auto-cap on new_end_date.
  1-day buffer: new_start_date must be > predecessor.end_date else
  apperr.Rule("PLACEMENT_PERIOD_OVERLAP",...).

All time math uses the Asia/Jakarta TZ layer for "today". Reuse `httpx` cursor helpers.
Define an `INVViolationDetails` struct (and `PlacementSummary`) in the service package matching the
spec field names (json tags: invariant, current_placement, current_assignment, existing_assignment,
company_id, employee_id, employee_placements_at_company, suggested_actions).
  </action>
  <verify>
    <automated>cd backend && go build ./... && grep -q "INV_1_VIOLATION" internal/service/placement/placement_service.go && grep -q "TERMINAL_STATE_IMMUTABLE" internal/service/placement/placement_service.go && grep -q "SUPERSEDED" internal/service/placement/placement_service.go && grep -q "InsertPlacementHistory" internal/service/placement/placement_service.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `placements_repo.go` implements the `PlacementRepository` interface; maps `pgx.ErrNoRows` to `domain.ErrNotFound`.
    - `placement_service.go` references `INV_1_VIOLATION`, `COMPANY_INACTIVE`, `TERMINAL_STATE_IMMUTABLE`, `PLACEMENT_OUTSIDE_CONTRACT`, `PLACEMENT_PERIOD_OVERLAP`, `RULE_VIOLATION`.
    - Transfer and Renew each wrap their multi-write in a single `InTx`; Renew supersedes predecessor before inserting successor.
    - Every lifecycle method calls `InsertPlacementHistory` AND `audit.Record` inside the tx.
    - Each resolution method has a `TODO(Phase-11 notifications)` stub comment.
    - The list response carries every field placements-screen.tsx columns render (employee_name, client_company_name, service_line_name, position_name, start_date, end_date, lifecycle_status) and the detail response carries every field placement-detail-screen.tsx destructures (placement.warnings, agreement_type, successor_id, termination_reason, resign_at + history_chain + current_shift_leader) — i.e. the request DTOs accept and the response DTOs emit exactly the names the overlays/screens use.
    - `go build ./...` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Shift-leader service+repo (INV-2/3/4 + leader_scope) + handlers/DTOs + roster + routes + main.go + seed</name>
  <read_first>
    - backend/internal/handler/people/agreements_handler.go    (handler shape: decode -> svc -> WriteJSON/WriteError; chi.URLParam)
    - backend/internal/handler/people/agreements_dto.go         (snake_case response mapping + toXResponse helpers)
    - backend/internal/server/server.go                        (route group pattern; the "Phase 5+ appends after this line" marker)
    - backend/cmd/api/main.go                                   (Deps wiring for people slice — repo->svc->handler->Deps)
    - backend/cmd/seed/seed.go                                  (Seed() order; seedAgreements; explicit-ID INSERT ... ON CONFLICT pattern; persona IDs)
    - docs/api/E3-placement/openapi.yaml                        (shift-leader-assignments POST/:replace/:end + roster GET; ShiftLeaderAssignment + CompanyRoster schemas)
    - frontend/apps/web/src/features/e3-placement/company-roster-screen.tsx     (AUTHORITATIVE roster consumer: CompanyRosterResponse fields rendered — company_name, placements, current_shift_leader{id,employee_id,employee_name}, summary{total_active,total_scheduled,total_expiring,by_service_line[{service_line_id,service_line_name}]}, has_more, next_cursor)
    - frontend/apps/web/src/features/e3-placement/placement-overlays.tsx        (AUTHORITATIVE SLA consumer: ShiftLeaderAssignModal/Replace/End request DTOs — client_company_id/employee_id/start_date/replace/notes; new_employee_id/start_date/replace_reason; reason/effective_at — and var shapes)
    - frontend/apps/web/src/features/e2-identity/pickers/shift-leader-picker.tsx (eligibility filter role=shift_leader+assigned=false that the seed must satisfy so SLA-assign E2E has a candidate)
    - docs/epics/E3-placement/prds/shift-leader-assignment.md   (SL-1..7, INV-2/3/4, C-#)
    - docs/epics/E3-placement/prds/company-roster.md            (RO-1..8, scope C-4)
    - docs/epics/E3-placement/FEATURE.md                        (leader_scope company|site leadership unit)
  </read_first>
  <action>
**Shift-leader service** `backend/internal/service/placement/shift_leader_service.go`:
Resolve the company's `leader_scope` (read client_companies.leader_scope from Phase-3; default
'company'). Determine the leadership unit: company-scope -> lock via
`GetActiveLeaderForCompanyForUpdate`; site-scope -> require site_id and lock via
`GetActiveLeaderForSiteForUpdate`.
- `CreateAssignment(ctx, req)` in InTx: request field names (ShiftLeaderAssignModal): `client_company_id`,
  `employee_id`, `start_date`, `replace` (bool), `notes`.
    * INV-4: employee must have an ACTIVE placement at company (and, for site-scope, at that site) ->
      else `apperr.ConflictWithDetails("INV_4_VIOLATION", {...}, INVViolationDetails{Invariant:"INV_4",
      CompanyID, EmployeeID, EmployeePlacementsAtCompany:[...], SuggestedActions:["assign_after_placement"]})`.
      PENDING_START does NOT satisfy INV-4 (SL-2 / C-2).
    * INV-3: `GetActiveAssignmentForEmployeeForUpdate`; if leading another unit ->
      `apperr.ConflictWithDetails("INV_3_VIOLATION", {...}, {Invariant:"INV_3", ExistingAssignment, SuggestedActions:["end_existing_first"]})`.
    * INV-2: existing active leader for unit AND replace=false ->
      `apperr.ConflictWithDetails("INV_2_VIOLATION", {...}, {Invariant:"INV_2", CurrentAssignment, SuggestedActions:["replace"]})`.
      If replace=true: end existing (vacated_reason=REASSIGNED) then create new, atomically.
    * COMPANY_INACTIVE if company archived. LEADER_NOT_ELIGIBLE (422 via apperr.Rule) if employee inactive.
    * Create row; history is on the placement side, but write audit.Record(action "shift_leader.assign").
- `ReplaceAssignment(ctx, id, req)`: request field names (ShiftLeaderReplaceModal): `new_employee_id`,
  `start_date`, `replace_reason`. Load assignment; if already ended -> `apperr.Conflict("ALREADY_ENDED")`;
  end it (REASSIGNED) + create new for new_employee_id (revalidate INV-3/INV-4), atomic. audit.
- `EndAssignment(ctx, id, req)`: request field names (ShiftLeaderEndConfirm): `reason`, `effective_at`.
  If already ended -> ALREADY_ENDED; else set unassigned_at + vacated_reason (MANUAL default). audit.
  (This is the same code path auto-vacate uses with PLACEMENT_ENDED + system actor.)
- `GetCurrentLeaderForCompany(ctx, companyID)` -> active SLA or 404 NO_ACTIVE_LEADER (used by roster + detail join).

**Roster service** method (can live in placement_service.go or a roster_service.go):
- `GetCompanyRoster(ctx, companyID, filter, principal)`: enforce RO-4 scope —
  `rbac.GuardCompany(ctx, companyID)` (shift_leader only own company; HR/super global) else
  OUT_OF_SCOPE/403. Return placements (active+scheduled by default; include_history adds terminal),
  current_shift_leader (GetCurrentLeaderForCompany, null on vacancy), and CompanyRosterSummary
  (total_active/scheduled/expiring + by_service_line + by_status). Cursor-paginated. The response
  MUST carry company_name + summary.by_service_line[{service_line_id,service_line_name}] +
  current_shift_leader{id,employee_id,employee_name} exactly as company-roster-screen.tsx renders them.

**Handlers** `backend/internal/handler/placement/`:
- `placements_handler.go` + `placements_dto.go` — ListPlacements, GetPlacement, CreatePlacement
  (201 + Location header), UpdatePlacement, TransferPlacement (201, TransferResponse{predecessor,
  successor, vacated_assignment, warnings}), RenewPlacement (201, RenewResponse), EndPlacement,
  ResignPlacement, TerminatePlacement. The DTO mapper `toPlacementResponse(p, now)` derives
  lifecycle_status at the boundary (Asia/Jakarta): if persisted ACTIVE and end_date<=today+30d ->
  emit EXPIRING; if PENDING_START and start_date<=today -> emit ACTIVE (mirror Phase-4 toAgreementResponse).
  Match the spec snake_case field names + nullable handling exactly (pointers, no omitempty on nullable
  required-but-null fields like ended_reason). Cross-check the emitted field set against what
  placements-screen.tsx / placement-detail-screen.tsx destructure (authoritative).
- `shift_leader_handler.go` — CreateShiftLeaderAssignment (201, {assignment, replaced_assignment}),
  ReplaceShiftLeaderAssignment (201), EndShiftLeaderAssignment (200).
- `roster_handler.go` — GetCompanyRoster (CompanyRosterResponse).

**Routes** in `server.go` — append AFTER "// PEOPLE change-requests slice end (04-04). Phase 5+
appends after this line." Two groups:
```go
// Placement reads: super_admin, hr_admin, shift_leader (company_or_global).
r.Group(func(r chi.Router) {
  r.Use(rbac.RequireRole(auth.RoleSuperAdmin, auth.RoleHRAdmin, auth.RoleShiftLeader))
  r.Get("/placements", d.Placement.ListPlacements)
  r.Get("/placements/{id}", d.Placement.GetPlacement)
  r.Get("/client-companies/{company_id}/roster", d.Placement.GetCompanyRoster)
})
// Placement + shift-leader writes: super_admin, hr_admin (global).
r.Group(func(r chi.Router) {
  r.Use(rbac.RequireRole(auth.RoleSuperAdmin, auth.RoleHRAdmin))
  r.With(d.Idempotency.Handler).Post("/placements", d.Placement.CreatePlacement)
  r.Patch("/placements/{id}", d.Placement.UpdatePlacement)
  r.With(d.Idempotency.Handler).Post("/placements/{id}:renew",     d.Placement.RenewPlacement)
  r.With(d.Idempotency.Handler).Post("/placements/{id}:transfer",  d.Placement.TransferPlacement)
  r.With(d.Idempotency.Handler).Post("/placements/{id}:end",       d.Placement.EndPlacement)
  r.With(d.Idempotency.Handler).Post("/placements/{id}:resign",    d.Placement.ResignPlacement)
  r.With(d.Idempotency.Handler).Post("/placements/{id}:terminate", d.Placement.TerminatePlacement)
  r.With(d.Idempotency.Handler).Post("/shift-leader-assignments",            d.Placement.CreateShiftLeaderAssignment)
  r.With(d.Idempotency.Handler).Post("/shift-leader-assignments/{id}:replace", d.Placement.ReplaceShiftLeaderAssignment)
  r.With(d.Idempotency.Handler).Post("/shift-leader-assignments/{id}:end",     d.Placement.EndShiftLeaderAssignment)
})
```
Add a `Placement *placementhttp.Handler` field to `server.Deps` (one handler struct aggregating the
sub-handlers, OR separate fields — match whatever the handler package exposes). Note GET /placements/{id}
x-rbac also allows agent, but the web app does not call it as agent — keep web roles only (Phase-4 precedent).

**main.go** — wire repos -> services -> handler -> Deps (mirror agreements wiring).

**Seed** `cmd/seed/seed.go` — add `seedPlacements(ctx, pool)` called AFTER seedAgreements (FK order),
and FIRST add active agreements for the personas that lack one. Concrete fixtures (explicit IDs,
`INSERT ... ON CONFLICT (id) DO NOTHING`, mirror seedAgreements):
  - Agreements: SWP-AG-7002 active PKWTT for SWP-EMP-1042 (Sari) ; SWP-AG-7003 active PKWT for
    SWP-EMP-1108 (Rudi). (SWP-AG-7001 already active for SWP-EMP-2891/Budi.)
  - Placements (lifecycle_status=ACTIVE, status_changed_at=now):
    * SWP-PL-5001  SWP-EMP-1108 (Rudi) @ SWP-CMP-0021 / SWP-SITE-0001 / SWP-SVC-003 (Parking) /
      SWP-POS-014, agreement SWP-AG-7003, start 2026-01-01, end 2026-12-31.  (Rudi = shift_leader persona's company → he is actively placed where he leads, so INV-2/4 hold.)
    * SWP-PL-5002  SWP-EMP-2891 (Budi) @ SWP-CMP-0022 / SWP-SITE-0002 / SWP-SVC-003 / SWP-POS-014,
      agreement SWP-AG-7001, start 2026-02-01, end 2026-12-31.
    * SWP-PL-5003  SWP-EMP-1042 (Sari) @ SWP-CMP-0021 / SWP-SITE-0001 / SWP-SVC-002 / SWP-POS-015,
      agreement SWP-AG-7002, open-ended (end_date NULL).
    * SWP-PL-5004  EXPIRING-soon: a placement with end_date = (today + 20 days). Use a distinct
      employee to avoid INV-1 with the above — seed SWP-EMP-3001 (Dewi) an active agreement
      SWP-AG-7004 + placement SWP-PL-5004 @ SWP-CMP-0021 / SWP-SITE-0001 / SWP-SVC-003 / SWP-POS-014,
      start (today-100d), end (today+20d). (DTO boundary derives EXPIRING.)
  - One active shift-leader assignment at SWP-CMP-0021: SWP-SLA-3001, employee SWP-EMP-1108 (Rudi),
    site_id NULL (company-scope), assigned_by 'system-seed', unassigned_at NULL.
  NOTE: Rudi (SWP-EMP-1108) must be a role=shift_leader employee so ShiftLeaderPicker lists him (and
  any future SLA-assign candidate the E2E needs must likewise be role=shift_leader + actively placed).
Insert a placement_history "create" row per seeded placement (optional but recommended for detail E2E).
Wire `seedPlacements` into `Seed()` after the seedAgreements call.
  </action>
  <verify>
    <automated>cd backend && make gen && go build ./... && go vet ./... && grep -q "INV_2_VIOLATION" internal/service/placement/shift_leader_service.go && grep -q "INV_4_VIOLATION" internal/service/placement/shift_leader_service.go && grep -q "GuardCompany" internal/service/placement/*.go && grep -q "/placements/{id}:transfer" internal/server/server.go && grep -q "seedPlacements" cmd/seed/seed.go && grep -q "SWP-SLA-3001" cmd/seed/seed.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `shift_leader_service.go` references `INV_2_VIOLATION`, `INV_3_VIOLATION`, `INV_4_VIOLATION`, `ALREADY_ENDED`, and uses the `...ForUpdate` locking queries.
    - SLA request DTOs accept the exact FE field names (assign: client_company_id/employee_id/start_date/replace/notes; replace: new_employee_id/start_date/replace_reason; end: reason/effective_at).
    - Roster path calls `rbac.GuardCompany` and returns `CompanyRosterResponse` with `company_name`, `current_shift_leader`{id,employee_id,employee_name}, and `summary`{total_active,total_scheduled,total_expiring,by_service_line[{service_line_id,service_line_name}]} — the exact fields company-roster-screen.tsx renders.
    - `server.go` contains routes for `/placements`, `/placements/{id}:renew|:transfer|:end|:resign|:terminate`, `/shift-leader-assignments`(+`:replace`,`:end`), and `/client-companies/{company_id}/roster`, under the correct RequireRole groups, appended after the Phase-5 marker.
    - `cmd/api/main.go` wires the placement repo/service/handler into `server.Deps`.
    - `cmd/seed/seed.go` defines `seedPlacements`, seeds SWP-PL-5001..5004, SWP-SLA-3001, and the supporting agreements SWP-AG-7002/7003/7004; `seedPlacements` is called after `seedAgreements`.
    - `make gen && go build ./... && go vet ./...` all exit 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd backend && make verify` (gen + build + vet + gofmt) is clean.
- Booting the API against a migrated+seeded ephemeral PG (done by 05-04 harness) returns:
  - POST /placements for an already-active agent -> 409 with `error.code=INV_1_VIOLATION` and `error.details.current_placement`.
  - POST /shift-leader-assignments for SWP-CMP-0021 (already has Rudi) -> 409 INV_2_VIOLATION.
  - GET /client-companies/SWP-CMP-0021/roster as Rudi (shift_leader) -> 200; as Rudi for SWP-CMP-0022 -> 403 OUT_OF_SCOPE.
- The GET /placements, GET /placements/{id}, and roster responses carry every field the built
  e3-placement components destructure (cross-checked against the authoritative screen files).
</verification>

<success_criteria>
- All 13 FE-used E3 endpoints implemented, matching openapi.yaml response shapes (snake_case, nullable handling)
  AND satisfying the field set the authoritative FE components consume.
- INV-1 (pre-check + DB index), INV-2/3/4 (FOR UPDATE locks + leader_scope), terminal-immutability, transfer/renew atomicity all enforced with the exact contract codes.
- placement_history + audit_log written on every lifecycle action.
- Seed provides active placements (1042/1108/2891 + expiring) and one active SLA at SWP-CMP-0021.
</success_criteria>

<output>
After completion, create `.planning/phases/05-e3-placement/05-02-SUMMARY.md`: the handler/service
method signatures, the exact apperr codes emitted per endpoint, the seeded IDs (placements,
agreements, SLA), the leader_scope handling decision, the lifecycle DTO-derivation rule, the exact
response JSON field set per endpoint (so 05-04 wiring confirms the FE renders against it), and any
deviations from the spec (with rationale). 05-03 and 05-04 depend on these details.
</output>
</output>
