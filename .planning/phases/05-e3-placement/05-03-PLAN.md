---
phase: 05-e3-placement
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - backend/internal/handler/placement/placements_handler_test.go
  - backend/internal/handler/placement/shift_leader_handler_test.go
  - backend/internal/handler/placement/roster_handler_test.go
autonomous: true
requirements: [PLC-01, PLC-02, PLC-03, PLC-04]
must_haves:
  truths:
    - "Every E3 endpoint has a contract test asserting the openapi.yaml response shape + status code"
    - "Every invariant 409 (INV_1..4) is asserted incl. error.code and error.details.invariant"
    - "Site-scope leadership path is contract-tested (FE E2E only covers company-scope)"
    - "`go test ./... -count=1` exits 0"
  artifacts:
    - path: "backend/internal/handler/placement/placements_handler_test.go"
      provides: "contract tests for placement CRUD + lifecycle incl. INV-1 + terminal-immutability"
      contains: "INV_1_VIOLATION"
    - path: "backend/internal/handler/placement/shift_leader_handler_test.go"
      provides: "contract tests for SLA create/replace/end incl. INV-2/3/4 + site-scope"
      contains: "INV_2_VIOLATION"
    - path: "backend/internal/handler/placement/roster_handler_test.go"
      provides: "roster shape + RBAC OUT_OF_SCOPE for shift_leader cross-company"
      contains: "OUT_OF_SCOPE"
  key_links:
    - from: "placement handler tests"
      to: "docs/api/E3-placement/openapi.yaml examples"
      via: "asserting field names + status codes + error envelope"
      pattern: "error.*code"
---

<objective>
Write Go contract tests for every E3 endpoint, asserting response shapes match
docs/api/E3-placement/openapi.yaml exactly (the drift gate that replaces server codegen —
the FE Orval client is generated from the same spec). Cover happy paths, every invariant 409
(with error.code + error.details.invariant), 422 business-rule codes, terminal-state
immutability, and the site-scope leadership path that the FE E2E does not exercise.

Purpose: prove the BE matches the contract before the FE is wired (05-04), and lock in the
invariant error envelopes the FE renders.
Output: 3 test files; `go test ./... -count=1` green.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-e3-placement/05-02-SUMMARY.md
@.planning/reference/backend-build-conventions.md

<interfaces>
<!-- MIRROR: backend/internal/handler/people/agreements_handler_test.go — fakeRepo + chi router +
     httptest pattern, dynamic principal injection, table-driven assertions. Copy its harness. -->

Test harness pattern (from agreements_handler_test.go):
  - in-memory fake implementing the service's repository interface (no testcontainers needed
    for handler contract tests — the people tests use fakes + a fakeTx for InTx audit calls).
  - mount the handler on a chi.Router with a mutable principal middleware (swap roles per case).
  - assert: status code, decoded JSON field names, error envelope {error:{code,message,fields,details,request_id}}.
  - fakeTx implements pgx.Tx (Exec no-op) because the service calls audit.Record inside InTx.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Placement CRUD + lifecycle contract tests</name>
  <read_first>
    - backend/internal/handler/people/agreements_handler_test.go   (fake repo + fakeTx + chi httptest harness to copy)
    - backend/internal/handler/placement/placements_handler.go     (05-02 handler signatures + DTO field names)
    - backend/internal/service/placement/placement_service.go      (apperr codes emitted per method)
    - docs/api/E3-placement/openapi.yaml                           (Placement, PlacementCreateResponse, TransferResponse, RenewResponse, ErrorResponse examples)
  </read_first>
  <action>
Create `backend/internal/handler/placement/placements_handler_test.go` (package placement_test).
Build a `fakePlacementRepo` implementing the service's repository interface + a `fakeTx`. Table-driven
contract tests asserting EXACT shapes/codes:
- `GET /placements` -> 200, PlacementListResponse{data[],next_cursor,has_more}; data item has
  employee_name, client_company_name, site_name, service_line_name, position_name, lifecycle_status.
- `GET /placements?expiring_within_days=30` -> only ACTIVE/EXPIRING within window; sorted end_date asc.
- `GET /placements/{id}` -> 200 PlacementDetailResponse{placement, history_chain[], current_shift_leader}.
- `GET /placements/{id}` not found -> 404 NOT_FOUND.
- `POST /placements` happy -> 201, body matches Placement, Location header `/api/v1/placements/SWP-PL-...`.
- `POST /placements` for already-active agent -> 409, `error.code=INV_1_VIOLATION`,
  `error.details.invariant="INV_1"`, `error.details.current_placement.id` present,
  `error.details.suggested_actions` contains "transfer" and "end".
- `POST /placements` inactive company -> 409 COMPANY_INACTIVE.
- `POST /placements` end_date<=start_date -> 400 INVALID_REQUEST with fields.end_date.
- `POST /placements` start outside agreement -> 422 PLACEMENT_OUTSIDE_CONTRACT.
- `PATCH /placements/{id}` on terminal placement -> 409 TERMINAL_STATE_IMMUTABLE.
- `POST /placements/{id}:end` happy -> 200 lifecycle_status=ENDED, ended_reason set.
- `POST /placements/{id}:terminate` wrong company-name confirm -> 400 with fields.type_company_name_confirm;
  happy -> 200 TERMINATED.
- `POST /placements/{id}:resign` -> 200 RESIGNED, resign_at set.
- `POST /placements/{id}:transfer` happy -> 201 TransferResponse{predecessor(TRANSFERRED), successor(ACTIVE,
  predecessor_id set)}; same-company-same-line -> 422 RULE_VIOLATION.
- `POST /placements/{id}:renew` happy -> 201 RenewResponse{predecessor(SUPERSEDED), successor(predecessor_id set)};
  1-day buffer violation -> 422 PLACEMENT_PERIOD_OVERLAP.
- RBAC: agent calling POST /placements -> 403 (the test injects a RoleAgent principal; route group is HR/super only).
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handler/placement/... -run Placement -count=1 && grep -q "INV_1_VIOLATION" internal/handler/placement/placements_handler_test.go && grep -q "TERMINAL_STATE_IMMUTABLE" internal/handler/placement/placements_handler_test.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists; asserts INV_1_VIOLATION with `details.invariant` and `details.current_placement`.
    - Asserts TERMINAL_STATE_IMMUTABLE, COMPANY_INACTIVE, PLACEMENT_OUTSIDE_CONTRACT, PLACEMENT_PERIOD_OVERLAP, RULE_VIOLATION.
    - Asserts transfer predecessor=TRANSFERRED + successor predecessor_id set; renew predecessor=SUPERSEDED.
    - `go test ./internal/handler/placement/... -count=1` exits 0 for these tests.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Shift-leader + roster contract tests (incl. INV-2/3/4 + site-scope + OUT_OF_SCOPE)</name>
  <read_first>
    - backend/internal/handler/placement/shift_leader_handler.go   (05-02 SLA handler signatures)
    - backend/internal/handler/placement/roster_handler.go         (roster handler signature)
    - backend/internal/service/placement/shift_leader_service.go   (INV-2/3/4 + leader_scope codes)
    - docs/api/E3-placement/openapi.yaml                           (ShiftLeaderAssignmentCreateResponse, CompanyRosterResponse, INV-2/3/4 examples ~896-946)
    - docs/epics/E3-placement/prds/shift-leader-assignment.md      (C-2 PENDING_START not eligible)
  </read_first>
  <action>
Create `backend/internal/handler/placement/shift_leader_handler_test.go` and
`backend/internal/handler/placement/roster_handler_test.go` (package placement_test), reusing the
fake repo harness:

shift_leader_handler_test.go:
- `POST /shift-leader-assignments` first leader (company-scope) -> 201,
  ShiftLeaderAssignmentCreateResponse{assignment{...,active:true,client_company_id,employee_id}}.
- second leader, replace=false -> 409 INV_2_VIOLATION, `error.details.invariant="INV_2"`,
  `current_assignment` present, suggested_actions ["replace"].
- replace=true -> 201, response includes `replaced_assignment` (the ended one, vacated_reason=REASSIGNED).
- employee already leads another company -> 409 INV_3_VIOLATION (details.existing_assignment).
- employee not actively placed at company -> 409 INV_4_VIOLATION (details.company_id/employee_id,
  employee_placements_at_company array). Include a PENDING_START-only case proving it does NOT satisfy INV-4 (C-2).
- inactive employee -> 422 LEADER_NOT_ELIGIBLE.
- **site-scope path** (company.leader_scope='site'): assigning a leader requires site_id; a second
  leader for the SAME site -> 409 INV_2_VIOLATION; a leader for a DIFFERENT site of the same company
  succeeds -> 201 (proves per-site leadership unit). This path is contract-only (FE E2E skips it).
- `POST /shift-leader-assignments/{id}:replace` -> 201; replacing an already-ended assignment -> 409 ALREADY_ENDED.
- `POST /shift-leader-assignments/{id}:end` -> 200 (unassigned_at set, active:false); ending again -> 409 ALREADY_ENDED.

roster_handler_test.go:
- `GET /client-companies/{id}/roster` as hr_admin -> 200 CompanyRosterResponse{company_id, placements[],
  current_shift_leader, summary{total_active,...,by_service_line[],by_status[]}, has_more}.
- as shift_leader for their OWN company -> 200; for ANOTHER company -> 403 OUT_OF_SCOPE.
- `include_history=true` includes terminal-state placements; default excludes them.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handler/placement/... -count=1 && grep -q "INV_2_VIOLATION" internal/handler/placement/shift_leader_handler_test.go && grep -q "INV_4_VIOLATION" internal/handler/placement/shift_leader_handler_test.go && grep -q "leader_scope\|site_id" internal/handler/placement/shift_leader_handler_test.go && grep -q "OUT_OF_SCOPE" internal/handler/placement/roster_handler_test.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - SLA test asserts INV_2_VIOLATION, INV_3_VIOLATION, INV_4_VIOLATION, ALREADY_ENDED, LEADER_NOT_ELIGIBLE.
    - SLA test includes a site-scope case proving a different site of the same company can have its own leader.
    - SLA test includes a PENDING_START case proving it fails INV-4 (C-2).
    - Roster test asserts the CompanyRosterResponse shape and `OUT_OF_SCOPE` for cross-company shift_leader.
    - `go test ./internal/handler/placement/... -count=1` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd backend && go test ./... -count=1` exits 0 (no regressions in earlier-phase tests).
- Every error envelope asserted matches the spec example field names (code, message, fields, details, request_id).
</verification>

<success_criteria>
- All 13 E3 endpoints have contract tests; all invariant 409s + 422 codes asserted with details.
- Site-scope leadership path covered (company-scope is left to FE E2E in 05-04).
- `go test ./... -count=1` green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-e3-placement/05-03-SUMMARY.md` listing which
scenarios are contract-covered (so 05-04 avoids duplicating site-scope) and any spec/impl
mismatches discovered + how they were resolved.
</output>
