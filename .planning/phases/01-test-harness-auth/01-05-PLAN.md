---
phase: 01-test-harness-auth
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-03, 01-04]
files_modified:
  - frontend/e2e/tests/e1/authentication.spec.ts
  - frontend/e2e/lib/db.ts
  - frontend/e2e/lib/api.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
must_haves:
  truths:
    - "Running pnpm e2e (headless) executes the full auth suite against the real FE↔BE↔ephemeral Postgres and passes"
    - "playwright test --list shows one named test() per Gherkin scenario / case from authentication.md, individually runnable in --ui"
    - "Specs cover: successful login→dashboard, INVALID_CREDENTIALS, ACCOUNT_DISABLED, token refresh, logout, forgot-password (202 generic), reset-password (success + expired token), and UNAUTHENTICATED redirect to /login"
  artifacts:
    - path: "frontend/e2e/tests/e1/authentication.spec.ts"
      provides: "Exhaustive auth E2E, one test per scenario, named with AU-#/C-#"
      contains: "INVALID_CREDENTIALS"
    - path: "frontend/e2e/lib/db.ts"
      provides: "Test DB helper to read/seed the reset token + disable a user (no email is sent)"
      contains: "password_reset_tokens"
  key_links:
    - from: "frontend/e2e/tests/e1/authentication.spec.ts"
      to: "the real login screen + /auth endpoints"
      via: "loginAs fixture + page interactions"
      pattern: "loginAs"
    - from: "frontend/e2e/lib/db.ts"
      to: "password_reset_tokens table"
      via: "pg query for the reset flow"
      pattern: "password_reset_tokens"
---

<objective>
Write the exhaustive auth E2E suite: one `test()` per Gherkin scenario / edge case in
`docs/epics/E1-foundations/prds/authentication.md`, each named with its AU-#/C-# and
individually runnable in `--ui`. The suite drives the REAL login/forgot/reset screens against
the REAL Go API + ephemeral Postgres (the 01-01 harness), proving AUTH-01..04 end-to-end and
acting as the contract-drift gate.

Purpose: This is the phase's proof — the success criterion that everything works together.
Output: `frontend/e2e/tests/e1/authentication.spec.ts` + small DB/API test helpers.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/reference/e2e-harness-spec.md
@.planning/phases/01-test-harness-auth/01-01-SUMMARY.md
@.planning/phases/01-test-harness-auth/01-02-SUMMARY.md
@.planning/phases/01-test-harness-auth/01-03-SUMMARY.md
@.planning/phases/01-test-harness-auth/01-04-SUMMARY.md
@docs/epics/E1-foundations/prds/authentication.md

<interfaces>
From the 01-01 harness (reuse — do NOT recreate):
- frontend/e2e/lib/fixtures.ts: extended `test`, `expect`, and `loginAs(page, persona)` (drives the real login screen).
- frontend/e2e/lib/personas.ts: `PERSONAS` map { hrAdmin, shiftLeader, superAdmin, agent } each { email, password, role, companyName? }. Passwords match `backend/cmd/seed` constants (see 01-02 SUMMARY).
- frontend/e2e/lib/reset-db.ts: `resetDb()` (truncate-and-reseed) — call in `beforeEach` for isolation.
- Test DATABASE_URL = postgres://hris:hris@localhost:5433/hris_e2e?sslmode=disable; API base = http://localhost:8081/api/v1; FE baseURL = http://localhost:4173.

From 01-03 (BE): forgot-password creates a row in `password_reset_tokens` (token_hash = sha256 hex of the opaque plaintext; expires_at = now+1h). NO email is sent — the E2E obtains/derives the reset token via the DB. Only the hash is stored. CONFIRM the mechanism in 01-03's SUMMARY: either (a) the BE logs/returns the plaintext in test env, or (b) the spec seeds a known token by inserting a row whose token_hash = sha256(known_plaintext). Use whichever 01-03 documents; if unspecified, use (b).

From 01-04 (FE): login screen at /login (#email, #password, submit), error banners via ?error=invalid|locked|disabled; forgot at /forgot-password (advances to 'sent'); reset at /reset-password?token=… (success state, expired-token banner); logout in the shell; index `/` is the post-login dashboard. Read 01-04 SUMMARY for the exact banner copy/keys and success-state markers.

Gherkin/cases to cover (authentication.md §7 + §8):
- AU-1/AU-3 successful login → token issued, last_login recorded, lands on dashboard.
- AU-2 disabled account cannot log in → ACCOUNT_DISABLED banner.
- AU-1 wrong password → INVALID_CREDENTIALS banner.
- AU-4 password reset: known email → 'sent'; using the token sets a new password → can log in with it.
- C-2 reset for UNKNOWN email → identical generic 'sent' response (no enumeration).
- AU-6/C-3 token refresh: /auth/refresh returns a new access token.
- AU-6 logout → session cleared → authed route redirects to /login.
- UNAUTHENTICATED redirect: authed route while logged out → /login.
- AU-5 rate-limit/lockout: DEFERRED this phase (test env uses a high limit) → add a `test.skip` placeholder so it is visible in --ui.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test helpers — DB access (reset token, disable user, last_login) and direct API calls</name>
  <files>frontend/e2e/lib/db.ts, frontend/e2e/lib/api.ts</files>
  <read_first>
    - .planning/phases/01-test-harness-auth/01-03-SUMMARY.md (how the reset token is obtained; reset-token lifetime; disable mechanism)
    - frontend/e2e/lib/reset-db.ts (existing pg connection pattern to reuse)
    - frontend/e2e/lib/personas.ts (persona emails)
  </read_first>
  <action>
Create `frontend/e2e/lib/db.ts` (uses `pg`, same DATABASE_URL as reset-db; reuse one Pool):
  - Reset-token helper per the 01-03 SUMMARY mechanism. If the BE exposes the plaintext, return it; otherwise implement `seedResetToken(email, plaintext)`: compute sha256 hex of `plaintext` (node `crypto`), look up the user id by email, INSERT a `password_reset_tokens` row (user_id, token_hash, expires_at = now() + interval '1 hour'), return `plaintext`. Add `seedExpiredResetToken(email, plaintext)` with expires_at = now() - interval '1 hour' for the expired-token scenario.
  - `disableUser(email)`: `UPDATE users SET status='disabled' WHERE lower(email)=lower($1)` — for ACCOUNT_DISABLED.
  - `getLastLoginAt(email)`: SELECT last_login_at — to assert AU-3.
  - `countResetTokensFor(email)`: COUNT rows joined to the user — to assert C-2 (no row for unknown email).
  Export typed functions.

Create `frontend/e2e/lib/api.ts`: a tiny fetch wrapper for direct API assertions (base http://localhost:8081/api/v1):
  - `apiLogin(email, password)`: POST /auth/login; return parsed LoginResponse plus the Set-Cookie header (the refresh cookie).
  - `apiRefresh(cookie)`: POST /auth/refresh sending the cookie; return status + parsed body.
  Used by the refresh scenario to deterministically assert /auth/refresh returns a new access token.
  </action>
  <verify>
    <automated>cd /Users/diaz/Documents/MIG/hris-outsource/frontend && grep -q 'password_reset_tokens' e2e/lib/db.ts && grep -q 'disableUser' e2e/lib/db.ts && grep -q 'last_login_at' e2e/lib/db.ts && test -f e2e/lib/api.ts && pnpm --filter @swp/e2e exec tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/e2e/lib/db.ts` contains `password_reset_tokens`, `disableUser`, `last_login_at`, a reset-token helper (`getResetTokenFor` or `seedResetToken`), and an expired-token variant.
    - `frontend/e2e/lib/api.ts` exports `apiLogin` and `apiRefresh`.
    - `cd frontend && pnpm --filter @swp/e2e exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>DB helpers can fetch/seed a reset token (matching the 01-03 mechanism), disable a user, read last_login_at, and count reset tokens; an API helper supports the refresh assertion; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: authentication.spec.ts — one named test per Gherkin scenario / case, full suite green</name>
  <files>frontend/e2e/tests/e1/authentication.spec.ts</files>
  <read_first>
    - docs/epics/E1-foundations/prds/authentication.md (§7 Gherkin + §8 cases — the test source)
    - frontend/e2e/lib/fixtures.ts (loginAs, test, expect)
    - frontend/e2e/lib/personas.ts (PERSONAS)
    - frontend/e2e/lib/db.ts + frontend/e2e/lib/api.ts (Task 1 helpers)
    - .planning/phases/01-test-harness-auth/01-04-SUMMARY.md (exact selectors / banner copy keys / success-state markers per screen)
  </read_first>
  <action>
Create `frontend/e2e/tests/e1/authentication.spec.ts` using the extended `test`/`expect` from fixtures. Add `test.beforeEach(async () => { await resetDb(); })` for isolation. Write ONE `test()` per scenario, NAMED with its AU-#/C-# so each is individually runnable in `--ui`:

  1. `test('AU-1/AU-3 · successful login lands on the dashboard and records last_login', ...)`: assert getLastLoginAt(hrAdmin) is null before; `loginAs(page, PERSONAS.hrAdmin)`; assert URL is the dashboard (not /login) and the shell shows the user name (Sari Hadi); assert getLastLoginAt is now non-null.
  2. `test('AU-1 · wrong password shows INVALID_CREDENTIALS banner', ...)`: goto /login, fill hrAdmin email + wrong password, submit; assert the invalid banner is visible and the URL carries ?error=invalid (per 01-04 mapping).
  3. `test('AU-2 · disabled account is rejected with ACCOUNT_DISABLED', ...)`: `disableUser(PERSONAS.agent.email)`; attempt login; assert the disabled banner (error=disabled).
  4. `test('AU-6/C-3 · refresh issues a new access token', ...)`: `apiLogin` to capture the refresh cookie; `apiRefresh(cookie)`; assert 200 + an access_token different from the original.
  5. `test('AU-6 · logout clears the session and protects authed routes', ...)`: loginAs(hrAdmin); trigger logout in the shell; assert redirect to /login; then goto an authed route (`/`) and assert it redirects back to /login.
  6. `test('UNAUTHENTICATED · authed route while logged out redirects to /login', ...)`: without logging in, goto an authed route; assert URL becomes /login.
  7. `test('AU-4 · password reset: request + use token sets a new password', ...)`: goto /forgot-password, submit hrAdmin email, assert 'sent' state; obtain the token via the db helper; goto /reset-password?token=<token>, set a new valid password (≥10 chars, upper+lower+digit+symbol), submit, assert success state; then login with the NEW password and assert dashboard.
  8. `test('C-2 · forgot-password for an unknown email returns the same generic response', ...)`: submit a non-existent email; assert the SAME 'sent' confirmation UI; assert countResetTokensFor(that email) == 0.
  9. `test('AU-4 · reset with an expired/invalid token shows RESET_TOKEN_EXPIRED', ...)`: seed an expired token (or use a bogus token); goto /reset-password?token=…; submit a valid new password; assert the expired-token banner.
  10. `test.skip('AU-5 · repeated failures are rate-limited (deferred to a later phase)', ...)`: placeholder visible in --ui but skipped.

Use resilient selectors (ids #email/#password; role/text from the 01-04 SUMMARY). No arbitrary sleeps — use waitForURL / expect(...).toBeVisible(). Each test independent (beforeEach resetDb).

Then run the full suite headless and ensure it is green (this requires Docker running; the harness globalSetup boots the stack).
  </action>
  <verify>
    <automated>cd /Users/diaz/Documents/MIG/hris-outsource/frontend && pnpm --filter @swp/e2e exec playwright test --list 2>&1 | grep -E 'AU-1|AU-2|AU-4|AU-6|UNAUTHENTICATED' && grep -q 'INVALID_CREDENTIALS' e2e/tests/e1/authentication.spec.ts && pnpm --filter @swp/e2e test:e2e 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/e2e/tests/e1/authentication.spec.ts` contains the strings `INVALID_CREDENTIALS`, `ACCOUNT_DISABLED`, `RESET_TOKEN_EXPIRED`, `UNAUTHENTICATED`, and `loginAs`.
    - `pnpm --filter @swp/e2e exec playwright test --list` shows at least 9 runnable tests whose names include AU-1, AU-2, AU-4, AU-6, C-2, and UNAUTHENTICATED (proves per-scenario naming for --ui).
    - `pnpm --filter @swp/e2e test:e2e` exits 0 with all non-skipped auth tests passing against the real stack (Docker required).
    - The AU-5 rate-limit test appears as skipped in the run output.
  </acceptance_criteria>
  <done>authentication.spec.ts has one named test per Gherkin scenario/case (AU-1/AU-3/AU-2/AU-4/AU-6/C-2/UNAUTHENTICATED, AU-5 skipped), each individually runnable in --ui, and the headless suite passes against the real FE↔BE↔ephemeral Postgres.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @swp/e2e exec tsc --noEmit` exits 0.
- `pnpm --filter @swp/e2e exec playwright test --list` lists each Gherkin scenario as its own named case.
- `pnpm --filter @swp/e2e test:e2e` (headless) is green; `pnpm e2e:ui` opens the UI with each scenario individually runnable.
- Reset flow obtains the token via the DB (no email sent); refresh/logout/disabled/invalid all asserted against the real stack.
</verification>

<success_criteria>
AUTH-01..04 proven end-to-end: exhaustive auth E2E (one test per authentication.md Gherkin scenario/case) runs the real web app against the real Go API + ephemeral Postgres (MSW off) and passes in headless mode; every scenario is individually runnable in the Playwright UI; the harness (HARN-01) is exercised live for the first time and confirmed working.
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-harness-auth/01-05-SUMMARY.md` documenting: the final list of test names (per scenario), the reset-token acquisition mechanism used, any flake mitigations, and confirmation that all three run modes work against the real stack.
</output>
