---
phase: 11-e10-reporting
plan: 04
type: execute
wave: 5
depends_on: ["11-02", "11-02b", "11-03"]
files_modified:
  - frontend/apps/web/src/features/e10-reporting/notifications-screen.tsx
  - frontend/apps/web/src/features/e10-reporting/billable-report-screen.tsx
  - frontend/apps/web/src/features/e10-reporting/dashboard-screen.tsx
  - frontend/apps/web/src/features/e10-reporting/use-export-flow.ts
  - frontend/e2e/lib/reset-db.ts
  - frontend/e2e/lib/e10-helpers.ts
  - frontend/e2e/tests/e10/dashboard.spec.ts
  - frontend/e2e/tests/e10/billable-report.spec.ts
  - frontend/e2e/tests/e10/notifications.spec.ts
  - frontend/e2e/tests/e10/notifications-autodispatch.spec.ts
  - frontend/e2e/tests/e10/exports.spec.ts
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, RPT-04]
user_setup: []

must_haves:
  truths:
    - "The e10-reporting + dashboard screens run against the real Go BE with MSW off; {data}-unwrap + classifyError fixes applied where the BE envelope differs from the generated types (mark-all-read marked_count, dashboard/report/notif unwrap)"
    - "Playwright E2E: dashboard renders role-aware counts (HR global, leader own-company), billable report renders summary + rows + pending callout, notifications list + mark-read + mark-all-read flip unread→read"
    - "Playwright E2E capstone: a REAL action (HR approves a seeded leave OR OT, or verifies attendance) auto-dispatches a notification that then APPEARS in the recipient's notifications list (real dispatch via the un-stubbed worker, not seeded)"
    - "Playwright E2E: POST /exports (ATTENDANCE_BILLABLE, EXCEL) → 202 + SWP-EXP id → the real ReportExportWorker flips export_jobs → DONE (asserted via DB poll); :cancel → CANCELLED; PDF → EXPORT_FORMAT_UNSUPPORTED"
    - "reset-db.ts TRUNCATEs notifications; the full e1..e10 suite passes headless with no regressions"
  artifacts:
    - path: "frontend/e2e/tests/e10/notifications-autodispatch.spec.ts"
      provides: "the milestone capstone — real action → auto-dispatched notification appears + mark-read"
      contains: "approve"
    - path: "frontend/e2e/tests/e10/exports.spec.ts"
      provides: "export 202 → worker DONE via DB poll + cancel + EXPORT_FORMAT_UNSUPPORTED"
      contains: "DONE"
    - path: "frontend/e2e/lib/reset-db.ts"
      provides: "notifications added to TRUNCATE_TABLES"
      contains: "notifications"
    - path: "frontend/e2e/lib/e10-helpers.ts"
      provides: "pollExportJob reuse + apiAs token helper + selectors for e10 screens"
      contains: "pollExportJob"
  key_links:
    - from: "frontend/e2e/tests/e10/notifications-autodispatch.spec.ts"
      to: "the real BE dispatch loop"
      via: "drive a real approve/verify action then assert the notification row appears in GET /notifications"
      pattern: "notifications"
    - from: "frontend/e2e/tests/e10/exports.spec.ts"
      to: "export_jobs table"
      via: "pollExportJob until status DONE (real ReportExportWorker)"
      pattern: "pollExportJob"
    - from: "frontend/apps/web/src/features/e10-reporting/notifications-screen.tsx"
      to: "mark-all-read response"
      via: "read res.data.marked_count (BE returns marked_count per openapi)"
      pattern: "marked_count"
---

<objective>
Wire the built E10 screens (dashboard, billable report, notifications, export flow) off MSW to the
real Go BE, applying the recurring {data}-unwrap + classifyError fixes where the BE envelope differs
from the generated types, and prove everything with exhaustive full-stack Playwright E2E under a NEW
frontend/e2e/tests/e10/ dir. The capstone spec drives a REAL prior-phase action and asserts the
auto-dispatched notification appears (the loop-closer). Anchor every selector on the REAL
e10-reporting + dashboard components (no invented locators). After this plan the whole web console
works end-to-end against the real backend — the milestone is closed.

Purpose: success-criteria 1–4 proven end-to-end; the final milestone phase.
Output: FE wiring fixes, e10-helpers, reset-db TRUNCATE, 5 e10 spec files, full e1..e10 suite green.
</objective>

<execution_context>
@/Users/diaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-e10-reporting/11-CONTEXT.md
@.planning/phases/11-e10-reporting/11-02-PLAN.md
@.planning/phases/11-e10-reporting/11-02b-PLAN.md
@.planning/reference/e2e-harness-spec.md

<interfaces>
<!-- The built FE screens to wire (AUTHORITATIVE — already render off MSW; flip to real BE):
  dashboard-screen.tsx        : query.data.data → role-discriminated Dashboard (isHrDashboard/isLeaderDashboard)
  billable-report-screen.tsx  : query.data.data → BillableReport; pending callout when pending_records>0; DataTable rows getRowId=group_key
  notifications-screen.tsx     : query.data.data → {data:Notification[], has_more, next_cursor}; NotifCard onClick → markRead.mutate({notificationId});
                                 markAllRead onSuccess reads res.data.marked (← FIX to res.data.marked_count); read-state pills (UNREAD/READ/ALL); kind FilterSelect
  use-export-flow.ts           : createExport onSuccess res.data.id; poll useGetExport(id) res.data.status; terminal COMPLETED/FAILED/CANCELLED -->

<!-- Phase-10 E2E patterns to REUSE (frontend/e2e/lib): -->
  backend.ts        : spawns cmd/api + cmd/worker (detached) + river-up — ALREADY boots the worker (notifications + exports both need it)
  reset-db.ts       : TRUNCATE_TABLES in FK order (payslip_*/payslips/export_jobs already listed) + re-seed — ADD 'notifications'
  e8-helpers.ts     : pollExportJob(jobId) polls export_jobs.status==='DONE' (reuse verbatim — DB status is DONE even though the wire is COMPLETED) + apiAs(token) for authed API calls + PERSONAS.*
  DataTable rows    : div.border-b (E2E row locator pattern — decision [02])
  reset-db note     : seed is ON CONFLICT DO NOTHING; test-created notifications reset by TRUNCATE; SWP-NTF-9000x seed fixtures restored on reseed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FE wiring fixes (MSW off) + e10-helpers + reset-db TRUNCATE</name>
  <read_first>
    - frontend/apps/web/src/features/e10-reporting/notifications-screen.tsx (markAllRead onSuccess reads res.data.marked — fix to marked_count; confirm list unwrap query.data.data.data)
    - frontend/apps/web/src/features/e10-reporting/dashboard-screen.tsx + billable-report-screen.tsx + use-export-flow.ts (the unwrap already reads query.data.data — verify against the real BE envelope; fix only if the BE returns a different shape than the cast expects)
    - frontend/e2e/lib/reset-db.ts (TRUNCATE_TABLES array + FK ordering — add 'notifications' before placements/employees since recipient_id has no FK; safe anywhere but keep it with the E10 group)
    - frontend/e2e/lib/e8-helpers.ts + backend.ts (pollExportJob, apiAs, PERSONAS, the worker spawn — to reuse in e10-helpers)
    - frontend/lib/api-error.ts classifyError (in case an E10 error code needs a branch)
  </read_first>
  <action>
    FE fixes (fix toward what the BE returns — established pattern):
      - notifications-screen.tsx: change markAllRead onSuccess to read `(res.data as { marked_count?: number }).marked_count ?? 0` (BE returns marked_count per openapi). Confirm the list page unwrap (`query.data?.data` → {data, has_more, next_cursor}) matches the real cursor envelope; adjust if the BE wraps differently.
      - dashboard-screen.tsx: confirm `(query.data as {data:Dashboard}).data` matches the real {data:<Dashboard>} envelope from the handler; keep the role branching.
      - billable-report-screen.tsx: confirm `(query.data as {data?:BillableReport}).data` matches; the pending callout reads pending_summary.pending_records/pending_hours_estimate.
      - use-export-flow.ts: confirm createExport `res.data.id` + poll `res.data.status` against the real ExportJob envelope; the FE ExportStatus enum is QUEUED/PROCESSING/COMPLETED/FAILED/CANCELLED (the BE maps DB→wire, so no FE change needed for status).
      Make the SMALLEST change that makes the real BE drive the screen; document each in the SUMMARY.

    e10-helpers.ts (frontend/e2e/lib): re-export/reuse pollExportJob + apiAs + PERSONAS from the e8 helpers; add any E10-specific helpers (e.g. listNotificationsVia(apiAs) to assert a dispatched row by kind/recipient; a helper to read the seeded leave/OT id for the capstone action). Keep .js import extensions per the E2E convention.

    reset-db.ts: add 'notifications' to TRUNCATE_TABLES (recipient_id has no FK to kept tables, so order is flexible — place it in the E10 group near export_jobs). Ensure the reseed restores the SWP-NTF-9000x fixtures.

    Build the FE: `cd frontend && pnpm -C apps/web build` (or the repo's typecheck) to confirm no type errors from the unwrap fixes.
  </action>
  <verify>
    <automated>cd frontend && grep -q "marked_count" apps/web/src/features/e10-reporting/notifications-screen.tsx && grep -q "notifications" e2e/lib/reset-db.ts && grep -q "pollExportJob" e2e/lib/e10-helpers.ts && pnpm -C apps/web exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - notifications-screen.tsx reads marked_count from the mark-all-read response.
    - reset-db.ts TRUNCATE_TABLES includes 'notifications'.
    - e10-helpers.ts reuses pollExportJob + apiAs + PERSONAS.
    - The web app typechecks with the unwrap fixes (no MSW dependency in the E10 path).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Dashboard + Billable report + Notifications E2E specs</name>
  <read_first>
    - frontend/e2e/tests/e8/*.spec.ts (the spec structure: test.describe, loginAs(PERSONAS.*), page.goto, the div.border-b row pattern, waitForToken)
    - frontend/apps/web/src/features/e10-reporting/dashboard-screen.tsx (the StatCard labels, the role_label text, the ApprovalInboxPanel rows, the leader today cards) + billable-report-screen.tsx (TitleBand "Ekspor" button, the period date inputs aria-labels, the StatCards, the DataTable columns, the pending Banner) + notifications-screen.tsx (the read-state pills, FilterSelect, NotifCard, "Tandai semua dibaca" button, the empty states)
    - frontend/e2e/lib/personas.ts (HR / shift_leader personas + which company the leader owns = SWP-CMP-0021)
    - the routes: where DashboardScreen / BillableReportScreen / NotificationsScreen mount (grep the router for /dashboards or the dashboard route, /reports/billable, /notifications)
  </read_first>
  <action>
    dashboard.spec.ts:
      - HR/super_admin: login, goto dashboard, assert the HR title + role_label, KPI StatCards render numbers, the ApprovalInboxPanel shows pending rows; assert a deep-link path is present (the panel row links into E5/E6/E7).
      - shift_leader: login as the leader (own company SWP-CMP-0021), goto dashboard, assert the leader title + company name, the today cards (clocked_in/late/absent), and that counts are SCOPED to own company (leader does NOT see global HR KPIs).
    billable-report.spec.ts:
      - HR: goto /reports/billable, the default current-month period loads, assert the StatCards (Jam Billable etc.) render, the DataTable renders rows (div.border-b) OR the empty state for a filter with no data; if seeded verified attendance exists, assert a row's billable hours; assert the pending callout Banner appears when pending records exist.
      - leader scope: a leader running the report only sees own-company data (the company filter is server-scoped / hidden).
    notifications.spec.ts:
      - goto /notifications: the seeded SWP-NTF-9000x list renders grouped by date; the read-state pills filter (UNREAD shows only unread); clicking a NotifCard marks it read (unread dot disappears) and the count drops; "Tandai semua dibaca" marks all → the button hides and UNREAD is empty (assert via the marked_count toast + refetch).
      - empty state when filtered to a kind with no rows.

    Run: `cd frontend && pnpm e2e --grep '@e10-dashboard|@e10-report|@e10-notif'` (tag the specs) headless.
  </action>
  <verify>
    <automated>cd frontend && ls e2e/tests/e10/dashboard.spec.ts e2e/tests/e10/billable-report.spec.ts e2e/tests/e10/notifications.spec.ts && pnpm e2e e2e/tests/e10/dashboard.spec.ts e2e/tests/e10/billable-report.spec.ts e2e/tests/e10/notifications.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.spec.ts proves HR global vs leader own-company role-aware counts + deep-link presence.
    - billable-report.spec.ts proves summary StatCards + rows/empty + pending callout, leader-scoped.
    - notifications.spec.ts proves list render + read-state filter + single mark-read (unread→read) + mark-all-read.
    - All three specs pass headless against the real BE (MSW off).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Auto-dispatch capstone + Export E2E + full e1..e10 regression</name>
  <read_first>
    - frontend/e2e/tests/e8/*export*.spec.ts (the export 202 → pollExportJob('DONE') pattern + the worker boot reliance — reuse for the report export)
    - frontend/e2e/lib/e10-helpers.ts (pollExportJob, apiAs) + frontend/e2e/lib/e8-helpers.ts
    - frontend/apps/web/src/features/e10-reporting/billable-report-screen.tsx (the "Ekspor" button → ExportModal → useExportFlow: format step → progress → success; the abort/cancel button)
    - the seeded leave/OT data (backend/cmd/seed/seed.go seedLeave/seedOvertime — a PENDING leave the HR persona can approve-final, and its submitter = the recipient of the dispatched LEAVE_APPROVED) + the leave approval route/screen OR drive the approve via apiAs (POST /leave-requests/{id}:approve-final) for determinism
    - backend/internal/service/leave/leave_service.go approve-final (the kind LEAVE_APPROVED + recipient=submitter the capstone asserts)
  </read_first>
  <action>
    notifications-autodispatch.spec.ts (THE CAPSTONE — real dispatch, not seeded):
      - Pick a seeded PENDING leave (or OT) whose submitter is a known persona. As HR, drive a REAL approve-final — preferably via apiAs(POST /leave-requests/{id}:approve-final) for determinism (UI approve is fine if a stable screen exists). This fires notify.Dispatch → River → the un-stubbed NotificationWorker inserts a notifications row for the submitter.
      - Poll the recipient's notifications (login as the submitter OR apiAs with the submitter token → GET /notifications) until a LEAVE_APPROVED (or OT_APPROVED) notification for that entity appears (give the async worker a bounded poll, like pollExportJob). Assert title/kind/deep_link reference the approved entity.
      - Then in the UI: as the recipient, open /notifications, assert the new unread notification is shown, click it → mark-read flips unread→read; mark-all-read clears the rest. This proves the loop-closer end-to-end (real auto-dispatch + read flow).
    exports.spec.ts:
      - From the billable report screen, click "Ekspor" → ExportModal format step → confirm EXCEL → progress; the FE POSTs /exports (ATTENDANCE_BILLABLE, EXCEL) → 202 + SWP-EXP id. Then pollExportJob(id) until export_jobs.status==='DONE' (the real ReportExportWorker) and assert the modal reaches the success/COMPLETED step (download affordance). [The FE polls GET /exports/{id} which maps DONE→COMPLETED; pollExportJob reads the DB DONE directly.]
      - Cancel: start an export, click abort → POST /exports/{id}:cancel → assert export_jobs.status==='CANCELLED' (DB poll) and the modal closes/errors.
      - PDF rejected: drive POST /exports with format=PDF via apiAs → assert 422 EXPORT_FORMAT_UNSUPPORTED (the UI only offers Excel, so this is an apiAs-level assertion).
    Full regression: run the ENTIRE suite headless and assert no regressions: `cd frontend && pnpm e2e 2>&1 | tail -20`. Expect e1..e10 green (the prior 225 e1..e8 + e9 + the new e10 specs). Fix any flake (waitForToken race, deep-route auth-restore redirect) using the documented Phase-7/9 patterns.
  </action>
  <verify>
    <automated>cd frontend && ls e2e/tests/e10/notifications-autodispatch.spec.ts e2e/tests/e10/exports.spec.ts && pnpm e2e e2e/tests/e10 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - notifications-autodispatch.spec.ts drives a REAL approve/verify action and asserts the auto-dispatched notification appears in the recipient's list (via the un-stubbed worker), then mark-read flips it — not a seeded-only assertion.
    - exports.spec.ts proves POST /exports → 202 → worker flips export_jobs to DONE (DB poll) + the modal success step; :cancel → CANCELLED; PDF → EXPORT_FORMAT_UNSUPPORTED.
    - The full `pnpm e2e` run is green headless (e1..e10, no regressions) — capture the pass/skip/fail counts in the SUMMARY.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd frontend && pnpm e2e` is green headless across e1..e10 (no regressions); the new e10 dir has 5 specs.
- The auto-dispatch capstone drives a real action (not seeded) and the notification genuinely appears.
- Export create→worker-DONE (DB poll) + cancel + EXPORT_FORMAT_UNSUPPORTED proven; reset-db TRUNCATEs notifications.
- Every selector anchored on the real e10-reporting + dashboard components.
</verification>

<success_criteria>
- All four Phase-11 success criteria proven end-to-end against the real backend; the milestone (whole web console works against the real Go BE) is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/11-e10-reporting/11-04-SUMMARY.md` with the final e1..e10 suite counts.
</output>
