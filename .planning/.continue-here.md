---
context: default
status: in_progress
last_updated: 2026-06-10T07:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: deepseek-reasoner incompatible with Workflow subagent structured output — Workflow agent() calls fail with "400 thinking options type cannot be disabled when reasoning_effort is set". Use direct implementation (no workflow subagents) when on deepseek-reasoner model, OR switch model before calling Workflow.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Workflow agent() on deepseek-reasoner | Subagent agent() calls fail because deepseek-reasoner doesn't support disabling thinking for structured output | blocking | Don't use Workflow agent() when on deepseek-reasoner model. Implement directly or switch model |
| Python edit scripts need absolute paths | Scripts run from unknown working dir — relative paths fail silently | blocking | Always use absolute paths in python edit scripts (prefixed with /Users/diaz/Documents/MIG/hris-outsource/) |

<current_state>
F5.6 Manual Attendance Entry — backend + tests + frontend complete. Remaining docs update: OpenAPI spec, feature doc.
</current_state>

<completed_work>

### Backend (F5.6)
- `backend/internal/domain/attendance/attendance.go` — added `FlagManualEntry = "MANUAL_ENTRY"`
- `backend/db/queries/attendance/attendance.sql` — added `CreateManualAttendance :one` INSERT query
- `backend/internal/repository/sqlc/querier.go` — added `CreateManualAttendance` to Querier interface
- `backend/internal/repository/sqlc/attendance.sql.go` — added `CreateManualAttendanceParams`, `CreateManualAttendanceRow`, function with full Scan
- `backend/internal/service/attendance/attendance_service.go` — `ManualCreate` method with placement/schedule resolution, lateness/early eval, worked_minutes calc, audit record
- `backend/internal/repository/attendance/attendance_repo.go` — `CreateManualAttendance`, `GetActivePlacement`, `GetTodaySchedule` impls
- `backend/internal/repository/attendance/mapping.go` — added `mapAttendanceFromCreate`
- `backend/internal/handler/attendance/attendance_dto.go` — added `manualCreateRequest` struct
- `backend/internal/handler/attendance/attendance_handler.go` — added `ManualCreate` handler with RFC3339 parsing, employee_id validation, 201 response
- `backend/internal/server/server.go` — added route `POST /attendance:manual-create` under admin/leader group

### Tests
- `attendance_testkit_test.go` — added `GetActivePlacement`, `GetTodaySchedule`, `CreateManualAttendance` to `fakeAttendanceRepo`; added `seedSchedule` helper; added `empNoPlacement` constant; added route to harness
- `attendance_handler_test.go` — added 5 test cases: `TestManualCreate_Success`, `TestManualCreate_NoActivePlacement_422`, `TestManualCreate_InvalidCheckIn_400`, `TestManualCreate_CheckOutBeforeCheckIn_400`, `TestManualCreate_MissingEmployeeID_400`
- All 47 tests pass

### Frontend
- `manual-attendance-modal.tsx` — modal with employee search/select via `useListEmployees`, datetime-local inputs, optional code/note, local mutation via `customFetch`, success/error toast, query invalidation
- `attendance-dashboard-screen.tsx` — "Buat Kehadiran Manual" button in title band (admin/super only, not SL), modal state + mount
- i18n: 11 new keys in `attendance` section (en.ts + id.ts): `manualCreateTitle`, `manualCreateDesc`, `manualSelectEmployee`, `manualCheckInAt`, `manualCheckOutAt`, `manualAttendanceCode`, `manualNote`, `manualSubmit`, `manualSuccess`, `manualError`, `manualNoPlacement`

### Docs (pending)
- OpenAPI spec update — add `POST /attendance:manual-create` path + `ManualCreateRequest` schema
- Feature doc update — document F5.6 in FEATURE.md
</completed_work>

<remaining_work>

1. **OpenAPI spec** (`docs/api/E5-attendance/openapi.yaml`):
   - Add `POST /attendance:manual-create` path between `:bulk-reject` and `/corrections`
   - Add `ManualCreateRequest` schema
   - Add `201` response + error responses for 400/422

2. **Feature doc** (`docs/epics/E5-attendance/FEATURE.md`):
   - Add F5.6 section with business rules, data flow, invariants
   - Update feature index if applicable

3. **EPICS.md decision log** (optional):
   - Log F5.6 decision: manual-entry bypasses geofence, always PENDING, MANUAL_ENTRY flag

</remaining_work>

<decisions_made>

- **Route location**: `POST /attendance:manual-create` in existing admin/leader group (same scope as verify/reject), not a separate group — HR/super_admin only, shared idempotency wrapper
- **No dedicated PRD doc**: Feature documented inline, not as separate PRD. Business rules MR-1 through MR-11 defined in workflow prompt
- **Flag strategy**: Always `MANUAL_ENTRY` + lateness/early flags when applicable + `PENDING` verification. No `AUTO_APPROVED` path for manual entries — HR fills in; leader/another HR must verify
- **Geofence**: Bypassed entirely for manual entries. `lat_in/lng_in = null`, `in_geofence = true`, `in_distance_m = 0`
- **Placement resolution**: Reused `GetActivePlacement` + `GetTodaySchedule` from ClockRepo pattern but added directly to `AttendanceRepository` interface (not shared via ClockRepository) to avoid circular dependency
- **Time validation**: check_out_at must be >= check_in_at (400 INVALID_REQUEST if violated). RFC3339 format required

</decisions_made>

<blockers>
- None
</blockers>

## Required Reading (in order)
1. `docs/api/E5-attendance/openapi.yaml` — insert manual-create path + schema
2. `docs/epics/E5-attendance/FEATURE.md` — add F5.6 section

## Critical Anti-Patterns (do NOT repeat these)
- [WORKFLOW]: Using Workflow agent() subagents on deepseek-reasoner fails silently with 400 errors. → Implement directly or switch model before Workflow
- [PATHS]: Python edit scripts using relative paths fail because working dir is unknown. → Always use absolute path `/Users/diaz/Documents/MIG/hris-outsource/`

## Infrastructure State
- Backend compiling clean on `feat/backend-impl` branch
- All changes uncommitted
- deepseek-reasoner model active

<next_action>
Update OpenAPI spec: add POST /attendance:manual-create path + ManualCreateRequest schema to docs/api/E5-attendance/openapi.yaml, then update FEATURE.md.
</next_action>
