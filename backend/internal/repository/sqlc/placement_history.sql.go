// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: placement_history.sql

package sqlcgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const insertPlacementHistory = `-- name: InsertPlacementHistory :one

INSERT INTO placement_history (
    placement_id, action, actor_user_id, reason, effective_date,
    status_before, status_after, notes
) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7,
    $8
)
RETURNING id, placement_id, action, actor_user_id, reason, effective_date,
          status_before, status_after, notes, created_at
`

type InsertPlacementHistoryParams struct {
	PlacementID   string
	Action        string
	ActorUserID   *string
	Reason        *string
	EffectiveDate pgtype.Date
	StatusBefore  *string
	StatusAfter   *string
	Notes         *string
}

// E3 placement_history queries — one row per lifecycle transition.
func (q *Queries) InsertPlacementHistory(ctx context.Context, arg InsertPlacementHistoryParams) (PlacementHistory, error) {
	row := q.db.QueryRow(ctx, insertPlacementHistory,
		arg.PlacementID,
		arg.Action,
		arg.ActorUserID,
		arg.Reason,
		arg.EffectiveDate,
		arg.StatusBefore,
		arg.StatusAfter,
		arg.Notes,
	)
	var i PlacementHistory
	err := row.Scan(
		&i.ID,
		&i.PlacementID,
		&i.Action,
		&i.ActorUserID,
		&i.Reason,
		&i.EffectiveDate,
		&i.StatusBefore,
		&i.StatusAfter,
		&i.Notes,
		&i.CreatedAt,
	)
	return i, err
}

const listPlacementHistory = `-- name: ListPlacementHistory :many
SELECT id, placement_id, action, actor_user_id, reason, effective_date,
       status_before, status_after, notes, created_at
FROM placement_history
WHERE placement_id = $1
ORDER BY created_at ASC, id ASC
`

func (q *Queries) ListPlacementHistory(ctx context.Context, placementID string) ([]PlacementHistory, error) {
	rows, err := q.db.Query(ctx, listPlacementHistory, placementID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []PlacementHistory{}
	for rows.Next() {
		var i PlacementHistory
		if err := rows.Scan(
			&i.ID,
			&i.PlacementID,
			&i.Action,
			&i.ActorUserID,
			&i.Reason,
			&i.EffectiveDate,
			&i.StatusBefore,
			&i.StatusAfter,
			&i.Notes,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
